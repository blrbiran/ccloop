# Re-review: Task 2 fix round 1 (scoped)

## 一、Important 裁定：ADDRESSED / NOT ADDRESSED

**裁定：ADDRESSED。**

自行核验（不采信报告文字）：

1. `git diff 26b0709..ce934c4` 只改了 `spec:2751`（§17 F1 行「修订处」列），把 `§4.0.4、§16 第 11/11b 行` 改成 `§4.0.4、§16 第 11b 行`。逐字符比对：唯一变化是删掉了 `11/`；F1 的「缺陷摘要」列（`§4.0 伪造裁决记录的论证结构…`）、「级别」列（`Critical`）一字未动；`| F2 |` 到 `| F11 |` 逐行核对（读了 HEAD 的 §17 表 F1–F11 全部行），除 F1 外无一行被触碰。
2. HEAD 上 `spec:2751` 现读作 `§4.0.4、§16 第 11b 行`——不再有任何一处指向裸「11」行的写法，F1 的「修订处」列现在只指向已就地更正、自带警告的 `11b` 行。
3. §16 表体 `11`（`spec:2735`：`S-3 退路完全遗漏 | §4.0.1–4.0.3`）与 `11b`（`spec:2736`：删除线 + 「本行的后半句…伪造的论证结构」+「不要把这一行当成『已修好』继承下去」警告）两行文字与本次修复前完全一致——不在这次 1 行 diff 范围内，逐字核对无变化。
4. §13（`spec:2330` 表、`spec:2351` 「债 3 的归类更正」小节、`spec:2345`/`2431` 第 3 笔）、§14 第 1 条（`spec:2459`）、§19（`spec:2374`/`2376` G10 正文、`spec:2828` G10 表行）均未被这次 diff 触碰——`git diff --numstat` 已确认整个文件本次只变了 1 行，上述位置结构上不可能被动到。

场景复核：L5 若沿 §17 F1 行追「伪结构改在哪」，现在只会被指到 `11b`（已更正、自带警告），不会再被误导去把有效的 S-3 退路（`11` 行）当成伪造的一部分。评审员指出的那条歧义路径已被切断。

## 二、完备性自查复核（独立搜索）

我没有复用实施者用过的任何模式（`第 11 行`、`第 11`、`11/11b\|11b/11`、按扩展名分文件类型跑），也没有复用控制器 brief 里给的任何模式。独立设计了 4 组，覆盖：其它拼接连接符、`11b` 的其它书写变体、无「第」字的裸行号引用、全角数字，且每组都是全仓库范围（`--exclude-dir=node_modules --exclude-dir=.git .`），不限 `.md`。

**命令 1 —— 全仓库裸 `11b` 字符串命中面**（不限扩展名，验证 `11b` 这个 token 本身是否在别处以任何方式被写出来过）：
```
grep -rn "11b" --include="*.md" --exclude-dir=node_modules --exclude-dir=.git .
```
输出（4 处，全部在同一份 spec 文件内）：
```
docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md:209:...§16 第 11b 行还把这个伪结构固化进了修订索引——该行已在第二轮就地更正...
docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md:2721:...本表的第 11b 行在第二轮被判定为错误结论，已就地更正...；其余各行（含第 11 行）仍然有效。
docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md:2736:| 11b | ~~「更宽不是更窄」只驳倒了裁决记录两条依据中的一条~~ ... |
docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md:2751:| F1 | Critical | ... | §4.0.4、§16 第 11b 行 |
```
结论：`11b` 这个 token 在整个仓库的 `.md` 文件里只出现这 4 次，全部指代明确、全部在同一份文件、全部与实施者报告一致。

**命令 2 —— 其它拼接连接符**（顿号、逗号、全角逗号、波浪号、「和」「与」，以及 `11 b` / `11B` 这类空格或大写变体）：
```
grep -rnE "11[、,，~和与]+11b|11b[、,，~和与]+11|11 ?[Bb]\b" --exclude-dir=node_modules --exclude-dir=.git .
```
输出：命中同上 4 行（`:209`、`:2721`、`:2736`、`:2751`），无新增位置。因为 `11 ?[Bb]\b` 本身也会匹配裸 `11b`，这组命令没有发现命令 1 之外的任何第四处。

**命令 3 —— 全角数字写法**（`１１`）：
```
grep -rn "１１" --exclude-dir=node_modules --exclude-dir=.git .
```
输出：无命中（exit 1）。仓库里不存在全角数字写法的行号引用。

**命令 4 —— 不带「第」字的裸行号引用**（如「16 表 11 行」「16.11」「行 11」「row 11」这类可能绕开「第 11」搜索的写法）：
```
grep -rnE "表.{0,4}11 ?行|行 ?11(行|号)?|16\.11|row 11" --exclude-dir=node_modules --exclude-dir=.git .
```
输出：无命中（exit 1）。

**命令 5 —— spec 文件之外，全仓库 `第 11` 命中面**（不限目录，检查是否有其它文档转述过这个歧义锚点）：
```
grep -rln "第 11" --exclude-dir=node_modules --exclude-dir=.git .
grep -rn  "第 11" --exclude-dir=node_modules --exclude-dir=.git . | cut -d: -f1 | sort | uniq -c
```
输出：
```
docs/handoff/handoff.md   (1 处)
docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md   (3 处：:209、:2721、:2751)
```
逐处核实 `docs/handoff/handoff.md:486`：
```
4. **有一条要人裁，不要自己发明**：pending 文件是否也原子化（清单第 11 条）。它与人已裁定的 marker 修法逐字同形。
```
这是另一份文档里一条**完全无关的清单**（关于 pending 文件是否原子化）的「第 11 条」，与 §16/§17 那个「S-3 退路」/「伪结构」的修订索引锚点族没有任何关系——纯字符串巧合，不是同族歧义的第四处。

**命令 6 —— 是否还有别的文档转述本 spec 的 §16/§17 修订索引结构**（防止有第二份文档复制了这张表、从而也带着同样的歧义）：
```
grep -rln "第二轮修订索引\|§16 第 1" --exclude-dir=node_modules --exclude-dir=.git .
```
输出：只有这一份 spec 文件本身，没有其它文档转述或复制这张索引表。

### 结论：本次独立搜索没有找到第四处未处理的同族锚点。

**覆盖面明写**：
- 覆盖了：全仓库（不限 `docs/`、不限 `.md`，`--exclude-dir` 只排除 `node_modules` 与 `.git`）里 `11b` 这个 token 的所有出现；`11`/`11b` 之间常见中文/符号连接词的组合写法；全角数字写法；不带「第」字的裸行号引用写法；以及 `docs/handoff/handoff.md` 与 `docs/superpowers/decisions/` 这两处最可能转述本锚点的文档。
- 没有专门覆盖：仓库里未跟踪（untracked，若存在）或被 `.gitignore` 排除但仍存在于工作区之外的文件（`grep -r .` 本身会遍历工作区里存在的文件，含 `.superpowers/sdd/` 下的 SDD 过程文档，但这些不是交付物，本轮任务范围也明确排除它们）；二进制文件；不在本机工作区里的历史提交内容（未跑 `git log -S` 之类的全历史搜索）。
- 没有测试语义等价但用完全不同措辞复述「11 号是伪结构」这类**不含「11」字面模式**的转述句——这类命中无法用字符串搜索穷举，只能靠对 §16/§17 全文的逐段人工通读来排除；本次复核已通读 §16 表体（全 19+ 行）与 §17 表体（F1–F39，见 review-package 全文）里提到「11」的每一处，均已在命令 1/5 里覆盖，未发现游离在字符串模式之外的转述。

因此：**在上述覆盖面内，没有发现第四处**。这是一个有限定的否定，不是无限定的全称否定。

## 三、diff 本身有无新破坏

范围：`git diff 26b0709..ce934c4`，1 file / +1 −1，只涉及 `spec:2751` 这一行（§17 表 F1 行）。

逐字符比对该行改动前后两版：

```
旧：| F1 | Critical | §4.0 伪造裁决记录的论证结构：把「更窄」说成有两条依据、其中 (a) 被驳倒；实际「更窄」只有一条依据且成立，(a) 是另一条独立否决 | §4.0.4、§16 第 11/11b 行 |
新：| F1 | Critical | §4.0 伪造裁决记录的论证结构：把「更窄」说成有两条依据、其中 (a) 被驳倒；实际「更窄」只有一条依据且成立，(a) 是另一条独立否决 | §4.0.4、§16 第 11b 行 |
```

差异仅为删掉 `11/` 三个字符（`1`、`1`、`/`）。表格分隔符 `|` 的数量、位置，「级别」列 `Critical`，「缺陷摘要」列全文，「修订处」列里 `§4.0.4、§16 第` 前缀与结尾的 `行` 字，均逐字符一致。没有破坏 Markdown 表格结构（列数不变、管道符不变），没有引入新的行号漂移（本行前后相邻的 F2/§17 表头/表尾未被触碰，已在 §一 核实）。

**结论：这次修复没有引入任何新破坏。**

## 四、「认领责任」记录是否完整

**结论：记录完整。** 位置：`task-2-report.md` 末尾「修复环第 1 轮」→「态度：同意」小节（约第 255–257 行）。原文：

> 我上一轮报告里的「不再有任何一处歧义的『第 11 行』」是用 `grep -n "第 11 行"` 这个收窄模式支撑的全称否定——该模式结构上匹配不到「第 11/11b 行」（中间是斜杠不是空格＋「行」），命中面本身就不覆盖断言的范围。**这是我的责任：全称否定的验证面由我核实，不能因为命令是 brief 给的就免责。** 接受这条 Important，不改判据。

核对三个要素是否都写清楚了：
1. **写了什么出错**：明确点名旧模式 `grep -n "第 11 行"`，并解释了它为什么结构上漏掉 `第 11/11b 行`（斜杠而非空格分隔）——不是泛泛道歉，是具体到字符层面的根因说明。
2. **写了责任归属**：逐字「这是我的责任：全称否定的验证面由我核实，不能因为命令是 brief 给的就免责」——直接对应评审员要求核实的那句话，且明确排除了「命令来自 brief」作为免责理由。
3. **写在哪一节**：在「修复环第 1 轮」这个专门小节里，且是该小节的开篇（先认，再改），不是散落在报告别处、也不是补在无关章节里含糊带过。

另外，报告在「新的完备性自查」小节里也确实换了新模式（`grep -n "第 11"`，不含「行」字，命中面严格包含旧模式），行为与承诺的认领一致——不是嘴上认责、行动上没变。

**范围说明**：评审 brief 提到「那个收窄的模式最初是控制器在 brief 里给的，已记账」——这是控制器侧的台账记录，不属于这次 1 file/+1-1 diff 或 task-2-report.md 的记录义务范围，本节未去核实控制器台账是否存在该条目（不在本次 scoped 复核任务内）。

## 五、范围外观察（不进入修复环）

- `docs/handoff/handoff.md:486` 有一处字面巧合的「第 11 条」（与本次修复的 §16/§17 锚点族无关，属于另一份文档里另一张清单），已在第二节命令 5 里核实为无关命中，不需要任何处理，纯粹记录以备将来有人做同类字符串搜索时不必重新排查。
- 工作区 `git status` 在核验期间确认为干净（无未提交改动），本次复核未产生任何副作用文件。
- 未发现其它需要标记的范围外问题。
