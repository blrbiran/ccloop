# 任务 2 独立评审（纯文档勘误，`6c0a647..26b0709`）

评审员：独立评审 agent（未参与实施）
日期：2026-08-05

## 0. 结论

- **规格符合性：✅**（brief 的 A.3 三要点、A 的两条边界、B.3 三条、格式选择理由、报告契约四项，逐条都落到了；见 §1–§4）
- **任务质量：有 findings** —— Critical 0 / Important 1 / Minor 3

被控制器点名的两处「实施者新加断言」**均判为真且有出处**（见 §3），不是任务 1 那条失效形状的复发。
Important 是**另一处**：站点 B 的消歧不完整，且实施者用一条比现象更窄的 grep 去支撑了「已无歧义」的完成断言。

## 1. 机械证据（每个数字附可重推命令与当时输出）

**(1) 范围与分支** — `git log --oneline -3` / `git branch --show-current` / `git status --porcelain`
```
26b0709 docs(errata): 就地更正 §5.2 债 3 归类矛盾，消歧 §16「第 11 行」双锚点
6c0a647 docs(errata): 删掉 spec:759 勘误正文里那句不实的「不格式化超出一次折行」
811a2e7 docs(errata): 就地勘误「回调=一次数组 push」族仍无勘误的 10 处
（status 无输出；branch = docs/pkg3-errata）
```
→ 单提交、在 `docs/pkg3-errata` 上、工作树干净、未碰 main、未 rebase 任务 1 的 `811a2e7`/`6c0a647`。**核实**。

**(2) 改动量** — `git diff --numstat 6c0a647..26b0709`
```
4	2	docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md
```
→ 1 file / +4 −2，与评审包一致。**核实**。

**(3) 三个 hunk 的逐词归属** — `git diff --word-diff=plain -U0 6c0a647..26b0709 -- <spec>`
输出（未过滤，全文见评审包同源）显示**只有三处**：
- `@@ -209 +209 @@`：一处 `[-…-]{+…+}` 的**同句改写**；
- `@@ -1152,0 +1153,2 @@`：**纯 `{+…+}` 插入**，无任何 `[-…-]` → 站点 A 的删除数确为 **0**；
- `@@ -2719 +2721 @@`：一处**同句改写**。
→ §13 / §14 / §19 / §16 表体**均不在 diff 内**。**核实**。

**(4) 锚点族全量（比 brief 给的自查更宽的模式）** — `grep -n "第 11" <spec>`，3 处命中：
```
209  … §16 第 11b 行还把这个伪结构固化进了修订索引——…
2721 … **本表的第 11b 行在第二轮被判定为错误结论，…；其余各行（含第 11 行）仍然有效。**
2751 | F1 | Critical | §4.0 伪造裁决记录的论证结构：… | §4.0.4、§16 第 11/11b 行 |
```
→ 第三处 `:2751` 是实施者的 `第 11 行` 模式**抓不到**的同族引用。见 finding I-1。

**(5) `部分关闭` 三处命中** — `grep -n "部分关闭" <spec>` → `:1151` / `:2376` / `:2830`，与实施者报告一致，且三处的解释我逐条复核后同意（`:1151` 是被 `:1153` 就地更正的原句；`:2376`、`:2830` 都是引述被更正的旧措辞）。**核实**。

**(6) `全域` 两处命中** — `awk '/全域/ {print NR}' <spec>` → `1153`、`2357`。这条是裁定「新加断言 1」的关键，见 §3。

**(7) 全套件** — **我没有重跑**（控制器明令不重跑）。实施者报 30 files / 514 tests / exit 0，`RUN` 路径 `/Users/biran/code/skills/loop/ccloop`；本轮 diff 只动一份 markdown，且我已用 word-diff 证明没有别的文件被碰。**这一条我采信但明写未验。**

## 2. 站点 A — §5.2 债 3 归类勘误

**越界检查（评审重点 1）：通过。**
- 机械面：`@@ -1152,0 +1153,2 @@` 是**纯插入**，删除数 0（§1(3)）。
- 语义面：我读了 `:1151` 全句，逐字确认「**因此一次并发 `stop()` 完全可以在 `writeBoundaryArtifacts` 飞行中 `releaseOwnerLease`。** 改动 A 不覆盖这一段，本层也不覆盖它（……）」**一字未动**。新段落只谈「归类」，从头到尾没有回头去弱化这两条技术结论 —— 它甚至复用了 span 外那段的事实性描述来支撑「它是另一条竞态」，方向与原论证一致，不是稀释。

**与 G10 / §13 的一致性（评审重点 2）：通过，我自己读了原文，没有采信报告。**
- `:2830`（§19 G10 行）逐字：「§13 把债 3 归为「部分关闭」错了：裁决记录对债 3 要的是**显式表态**，本层已按可接受方式表了态，**按裁定它是关闭的**；而 span 外那段是**本轮新发现**，不该写成「债 3 的未关闭部分」（会让 L5 以为它已被裁过归属）」。
- `:2378` 逐字：「**因此**：债 3 记为**本层关闭**；span 外那段作为**本轮新发现**具名传 L5（下面第 3 笔，措辞已改），并明写它需要一次归属裁决。」
- 勘误段 `:1153` 的三项内容（本层关闭 / 本轮新发现·从未被裁决处理过·归属重新裁 / 不是「债 3 的未关闭部分」）与上述两处**逐项对得上**，措辞「不是「债 3 的未关闭部分」」甚至与 `:2830` 同词。**一致。**

**brief A.3 三要点**：(1) 明写已被 §19 G10 更正 ✅；(2) 明写本轮新发现、从未被任何裁决记录处理过、归属应当重新裁 ✅；(3) 指向 §13「债 3 的归类更正」与 §19 G10 ✅ —— 两个指向今天都能落地（§13 该小节在 `:2353`，G10 行在 `:2830`）。

**未越界改 §13/§14/§19** ✅（word-diff 证明）。实施者另报「那四处都已改到位」，我抽查到 `:2347`（§13 表第 3 笔「**措辞更正**：它是**本轮新发现的独立事实**」）、`:2378`、`:2433`（§13 第 3 笔「`writeBoundaryArtifacts` 及其前置 `assertHeld` 落在 exclusive span 之外（*本轮新发现，需重新裁…*）」）三处确已改到位；**§14 第 1 条我只看到 `:2461` 的合计口径行，未做完整语义审计**（见 §7）。

## 3. 站点 A — 两处「实施者新加断言」的裁定

### 断言 1：「裁决记录对债 3 要的是显式表态，**不是全域关闭**」→ **真，且逐字有出处。不是新造。**

- 「不是全域关闭」这半句**确实不在 G10 行（`:2830`）里** —— 控制器的怀疑点成立。
- 但它**逐字**出现在 `:2357`，即 §13「债 3 的归类更正」小节的第一条：
  > - **裁决记录对债 3 要的是「显式表态」，不是「全域关闭」，而本层已按可接受方式表了态——按裁定它是*关闭的*。** 引全句：
- 而勘误段自己写的是「完整论证见 **§13「债 3 的归类更正」**与 §19 的 G10」—— **它引的正是这一节**，出处与指向自洽。
- 再往上游一层也成立：`:2364` 引的裁决记录 `:189` 原文是「L3 spec **必须显式对债 3 表态**，不得沉默继承。可接受的表态包括……——但不接受不提。」—— 要的是表态，可接受项里包含「论证不可达 + 守护测试」，**确实不是要求全域关闭**。
- 交叉验证：`awk '/全域/'` 全文只有 2 处，`1153`（本次新增）与 `2357`（既有）。**没有第三处会与之打架。**

**裁定：通过。这是对既有小节的忠实转述，不是任务 1 那种「勘误正文自造事实」。**

### 断言 2：「span 外那段（**`writeBoundaryArtifacts` 与其前置 `assertHeld`**）」→ **真，且逐字有出处；`assertHeld` 确实在 span 外。**

- 文档出处两处，措辞近乎同词：
  - `:2368`：「**`writeBoundaryArtifacts` 及其前面那个 `assertHeld` 落在 span 外，是*本轮新发现的一条独立事实*，不是「债 3 的未关闭部分」。**」
  - `:2433`（§13 第 3 笔）：「**`writeBoundaryArtifacts` 及其前置 `assertHeld` 落在 exclusive span 之外（*本轮新发现，需重新裁…*）**」——「其前置 `assertHeld`」这个说法本身就是文档既有用词。
- 代码侧我自己核了，没有采信报告 —— `grep -n 'assertHeld\|runExclusive\|writeBoundaryArtifacts' src/controller/runLoop.ts` 输出（节选自当次完整输出）：
  ```
  784:  // parked. `writeBoundaryArtifacts` below stays OUTSIDE: there is no reason to make the
  786:  const { ownerRecord, ownership, nextOwnerEpoch, eligibleForContinuation } = await heartbeat.runExclusive(
  891:  await heartbeat.assertHeld();
  903:    await writeBoundaryArtifacts(runDir, { boundaryAnalysis }, { onReconciliationWriteAbandoned });
  905:    await writeBoundaryArtifacts(runDir, {
  ```
  `runExclusive` 的 span 起于 `:786`；`:891` 的 `assertHeld` 与 `:903/:905` 的两个 `writeBoundaryArtifacts` **都排在 span 之后**，`:784` 的注释自己写着 `stays OUTSIDE`。**「其前置 `assertHeld` 属于 span 外那段」为真。**

**裁定：通过。**

### 我另找的第三处新加断言

勘误段里唯一不属于转述的成分是开头的定位语「**上一句**」。它是位置锚点而非逐字锚点 —— 见 finding M-3。除此之外，`:1153` 的每一个分句我都能在 `:2357` / `:2368` / `:2378` / `:2830` 找到对应，**没有找到第三处新造事实**。

## 4. 站点 B — §16 锚点消歧

**「改锚点，不重写行」（评审重点 4）：通过。**
- `:2735` / `:2736` 两行**不在 diff 内**（word-diff 只有三个 hunk，无一落在表体）。我另行读了 `:2736`，`11b` 行仍逐字带着「**不要把这一行当成「已修好」继承下去。**」和 `~~…~~` 删除线；`:2735` 仍是「| 11 | S-3 退路完全遗漏 | §4.0.1–4.0.3 |」。**一字未动，核实。**
- `:209` 的改写幅度：word-diff 显示替换区间**恰好**是「11 行还把……，会被 L5 继承。」→「11b 行还把……，今天不存在会被 L5 无察觉继承的风险。」。同句前半（「第一轮修订在这里写错了」整段论证、(a)/(b) 两条依据的引述、「本 spec 自己造出来再打倒的」）**逐字未动**。**没有超出「改锚点 + 处理已腐坏的『会被 L5 继承』半句」这一授权。**

**「（含第 11 行）」是消歧还是新歧义（评审重点 3）：判为消歧，但不完整。**
- 句内并置：「本表的第 **11b** 行……已就地更正……；其余各行（含第 **11** 行）仍然有效。」两个编号在同一句里显式对照，读者无法再把 `11` 读成被否定的那行。**就这一句而论，歧义消除。**
- 「（含第 11 行）」这个补充本身也**为真**：`11` 行的缺陷陈述（S-3 退路完全遗漏）从未被推翻，且 G10、G11 的修订处清单里都没有 §16 `11` 行。
- **但它不完整**：`:2751` 的「§16 第 11/11b 行」把 `11` 行钉成了 F1（伪造论证结构）的修订处，与新加的「`11` 行未被动、仍然有效」并置在同一份文档里。见 **finding I-1**。

**新句为真吗（评审重点 5）**：`:209` 的四项断言我逐项核过 ——
(a) 该行编号是 `11b` ✅（`:2736`）；(b) 已在第二轮就地更正 ✅（`:2736` 删除线 +「本行的后半句在第二轮被判定为伪造的论证结构」+ 修订处「§4.0.4（就地更正）」）；(c) 自带那句警告 ✅（逐字一致）；(d) 「今天不存在会被 L5 无察觉继承的风险」—— **我找不到反例，但这是无限定的全称否定**，见 **finding M-1**。

**前后半句自洽吗（评审重点 6）**：不构成事实错误，但缺一个转折词，见 **finding M-2**。

## 5. 实施者自报 concern 的独立判断

**concern 1（删除数 2 而非 0）：成立，两处删除都确实是同句改写，没有夹带。**
判据不是他的解释，是 `git diff --word-diff=plain -U0` 的逐词输出：两个 hunk 各只有一段 `[-…-]{+…+}`，替换区间分别止于「会被 L5 继承。」与「其余各行仍然有效。**」，**同行的其余字符全部落在未标记区**（word-diff 对未变词不加标记）。第三个 hunk 是纯 `{+…+}`。**没有任何内容被删掉而不还回来。** 站点 A 的删除数确为 0，机械判据在它该适用的地方成立。

**concern 2（直接改写 vs 挂 `Amended`）：理由成立，且与仓库立场的例外条件对得上。**
brief 的「格式选择」一节把这两处**定性为锚点更正**并显式授权直接改写，条件是「必须写明理由」—— 他写了，且理由不是口号：修订索引这类**索引体裁**的效用就在于一跳直达，挂 `Amended` 会让读者为拿到正确锚点多跳一次。两个站点用了不同格式也不是随意：站点 A 改的是**会被继承的结论**（保留原句 + 注解，能让人看见结论如何演变），站点 B 改的是**指代哪一行**。这个分界我认同。
**唯一保留**：`:209` 里「会被 L5 继承 → 今天不存在……风险」这半句**不是锚点更正，是一次实质判断的反转**，严格说不在「锚点更正」这个例外的字面覆盖内。他给的理由（同句、同因，拆成两种格式会割裂）我接受，且 brief B.2 明确要求「勘误要一并处理这半句」，所以**不升级为 finding**；但记在这里，供以后引用这条先例的人知道边界被拉宽过一次。

## 6. Findings

### Critical

无。

### Important

**I-1 — §16「11 行 / 11b 行」这一族锚点在 §17 F1 行还剩第三处未消歧；实施者的「已无歧义」是用比现象更窄的 grep 支撑的绝对断言。**

逐字锚点（spec 今 `:2751`，§17 第二轮修订索引 F1 行的「修订处」列）：

> | F1 | Critical | §4.0 伪造裁决记录的论证结构：…… | **§4.0.4、§16 第 11/11b 行** |

- 这是**同一族**的第三处引用：它把 F1（伪造论证结构）的修订处同时钉在 §16 的 `11` 与 `11b` 两行上。
- 实施者的收尾自查用的模式是 `第 11 行`（brief 给的），**结构上就抓不到 `第 11/11b 行`**；他据此在报告里写下「**不再有任何一处歧义的「第 11 行」。预期达成**」。我用更宽的 `第 11` 重跑，命中 3 行（`:209` / `:2721` / `:2751`），第三行从未被检视。命令与输出见 §1。
- 更尖锐的是：本轮**新加**的 `:2721`「其余各行（**含第 11 行**）仍然有效」与 `:2751`「F1 的修订处含 **§16 第 11 行**」现在并置在同一份文档里 —— 一处说 11 行没被动过且仍有效，另一处说 11 行是 F1 的修订落点。

**可构造场景**：L5 沿 §17 的 F1 行追「伪造论证结构这件事改在哪」，被 `:2751` 指到 §16 第 `11` 行，读到的却是「S-3 退路完全遗漏」，于是得出「S-3 那条也是本 spec 自己造出来的伪结构」——**而 S-3 恰恰是后来被 G11（Critical，人裁）证明真实可达、并逼出 §4.0.3a 整节的那条**。这正是站点 B 立项要消灭的那个误读，只是换了一个入口。

**不确定性（明写）**：我**没有**做 git 考古去确认 F1 的修订当年是否真的编辑过 `11` 行（例如把原来的一行拆成 `11` 与 `11b`）。若确实编辑过，`:2751` 就不算事实错误，但**仍然是一个会把读者带到有效行上的锚点**，与本轮站点 B 的整改目标同类。**按房规我只上报，不自改判据、不动手改它。**

### Minor

**M-1 — `:209` 的收尾是一句无限定的全称否定。**
逐字：「……并自带「不要把这一行当成『已修好』继承下去」的警告，**今天不存在会被 L5 无察觉继承的风险**。」
证据只覆盖「经由 §16 第 `11b` 行这一条路径」（该行确实带警告，`:2736` 已核），断言却写成了无主语域的「不存在……风险」。我**找不到反例**（`§4.0.4` 本节、`§16` 11b、`§17` F1 三处今天都已把伪结构标成伪的），所以不升级；但这正是本仓库 §16 第 13 行自己记下的失效形状（「用收窄的 grep 支撑绝对断言」）。建议收窄为「**经由该行**不存在……」。

**M-2 — `:209` 前半句与后半句之间缺一个转折，读起来像先断言后自否。**
逐字：「§16 第 11b 行**还把**这个伪结构固化进了修订索引——该行已在第二轮就地更正……」
「还把……固化进了」是完成体的现状指控，紧跟破折号就说该行已更正。中文完成体在这里可以读成「曾经固化过」，所以**不构成事实错误**（`:2736` 该行的前半句确实带 `~~删除线~~`，已不再固化）；但少一个「但」字，快读的人会觉得同一句自相矛盾。属措辞，不属实质。

**M-3 — `:1153` 勘误段用了位置锚点「上一句」。**
逐字：「**Amended 2026-08-05：上一句对债 3 的归类已被 §19 的 G10 更正。**」
它靠「上一句」定位，而不是逐字引用被更正的那句。今天靠「对债 3 的归类」这个限定仍然唯一可解；但只要有人在 `:1151` 与 `:1153` 之间再插一段，这个锚点就会静默指错——本文档的行号本轮已经大面积移位过一次，正是这个风险的样本。

## 7. 我没有验的部分

明写，不含糊：

1. **全套件我没跑**（控制器明令不重跑）。30 files / 514 tests / exit 0 与 `RUN` 路径行**采信实施者报告**，未独立复现。缓解证据：word-diff 证明本轮只动了一份 markdown 的三行。
2. **§14 第 1 条是否已按 G10 改到位，我没有做完整语义审计** —— 只确认了 `:2461` 存在且谈的是「输入合计 6 项」的口径。§13 表、§13「债 3 的归类更正」、§13 第 3 笔三处我核过（见 §2）。
3. **finding I-1 里 `:2751` 的历史归属我没做 git 考古** —— 即「F1 当年的修订是否真的编辑过 §16 第 `11` 行」。这决定 `:2751` 是「事实错误」还是「仅仅是会误导的锚点」，但**不影响 I-1 的可构造场景成立**。
4. **`11b` 行以外是否还有别处仍在无警告地呈现那个伪结构** —— 我检查了 §4.0.4 本节、§16 `11b`、§17 F1 三处，都已标为伪；**没有做全文穷举**，所以 M-1 我按「找不到反例」记，不按「已证明无反例」记。
