# Whole-Branch Review — `docs/pkg3-errata` (4f3b790..6a714b1)

Reviewer: independent whole-branch reviewer (no participation in any task or prior review on this branch).

## 0. 合并裁定

**合并前必须修 1 条**，其余可以合并。

| # | 级别 | 一句话 | 修法 |
|---|---|---|---|
| **I-1** | Important | `2026-07-28-run-registry-design.md` 文首「Amended 2026-07-28 in **eight places (a)–(h)**」在本分支加入 `(k)`/`(l)` 之后仍未跟进；**同一个提交 `6a714b1` 恰好为兄弟文档 L1b 的同形总账补了勘误注，却没为这份补** | 照 L1b 的模板，在文首那段下面加一条 `Amended 2026-08-06 — note on the count above` 式的注（一行） |

### 基线（我自己重跑，未过滤）

```
$ rtk proxy "npx vitest run"
 RUN  v2.1.9 /Users/biran/code/skills/loop/ccloop
 ...
 Test Files  30 passed (30)
      Tests  514 passed (514)
```

`RUN` 首行路径 = `/Users/biran/code/skills/loop/ccloop` ✅；30 files / 514 tests ✅，与基线一致。

### 分级统计

- **Critical：0**
- **Important：1**（I-1）
- **Minor：3**（m-1 `spec:2322` 同族短语未跟、m-2 本分支内部日期口径不一致、m-3 `spec:209` 的改写超出「消歧锚点」的字面授权但在界内）
- **三件具名事项**：事项一裁定 **不算缺陷（原判据被推翻）**；事项二给出**可重数计数**；事项三 12 条 minor 全部分诊完毕。
- **12 条 deferred minor 分诊**：**必修 0 条 / 可以延后 9 条 / 撤销 3 条**。

## 1. Findings — Critical

**无。** 我逐句核过本分支写下的每一条勘误正文（见 §7 第 2 条），没有发现「勘误正文自身为假」「勘误互相矛盾」「越界改结论」这三类里的任何一条。

## 2. Findings — Important

### I-1（**合并前必须修**）L2 spec 文首总账「eight places (a)–(h)」没跟上本分支新加的 `(k)`/`(l)` —— 而同一个提交刚为兄弟文档修了同形的一处

**逐字锚点**（`docs/superpowers/specs/2026-07-28-run-registry-design.md` 文首）：

> Status: approved 2026-07-28. Amended 2026-07-28 in eight places (a)–(h) after an
> adversarial review against the code; every amendment is a document defect, not an
> implementation defect. Index:

紧接其下的 Index 今天列到 **(l)**，共 **12** 条。本分支的 `d5b3e79`/`6a714b1` 加的正是 `(k)` 与 `(l)`。

```
$ rtk proxy "grep -c '^- \*\*(' docs/superpowers/specs/2026-07-28-run-registry-design.md"   # 我未跑此条计数，改用直读
$ rtk proxy "sed -n '1,40p' docs/superpowers/specs/2026-07-28-run-registry-design.md"
  → 文首写 "eight places (a)–(h)"；Index 实列 (a)(b)(c)(d)(e)(f)(g)(h)(i)(j)(k)(l)
```

**为什么这是 Important 而不是 Minor**：

1. **同一个提交 `6a714b1` 为兄弟文档 L1b 的同形总账补了勘误注**，逐字：
   > `> **Amended 2026-08-06 — note on the count above**: the paragraph above describes only the original whole-branch-review batch and is left as-is. A sixth amendment, **Amended 2026-08-06 (f)**, now also exists in §5.3 …only the count and the "all found by" qualifier are stale.`

   L1b 的病（文首写「five, all found by the final whole-branch review」，实际有六条且第六条来源不同）与 L2 的病（文首写「eight places (a)–(h)」，实际十二条且 (j)(k)(l) 来源不同）**是同一个形状**。**一改一没跟**，正是本轮三个任务各栽过一次、并被明确要求「假设还有第四处」的那个形状 —— **这就是第四处。**
2. 文首那句还带一个**限定语**：「Amended **2026-07-28** in eight places … every amendment is **a document defect, not an implementation defect**」。`(j)`/`(k)`/`(l)` 三条**都自陈不是 document defect**（`(k)` 逐字：「**unlike (a)–(i) this is not a document defect**」）。所以文首那句今天在**三处**为假：日期、条数、性质限定。

**可构造的场景**：L5 的作者打开 L2 spec 找「这层给我留了几笔债」。文首告诉他这份文档的勘误集是 `(a)–(h)`、且**全部是文档缺陷**。`(k)` 与 `(l)` —— 恰恰是唯二告诉他「**L5 继承清单现在只剩 1 笔（债 2），不是 3 笔**」的两条 —— 落在他被告知的字母区间**之外**，且被文首的性质限定排除（它们不是 document defect，是外部裁决导致的 stale）。他于是按 §2 item 6 与 §13 的**未勘误正文**去理解，继承三笔债 —— **这正是本轮任务 3 D7 想根治的那个错误结论，只是换了一条路径抵达。**

**修法**（一行，照 L1b 模板）：在文首那段下面加一条 `Amended 2026-08-06 — note on the count above` 式的注，说明 (i)–(l) 的存在，并点明 (j)(k)(l) 三条不属「document defect」那个限定。**不要**去改原句 —— 本仓库的房规是就地留痕、不重写。

## 3. Findings — Minor

### m-1（可以延后）`spec:2322` 与刚被就地更正的 `spec:2326` 隔四行，用同一个短语，未跟

**逐字锚点**（`2026-08-01-…-design.md` §12）：

- 已更正处（`:2326`）：「**本节不界的东西，明写出来**：`--max-runs` 界的是**付费调用**，不界事件追加。**⚠️ 就地更正：「界的是付费调用」这半句与本节上方…矛盾…**」
- 未跟处（`:2322`）：「被门拒绝的 run 一次付费调用都没发生，让它吃掉配额既不符合**「界的是付费调用」这个目的**，又会造成确定性饿死。」

**为什么仍判 Minor 而不是 Important**：`:2322` 说的是配额的**目的**（配额存在是为了界住付费调用），这在 `N × maxAttempts` 之下**仍然为真**；`:2326` 被更正的是**「界的是什么」这个断言**。两者不是同一条主张。**但**：同一节内、相隔四行、同一个加引号的短语，一处被明确标为与上文矛盾、另一处原样留着，读者会合理地怀疑更正没做完。

**可构造的场景**：下一位改 §12 的人做同族清扫，`grep '界的是付费调用'` 命中两处，其中一处带「就地更正」、一处不带，他无从判断第二处是**有意保留**还是**漏改**，于是要么误删一句真话，要么再开一轮。**处置建议**：在 `:2322` 那处加半句「（此处说的是配额的*目的*，不是它界住的量，与 `:2326` 的更正不冲突）」。**不必阻塞合并。**

### m-2（可以延后）本分支内部 `Amended` 日期口径不一致，根因是这条约定从未写成文字

`26b0709`（08-06 00:39）标 `2026-08-05`，`d5b3e79` 的注（08-06 01:17 写、由 `6a714b1` 09:16 补日期）标 `2026-08-06` —— 同一个通宵会话、相隔 38 分钟的两批注拿了两个日期。两者各自都能被一种读法辩护（见事项一），**所以两边都不该改**。

**可构造的场景**：下一位做 git 考古的评审员按「落注日」读，会把 `Amended 2026-08-05` 判成错一天（本轮就真的发生了一次，并耗掉一名 scoped 再评审员 ＋ 一次控制器实跑）；按「工作日」读，会把 `(f)/(k)/(l)` 的 `2026-08-06` 判成错一天。**处置建议**：把这条约定写进 `CLAUDE.md` 或 handoff 的房规节，明写「`Amended <日期>` 取该注所属工作会话的起始日；跨午夜不进位」。**这是一条规约债，不是本分支的缺陷，不阻塞合并。**

### m-3（撤销 / 记录在案）`spec:209` 的改写超出了「消歧锚点」的字面授权，但我判它**在界内**

原文（merge-base）：「§16 **第 11 行**还把这个伪结构固化进了修订索引，**会被 L5 继承**。」
今天：「§16 **第 11b 行**还把这个伪结构固化进了修订索引 —— **该行已在第二轮就地更正，并自带「不要把这一行当成『已修好』继承下去」的警告，今天不存在会被 L5 无察觉继承的风险。**」

任务 2 的授权是「消歧一个歧义锚点」，而这次改写**同时删掉了一句后果断言**（「会被 L5 继承」）并换上一句风险评估。我核过它今天为真：`§16` 的 `11b` 行确实带删除线 ＋ 那句警告，`§16` 节首也确实写了「本表的第 11b 行…已就地更正…其余各行（含第 11 行）仍然有效」。且原句的前提（该行未被更正）今天为假，留着才是新的不实描述。**因此我不把它记为越界**，只在此留痕：**这类「顺手把一句同段的过期后果一并勘误」的扩张，下次应当在 brief 里显式授权，而不是由实施者自行决定。**

## 4. 事项一 —— `Amended 2026-08-05` 日期约定与日期偏差

### 裁定：**不算缺陷。** 这条 finding 所依据的「约定」本身不成立，我用四条反例推翻了它。

#### ① 那条约定是否成立 —— **不成立**

再评审员立的规约是：`Amended <日期>` = **该注自身被提交的那个日历日**。他的两个「大样本」（`Amended 2026-08-04` → `b427c8b`(08-04)、`Amended 2026-08-05` → `811a2e7`(08-05)）**不具判别力**：它们与「落注日」和「工作日/裁决日」两种读法都相容，因此证不了前者。

我改用「对每个 opener 逐条 `git log -S` 找**首次引入**它的提交，再比该提交的日历日」来检验。命令与逐字输出：

```
$ rtk proxy "git log -S'**Amended 2026-08-04：本条判据为假' --format='%h %cd %s' --date=format:%Y-%m-%d --reverse"
cad6236 2026-08-05 fix(sweep): C3 fix round 1 — write abandonment notes at the callback, ...

$ rtk proxy "git log -S'**Amended 2026-08-04：「一次数组 push」' ... --reverse"
1564cba 2026-08-05 test(sweep): C3 fix round 2 — pin the note-before-error stderr order, ...

$ rtk proxy "git log -S'**Amended 2026-08-04：本步括号里的「回调=一次数组 push」' ... --reverse"
1564cba 2026-08-05 test(sweep): C3 fix round 2 — ...

$ rtk proxy "git log -S'**Amended 2026-08-02 (g)：' ... --reverse"
519ae55 2026-08-03 docs(plan): correct the -t census inside the anti-fake-green amendment to 46 ...
```

再取这几个提交的**精确时刻**：

```
$ for c in cad6236 1564cba 1b54190 519ae55 811a2e7 6c0a647 26b0709; do git show -s --format='%cI  %s' $c; done
cad6236 2026-08-05T00:02:57+08:00   → 注写的是 Amended 2026-08-04
1564cba 2026-08-05T00:11:41+08:00   → 注写的是 Amended 2026-08-04
1b54190 2026-08-03T00:03:07+08:00   → 注写的是 Amended 2026-08-02
519ae55 2026-08-03T00:37:56+08:00   → 注写的是 Amended 2026-08-02
811a2e7 2026-08-05T23:54:56+08:00   → 注写的是 Amended 2026-08-05
6c0a647 2026-08-06T00:22:49+08:00   → 沿用 Amended 2026-08-05
26b0709 2026-08-06T00:39:43+08:00   → 注写的是 Amended 2026-08-05   ← 被指控的这处
```

**本仓库此前每一次跨午夜落注，标的都是「上一日」，共四次（`cad6236`、`1564cba`、`1b54190`、`519ae55`），零例外。** `519ae55` 是 00:37:56 标前一日 —— 与 `26b0709` 的 00:39:43 标前一日**分秒级同形**。

所以本仓库实际通行的读法是「**该注所属的那个工作会话/裁决日**」，不是「commit 的日历日」。`26b0709` 的 `Amended 2026-08-05` **合规**。

> **对再评审员方法的具名批评**（这也是本轮第五次栽在同一形状上）：他用 `git log -S` 取一个 `Amended <日期>` **家族的第一个命中**，得到 `b427c8b`(08-04)，就断言整个家族。但 `git log -S` 对同一字符串只报**计数变化**的提交；一个家族里更晚落的成员（`cad6236`、`1564cba`）压根不在那次查询的结果里。**这是「用收窄的检索支撑一个关于全集的结论」**，与本轮台账已记四次的形状完全相同。

#### ② 该不该在合并前修 —— **不修。** 它符合本仓库四次一致的实际做法；按被推翻的那条规约去改，反而会制造第五种写法。

#### ③ 同族第二处 —— 有一处**口径不一致**，但不是缺陷（见 Minor m-2）

本分支 6 个提交跨两个日历日，我逐提交查过全部 opener：

```
$ for c in 811a2e7 6c0a647 26b0709 ce934c4 d5b3e79 6a714b1; do
    git show $c --format= --unified=0 | grep '^+' | grep -oE "Amended 2026-[0-9]{2}-[0-9]{2}"; done
811a2e7(08-05 23:54) → 16× Amended 2026-08-05 ＋ 3× Amended 2026-08-04（后者是交叉引用）
6c0a647(08-06 00:22) →  2× Amended 2026-08-05（改写既有注，日期不重置 —— 与 (j)/(e) 等既有做法一致）
26b0709(08-06 00:39) →  1× Amended 2026-08-05
ce934c4(08-06 00:55) →  0
d5b3e79(08-06 01:17) →  0 个带日期的 opener（写的是无日期的 **Amended (f)/(k)/(l)**）
6a714b1(08-06 09:16) →  8× Amended 2026-08-06（给 d5b3e79 那批无日期 opener 补日期）
```

**没有第二处同形偏差。** 唯一的口径分歧是：`d5b3e79`（08-06 01:17，仍属 08-05 那个通宵会话）写的注，被九小时后另起的 `6a714b1` 标成 `2026-08-06`；而同一会话里 38 分钟前的 `26b0709` 标的是 `2026-08-05`。两者各自都能被一种读法辩护，**根因是这条约定从未被写成文字**。处置见 m-2。

## 5. 事项二 —— `Amended <日期>` 标签的可重数计数

**我给得出可重数的计数。** 关键是找到一条**不靠语义、只靠版式**的判别式：本仓库的**标签开头**一律是 markdown 强调标记紧贴 `Amended`（`**Amended` / `*Amended`），而**交叉引用**一律把它放进反引号（`` `Amended 2026-08-05` ``）。这条判别式在本分支上 28 次出现里 100% 有效。

### 口径定义

- **范围**：`4f3b790..6a714b1` 全分支 diff 的**新增行**（`^+`），限 `docs/` 与 `src/`。用整分支 diff（不是逐提交求和）是为了避免 `6c0a647`/`6a714b1` 的改写被重复计。
- **标签（tag）**：一条勘误注的**开头**，形如 `**Amended <日期>…` 或 `*Amended <日期>…`（`*` 直接贴着 `A`）。
- **交叉引用（xref）**：出现在**反引号内**的 `` `Amended <日期>` ``，即「见……下方的 `Amended …`」。
- **其余提及**：既不是开头也不在反引号内的散文/索引提及。

### 命令与逐字输出

```
$ rtk proxy "git diff 4f3b790..6a714b1 -- docs src" | grep '^+' \
    | grep -o '.\{0,3\}Amended 2026-[0-9-]*.\{0,4\}' | sort | uniq -c | sort -rn
   3 的 `Amended 2026-08-05`。
   2 +**Amended 2026-08-05：上表
   2 +**Amended 2026-08-05：上段括
   2 +**Amended 2026-08-05：上段「
   2 的 `Amended 2026-08-05`）。这
   2 的 `Amended 2026-08-04`。
   2  **Amended 2026-08-05：本条变
   1 s. Amended 2026-08-06, sa
   1 5. Amended 2026-08-06.
   1 +*Amended 2026-08-06 (l)
   1 +*Amended 2026-08-06 (k)
   1 +**Amended 2026-08-06 (f)
   1 +**Amended 2026-08-06：上面那
   1 +**Amended 2026-08-05：上一句
   1 的 `Amended 2026-08-05`；实现
   1 的 `Amended 2026-08-05`（缓冲
   1 的 `Amended 2026-08-04`；sp
   1  **Amended 2026-08-06 (f)
   1  **Amended 2026-08-06 — n
   1  **Amended 2026-08-05：本条最
```

（`+` 是 diff 标记；行首为 `+**` 表示标签顶格，行首为空格 + `**` 表示标签在列表项内缩进。）

### 结果

| 类别 | 数 | 构成 |
|---|---|---|
| **标签（tag）** | **15** | 顶格 8（`上表`×2、`上段括`×2、`上段「`×2、`上一句`、`上面那`）＋ 缩进 4（`本条变`×2、`本条最`、`— note on the count`）＋ `**Amended 2026-08-06 (f)` 顶格 1 ＋ `*Amended 2026-08-06 (k)`/`(l)` 2 |
| **交叉引用（反引号内）** | **10** | `Amended 2026-08-05` ×8、`Amended 2026-08-04` ×2 |
| **其余提及** | **3** | L1b 文首注里散文提及的 `**Amended 2026-08-06 (f)**` 1；L2 文首 Index 里 `(k)`/`(l)` 条目结尾的 `Amended 2026-08-06.` / `Amended 2026-08-06, same ruling as (k).` 2 |
| **合计** | **28** | — |

**结论：本分支新增 15 个 `Amended <日期>` 标签、10 处反引号交叉引用、3 处散文/索引提及，合计 28 次字面出现。**

### 对既往三个数与再评审员那个数的裁定

- 控制器的 **10 / 12 / 28**：`28` 是**正确的**，但它是「全部字面出现数」，不是「标签数」；`10` 恰好等于交叉引用数，`12` 无法用任何一条口径重推 —— 三个数的病不是算错，是**没写口径**。
- 再评审员的 **17 标签 ＋ 11 交叉引用**：范围只到 `d5b3e79..6a714b1`，却断言整分支。按我的口径重数，整分支是 **15 ＋ 10**；他的 17/11 我无法用任何口径重推，且他的范围**漏掉 `811a2e7`（16 个 08-05 opener 的来源）** —— **一个比全集小的窗口不可能得出比全集大的数，这本身就说明他的口径与我的不同，而他没写口径。** 该断言按本仓库常设规则**作废**。

### 完备性声明

以上是**搜索面**，不是完备性证明：判别式建立在「本仓库标签一律带强调标记、交叉引用一律带反引号」这条**经验规律**上，我在本分支 28 次出现上验证了它 100% 成立，但**没有**验证它在仓库全史上成立。若将来有人写出不带强调标记的标签，这条计数会漏。

## 6. 事项三 —— 12 条 deferred minor 逐条分诊

**统计：必修 0 / 延后 9 / 撤销 3。**

### 任务 1（5 条）

| # | 台账原文 | 裁定 | 理由 |
|---|---|---|---|
| **T1-M1** | 变异动作对「数组的作用域与是否清空」欠一句 | **撤销（不是缺陷）** | 我**照字面执行过**这条变异（§7 第 4 条），只凭正文写的「声明在 `await resume(...)` **之外**的局部数组」就构造出了唯一可行的形状，并拿到与正文完全一致的实测。**「是否清空」不影响结论**：数组每个 run 迭代内新建，冲出发生在 `await resume` 返回后。**一条被实际执行并复现出预期输出的指令，不缺那一句。** |
| **T1-M2** | `plan:2020` 的粗体嵌套，把勘误后的正确读法排版成了非粗体 | **可以延后** | 真实存在（`**…是**当场 `options.stderr(...)`**，不是数组 push。**` 的嵌套 `**` 会被 markdown 吞掉），但**渲染后文字仍然完整可读**，不改变任何语义。排版债，下一轮顺手修。 |
| **T1-M3** | `spec:673` 新引入的「比原句更强」略被说满 | **可以延后** | 我核过这句为真（SIGKILL 论证成立，见 §7 第 3 条）。「略被说满」指它是一个无限定的强度比较；严格讲两种落脚点在「进程正常结束」这一支上等强。**不影响结论、不误导执行**，可延后加一个限定语。 |
| **T1-M4** | `fileStore.ts` 的注释把一个跨两层的调用方写死进了本层注释 | **可以延后（但值得进陷阱清单）** | 属实：`fileStore` 层的注释写了 `(sweepRuns passes SweepOptions.stderr)`，跨了两层。**这是真的耦合债** —— 将来 sweep 侧改名/改注入点，这段注释会腐而无人发现（没有任何测试钉它）。但它今天为真（§7 第 2 条已核），且**去掉它这段论证就没有支点**（支点正是「落点是调用方注入的槽」）。**不阻塞合并，建议记进 handoff 的陷阱清单。** |
| **T1-M5** | 站点 10 是全轮唯一没有回指指针的站点，链接是单向的 | **可以延后** | 站点 10 即 `fileStore.ts` 的源码注释：文档侧指向它（`spec:759` 逐字「源码里同形的那段注释在 `src/persistence/fileStore.ts` 的 `writeBoundaryArtifacts` 内，已同步」、`plan:878` 同），它不回指文档。**这是本仓库既有做法**（源码注释不引 spec 章节号，避免代码依赖文档编号），**不是本轮引入的偏差**。 |

### 任务 2（3 条）

| # | 台账原文 | 裁定 | 理由 |
|---|---|---|---|
| **T2-M1** | `:209` 收尾是无限定全称否定「今天不存在会被 L5 无察觉继承的风险」 | **可以延后** | 这是真的 Rule 12 形状问题（无限定全称否定 ＋ 证据只覆盖一行）。**我把证据面扩到了三行**：`§16` 节首（`:2723`）明写「本表的第 11b 行…已就地更正…」、`§16` 表内 11b 行（`:2738`）带删除线 ＋「不要把这一行当成『已修好』继承下去」、`§17` F1（`:2753`）指向 11b。**三处都在告知 L5，所以那句在今天为真。** 但措辞仍应加限定（「就本表与 §17 F1 而言」）。**不阻塞合并。** |
| **T2-M2** | `:209` 前后半句缺转折词，读感冲突 | **可以延后** | 纯可读性。 |
| **T2-M3** | `:1153` 用了位置锚点「上一句」，非符号/逐字锚点 | **可以延后（但这条最该修）** | 本轮的硬约束就是「锚点用逐字引用，不用行号」，而「上一句」是**比行号更脆的位置锚点** —— 行号至少能被一次 `git diff` 修复，「上一句」在任何一次插入后会**静默指向别人**。今天它指对了（我核过 `:1153` 上一句就是「§13 据此把债 3 记为『exclusive span 部分关闭』…」）。**建议下一轮统一改成逐字引用**，但今天不误导任何人，不阻塞合并。 |

### 任务 3（4 条）

| # | 台账原文 | 裁定 | 理由 |
|---|---|---|---|
| **T3-M1** | `(k)`/`(l)` 把「debt 3 也归 L3」挂在 `:246`/`:248` 两个不含该结论的行号上 | **可以延后** | 我复核了被引的两行（`rtk proxy "sed -n '240,250p' docs/superpowers/decisions/2026-07-29-technical-debt-attribution.md"`）：`:246` = 「2. **L5 继承清单第 1 条描述有误**…该条应改写为跨文件事务性缺陷，**并从 L5 清单移到 L3**」（**只覆盖债 1**）；`:248` = 「4. **L5 继承清单现在只剩 1 笔**（债 2），不是 4 笔。」（**只给残量，不点名债 3**）。所以「debt 3 → L3」在这两行里**只能被推出、不能被读出**。**结论本身为真**（L3 spec §13 逐字「3 `heartbeat.stop()` 释放窗口 \| **本层修并关闭**（§5）」，且 `:248` 的残量与之自洽），坏的只是**引证精度**。**Minor，延后。** |
| **T3-M2** | 报告称裁定三标题在 plan `:223`，实跑首行是别的段落，标题在 `:225` | **撤销（不是本分支的缺陷）** | 台账自己已经写明「**产物引的 `:225` 是对的，坏的是报告的自证**」。**合并的是产物，不是报告。** 这条是执行过程记录的缺陷，已在台账留痕，**不在合并裁定的范围内**。 |
| **T3-M3** | 四条收尾自查的第三条输出被摘要成一行，违反铁律 2 | **撤销（不是本分支的缺陷）** | 同上：这是**实施者报告的过程缺陷**，不是产物缺陷。**但它的教训必须留在台账里**，因为台账已当场量化了代价（「**I1 那一行就藏在被摘要掉的部分里**」）—— 这是本仓库「过滤输出的代价」第一次被具体计价，**价值高于这条 minor 本身**。我在此确认该记账成立，并建议把它提升为一条房规而不是一条 minor。 |
| **T3-M4** | 报告 concern 3 断言「文件把 `(j)` 用了两次是既有不一致」为假 | **可以延后（记账已成立，无需动产物）** | 我独立复核：L2 文首 Index 里 `(j)` 只有**一条**条目，逐字「**(j)** §8.1, §13 item 4 — …」，即**一条勘误两个落点**，不是字母重复。**该假前提确实为假。** 台账已记下控制器把它「原样转述并放大」（本轮第 4 次栽在「用收窄的 grep 支撑全集结论」）。**产物未受污染**（`(k)`/`(l)` 用了新字母，没有复用），所以无需改产物。**但这条假前提当时正是「不修文首」的论证依据 —— 而文首今天确实需要修（I-1）。** 换言之：**T3-M4 的假前提直接导致了 I-1 被漏掉。** 这条因果关系应当写进台账。 |

## 7. 六件重点判断

### 1) 整分支互相矛盾 / 同族一改一没跟 —— **找到第四处，就是 I-1**

我按「族」而不是按「任务」重扫了四族。**每族的命令与实测结论**：

**族 A —「一次数组 push」**（命令 `rtk proxy "grep -rn -e '数组 push' -e '备注数组' -e 'array push' docs/ src/ tests/"`，实测 19 行）：`plan:876/1006/1721/1771/2011`、`spec:683/696/757/1582`、`spec:673`（§4.3 决定性论证）、`fileStore.ts` 注释 —— **每一处正下方都有勘误注**（`plan:878/1008/1723/1773/2020`、`spec:685/698/759/1590`、`spec:673` 本身）。另有 `handoff.md:52/139` 两处，那是**描述本轮勘误工作本身**的元文本，不是主张，且 `handoff.md` 不在本分支 diff 内。**族 A 完整，无残余。**

同族里有一处我特意去核了**没有**被误伤的：`spec:2717`（§15 验收 5）的变异写的是「把记录时机改到「`resumeLoop` 正常返回后」→ 12d(ii) 必红」—— 它**从来没有预设「数组」这个基线**，措辞恰好与勘误后的正确变异一致，**不需要改，也确实没被改。** 这是对的。

**族 B —「三笔债 / three debts」**（命令 `rtk proxy "grep -rn -e 'three debts' -e '三笔债' -e 'Debts 1–3' -e '债 1–3' docs/"`，实测 6 行）：`L2:63` 与 `L2:481` 两处正文各配 `L2:66`(k) 与 `L2:486`(l)，Index 里 `(k)`/`(l)` 各一条。正文层面**无残余** —— **残余在文首总账，即 I-1。**

**族 C —「`--max-runs` 界的是付费调用」**（命令 `rtk proxy "grep -rn 'max-runs' docs/superpowers/specs/2026-08-01-…-design.md docs/superpowers/plans/2026-08-02-….md"`，实测 40 行）：`spec:2326` 已就地更正；`spec:1453/1462`、`spec:2321`、`plan:1473`、`spec:2905`(M2 记录) 五处**本来就是那条更正本身**，措辞正确。**残余一处：`spec:2322`（见 m-1）。**

**族 D —「§16 第 11 行」**（命令 `rtk proxy "grep -rn -e '第 11 行' -e '11b' …"`，实测 4 行）：`spec:209`、`spec:2723`、`spec:2738`（表内 11b 行本身）、`spec:2753`（§17 F1）—— 四处**全部**指向 `11b`，且 `:2723` 明写「其余各行（**含第 11 行**）仍然有效」。我另核了 §16 表的第 11 行内容是「S-3 退路完全遗漏 | §4.0.1–4.0.3」，与 F1 的伪论证结构**确实无关**，所以把 F1 的指向从 `11` 改到 `11b` 是**修正一个真的错指**，不是消歧一个无害歧义。**族 D 完整，无残余。**

### 2) 每一句勘误正文今天是否为真 —— **是**，逐条核过；那段源码注释我回读了真实回调体

`src/sweep/sweepRuns.ts:205`–`:209` 的真实回调体（`rtk proxy "grep -n -A 30 'onReconciliationWriteAbandoned' src/sweep/sweepRuns.ts"` 实测）：

```ts
        onReconciliationWriteAbandoned: (detail) => {
          options.stderr(
            `note  ${candidate.path}  reconciliation_write_abandoned  ${detail.replace(/\r?\n/g, " ")}`,
          );
        },
```

对照 `fileStore.ts` 那段新注释的每一个断言：

| 注释断言（逐字） | 真值 | 依据 |
|---|---|---|
| `a single synchronous call` | ✅ | 回调体只有一次 `options.stderr(...)`，无 `await` |
| `into a sink the caller injected (sweepRuns passes SweepOptions.stderr)` | ✅ | `src/sweep/sweepRuns.ts:95` `stderr: (line: string) => void;` 是 `SweepOptions` 的成员 |
| `touching no filesystem and awaiting nothing` | ✅ | 同上，回调体内无 `fs`、无 `await` |
| `it writes a note line to stderr on the spot` | ✅ | 同上 |
| `a buffered note dies with the process on a SIGKILL mid-sweep` | ✅ | 结构性为真（缓冲数组随进程消失）；我**没有**实跑 SIGKILL 验证 |
| `The asymmetry turns on who can fix it, not on whether the callback performs I/O` | ✅ | 这是论点重述，与 §9/§4.3 的勘误正文一致 |

`\r?\n` 折叠这一句（多处勘误正文都写了「含 `\r?\n` 单行折叠」）在代码里逐字对应 `detail.replace(/\r?\n/g, " ")`。✅

### 3) 有没有越界 —— **没有**，包括 `spec` §4.3 那处决定性论证

`spec:673` 的勘误自陈「**⚠️ 结论不动**：本条是「为什么选 (b) 不选 (a)」那条决定性论证，**(b) 仍然是被选中的方案，选型与本条前半段…一个字不改**」。我核了：选型（(b)）没动，(a) 的失效论证（`runLoopFromState` 顶端两个 `await` 不在 try 内）没动，改的只有 (b) 为什么仍然对的**落脚点**。

它另加了一句**新论证**：「今天的落脚点比原句更强，不是更弱」（SIGKILL 下已写出的记录不丢、缓冲的会丢）。这是**新增一条支持既有结论的论据**，不是改结论、改选型、改判据。我判它**在界内**，且这句本身为真。（`spec:209` 那处的扩张见 m-3，同样判在界内。）

### 4) 两处「会被执行的变异指令」—— **我照着做了，真的钉住了**

我按 `spec:415`–`:419` / `plan:87`–`:91` 勘误后的正文，在 `src/sweep/sweepRuns.ts` 做了那条变异（把回调体的 `options.stderr(...)` 换成 push 进声明在 `await resume(...)` 之外的局部数组，在 `await resume(...)` 正常返回后才逐条冲出），然后实跑：

```
$ rtk proxy "npx vitest run tests/sweep/sweepRuns.test.ts"
 ❯ tests/sweep/sweepRuns.test.ts (13 tests | 1 failed) 10ms
   × sweepRuns > keeps the abandonment note on stderr even when the run throws afterwards 5ms
     → expected [ …(2) ] to deeply equal [ …(3) ]
...
 ❯ tests/sweep/sweepRuns.test.ts:652:27
    652|     expect(h.stderrLines).toEqual([
 Test Files  1 failed (1)
      Tests  1 failed | 12 passed (13)
```

**与勘误正文写的「实测」逐字吻合**：`Tests  1 failed | 12 passed (13)`、唯一失败的是 12d(ii)、失败形态是 `expect(h.stderrLines).toEqual([...])` 只收到 2 行、缺的正是 `note  …  reconciliation_write_abandoned  …` 那一行（diff 里那条 `-` 就是它）。**12d(i) 保持绿**，与正文的解释一致。

裸 `it` 名也核过：`rtk proxy "grep -n '  it(' tests/sweep/sweepRuns.test.ts"` 实测第 616 行 `it("keeps the abandonment note on stderr even when the run throws afterwards", …)` —— 勘误正文引的名字**逐字正确**。

变异已回滚：`rtk proxy "git checkout -- src/sweep/sweepRuns.ts"`，随后 `rtk proxy "git status --porcelain src/"` **无输出**。

**裁定：这两条变异指令今天可执行、非等价、且确实钉住「记录必须在回调当场离开 sweep」这条性质。** 修好了，不再构造假基线。

### 5) `src/persistence/fileStore.ts` 有没有动到可执行代码 —— **没有**

`git diff` 在该文件上只有**一个 hunk**（`@@ -459,23 +459,27 @@`），删 3 行 / 加 7 行，**每一行都以 `//` 开头**（删的是 `// control (a single array push, no I/O), so a throw…` 三行，加的是 `// control — a single synchronous call into a sink the caller injected…` 七行）。紧随其后的 `options?.onReconciliationWriteAbandoned?.(String(decision.error));` 与 `try { await appendEvent(…` 都在上下文行里，未被触及。numstat `7 3 src/persistence/fileStore.ts` 与之相符。

### 6) 新格式是否与既有房规一致 —— **两项合规，一项不合规（= I-1）**

- **`Amended <日期>` 的写法**：✅ 合规。开头带强调标记、正文第一句是「这纠正的是*本文档/本计划*的缺陷，不是实现的缺陷」、末尾给完整理由的指针 —— 与 `(a)`–`(j)` 及 `Amended 2026-08-04` 那批同形。日期取值见事项一，亦合规。
- **L1b 的 `(a)`–`(f)` 字母序列**：✅ 合规。`(f)` 接在 `(e)` 之后、落在 §5.3（与 (e) 同节，Index 已含 §5.3 故不需新增位置），且文首总账**已补勘误注**说明 (f) 不属原来那批。
- **L2 文首 Index 的维护方式**：❌ **不合规 —— 这就是 I-1。** `(k)`/`(l)` 的条目本身写得很规范（含「unlike (a)–(i) this is not a document defect」的自陈，与 `(j)` 同形），但文首那句总账没跟。

### 附：那条挂人裁的「回调不抛出」不对称论证 —— **我没看到任何改变判断的东西**

按要求不重开、不替裁。仅报告一条整分支视角下的事实核查：`SweepOptions.stderr` 在生产侧**仍然只有一个注入点**。

```
$ rtk proxy "grep -rn 'stderr' src/sweep/sweepRuns.ts src/cli.ts"
src/sweep/sweepRuns.ts:95:  stderr: (line: string) => void;      ← 类型声明
src/sweep/sweepRuns.ts:121/145/206/232                            ← 四处消费
src/cli.ts:234:          stderr: (line) => console.error(line),   ← 唯一生产注入点
```

**没有第二个注入点。** 那条挂账的处置不需要因本分支而改变。（`SweepOptions.stderr` 旁仍无 `must not throw` 契约注释 —— 这条事实我确认仍然成立，但按要求不裁。）

## 8. 我没验的东西（明写）

1. **`npx tsc` / build 我没跑。** 全套件跑了（30/514，未过滤，见 §0）。本分支对可执行代码零改动（§7 第 5 条），typecheck 与 build 不可能被 markdown 与注释改动影响 —— **但这是我的推理，不是我的实测。**
2. **L1b `(f)` 里引的三处台账行号我没有逐字核。** `(f)` 引了 `.superpowers/sdd/2026-08-02-…/progress.md` 的 `:34`、`:37`、`:42` 三处并给了引文。**我没有打开那份 progress.md 比对。** 我核的是它引的**结论**与 L3 spec §13 自洽（「债 3 本层修并关闭」）。**若要合并前确保引证精度，这三处需要另一双眼睛。**
3. **`Amended <日期>` 约定我只在 `docs/superpowers` 的最近 40 个提交上做了全扫，加上 6 条 opener 的 `git log -S` 逐条溯源。** 我**没有**扫全仓库全史，也没有扫 `.superpowers/sdd/` 下的台账文档。**四条反例足以推翻「落注日」那条规约（推翻只需一条反例），但不足以证明「工作会话日」是完备的规约** —— 我给的是「本仓库四次跨午夜落注全部标上一日、零例外」这个事实，不是一条被证明的规则。
4. **SIGKILL 论证我没有实跑。** `fileStore.ts` 注释与多处勘误正文里「缓冲的备注会随进程消失」这条，我按结构判为真，**没有真的去 SIGKILL 一次 sweep**。
5. **事项二的判别式我只在本分支的 28 次出现上验证过**，没有在仓库全史上验证。见 §5 的完备性声明。
6. **12 条 deferred minor 我没有全部去改**（本轮是评审，不是修复环）。T1-M2 的 markdown 嵌套我**没有**用渲染器实际渲染验证，是按 markdown 规则推的。
7. **`review-package-final.txt` 与真实 diff 的一致性我做了抽查而非逐行比对**：commit 列表、numstat（5 files / +70 −7）、fileStore 那个 hunk 我用 `rtk proxy "git ..."` 独立重取过并吻合；**中间的 markdown diff 我没有逐行 diff 过那份 txt。**
8. **我没有重跑三个任务各自的 scoped 再评审。** 我是整分支视角，对每个任务只做了「族级」重扫（§7 第 1 条），**不保证覆盖各任务 brief 里的每一条具体要求。**
