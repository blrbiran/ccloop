# SDD ledger — 包 3：纯文档勘误（无 plan 文件；授权源 = `.superpowers/sdd/2026-08-05-l5-input-scan/progress.md` 的「包 3」一节）

本轮**不进 L5**，独立收口。范围由上一轮的 lane 2 复核逐条定死，**不照抄 scan-C 的建议**
（C 用收窄的 grep 支撑全称否定，已被一条命令证伪；C 的「3 条勘误覆盖 6 处」覆盖不全）。

分支 `docs/pkg3-errata`，基点 `4f3b790`（main）。工作区 = 主仓库（**不开 worktree**：本轮纯文档，
开 worktree 会引入陷阱 5「新 worktree 无 node_modules ⇒ 9 条 run-scenario CLI 假失败」
与陷阱 6「subagent bash cwd 重置 ⇒ 跑错仓库」，而隔离价值为零）。

--------------------------------------------------------------------------------
STEP 0 — 基线复跑。2026-08-05。控制器亲跑，未过滤，`rtk proxy` 单条命令。
--------------------------------------------------------------------------------

环境 `ECC_GATEGUARD=off DISABLE_OMC=1`（先 `export` 再 `rtk proxy`）。HEAD `9cda341`，
`git status --short` 干净。上一轮 STEP 0 记的是 `e9021ef`，其后 main 落了 `9cda341`
（`update handoff doc`，纯 docs），**那份绿不继承**。

  RUN 路径 = `/Users/biran/code/skills/loop/ccloop`（vitest 首行，组 C 常设规则 2 验收）
  Test Files 30 passed (30) / Tests 514 passed (514) / TEST_EXIT=0 / 17.68s
  两条允许的 flake 都没出现；没有名单外的失败；没有重跑。

30 / 514 与 GATE-C、与上一轮 STEP 0 三处一致。

--------------------------------------------------------------------------------
开工前的两项处置（控制器亲做，各一笔提交）
--------------------------------------------------------------------------------

1. *** 上一轮九份报告与台账**从未入库**，一次 `git clean -fdx` 就会全灭。已修。 ***
   `.superpowers/sdd/.gitignore:1` 是 `*`；历史各轮全靠 `git add -f` 强制入库
   （`git ls-files .superpowers/ | wc -l` = 66，**其中本轮 0 个**）。
   已 `git add -f` 十份产物并提交 `4f3b790`（10 files / 9098 insertions）。
   **教训：把「唯一可信进度源」放在一个 `*` 忽略的目录里，等于把它托付给运气。**

2. **人裁 4（包 2 写 `tests/`）已落上一轮台账**，随 `4f3b790` 一并入库。授权边界逐条在那里。

--------------------------------------------------------------------------------
任务分解（3 条，各配一名独立评审员；不接受实施者自证）
--------------------------------------------------------------------------------

- **任务 1** —「数组 push」族 **10 处**就地勘误。含**两处会被执行的变异指令**
  （`spec:2006`、`plan:1735`）与**一处在源码注释里**（`src/persistence/fileStore.ts:469`）。
- **任务 2** — §5.2 `:1143`「债 3 部分关闭」的同文档内矛盾 ＋ §16「第 11 行」锚点歧义
  （**改锚点，不重写行**）。
- **任务 3** — D7（L2 spec 仍写 three debts）/ D4（L1b 人裁已关闭未落勘误）/
  `spec:2306`（与 `:2301` 直接矛盾）/ D12（lane 2 已补完，直接落）。

三份 brief 已写好并落盘（`task-1-brief.md` / `task-2-brief.md` / `task-3-brief.md`），
每份都带：四条铁律、落盘协议、**「行号会移位、一律用逐字引用定位、编辑倒序处理」的警告**
（任务 1/2/3 都改同一份 382K 的 spec，这是本轮最容易静默出错的地方）、验证命令与 `RUN` 路径验收、
以及「不确定/认为 brief 判错」的强制上报位。

--------------------------------------------------------------------------------
进度
--------------------------------------------------------------------------------

BASE = `4f3b790`。

Task 1: implementer DONE_WITH_CONCERNS，提交 `811a2e7`（3 files / +37 −3）。评审员已派（opus，独立）。

  **控制器亲自复核过的四条**（不信实施者的文字）：
    git log --oneline 4f3b790..HEAD                       -> 811a2e7 一笔
    git diff --stat 4f3b790..811a2e7                      -> 3 files, 37 insertions(+), 3 deletions(-)
    grep -rn "array push\|数组" src/ tests/                -> **零命中，exit 1**（源码侧归零）
    git diff 4f3b790..811a2e7 -- src/persistence/fileStore.ts | 过滤掉 `//` 开头的增删行
                                                          -> **无输出**（只动注释，未动可执行代码）
    git status --porcelain                                -> 干净

  **实施者自报三条 concern，全部转给评审员独立判，不由控制器自裁：**
  ① 站点 4/6/10（「谁能修好它」那条不对称论证）性质比 brief 标的「描述」重一档 ——
     失效的是**论证依据本身**（「不做 I/O」是「回调抛出只可能是编程错误」的依据，
     而今天回调确实在写 fd 2）。他未发明新论证，引用了本仓库已裁定过的原话，
     但明说替代依据**比原依据弱**（结构性 → 约定性），并给了缓解事实
     （生产接线 `src/cli.ts:234` 的 `console.error`，Node 默认 `ignoreErrors: true` 不抛）。
     *** 这条要人裁：写成显式契约（注入方须注入不抛的 sink），还是这条不对称需要重新论证。 ***
     **他没有动处置（回调仍不吞）—— 这是对的，改处置是设计与人裁的事。**
  ② 两处变异指令他**实测跑过**（变异后 12d(ii) 红、12d(i) 保持绿），并称还原经逐字节 `diff` 验证。
     **本仓库出过「用错误的标记去证明还原」的先例，还原其实从未被证明** —— 已要求评审员自验。
     顺带发现 12d(ii) 今天用 `toEqual` 整数组含顺序断言，比计划描述的更强。
  ③ *** 控制器记账（本轮第 1 次）：brief 给的自查命令 `grep -rno "数组" A B | wc -l`
     根本不能判完成度 —— 房规要求保留原句、勘误正文自身还要反复提「数组」，
     这个数**做完必然上升**（实测 25 → 43）。由实施者当场证伪。 ***
     替代判据（源码侧零命中 ＋ 文档侧逐条归属）已转给评审员判够不够。

  选 opus 的理由：10 处里有 3 处不是同义替换 —— `spec:671`（用已被推翻的形状论证了一个
  做对了的选择，只许修论据不许碰结论）、`spec:2006` 与 `plan:1735`（**会被执行的变异指令**，
  勘误必须写出今天正确的变异动作，否则下一个执行者仍会构造假基线）。

Task 1: 评审完成（独立评审员，opus，未参与过任何一条）。报告 `review-task1.md`（455 行）。
  **规格符合性 ✅** —— 10 处全做、不多不少、brief 的免动清单一处未动。
  评审员给了两条**机械判据**（比控制器 brief 里那条强，值得沿用）：
    ① `git diff --numstat` 两份文档为 `16 0` / `14 0`，**删除列恒为 0 ⇒ 原件未被改写**（纯新增）；
    ② 「每条 `数组|push` 命中要么紧邻 `Amended`、要么在免动清单」⇒ 可机械覆盖完成度与不越界。
  **任务质量：0 Critical / 1 Important / 5 Minor。**

  *** Important-1（第十六波修复自带缺陷，同样不是作者自己发现的）***
  `spec:759` 的勘误正文**自己引入了一句新的不实描述**：「回调体……不格式化超出一次折行」。
  **控制器亲自复核成立**：真实回调体拼装整条成品行，折行只是其中一步 ——
    src/sweep/sweepRuns.ts  onReconciliationWriteAbandoned:
      options.stderr(`note  <path>  reconciliation_write_abandoned  <detail.replace(/\r?\n/g," ")>`)
  三条加重情节：**与同一笔提交里自己写的兄弟勘误 `spec:698`（「『不格式化』这一句今天不能照留」）
  直接矛盾**；与同族拷贝 `plan:878`（没有这半句）失同步；**正是上一笔 `b9afbf3` 刚开过的
  「同族句一改一没跟」形状**。场景：下一位据此判定回调「违规格式化」，把行拼装挪出回调体，
  **反向复活人裁明令禁止的缓冲形状**。修法：删掉那半句。

  **评审员独立复核的三条 concern（不采信实施者自述）：**
  ① 替代依据**站得住但确实弱一档**，实施者自评诚实；原话在 `spec:1586` 与 `sweepRuns.ts`
     现场注释里逐字找到，确非发明；缓解事实属实（`src/cli.ts:234` 就是 `console.error`）。
     *** 评审员补一条：**`SweepOptions.stderr` 旁没有任何「must not throw」契约注释**，
     今天安全只因生产侧只有一个注入点 —— 建议人裁时一并写成显式契约。 ***
     （`ignoreErrors: true` 是按 Node 文档判的，**未构造真实 EPIPE**，评审员自陈。）
  ② **还原是真的干净，且未用实施者的标记验**：`git show 4f3b790:src/sweep/sweepRuns.ts`
     与工作树 `diff` identical；`git diff 4f3b790..811a2e7 -- src/sweep/sweepRuns.ts` 为空。
  ③ 控制器那条命令的判断成立（见上「控制器记账 ①」）。

  **评审员自己也施加了那条变异并跑过**（不采信实施者的实验）：变异前 `13 pass / 0 fail`，
  变异后 `12 pass / 1 fail`，唯一失败即 12d(ii)（`tests/sweep/sweepRuns.test.ts:652:27`）；
  **独立判「12d(i) 保持绿是正确的」可信**（12d(i) 的替身正常返回，断的是终态，
  正常路径上两种实现不可区分）。跑完已还原，工作树干净。
  **`spec:671` 没有越界**：结论、选型、前半段逐字未动（`numstat` 删除数 0 是机械证据）。
  附带如实记下的环境事实（**值得进陷阱清单**）：`npx tsc` 会解析到另一份缓存工具链吐
  `@types/node` 语法错，**改用 `./node_modules/.bin/tsc` 后 0 行 exit 0，非本轮回归**。

Task 1: minor (deferred) ×5 —— **不进修复环，留给本轮最后的整分支评审分诊**：
  M1 变异动作对「数组的作用域与是否清空」欠一句
  M2 `plan:2020` 的粗体嵌套，把勘误后的正确读法排版成了非粗体
  M3 `spec:673` 新引入的「比原句更强」略被说满
  M4 `fileStore.ts` 的注释把一个跨两层的调用方写死进了本层注释
  M5 站点 10 是全轮**唯一没有回指指针**的站点，链接是单向的

Task 1: fix round 1/5 dispatched（resume 原实施者；只派 1 条 Important，5 条 Minor 明确不动）。

Task 1: fix round 1/5 —— 实施者 DONE，提交 `6c0a647`（**另起一笔，`811a2e7` 未被 amend**；
  1 file / +1 −1）。**实施者对判定明确表态「同意，无异议」，并逐条自行复核了三条加重情节。**
  修法：只删「、不格式化超出一次折行」半句。他补的理由（控制器认为成立）：
  **删而不是改写成一句真话** —— 能描述准回调体的说法对这段论证毫无用处（支点是「谁能修好它」，
  与回调格式化多少无关），留着只会再给下一位读者一个可拿去校核实现的把手。

  **控制器亲自复核（不信实施者的文字）：**
    git diff --stat 811a2e7..6c0a647          -> 1 file changed, 1 insertion(+), 1 deletion(-)
    git diff 811a2e7..6c0a647                 -> 逐字比对：只删那半句，同行其余内容一字未动
    grep -rn "不格式化超出" docs/ src/ tests/  -> **零命中，exit 1**
    两处同族句现状（逐字同形）：
      spec:759  「…（`SweepOptions.stderr`），回调体本身只有一次同步调用 —— 不碰文件系统、不 await。」
      plan:878  「…（`SweepOptions.stderr`），回调体本身只有一次同步调用 —— 不碰文件系统、不 await。」

  *** 实施者自曝一次程序违规（本轮第 1 次，值得记）***：第一次跑全套件用了 `| tail -60`，
  属过滤输出、违反铁律 2（`grep` 与 `tail` 同罪），**且恰好切掉了必须核对的 `RUN` 路径首行**；
  他**当场作废该次运行并未过滤重跑**，写进报告 R1.4。**自曝并作废重跑正是本仓库期望的行为**
  （L3 有过三次同形自曝：两次实施者、一次评审员，另有一次是控制器自己）。
  另主动上报未重跑 `tsc`（diff 只有一个 markdown 文件的一行、未触及任何 `.ts`）——
  两条都已转给再评审员核实，不由控制器自裁。

Task 1: fix round 1/5 —— scoped 再评审完成（sonnet，独立）。报告 `re-review-task1-fix1.md`。
  **Important-1：ADDRESSED**（半句全仓零残留；两处目标句**逐字符相同**；
  「删而不是改写」经独立核验站得住 —— 再评审员自己读 `sweepRuns.ts:205-210` 确认
  回调体确实只做一次同步调用、不碰文件系统、不 await，删后论证支点与结论均未受损）。
  **本次修复 diff 无新破坏**：结论两句逐字保留、`spec:698` 与 `plan:878` 均未被顺手改、
  五条 Minor 与全部免动项都不在本次 hunk 行号区间内、长行前后两版逐字符比对无误伤。
  **两条上报核实**：① `tail -60` 自曝**记录完整**（点名违规命令、说清切掉了 `RUN` 路径首行、
  明确作废声明、重跑证据含 `RUN` 首行）；② 未重跑 `tsc` 的理由**成立**
  （`git diff --name-only` 只有一个 `.md`，`tsc` 输入集合未变）。

Task 1: fix round 1/5 (1 addressed, 0 open; commits 811a2e7..6c0a647)
Task 1: complete (commits 4f3b790..6c0a647, review clean; 5 minor deferred 见上)

--------------------------------------------------------------------------------

Task 2: dispatched（实施者，sonnet；§5.2 归类矛盾 ＋ §16 锚点歧义）。BASE = `6c0a647`。
  选 sonnet 的理由：站点只有 3 处且 brief 已把三段逐字原文与判定全部写死，
  判断量远小于任务 1；风险集中在「越界改到技术论证」这一种形状上，由独立评审员兜。

Task 2: implementer DONE，提交 `26b0709`（1 file / **+4 −2**）。评审员已派（opus，独立）。

  **控制器亲自复核（不信实施者的文字）：**
    git diff --numstat 6c0a647..26b0709   -> `4  2  <spec>`（**单文件**）
    git status --porcelain                -> 干净
    站点 A（§5.2）**纯增 +2 −0**，原句一字未动 —— 与房规「保留原句、就地注解」一致。
    站点 B 两处各 +1 −1，均为**同句改写**（brief 授权的锚点更正例外）：
      `:209`   「§16 第 11 行…会被 L5 继承」 -> 「§16 第 11b 行…该行已在第二轮就地更正，
               并自带『不要把这一行当成「已修好」继承下去』的警告，今天不存在会被 L5 无察觉继承的风险。」
      `:2721`  「本表的第 11 行…；其余各行仍然有效。」 -> 「本表的第 11b 行…；其余各行（含第 11 行）仍然有效。」
    grep "第 11 行" 今天仍有 1 处命中 = `:2721` 那句**有意保留的澄清**（「含第 11 行」），非残留。
    表内 `11` / `11b` 两行、§13/§14/§19 四处 G10 修订处均未出现在 diff 里。

  *** 控制器读 diff 时发现两处**实施者自己新加的断言**，已点名交评审员核，不自裁： ***
    ① 站点 A 写「裁决记录对债 3 要的是显式表态，**不是全域关闭**」——「不是全域关闭」
       **不在 G10 原文里**（G10 原话止于「本层已按可接受方式表了态，按裁定它是关闭的」）。
    ② 站点 A 写「span 外那段（**`writeBoundaryArtifacts` 与其前置 `assertHeld`**）」——
       把具体符号写进括号的限定，**出处待核**，尤其「其前置 `assertHeld`」是否确属「span 外那段」。
    **这正是任务 1 那条 Important 的形状（勘误正文自己引入新断言）**，故列为评审的首要视角。
    另点名请评审员单独判 `:209` 新句里那个**全称否定**（「今天不存在会被 L5 无察觉继承的风险」）——
    本仓库反复栽在「用收窄的证据支撑绝对断言」上。

  **实施者自报两条 concern**（均已转评审员，不由控制器自裁）：
    ① 站点 B 的 numstat 删除列为 2、非机械判据字面的 0 —— 他称是 brief 明写的唯一例外并逐条解释；
    ② 站点 B 选了「直接改写原句」而非「挂 `Amended` 段」，brief 两种都允许但要求写明理由。
       **本仓库立场是「就地注解、不改原件」，直接改写是该立场的例外。**

Task 2: 评审完成（独立评审员，opus，未参与过任何一条）。报告 `review-task2.md`。
  **规格符合性 ✅** —— brief 的 A.3 三要点、A 的两条不许越界、B.3 三条、格式选择理由、
  报告契约四项逐条落到；word-diff 证明只有三个 hunk，§13/§14/§19 与 §16 表体一字未动。
  **任务质量：0 Critical / 1 Important / 3 Minor。**

  *** 控制器点名的两处「新加断言」——**评审员判为真且有出处，不是任务 1 那条形状的复发** ***
  ① 「不是全域关闭」**确实不在 G10 行（`:2830`）**，但**逐字出现在 `spec:2357`**
     （§13「债 3 的归类更正」第一条），与勘误段自己写的「完整论证见 §13……与 §19 的 G10」自洽；
     全文「全域」只有两处（`1153` 新增、`2357` 既有），无第三处打架。
  ② 「（`writeBoundaryArtifacts` 与其前置 `assertHeld`）」出处在 `:2368` 与 `:2433`
     （「及其前置 `assertHeld` 落在 exclusive span 之外」，近乎同词）。**评审员另去代码侧自核**：
     `runExclusive` span 起于 `runLoop.ts:786`，`assertHeld` 在 `:891`、两个 `writeBoundaryArtifacts`
     在 `:903/:905`，均在 span 之后，`:784` 注释自己写着 `stays OUTSIDE` —— **为真**。
  **=> 控制器这次的点名是对的（该问），但两条都不是缺陷。记此以免下次把「问」当成「有」。**

  *** Important —— 站点 B 的消歧**不完整**：同族第三处引用没改。 ***
  `spec:2751`（§17 **F1 行**）「修订处」列逐字写着 `§4.0.4、§16 第 11/11b 行` ——
  **把只属于 `11b` 的伪结构缺陷同时指到了有效的 `11` 行**（S-3 退路完全遗漏）。
  场景：L5 沿 F1 追「伪结构改在哪」，被指到第 `11` 行，得出「S-3 也是伪造的」——
  **而 S-3 恰是后来被 G11 人裁证明真实可达的那一条**。
  **控制器已独立复现**：`grep -n "第 11" <spec>` -> **3 行命中**（`:209` / `:2721` / `:2751`）。

  *** 漏法比缺陷本身重要 —— 第三次栽在同一个形状上（本轮台账已立为常设规则）***
  实施者的自查模式 `grep -n "第 11 行"` **结构上匹配不到 `第 11/11b 行`**（中间是斜杠），
  他据此写下「**不再有任何一处歧义的『第 11 行』**」= **用收窄的 grep 支撑全称否定**。
  *** 控制器记账（本轮第 2 次，这条有一半是我的）：**那个收窄的自查命令是 brief 里我给的。**
  我给了匹配不全的模式，实施者照用，两人都没发现。记账归记账 —— 报告里下断言时验证面
  仍是实施者的责任，这条要求不因 brief 有错而减轻。 ***
  => **修复环第 1 轮已派（resume 原实施者）**，并要求他换一个能论证覆盖面的模式重做完备性自查，
     且**不许再下无限定的全称否定**（要么验、要么明写没验哪一面）。

Task 2: minor (deferred) ×3 —— **不进修复环，留给最后的整分支评审分诊**：
  M1 `:209` 收尾是无限定全称否定「今天不存在会被 L5 无察觉继承的风险」
     （评审员找不到反例，但证据只覆盖一行）
  M2 `:209` 前后半句缺转折词（「还把……固化进了索引」紧接「该行已更正」，读感冲突）
  M3 `:1153` 用了位置锚点「上一句」，非符号/逐字锚点

  **评审员自陈未验的三项（如实记下）**：全套件未重跑（采信实施者报告）；
  §14 第 1 条未做完整语义审计；`:2751` 的 git 考古未做。

Task 2: fix round 1/5 dispatched（resume 原实施者；只派 1 条 Important，3 条 Minor 明确不动）。

Task 2: fix round 1/5 —— 实施者 DONE，提交 `ce934c4`（**另起一笔，`26b0709` 未被 amend**；1 file / +1 −1）。
  **实施者明确表态「同意，不认为 brief 或判定有误」，并在报告里认领：
  用收窄模式支撑全称否定是自己的失效，不因该命令来自 brief 而免责。**（态度记此，核实交再评审员。）

  **控制器亲自复核（不信实施者的文字）：**
    git diff --numstat 26b0709..ce934c4  -> `1  1  <spec>`
    逐字比对该行前后两版 -> **只改「修订处」列**（`§4.0.4、§16 第 11/11b 行` → `§4.0.4、§16 第 11b 行`）；
                            F1 的缺陷摘要与级别**逐字节未动**。
    grep -rn "第 11" docs/ src/ tests/ -> `docs/` 下只剩那三处已正确的锚点，
      外加 `docs/handoff/handoff.md:486` 一处 —— **属于另一族**（讲 pending 原子化的
      「清单第 11 条」），与 §16 锚点无关。`src/` `tests/` 零命中。
    git status --porcelain -> 干净

  **⚠️ 再评审员被要求做一件超出常规的事**：**自己独立设计一次完备性搜索，不许复用实施者的
  模式、也不许复用控制器给的任何模式**，专门去找「有没有第四处写法」（斜杠/顿号/`和`/`与`
  等拼接、全角半角、`11 b`、表格其它列、§16/§17 之外、以及本 spec 之外的文件），
  **并明写搜索面覆盖了什么、没覆盖什么，不许下无限定的全称否定**。
  理由：本轮已经因「收窄的搜索面 + 全称否定」栽了一次，不能用同一种搜索去证明它被修好了。

Task 2: fix round 1/5 —— scoped 再评审完成（sonnet，独立）。报告 `re-review-task2-fix1.md`。
  **Important：ADDRESSED**（再评审员**直接读 HEAD 而非采信报告**：F1 的缺陷摘要与级别列、
  §17 其它各行、§16 表体 `11`/`11b` 两行、§13/§14/§19 全部未变；逐字符比对确认本次改动
  只是删掉 `11/` 三个字符，表结构与管道数一致）。
  *** **再评审员自己独立设计的完备性搜索：6 条命令，一条都没复用实施者或控制器的模式。** ***
    覆盖：裸 `11b` token 全仓（不限 `.md`）；其它连接写法（顿号/逗号/波浪号/和/与/`11 b`/`11B`）；
    全角数字 `１１`；不带「第」的裸行引用（`表…11 行` / `16.11` / `row 11`）；全仓 `第 11` 命中图；
    以及「有没有别的文档抄过 §16/§17 那张表」。
    **结论：无第四处。** 全部归到同一份 spec 的 4 行已知站点，外加 `handoff.md:486` 一处
    **无关的巧合命中**（另一份文档另一张清单里的「第 11 条」）。
    *** 且它**明写了搜索面的边界**（全仓、不限 `.md`、只排除 `node_modules`/`.git`；
    **未覆盖工作树之外的未跟踪/被忽略文件，未用 `git log -S` 查全历史**）——
    **没有下无限定的全称否定。这正是本轮要立的规矩，它做到了。** ***
  **第（四）项核实**：实施者的责任认领**记录完整**，逐字为「这是我的责任：全称否定的验证面
  由我核实，不能因为命令是 brief 给的就免责」，位置在报告修复环一节开头，
  **且有实际换用更宽模式的行动backing，不只是口头声明**。

Task 2: fix round 1/5 (1 addressed, 0 open; commits 26b0709..ce934c4)
Task 2: complete (commits 6c0a647..ce934c4, review clean; 3 minor deferred 见上)

--------------------------------------------------------------------------------

Task 3: dispatched（实施者，sonnet；D7 / D4 / `spec:2306` / D12 四条跨文档勘误）。BASE = `ce934c4`。
  派单额外带了任务 1 与任务 2 各自换来的那条教训（勘误正文本身也是断言；
  不许用收窄的搜索面支撑全称否定），两条都是本轮实测抓到的，不是泛泛提醒。

Task 3: implementer DONE_WITH_CONCERNS，提交 `d5b3e79`（3 files / **+21 −1**）。评审员已派（opus，独立）。

  **控制器亲自复核（不信实施者的文字）：**
    git diff --numstat ce934c4..d5b3e79 ->
      `2  0  2026-07-27-owner-transfer-contention-design.md`（D4 的 `(f)`）
      `16 0  2026-07-28-run-registry-design.md`（D7 两处）
      `3  1  2026-08-01-…-design.md`（D12 ＋ `:2326`）
    **那唯一一处删除已用 `--word-diff` 亲验为机械副作用**：`:2326` 是一整段长行，
    插入发生在行中，git 行级 diff 记成整行替换；word-diff 显示原文
    「界的是**付费调用**，不界事件追加。」与「一次 sweep 扫到 M 个…」**逐字保留**，
    新增的 ⚠️ 更正段插在两者之间。**无真实删除。**
    git status --porcelain -> 干净

  *** 控制器读 diff 时发现一处实施者可能漏了的东西，已点名交评审员，不自裁： ***
    **两份被改文件的文首摘要都自带修订计数，而本次新增的注可能让它们腐坏，且文首都不在 diff 里。**
      L1b `:4`  「Amendments: **five**, … marked inline as **Amended 2026-07-28 (a)–(e)**」
                （后面还逐条列举 (a)–(e) 各是什么）  <- 本次新增了 `(f)`
      L2  `:3`  「Amended 2026-07-28 in **eight places (a)–(h)**」  <- 本次新增了 `(k)`、`(l)`
    **这正是本轮通篇在治的那个形状（同族一改一没跟、读者对上名字就勾销）。**
    已要求评审员给出明确裁定，**若判不算缺陷也要写清理由，不许回避**。
    顺带交给评审员的既有问题：L2 实际用到的字母是 a,b,c,d,e,f,**j,j**（**(j) 重复、无 g/h/i**），
    文首却写「(a)–(h)」——**改动前就存在**，实施者在 concern 4 里点名并声明不在本任务范围，
    **请评审员判这个「不修」的边界划得对不对**。

  **实施者自报四条 concern（全部转评审员，不由控制器自裁）：**
    ① 起草 D7 时他自己写过一句不实断言（「item 1 已有匹配注、item 3 没有」），
       **重读原文后发现是假的，落盘前自行改正并如实记录了这个过程** —— 自曝行为正确，
       但**改正后的正文对不对要评审员核**。
    ② *** 控制器记账（本轮第 3 次）：brief 里「lane 2 已把 D12 勘误正文补完，可直接落」
       **略有夸大** —— lane 2 是调查记叙而非成品勘误句，实施者据其证据自撰了正文。
       该措辞源自上一轮台账的「lane 2 已就地补完并关闭」，我未复核就转写进了 brief。 ***
    ③ D4 的 `(f)` 日期取自台账落盘提交日 `2026-08-02`，非评审员点名当天的精确日期；
       **同文件既有 (a)–(e) 全标 `2026-07-28`，那是做出勘误的日期而非事件发生日** —— 交评审员判。
    ④ 给 D7 两处新注选了 `(k)`/`(l)`（见上）。

  **实施者的完备性自查：8 条命令、覆盖全仓与中英文变体** ——
    `three debts`(2)、`bequeathed`(4)、「三笔债」(0)、`L5's problem`(2)、「是 L5 的问题」(0)、
    `max-runs`＋付费变体(6 命中/2 文件)、`代码零改动`/`零改动`(20，逐条读过排除同族误判)。
    **明写未覆盖**：未跑 `git log -S` 查历史提交、未查其它 worktree/stash、
    中文变体检索天花板受限于自己主观设计的搜索词。**没有下无限定的全称否定 —— 合规。**

Task 3: 评审完成（独立评审员，opus，未参与过任何一条）。报告 `review-task3.md`（454 行）。
  **⚠️ 该评审员在写最后一节时被流中断**（本会话已知问题；这是第 6 名）。
  **落盘协议救了它**：§0–§10 已全部落盘，只有末尾 `## 11. Findings` 是「（待填）」。
  控制器 resume 它、只让它补那一节（禁止重新检索、禁止改动 §0–§10），一次 Edit 补完。
  **=> 协议有效性再次实证：中断发生在落盘阶段，但已落的部分一字未损。**

  **规格符合性 ❌ / 任务质量：0 Critical / 4 Important / 4 Minor。**
  *** ❌ **不是因为写错了** —— 四条勘误正文的事实断言**全部为真**（评审员逐条自己重跑核实），
  `--max-runs` 的「不界事件追加」半句未被误伤，那处 `−1` 确为机械副作用（他自看 word-diff）。
  ❌ 是因为**两类「同族一改一没跟」**。 ***

  **Important 4 条：**
  I1 §12 里「界的是付费调用」出现**两次**（`:2322` 与 `:2326`），只改了后一处；
     *** `:2322` **出现在实施者自己的收尾自查 grep 输出里，报告全程未裁** —— 未裁的命中
     就是完备性论证的缺口，与它该不该改无关。 ***
  I2 L2 文首 Index 是**维护中的总账**（`(i)`/`(j)` 都是 07-28 之后补进去、各自注明来历），
     本次新增的 `(k)`/`(l)` 一条没进；下一个落勘误的人按 Index 挑字母**会撞上 `(k)`**。
  I3 L1b 文首「Amendments: **five**, **all** found by the final whole-branch review … (a)–(e)」
     在 `(f)` 落地后为假（**无条件基数 ＋ 全称限定两处都坏**），未同步。
  I4 `(f)` 标 `2026-08-02`（被勘误**事件日**），而本仓库约定是**落注日**
     （该注提交时间 `2026-08-06 01:17:57`），**且与该注自己结尾的 `landed now` 自相矛盾**。

  **Minor 4 条（deferred，留给整分支评审分诊）：**
  M1 `(k)`/`(l)` 把「debt 3 也归 L3」挂在 `:246`/`:248` 两个**不含该结论**的行号上
     （真出处是裁决记录结论速查表那一行）
  M2 报告称裁定三标题在 plan `:223` 并贴了对应「输出」，**实跑该命令首行是别的段落**、标题在 `:225`；
     **产物引的 `:225` 是对的，坏的是报告的自证**
  M3 四条收尾自查的第三条输出被**摘要成一行**（「此处不重复贴同一段长文本」），违反铁律 2，
     *** **而 I1 那一行就藏在被摘要掉的部分里** —— 过滤输出的代价第一次被当场量化。 ***
  M4 报告 concern 3 断言「文件把 `(j)` 用了两次是既有不一致」**为假**：文首 Index 明写
     `(j)` 覆盖 §8.1 与 §13 item 4，是**一条勘误两个落点**；**该假前提正是「不修文首」的论证依据**。

  *** 控制器记账（本轮第 4 次）：M4 那个假前提，**控制器在派单里原样转述并放大了它** ***
  —— 我用 `grep -no "Amended [^(]*([a-z])"` 数出「a,b,c,d,e,f,**j,j**，(j) 重复、无 g/h/i」，
  据此告诉评审员「L2 既有重复使用字母的惯例」。**这本身就是「用收窄的 grep 支撑一个关于全集的结论」——
  本轮第四次栽在同一个形状上，而这次是控制器自己栽的。** 评审员读了文首 Index 后证伪。

  *** 控制器另找到一条评审员未列的同族项（已并入修复环，编号 C1）***：
  本分支写下的 `Amended` 标签共 12 处（`rtk proxy "git diff …" | grep` 实测：
  10 处 `Amended 2026-08-05：`、1 处 `Amended 2026-08-02 (f)`、
  **1 处 `Amended（计划阶段裁定三，落在此处）：` —— 根本没有日期**）。
  最后这处是任务 3 的 D12 注，**与房规 `Amended <日期>：` 不符，与 I4 同族**。

  **⚠️ 交给整分支评审的一条（不在任务 3 范围，因任务 2 已收口）**：
  *** 本轮跨了午夜 ***。`811a2e7`（任务 1）落于 `2026-08-05 23:54:56`，其 `Amended 2026-08-05` 正确；
  但 `26b0709`（任务 2 站点 A）落于 `2026-08-06 00:39:43`，**它写的也是 `Amended 2026-08-05`**。
  按 I4 确立的「落注日」约定，**那一处可能同样偏一天**。请整分支评审逐条审全部 12 处日期标签。

Task 3: fix round 1/5 dispatched（resume 原实施者；4 条 Important ＋ 控制器发现的 C1，
  4 条 Minor 明确不动）。**I1 明确授权两种结论**：改，或**在报告里写明「不改，因为……」**——
  并附控制器自己的全族扫描结果（`grep -rn "界的是" docs/ src/ tests/` 共 7 处，
  其中 5 处已带更正或本就是更正/缺陷记录，**只有 `:2322` 待裁**），
  且明写「不要反射性地去改一句本来对的话 —— 过度勘误本身也是缺陷」。

Task 3: fix round 1/5 —— 实施者 DONE_WITH_CONCERNS，提交 `6a714b1`（**另起一笔**；3 files / +11 −4）。
  5 条处置：
    I1 *** **判定「不改」**，并给出理由：`:2322` 的「界的是付费调用」是挂在「配额计入时点」
       论证下的**目的性短语**，不是给 `--max-runs` 下定义；底层推理在 `N × maxAttempts`
       更正后依然成立；且它不是 L5 的交接句本身（`:2326` 才是）。**原句一字未动。** ***
       （控制器当时明确授权「改」或「写明不改的理由」两种结论，这是后者。）
    I2 L2 文首 Index 补上 `(k)`/`(l)`，仿 `(i)`/`(j)` 的来历说明格式。
    I3 L1b 文首原句不动，紧随其后插入说明：`(f)` 不属于那批发现、§5.3 已在既有小节列表内。
    I4 `(f)` 日期 `2026-08-02` → `2026-08-06`（落注日）。
    C1 D12 的 `Amended` 注补上缺失日期 `2026-08-06`。
  **自查中额外发现并已修（超出指令名单，已自报）**：D7 的 `(k)`/`(l)` 两条注**同样缺日期**
  （与 C1 同形、原评审未点名），一并补齐而非只上报。

  **控制器亲自复核：**
    git diff --numstat d5b3e79..6a714b1 -> `2 1` / `8 2` / `1 1`（3 files，+11 −4）
    *** 任务 1/2 已收口的勘误**一处未碰**：fix diff 里 `Amended 2026-08-05` 命中数 = **0** ***
    无日期的 `Amended（` 在分支新增行里 -> **归零**（C1 确已修）
    git status --porcelain -> 干净
  *** 控制器再次自曝同一形状（本轮第 5 次记账，第 2 次是我自己）***：我先前报给你的
  「本分支写下 12 处 `Amended` 标签」是用 `grep -o "Amended[^：]*："` 数的，**只数了以中文冒号
  结尾的那种**；改用裸串统计，分支新增行里 `Amended 2026-08-0x` 共 28 次
  （04:3 / 05:17 / 06:8）—— **但这个数把「标签」和「正文里对既有勘误的交叉引用」混在了一起**，
  同样不是可重数的完备计数。**我没有再给一个数，而是把「建立可重数的标签计数」派给了再评审员。**

Task 3: fix round 1/5 —— scoped 再评审已派（sonnet）。四项特别指令：
  ① **独立判 I1 的「不改」理由成不成立**（三种结论都可以，但要判理由本身，
     既不许因「改了更保险」就判 NOT ADDRESSED，也不许因「他给了理由」就自动放行）；
  ② 判**超范围自行修复**（`(k)`/`(l)` 缺日期）在本轮流程下可不可接受；
  ③ **独立确立本仓库的日期约定**（实施者自陈取 `2026-08-06` 是判断、不是验证到的规则），
     并**建立一个可重数的标签计数，明确区分标签与交叉引用** —— 控制器已在这上面栽过一次；
  ④ 落盘协议加重提示：**上一位评审员就是在写最后一节时被中断的**，要求结论一节最先写。

Task 3: fix round 1/5 —— scoped 再评审完成（sonnet，独立）。报告 `re-review-task3-fix1.md`。
  **I1 / I2 / I3 / I4 / C1 全部 ADDRESSED。无新破坏。**
  *** I1（本次关键一条）：再评审员**独立重读 `:2322` 与 `:2326` 的完整上下文**后，
  判实施者的「不改」成立 —— `:2322` 的「界的是付费调用」在语法上是修饰「这个目的」的
  **目的性短语**，不是像 `:2326` 那样对 `--max-runs` 下定义；其「零付费调用的 run 不该吃配额」
  这条底层论证在 `N × maxAttempts` 更正下**原封不动**；且它不是 L5 交接句
  （`:2326` 才是，那处明写「这一笔具名传给 L5」）。实施者的三条文本/逻辑主张**独立复读全部为真**。 ***
  **超范围修复（D7 `(k)`/`(l)` 缺日期）判为可接受**：同形机械补齐、已自报、回滚成本低，
  **不是对判据的改写**。
  *** **日期约定已被独立确立**（不是复验实施者的说法）：`Amended <日期>` = **该注自身被提交的
  那个日历日**。证据：用 `git log -S` 对两个大样本回溯 —— 10 处 `Amended 2026-08-04` 全部
  溯到提交 `b427c8b`(2026-08-04)、一批 `Amended 2026-08-05` 溯到 `811a2e7`(2026-08-05 23:54:56)；
  本轮 7 处新 `2026-08-06` 与 `6a714b1` 的提交时间戳吻合。 ***
  自报两条：I3 里「left as-is」那类元话语**判为应当保留**（与本仓库既有房规
  「一个字不改」「逐字不变」「原样保留」一脉）；`(j)` 重复说法的撤回**写得清楚**
  （引原话、解释错因、界定影响面、并明确停止引用它）。

Task 3: fix round 1/5 (5 addressed, 0 open; commits d5b3e79..6a714b1)
Task 3: complete (commits ce934c4..6a714b1, review clean; 4 minor deferred 见上)

--------------------------------------------------------------------------------
*** 控制器复核再评审员时证伪了它的一句断言（本轮第 6 次同形，这次是评审员栽的）。 ***
--------------------------------------------------------------------------------

再评审员在报告里写「本分支（`4f3b790..6a714b1`）共 **17 个标签**，**逐个核过日期、零不符**」。
**控制器复核证伪：**

    rtk proxy "git diff 26b0709~1..26b0709" | grep '^+' | grep -o "Amended 2026-08-0[0-9]"
      -> 1 处 `Amended 2026-08-05`
    26b0709（任务 2）的提交时间 = **2026-08-06 00:39:43**

*** 按它**自己刚独立确立**的约定（标签日期 = 该注被提交的那一天），**任务 2 站点 A 的那个标签
应为 `2026-08-06`，实为 `2026-08-05` —— 这是一处真实不符。** ***

**成因**：该再评审员的**范围是 `d5b3e79..6a714b1`**（任务 2 的提交不在其中），
**却给出了一个覆盖整分支 `4f3b790..6a714b1` 的完备性断言**。
**又一次「搜索面不覆盖断言范围」** —— 本轮第六次，前五次分别是：扫描员 C（上一轮）、
任务 2 实施者、控制器两次、以及这次的再评审员。**这个形状在本仓库已经稳定复现，
不是个别 agent 的疏忽。**

**处置**：**不重开任务 2**（已收口，且该缺陷不在任务 3 范围内）。
*** 作为一条**具名、带证据**的待办交给整分支评审 ***，而不是一句含糊的「审一下日期」：
  站点 = 任务 2 站点 A 的 `Amended 2026-08-05` 标签（`spec` §5.2 那条债 3 归类勘误）；
  证据 = 上面两条命令；判据 = 再评审员用 `git log -S` 独立确立的落注日约定。
**并请整分支评审顺带复核「17 个标签」这个计数本身是否可重数**（控制器自己数过三次、
三次口径都不同：10 / 12 / 28，**没有一次是可重数的**）。

--------------------------------------------------------------------------------
*** 整分支评审（opus，未参与过本分支任何一条）。范围 `4f3b790..6a714b1`，5 files / +70 −7。 ***
--------------------------------------------------------------------------------

报告 `review-final.md`。**合并裁定：合并前必须修 1 条（I-1），其余可以合并。**
基线自跑未过滤：`RUN v2.1.9 /Users/biran/code/skills/loop/ccloop` / 30 passed (30) / 514 passed (514)。
**Critical 0 / Important 1 / Minor 3。**

*** I-1（必修）—— 第四处「同族一改一没跟」，且形状最难看 ***
  `2026-07-28-run-registry-design.md` 文首 `Status:` 段仍写
  「Amended **2026-07-28** in **eight places (a)–(h)**；**every amendment is a document defect**」，
  而 Index 今天已列到 `(l)`。**三处皆假**：日期、条数、性质限定
  （评审员查明 `(j)(k)(l)` **都自陈不是 document defect**）。
  *** **同一笔提交 `6a714b1` 恰好给兄弟文档 L1b 补了同形的总账注，L2 却没补。** ***
  控制器亲自复核成立（读 L2 `:2`–`:5` 原文 ＋ `rtk proxy "git diff d5b3e79..6a714b1 -- <L1b>"`）。
  => **最终修复已派（唯一一次，之后只有一次 scoped 再评审，没有第二波）。**

*** 事项一：**不算缺陷 —— 那条「落注日」约定本身被推翻了。** ***
  控制器上一条记账里那处「已确认的跨午夜日期缺陷」**作废**。
  评审员用**逐 opener 的 `git log -S` 找首次引入**，拿到四条反例：
    `cad6236` 08-05T00:02:57 标 **08-04**    `1564cba` 08-05T00:11:41 标 **08-04**
    `1b54190` 08-03T00:03:07 标 **08-02**    `519ae55` 08-03T00:37:56 标 **08-02**
  *** **本仓库每一次跨午夜落注都标上一日，零例外**；`519ae55`(00:37:56) 与
  `26b0709`(00:39:43) **分秒级同形**。所以任务 2 那个 `Amended 2026-08-05` 是对的。 ***
  **前一名再评审员错在哪**：他的两个「大样本」不具判别力，且他的 `git log -S`
  **只取家族首个命中、漏掉同家族更晚落的成员** —— **又一次「用收窄检索支撑全集结论」**。
  同族无第二处偏差。
  **=> 本轮第 6 次同形失效的归属由此更正：不是任务 2 的缺陷，是再评审员的断言错。
     控制器不加复核就把它当成「已确认、带证据」上报给人，是控制器第 6 次记账。**

*** 事项二：可重数的计数给出来了。 ***
  **判别式是版式** —— 标签 = 以 `**Amended` / `*Amended` 开头；交叉引用 = 出现在反引号内。
  一条命令 ＋ 逐字输出得：**15 标签 ＋ 10 交叉引用 ＋ 3 散文/索引提及 = 28 次字面出现**。
  控制器的 `28` **对但没写口径**；`10` **恰好等于交叉引用数**（巧合，不是标签数）；`12` 不可重推。
  再评审员的 `17/11` **用任何口径都推不出**，且其范围漏掉 `811a2e7`，**该断言作废**。
  评审员附了完备性声明：判别式只在本分支这 28 次上验过。

*** 事项三：12 条 deferred minor 全部分诊 —— **必修 0 / 延后 9 / 撤销 3**。 ***
  撤销的三条：T1-M1（评审员**照字面执行过那条变异**并复现出正文的实测，**指令不缺那句**）、
  T3-M2 与 T3-M3（**属实施者报告的过程缺陷，不是产物缺陷；合并的是产物**）。
  *** 评审员另找到一条因果，点名要求入台账：**T3-M4 那个被证伪的假前提
  （「文件把 `(j)` 用了两次」）正是「不修文首」的论证依据 —— 它直接导致 I-1 被漏掉。** ***
  **一个假前提支撑了一个「不修」的决定，而那个决定漏掉了一条必修缺陷。**

**评审员自己重跑的关键几条（不采信任何下游报告）：**
  两处**会被执行的变异指令**：实跑得 `Tests 1 failed | 12 passed (13)`，唯一失败是 12d(ii)，
    **失败形态与勘误正文逐字吻合 —— 今天真的钉得住**。跑完已还原（`git diff HEAD -- src` 0 行）。
  `fileStore.ts`：单 hunk、每行以 `//` 开头，**零可执行代码改动**。
  `SweepOptions.stderr` 生产侧**仍只有 `src/cli.ts:234` 一个注入点** ——
    **那条挂人裁的项不因本分支改变。**

**新增 Minor 3 条（随分支带走，不进本次修复）：**
  F-M1 `spec:2322` 同族短语未跟
       *** ⚠️ **与 I1 的「不改」裁定冲突** —— 实施者与 scoped 再评审员**两方独立**
       深读上下文后判「不改」并各自给了理由（目的性短语 vs 定义性断言）；
       整分支评审把它记为 Minor。**按 Rule 7 不做平均**：**采信两方的深读，维持「不改」**，
       把这条冲突原样上报给人，**不重开**。 ***
  F-M2 本分支内部日期口径不一致（规约债 —— 本仓库的落注惯例从未写成文字）
  F-M3 `spec:209` 的改写超出字面授权，但**判在界内**

--------------------------------------------------------------------------------
*** 最终修复 ＋ 最后一次 scoped 再评审。本轮到此结束（无第二波）。 ***
--------------------------------------------------------------------------------

最终修复：提交 `45776e1`（另起一笔，1 file / **+12 −0 纯增**）。
  给 L2 文首加一条注，覆盖三处失效（日期 / 条数 / 性质限定），**`Status:` 原句一字未动**。
  **控制器亲自复核**：`git diff --numstat 6a714b1..45776e1` -> `12 0`；`Status:` 逐字节未变；
  工作树干净。

*** 控制器在派最后一道工序时点名的风险：**这条新注本身说了五句事实断言** ***
—— 本轮已栽过一次「勘误正文自己引入不实描述」，**而这是最后一笔、后面没有门了**。
特意把「runs to (l)」与「twelve entries」**拆成两个断言**要求分开核，
理由：字母跑到 `(l)` ≠ 恰好十二条（**本仓库正好有 `(j)` 一条勘误两个落点的先例** ——
就是害任务 3 漏掉 I-1 的那个假前提所在）。

**最后一次 scoped 再评审（sonnet，独立）：I-1 ADDRESSED，无越界，未发现需送回修复的问题。**
  **五句事实断言逐句核实，全部为真：**
    ①「(j) is dated 2026-07-30」        真（原文逐字 "Amended 2026-07-30 by that branch"）
    ②「(k)/(l) are dated 2026-08-06」   真（两条原文末句均逐字 "Amended 2026-08-06"）
    ③「runs to (l)」＋「twelve entries」**分别独立核验，均真**
       （`a`–`l` 十二个字母各恰好出现一次，**无跳号无复用**；`(j)` 那条先例是
        一条勘误引用两处正文，**不影响字母计数**）
    ④「(j)/(k) 各自条目内逐字写有 "unlike (a)–(i) this is not a document defect"」
       真，**逐字比对两处完全一致**
    ⑤「(l) shares (k)'s ruling and is the same in kind」（推断）**成立且比表面更扎实**
       —— `(l)` 原文自己写了「same ruling as (k)」；**注文正确区分了「②③④是逐字引用」
       与「⑤是同裁推断」，未混淆**
  「provenance notes」：核实属实是概括用词（全文 "provenance" 只在新注自身出现一次），
    **未加引号、不构成伪引用**，指向准确，不误导。
  **日期 `2026-08-06` 正确，且理由比预想的干净**：提交时刻 `23:01:03 +0800` **未跨午夜**，
    走的是「提交当天」这条平凡规则，**根本不经过那条存疑的「跨午夜零例外」惯例** ——
    *** 即使该惯例本身站不住，也不影响这次的正确性。 ***
  格式与 L1b 兄弟注一脉（粗体 `Amended <日期>` 开头 / 原句留痕不改 / 逐点证据 / 收尾定位注的作用）。

**新增 deferred 1 条（再评审员建议记）：**
  F-M4 全仓另有**两个同形文件**（L1 的 spec 与 plan，文首同样带修订总账/计数）——
       **本分支从未碰过它们（`git diff --stat` 空输出），范围界定正确**，
       但**它们自身是否已过期从未被验证**。留给后续。

--------------------------------------------------------------------------------
*** 包 3 实施部分结束。分支 `docs/pkg3-errata` = 7 笔提交，基点 `4f3b790`（main）。 ***
--------------------------------------------------------------------------------

  811a2e7  任务 1（数组 push 族 10 处）
  6c0a647  任务 1 修复环 1/5（Important：勘误正文自引不实描述）
  26b0709  任务 2（§5.2 归类矛盾 ＋ §16 锚点消歧）
  ce934c4  任务 2 修复环 1/5（Important：同族第三处 `spec:2751` 漏改）
  d5b3e79  任务 3（D7 / D4 / `:2306` / D12）
  6a714b1  任务 3 修复环 1/5（4 Important ＋ 控制器发现的 C1）
  45776e1  整分支评审修复（I-1：L2 文首总账没跟）

  累计 5 files / +82 −7。**src/ 只动注释（零可执行代码），tests/ 一字未动。**
  三个任务各一名独立评审员、各一轮修复环、各一次 scoped 再评审；
  整分支评审一名（opus，未参与过任何一条）＋ 最终 scoped 再评审一名。**全程无实施者自证。**

  **门尚未开** —— 合并需人明确下指令，结论写在 merge 的主题行（本仓库铁律）。

  **合并前终验（控制器亲跑，未过滤，`rtk proxy` 单条命令）：**
    RUN 路径 = `/Users/biran/code/skills/loop/ccloop`（vitest 首行）
    Test Files 30 passed (30) / Tests 514 passed (514) / **TEST_EXIT=0** / 16.63s
    两条允许的 flake 都没出现；没有名单外的失败。
    `rtk proxy "git diff 4f3b790..HEAD -- src/"` 过滤掉 `//` 开头的增删行 -> **无输出**（只动注释）
    `git diff --stat 4f3b790..HEAD -- tests/` -> **空**（tests/ 一字未动）
    30 / 514 与 GATE-C、上一轮 STEP 0、本轮 STEP 0 四处一致。

  *** 控制器记账（本轮第 7 次，也是最难看的一次）：终验第一跑用了 `tail -c 1200`，
  **过滤输出，违反铁律「验证跑绝不过滤，`grep` 与 `tail` 同罪」**，且 `TEST_EXIT` 也没取到。
  **本轮我刚以这条铁律要求过三名 agent（其中一名因此作废重跑）。已当场作废并未过滤重跑。**
  上方记录的是重跑结果。 ***
  **这是 L3 以来第四次「控制器自己违铁律」（前三次：一次 `grep -v` 过滤、两次收窄检索）。**

--------------------------------------------------------------------------------
*** 本轮最值钱的四条（都拿缺陷换来） ***
--------------------------------------------------------------------------------

1. *** 「用收窄的搜索面支撑全称否定」在本轮出现 **6 次**，其中 **控制器自己占 2 次**，
   **一名再评审员占 1 次**（他范围只到 `d5b3e79..6a714b1`，却下了覆盖整分支的完备性断言，
   害控制器把一处不存在的缺陷当成「已确认、带证据」上报给人）。 ***
   **这个形状已在本仓库稳定复现，不是个别 agent 的疏忽。**
   **有效对策（本轮实证）**：把「建立一个可重数的计数」当作一项**独立派单**交给下游，
   并**要求给出判别式口径**。整分支评审据此给出 **15 标签 ＋ 10 交叉引用 ＋ 3 提及 = 28**，
   判别式是**版式**（标签以 `**Amended` 开头、交叉引用在反引号内）—— 控制器先前三次口径
   （10 / 12 / 28）**没有一次可重推**。

2. *** 一个假前提可以直接买单一条必修缺陷。 *** 任务 3 报告里「文件把 `(j)` 用了两次」为假，
   而**那正是「不修文首」的论证依据** —— 它直接导致 I-1 被漏到整分支评审才抓出来。

3. *** 「验证跑绝不过滤输出」的代价第一次被当场量化 ***：任务 3 把一条自查输出摘要成一行，
   **而 I1 那一行恰好就藏在被摘要掉的部分里**。

4. *** 落盘协议两次救场 ***：一名评审员的 454 行报告在写最后一节时被中断，
   **已落的十节一字未损**，resume 后一次 Edit 补完。本会话 12 名 agent 中 6 名被中断，
   **全部发生在准备落盘时**。
