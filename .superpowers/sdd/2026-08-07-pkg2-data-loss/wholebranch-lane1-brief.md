# 包 2 整分支评审 —— Lane 1 brief（生产代码面）

> 你是**独立评审员**，不是实施者。包 2 的四笔（债 2 ／ 第 1 笔 ／ S4 ／ 第 4 笔）都已实施并各自做过独立评审，
> **但从来没有一次跨全部四笔的整分支评审**。你就是那一次的其中一条车道。
> **包 1 的先例是承重的**：包 1 每个任务级评审都是 0 Critical，而整分支评审把一条 Minor 升成了唯一那条 Critical。
> **任务级全绿不能替代整分支级 —— 你要找的正是任务级评审结构上看不见的东西。**

## 0. 你的交付物与落盘协议（**先做这一步，做完再开始检索**）

1. **立刻** `Write` 文件 `.superpowers/sdd/2026-08-07-pkg2-data-loss/wholebranch-lane1-report.md`，
   内容只有小节标题骨架（见下方 §6），**立刻落盘**。在这之前不要做任何检索。
2. 之后每次 `Edit` **只填一节**，**「结论」那一节最先填**。
   （历史教训：曾有一会话 12 名 agent 死 6 名，全部发生在准备一次性落盘时。分节增量落盘后交付率 100%。）
3. 该目录 `.gitignore` 是 `*`，**入库要 `git add -f`** —— 入库由控制器做，你只管写文件。

## 1. 评审范围（**严格**）

- **代码面 = `e42e062..HEAD` 触及的 9 个文件**（控制器已实测：包 1 那段对 `src/`、`tests/` 改动为 0 文件）：
  - `src/controller/ownedRunStateWriter.ts`（新增，170 行）
  - `src/controller/runLoop.ts`（+73）
  - `src/controller/resumeLoop.ts`（+57）
  - `src/persistence/fileStore.ts`（+150）
  - `tests/controller/runLoop.integration.test.ts`（+821）
  - `tests/persistence/fileStore.test.ts`（+211）
  - `tests/controller/resumeLoop.integration.test.ts`（+119）
  - `tests/controller/leaseLifecycle.integration.test.ts`（+57）
  - `tests/controller/ownedRunStateWriter.structure.test.ts`（新增，125）
- **你的主责 = `src/` 四个文件的生产行为**，尤其是**四笔叠加后的组合行为**。
  测试面有另一条车道（Lane 2）主责，但你**仍要对下面五个跨笔面全部给出判断**（见 §3）。
- ⛔ **不在范围**：包 1 的 L5 spec（`.superpowers/sdd/2026-08-07-pkg1-l5-spec/` 及其 spec 文档）。
  那是**另一条线**，有它自己未开的修复环 2。**读串了会污染两条线，明令不许碰。**
- ⛔ 待裁点 A / B / C：**人明令先不裁**。你可以指出它们与你的 finding 的关系，
  **但不得据此提出方向性重构建议，也不得把「应当裁 B」当成 finding。**

## 2. 铁律（本仓库买过血的，逐条都要遵守）

1. *** **不接受实施者自证。** *** 台账（`progress.md`）、各任务报告、各评审报告里的**任何声明**都**只是线索，不是证据**。
   凡你要在报告里当作承重前提的，**必须自己实证**。已有先例：独立评审员实跑证伪了实施者的承重前提，换来了一条 Critical。
2. *** **读代码的机械论证不等于实测。** *** 控制器本仓库有一次推理**每一环都读对了、结论仍然是错的**，
   被自己要求的实测证伪。**凡结论涉及「会不会发生」，就要跑，不要只读。**
3. *** **验证跑绝不过滤。** *** `grep` / `tail` / `sed` **同罪**，**过滤显示与过滤落盘同罪**。
   要看测试输出就把完整输出落盘再整份读。
4. *** **坏探针不能证明「不存在」。** *** 下任何全称否定（「全仓没有 X」「不存在这样的路径」）之前，
   **必须在同一次跑里放一条已知必命中的 sanity 探针**，证明你的检索面是活的。
   本仓库栽过的具体形状：`$?` 取到前一条命令 ／ zsh 的 `:s` 修饰符 ／ 未加引号的 `--include=*.ts` ／
   把 `--numstat` 限定到单文件因而错怪别人的报告。
5. **允许为验证做临时变异**（改代码看测试红不红是本仓库认可的手段），**但必须证明还原**：
   - **变异前先把基线提交状态确认干净**（`git status --porcelain`），否则 `git checkout --` 会还原到错误目标并静默销毁工作；
   - 还原后**同时**验 `git diff` 与 `git diff --cached` 均为 **0 字节** —— `git checkout <commit> -- path` **会进暂存区**，只验前者是假证明。
   - 报告里逐次列出：变异内容、观察结果、还原证明。
6. ⛔ **不得留下任何未还原的改动**；⛔ 不得 `git commit` / `push` / 建删分支 / 合并；⛔ 不得改测试判据。
7. **验证命令一律走 `rtk proxy`**（默认改写会把输出折叠成假计数）；环境变量 `ECC_GATEGUARD=off DISABLE_OMC=1`。
8. *** **finding 与它的「处置建议」是两回事。** *** 报告里**分开写**：先写 finding（缺陷本身），
   再单独写「处置建议」，并**明说这一项是否应该在本轮修**。（控制器上一轮只读了 finding 就派工，
   做了评审员明说不该在本项做的修改。）

## 3. 你必须正面回答的五个跨笔面（**任务级评审结构上看不到它们**）

对每一面给出：**你自己的独立判断 ＋ 支撑它的实测证据 ＋ 分级**。
下面每条括号里是控制器/前几轮的既有说法 —— *** **它们是待检验的断言，不是给你的结论。推翻它们同样是有效交付。** ***

1. **D-1（结构性保证）仍敞开三条路**：（既有说法）已挡住 #1–#6、#8、#11，**仍敞开 #7 直接
   `writeFile(join(runDir,"loop-state.json"))` ／ #9 动态 `await import()` ／ #10 委托第三模块**。
   守卫在 `src/controller/ownedRunStateWriter.ts`，结构判据在 `tests/controller/ownedRunStateWriter.structure.test.ts`
   （用 `ts.createSourceFile` 解析，不是正则）。
   ⇒ **你实测这三条是否真的还敞开**（临时变异 → 全套件是否变红），**还有没有第四条没被枚举到的绕过形状**。
2. **第 4 笔是「降级」不是「关闭」**：D2 把输家的「读 → 判定 → 写」整段放进跨进程 `.owner-transfer.lock`。
   （既有说法）顺序无关性**以「转移锁不可被偷」为前提，而那一条今天零判据钉住**。
   ⇒ 这个前提在今天的代码里**是否真的无判据**？**锁被偷之后**四笔叠加的行为是什么？给可构造场景。
3. **四个具名例外（人裁 13 / 14 / 17 / 37）已全部用掉**，**第五个必须问人**。
   ⇒ 逐条核：每个例外**是否只在其具名范围内被使用**？有没有**未申报的既有判据被改动/被削弱**？
   （口径提示：本仓库对「既有」有两种读法且**没有消解**，遇到就**两种口径分别报**，不要自己选一个。）
4. **`reconciliation-record.json` 缺席的处理，三处口径不一致**：（既有说法）
   `readPersistedReconciliationRecord` 有 `catch → undefined`（安全）；registry **结构上读不到它**
   （`pickReader` 对它抛，`sweepRuns.ts` 逐字说它不在 L2 的 `OBSERVED_FILES` 里）；
   **而 `readReconciliationRecord` 完全无守卫，且直通 `resumeLoop.ts` 的 `Promise.all`**。
   ⇒ 这三处口径今天**是否仍如此**？**哪一处会在真实轨迹上炸**？给可构造场景。
   **这与 D-1、与包 2 那条 Critical F-1 同族：一个在多数地方成立、于是被当成普遍成立的假设。**
5. **重试覆盖的同形缺口**：resume 侧（`resumeLoop.ts` 的有界重试，人裁 38 批准：约 100ms、3 次）
   与转移侧**各有一处「争用清空」分支只被 mock 覆盖**；耗尽分支虽由既有判据端到端驱动
   （实测：退避常数 50 → 700 让它从 222ms 变 1515ms，Δ = 恰好两次退避），**但分不清 1 次与 3 次**。
   ⇒ 你自己验：**今天能不能把重试次数从 3 改成 1（或 2）而全套件仍全绿**？若能，这就是判据缺口的实证。
   ⚠️ **CAS 从不重试**是既有约定，别把它当缺陷。

## 4. 除了这五面，重点搜索这两个「已在本仓库稳定复现」的根因形状

1. *** **「一个没有执行机制的完整性断言」** *** —— 设计上承重、但**没有任何 lint / CI / 测试在跑它**的保证。
   已复现三次（D-1 的 grep 探针、任务 3 的 `release()` 移出 `finally` 后 518 条全绿、Critical F-1）。
   **在四笔叠加后的代码里再找一遍。**
2. *** **测试「靠异常/超时变红」而不是「靠断言变红」** *** —— 一条回归的实际红法是 `Promise.all` 抛 ENOENT
   或超时，而不是它自己那条断言。这类测试**在业务逻辑改变时不会变红**（本仓库 Rule 9 的原话）。
   **主责在 Lane 2，但你在生产代码侧发现的同形请照报。**

## 5. 基线事实（控制器亲跑，`rtk proxy`，未过滤，`RUN` 路径已核）

`HEAD = bfb366a`。`Test Files 31 passed (31)` ／ `Tests 524 passed (524)` ／ `TEST_EXIT=0` / `TSC_EXIT=0` / `BUILD_EXIT=0`。
⚠️ **不要继承这个绿，自己重跑一次做你自己的基线。**
⚠️ **本轮零红**（连 flake 与人裁 10 那条都没红）—— **「本次没跑出来」不构成它们消失的证据。**
- **允许出现的 flake 只有两条**：
  (B) `evidence.test.ts > run-scenario CLI > records env names only and tracks descendants rooted at the spawned pid`（超时）
  (F) `continues normally when execute returns a complete result during the recovery window`
- **另有人裁 10 那条已挂账**：`persists phase usage evidence from the subprocess adapter without recomputing controller totals`
  （ENOENT `plan.json`，累计约 1/11 红，根因至今是空的）。**见到它按完整测试名比对、挂账即可，不要重新调查。**
- ⚠️ **任何不在上述名单内的失败一律按新缺陷处理，不许挥手放过。**

## 6. 报告骨架（`wholebranch-lane1-report.md`，先落盘这几个标题）

```
# 包 2 整分支评审 —— Lane 1 报告（生产代码面）
## 0. 结论（最先填）
## 1. 我自己的基线（重跑结果，未过滤）
## 2. 五个跨笔面的逐条判断
## 3. Findings（Critical / Important / Minor，与处置建议分开写）
## 4. 我做过的临时变异与还原证明
## 5. 我下过的全称否定，以及证明检索面为活的 sanity 探针
## 6. 我没能验到的、以及为什么（诚实留白）
## 7. 预算：harness 可数事实
```

**分级口径**：
- **Critical** = 今天可达的数据丢失/正确性破坏，或一条会让评审结论本身失效的错误前提。
- **Important** = 承重保证缺执行机制、判据钉不住设计的承重细节、跨笔组合下的行为与文档声明不符。
- **Minor** = 其余。

**每条 finding 必须带**：① 可构造场景（具体输入/状态 → 错误输出或错误终态）；
② **锚点用符号名**（函数名 / 测试全名），**不要用行号**；③ 支撑它的实测证据（不是推理）。

## 7. 预算

**人裁 45：预先放行，记账不停。** ⇒ 不要为省预算削减实证。
⚠️ **不要自报预算估计**（本仓库明令：一律以 harness 实测为准）。报告 §7 只写**可数事实**：
你跑了几次全套件、做了几次变异、读了哪几个文件。**拿不到精确数字就说拿不到，不要估。**
