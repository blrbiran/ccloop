# 扫描员 1 报告 — 包 3 遗留 deferred minor 开工前冲突扫描

## 0. 结论（最先写）

**F-M4（优先项）：premise 有一半不成立，另一半今天验为真，无剩余动作。**
`git log --oneline -- <plan>`（见 §2）显示 L1 的 plan 文件（`docs/superpowers/plans/2026-07-26-run-lease-and-heartbeat.md`，2530 行）从未被修订过（唯一一笔提交是创建），**它压根没有「修订总账/计数/全称限定」这种文首结构**——`grep -n 'Amended\|Amendment'` 对它是 exit 1（零命中）。F-M4 原文说「两个同形文件」，**其中一个不同形**。另一个——L1 的 spec（`docs/superpowers/specs/2026-07-26-run-lease-and-heartbeat-design.md`）——**确实同形**（文首「Amendments: four, ... marked inline as Amended 2026-07-26 (a)–(d)」），但逐条核验后**今天为真**：4 处内联 `Amended 2026-07-26` 标记、位置（§4.4/§8.1/§8.1/§6）、日期全部与文首声明一致，`git log` 显示最后一笔改动就是补上第 4 处（`14b7d14`），此后再无提交碰过这份文件。**=> 无需任何修复，F-M4 到此可以关闭。**

**分母重数：独立推出 12，不是人裁的 10，也不是照抄控制器的 12——判别式在 §1 给出。** 差额（10 vs 12）已在 `docs/handoff/handoff.md:71-73` 找到实锤：该表把「10 条」写成「9 条延后 ＋ **1 条新增**」，紧接着那一条新增原文逐字就是 F-M4 的描述——**「1 条新增」指的就是 F-M4 一条，F-M2／F-M3（在更早的整分支评审里新增）没有被计入这张表**。这不是我的推测，是同一份 handoff 文件里 F-M4 原文紧跟在「10 条」表格下面的直接证据（见 §1）。

**A 组 9 条**：逐条用今天的文件内容核验（不信旧行号），**9 条全部仍然成立**——没有一条被静默修掉或被后续改动证伪。其中 **T1-M4、T1-M5 落点都在 `src/persistence/fileStore.ts` 的注释里，包 1 只写 `docs/`（人裁 6 / Q2），这两条在包 1 里做不了，必须转包 2**。T3-M4 的「剩余动作」核验为**无**——I-1 的修复注已经显式把 `(j)` 讲清楚（一条勘误两个落点，非重复字母），假前提没有活着的下游影响。

**B 组**：F-M2（落注日惯例从未写成文字）今天依旧成立——全仓找不到任何写明这条惯例的文档。F-M3（`spec:209` 改写超出字面授权但判在界内）是一个已closed的判断，没有剩余动作。

**没做完的事**：见 §7，不在这里凑数。

## 1. 分母重数：判别式口径、命令、输出、我的数字与控制器的 12 / 人裁的 10 的差异裁断

## 1. 分母重数：判别式口径、命令、输出、我的数字与控制器的 12 / 人裁的 10 的差异裁断

**判别式**：一条「deferred minor」计入分母，当且仅当（a）它在包 3 台账里以 Minor 身份被逐条列出（不是 Important/Critical，也不是散文提及），且（b）没有被同一份台账明确「撤销」，且（c）不是 F-M1（本轮委任状明写排除，人裁 5 第 2 条已定案「维持不改，到此为止」）。

**A 组原始定义（逐条数过，非估算）**：
- Task 1 评审 deferred 清单，台账 `:123-128`，逐条列出 `M1 变异动作对「数组的作用域…」欠一句` / `M2 plan:2020 粗体嵌套` / `M3 spec:673「比原句更强」` / `M4 fileStore.ts 注释跨层` / `M5 站点10无回指指针` —— **5 条**。
- Task 2 评审 deferred 清单，台账 `:230-234`，`M1 spec:209 全称否定` / `M2 spec:209 缺转折词` / `M3 spec:1153「上一句」锚点` —— **3 条**。
- Task 3 评审 deferred 清单，台账 `:349-357`，`M1 (k)/(l) 行号误指` / `M2 报告自证坏了` / `M3 输出被摘要藏了 I1` / `M4 (j) 用两次的假前提` —— **4 条**。
- 合计 5+3+4 = **12**，与台账自己的自报数字逐字吻合：`:497`「事项三：12 条 deferred minor 全部分诊 —— **必修 0 / 延后 9 / 撤销 3**」。
- 撤销 3 条，台账 `:498-499` 逐字点名：`T1-M1、T3-M2、T3-M3`。
- => **A 组分母 = 12 − 3 = 9**，与 brief 表格给出的 9 条 ID 逐一核对一致（T1-M2/M3/M4/M5 ＋ T2-M1/M2/M3 ＋ T3-M1/M4 = 4+3+2=9）。

**B 组原始定义**：
- 整分支评审新增 3 条，台账 `:511-518`：`F-M1`（`:2322` 同族短语未跟，与 I1 裁定冲突，**已被人裁 5 第 2 条定案排除**）、`F-M2`（日期口径规约债）、`F-M3`（`spec:209` 改写超授权但判在界内）。
- 最终 scoped 再评审新增 1 条，台账 `:554-557`：`F-M4`（L1 的 spec/plan 是否过期未验证）。
- B 组新增合计 = 3 + 1 = **4**；剔除 F-M1（不在本轮范围）=> **B 组分母 = 3**（F-M2、F-M3、F-M4）。

**=> 我独立推出的总分母 = 9 + 3 = 12。** 这个数字恰好等于控制器给的 12，但**不是照抄**——推导路径是「读台账两处自报数字＋逐条数原始定义清单」，不是复用控制器给出的差额提示。

**10 从哪来、为什么少 2 条（实锤，非猜测）**：
```
docs/handoff/handoff.md:71
| 3 | **10 条 deferred minor**（9 条延后 ＋ 1 条新增） | **并进包 1 的开工前冲突扫描当输入清单，不单开清理轮** | ...

docs/handoff/handoff.md:73
*** 第 3 条里有一条建议在扫描中优先处理 ***：全仓另有**两个同形文件**（L1 的 spec 与 plan，
文首同样带「修订总账 / 计数 / 全称限定」），包 3 从未碰过它们，其自身是否已过期从未被验证。
```
`:73` 紧跟在「10 条」表格行下面，逐字就是 F-M4 的原始描述。**「1 条新增」在字面上唯一对应的就是 F-M4**——F-M2、F-M3（更早在整分支评审里新增，台账 `:517-518`）在这张表和这段说明里都没有被单独提及或计入。这解释了 10 与 12 之间恰好差 2（F-M2 + F-M3），与 brief §3 给出的「差额具名为 F-M2 与 F-M3」完全对得上，但我是从 `handoff.md` 原文独立复核出来的，不是照抄 brief 的结论。

**裁断**：**12 是可重推的正确分母**；10 是把 B 组 4 条新增里的 3 条（F-M2/F-M3 漏计、F-M1 应排除但未见 handoff 明确说明其被排除的推理）压缩成了「1 条新增」的结果，属于转述压缩，不是分母本身有争议。

## 2. F-M4（优先项）：两个同形文件的定位与逐条核验

### 2.1 定位（命令 + 输出，不猜路径）

F-M4 的原始出处是 `.superpowers/sdd/2026-08-05-pkg3-doc-errata/re-review-final-fix.md:135`，逐字：
```
1. **L1 的两份同形文件是否过期未知**（呼应 §7.3）：
   `docs/superpowers/plans/2026-07-26-run-lease-and-heartbeat.md`、
   `docs/superpowers/specs/2026-07-26-run-lease-and-heartbeat-design.md`
   头部是否也有「Amendments: N / in N places / every amendment is...」形态的过期账本，
   未经任何一轮验证。
```
路径由报告原文给出（我没有猜），但为独立核实这两个文件确实是「L1」，我核对了 spec 正文自称：
```
docs/superpowers/specs/2026-07-26-run-lease-and-heartbeat-design.md:37
| **L1 (this design)** | lease + heartbeat | none |
```
—— 确认自称 L1；plan 文件标题行 `# Run Lease and Heartbeat (L1) Implementation Plan` 同样自称 L1。**两个文件路径与「L1」身份均已核实，非推定。**

### 2.2 逐条核验：spec（`docs/superpowers/specs/2026-07-26-run-lease-and-heartbeat-design.md`）

文首（`:4`）逐字：
```
> Amendments: four, all found by implementing this document and marked inline as
> **Amended 2026-07-26 (a)–(d)** in §4.4, §6, §8.1 and §8.1. Each corrects a defect
> in *this document*, not in the implementation — ... (a) ... (b) ... (c) ... (d) ...
```
三个可证伪断言逐条核：
1. **计数「four」**：`grep -n 'Amended 2026-07-26' <spec>` → 5 处命中（`:4` 文首本身 + 内联 `:70(a)` `:126(d)` `:203(c)` `:231(b)`）。内联恰好 4 处，**为真**。
2. **位置「§4.4, §6, §8.1 and §8.1」**：核对该文件 `## /### ` 标题表（`grep -n '^## \|^### '`），`:70` 落在 §4.4（`:66-76`）区间，`:126` 落在 §6（`:117-136`）区间，`:203`/`:231` 均落在 §8.1（`:199-243`）区间。**逐条位置为真**。
3. **日期「2026-07-26」**：4 处内联标记逐字都是 `Amended 2026-07-26`，无一处日期不符。**为真**。
4. 佐证：`git log --oneline -- <spec>` 的最新一笔是 `14b7d14 docs: amend the L1 spec with the four defects implementing it exposed`，其后无任何提交再碰过这个文件——**没有"补了第 5 处却没更新头部"的窗口**。

**结论：spec 文首的修订总账今天完全为真，无需修复。**

### 2.3 逐条核验：plan（`docs/superpowers/plans/2026-07-26-run-lease-and-heartbeat.md`）

```
grep -n 'Amended\|Amendment' <plan>   -> exit 1（零命中，2530 行全文）
grep -n 'Status:\|Revision\|Corrected\|corrected\|Fixed:\|changelog\|Changelog' <plan> -> 仅命中若干
   代码字面量 `ownerStatus: "current"` 等误报，无一处是文档级修订声明
git log --oneline -- <plan>           -> 唯一一笔 `6a32c80 docs: turn the L1 lease/heartbeat
                                          spec into a 13-task implementation plan`（只创建，从未修订）
```
**这个文件根本不带「修订总账/计数/全称限定」这种文首结构。** F-M4 原文用「两个同形文件」概括，**对 plan 这一半不成立**——它无账可查，因此无所谓「过期」。

### 2.4 佐证：全仓再无第三个同形文件

`docs/superpowers/specs/*.md` 头部逐个核对（见 §6 命令），只有三份文件带「Amended <日期> in N places (a)-(x)」这种字母索引式修订总账：L1 spec（本节，验真）、L1b spec（包 3 已修，`(f)` 已补入头部）、L2 spec（包 3 已修，即 I-1 那处）。`docs/superpowers/plans/*.md` 头部逐个核对，**没有一份带这种结构**。**=> F-M4 担心的「未验证的同形过期账本」今天在全仓范围内已不存在——三份 spec 全部验证过（两份在包 3 修过，一份在本次验真），零份 plan 带这种结构。**

## 3. A 组 9 条逐条

行号锚点均为 **2026-08-07 实测值**（今天的 HEAD `2f8f2d8`），用逐字引用定位，不信旧行号。

### T1-M2 — `plan:2020` 粗体嵌套把正确读法排成非粗体
文件：`docs/superpowers/plans/2026-08-02-sweep-and-transactional-continuation.md`（今天仍是 `:2020`，此文件自 Task1 后未再被 T2/T3 触碰，行号未漂移——`git diff --stat` 见 §1 附表可核）。
```
:2020 **Amended 2026-08-05：上表 ...已被人裁推翻 —— C3 侧的回调是**当场 `options.stderr(...)`**，
      不是数组 push。读作「C3（回调=当场写 stderr）」。** 这纠正的是...
```
逐字比对 `**` 配对：加粗从「Amended…回调是」开始，在「是」后闭合；「当场 `options.stderr(...)`」这段**正确读法**落在两个 `**` 之间，**渲染为非加粗**，随后又重新加粗到句尾。**今天仍成立，未被修。**
**今天是否仍成立**：结论腐坏——否（原判断仍对）；论据腐坏——否；**纯排版缺陷，未变化**。
**撞车面**：纯 `docs/` 文本，包 1 权限内可改，改动是单行 `**` 位置调整，零语义风险。
**建议处置**：**可顺手并入包 1**（如果包 1 会读/引用这份 plan；若不读，留给包 2 也可以，成本一样低）。

### T1-M3 — `spec:673`「比原句更强」略被说满
文件：`docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md:673`（行号未漂移，Task2/Task3 未触碰此段落——见 §1 附表 `git diff` 输出）。
```
:673 ...**即今天的落脚点比原句更强，不是更弱。** 落点改写的完整理由见本文档 §9 模块表...
```
**今天是否仍成立**：论据腐坏方向——未变化，今天仍是同一句强断言，未加「本层内」一类限定语。是否需要收窄措辞是文字风格判断，不影响正文事实。
**撞车面**：纯 `docs/` 文本。**建议处置**：留给包 2 或与其它文字类 Minor 一起批处理；非阻塞，不建议单独在包 1 里改一句话。

### T1-M4 — `fileStore.ts` 注释把跨两层调用方写死进本层注释【做不了在包 1】
文件：`src/persistence/fileStore.ts:469`：
```
:468-470  // The callback deliberately does NOT get this treatment: its body is inside this layer's
          // control — a single synchronous call into a sink the caller injected (sweepRuns passes
          // SweepOptions.stderr), touching no filesystem and awaiting nothing — ...
```
**今天是否仍成立**：是——`sweepRuns` 字面仍硬编码在注释里，未改写成弱化措辞（如 review-task1.md 建议的「the caller's sink（today: sweep's stderr）」）。
**撞车面**：**`src/` 文件，包 1 落盘面只写 `docs/`（人裁 6/Q2），这条在包 1 里做不了。**
**建议处置**：**必须留给包 2**（包 2 有 `tests/` 授权，可与钉住它的测试一起改）。

### T1-M5 — 站点 10 是唯一没有回指指针的站点【做不了在包 1】
同一段注释（`fileStore.ts:440-482`）核对：全段没有任何一句指回 `docs/superpowers/specs/2026-08-01-...-design.md §4.3/§9` 或 `plan Task C3`——`grep -n 'see §\|see docs/superpowers' src/persistence/fileStore.ts` 在这段范围内零命中（我读了完整 440-490 行，逐字确认无回指句）。
**今天是否仍成立**：是，`plan:878` 单方面声称「已按本条同步」，但代码注释侧仍无回链。
**撞车面**：修法是往 `fileStore.ts` 加一行注释，**`src/` 文件，包 1 做不了**。
**建议处置**：**留给包 2**，可与 T1-M4 一起顺手处理（同一段注释、同一次编辑窗口）。

### T2-M1 — `spec:209` 无限定全称否定
```
:209 ...该行已在第二轮就地更正，并自带「不要把这一行当成『已修好』继承下去』的警告，
     今天不存在会被 L5 无察觉继承的风险。
```
`grep -n '今天不存在会被 L5 无察觉继承的风险' <spec>` → 唯一命中 `:209`，行号与 T2 报告一致（此段落之后未被 T3/final-fix 触碰，见 §1 T3 diff 摘要不含 `:209` 附近）。
**今天是否仍成立**：是——无限定全称断言原样保留，评审员当时的评价「证据只覆盖一行」仍适用（我没有去扩大证据面，只核实文字本身未变）。
**撞车面**：纯 `docs/`。**建议处置**：留给包 2 批处理，或包 1 若认为这句会误导 L5 spec 的读者可顺手加限定语（低优先级）。

### T2-M2 — `spec:209` 前后半句缺转折词
同一行 `:209`：「该行已在第二轮就地更正，并自带...警告，今天不存在...风险」三个分句直接并列、无转折连词，读感冲突——**今天原文逐字未变，缺陷仍在**。
**撞车面**：纯 `docs/`，同一行、同一次编辑即可与 T2-M1 一起处理。**建议处置**：与 T2-M1 合并处理，留给包 2 或包 1 顺手。

### T2-M3 — `spec:1153` 用「上一句」位置锚点
今天实测行号已漂移到 **`:1155`**（Task3 的 D12 在前面插入 2 行所致，见 §1）：
```
:1155 **Amended 2026-08-05：上一句对债 3 的归类已被 §19 的 G10 更正。** ...
```
**今天是否仍成立**：是，仍用「上一句」而非符号/逐字锚点。**这本身印证了 brief 铁律 6（行号会腐坏）——旧引用 `:1153` 今天已不指向这句，必须用逐字定位。**
**撞车面**：纯 `docs/`。**建议处置**：留给包 2；若包 1 要引用这段作为 L5 输入，顺手把「上一句」换成具体引用会降低后续腐坏风险。

### T3-M1 — `(k)`/`(l)` 把「债 3 也归 L3」误指到不含该结论的 `:246`/`:248`
文件：`docs/superpowers/specs/2026-07-28-run-registry-design.md`（L2），`(k)`/`(l)` 均引用
`docs/superpowers/decisions/2026-07-29-technical-debt-attribution.md:246, :248`。核对该裁决文档今天这两行：
```
:246  2. **L5 继承清单第 1 条描述有误**：「reconciliation 合成责任无人认领」不成立，见债 1。...
:248  4. **L5 继承清单现在只剩 1 笔**（债 2），不是 4 笔。
```
**今天是否仍成立**：是——`:246` 讲的是**债 1**，`:248` 只给出汇总结论（只剩 1 笔），**都不含「债 3 归 L3」这句具体结论**；真出处是文档更前面的「结论速查表」（`:20`：`| 3 | ... | **L3** | ... |`）。**引用位置仍然是错的。**
**撞车面**：纯 `docs/`（改的是 L2 文件里 `(k)`/`(l)` 的引用行号）。**建议处置**：**候选顺手项**——L2 是 L5 的直接输入文档，包 1 大概率要读它；若包 1 已经在读这两条注，顺手把 `:246/:248` 改成正确锚点（`:20` 或改引"结论速查表"这个符号名）成本很低。否则留给包 2。

### T3-M4 — 报告假前提「(j) 用了两次」，产物侧是否有剩余动作
```
docs/superpowers/specs/2026-07-28-run-registry-design.md:38
- **(j)** §8.1, §13 item 4 — **the two "Atomic? no" rows are no longer true, ...
:10-14 **Count**: "in eight places (a)–(h)" is stale — the Index below now runs to
       (l), twelve entries, not eight. ... (j) and (k) each say explicitly ...
```
I-1 的修复注（`:7-17`）已经显式讲清楚 `(j)` 是「一条勘误、两个落点（§8.1 与 §13 item 4）」，且明确数出 a–l 十二个字母各恰好出现一次。
`grep -rn '(j) 用了两次\|(j) 重复' docs/ .superpowers/` → 唯二命中都在 `pkg3-doc-errata/progress.md`（历史台账自己的记账文字，非活文档断言）。
**今天是否仍成立**：假前提本身早已判假；**是否还有剩余动作 = 否**，产物侧（L2 文件本身）已经把 `(j)` 讲清楚，没有任何活文档仍持有「(j) 用了两次」这个错误说法。
**撞车面**：无需改动。**建议处置**：**关闭，无剩余动作**。

## 4. B 组 F-M2 / F-M3 逐条

### F-M2 — 本分支内部日期口径不一致（规约债：落注惯例从未写成文字）

台账 `:517` 原文：「本分支内部日期口径不一致（规约债 —— 本仓库的落注惯例从未写成文字）」。
再评审员在包 3 里独立确立的口径是：「`Amended <日期>` = 该注自身被提交的那个日历日」
（`re-review-task3-fix1.md`，用 `git log -S` 对多个样本回溯验证）。**但这条口径今天仍只活在
两份会话台账（`pkg3-doc-errata/progress.md`、其复核报告）里，没有落进任何一份「本仓库约定」
类的常设文档。** 核验：
```
find .superpowers -maxdepth 1        -> 只有 sdd/ 一个子目录，没有 conventions/housestyle 类文件
grep -rl 'Amended' docs/superpowers/decisions/  -> 无命中（决策记录用别的格式，不用 Amended 标签）
find docs -maxdepth 2 -iname '*convention*' -o -iname '*housestyle*' -o -iname '*house-rule*' -> 空
```
**今天是否仍成立**：是——这条口径依然「只存在于台账，未写成文字」，规约债原样成立，**没有腐坏也没有被还清**。
**撞车面**：如果要「写成文字」，落点应该是某份仓库级约定文档，**今天这样的文档不存在，等于要新建一个**——这超出「新建 L5 spec ＋ 必要的就地勘误」这个包 1 落盘面的字面授权，是否算「必要」需要人裁。**建议处置**：需人裁——是否借包 1 顺手新建一份约定文档，还是继续留作规约债。

### F-M3 — `spec:209` 的改写超出字面授权，但判在界内

台账 `:518`：「`spec:209` 的改写超出字面授权，但判在界内」。这是整分支评审员对 Task2 站点 A
（`spec:209`，即 T2-M1/M2 所在的同一处改写）越权程度的一个**已裁定**的观察，不是待修复项——
判决已经是「在界内」，没有开放的正确性问题。**今天是否仍成立**：这是一个历史性质判断
（"当时的改写方式超出了 brief 字面给的两种选项之一"），文字本身不会随时间腐坏；`spec:209`
内容自 Task2 收口后未再变动（见 §1 附表，Task3/final-fix 的 diff 均不含 `:209` 附近）,
**判断的适用对象没有漂移**。
**撞车面**：无——这是已闭合的判断记录，不涉及任何文件需要修改。
**建议处置**：**存档，无剩余动作**；如果包 1 的 spec 写作规范里要提炼「就地注解 vs 直接改写」的
边界经验，这条可以作为一个已验证的先例引用，但这是可选项，不是必须。

## 5. 与包 1 的撞车面（哪些能顺手并进、哪些做不了）

包 1 落盘面 = 只写 `docs/`（新建 L5 spec ＋ 必要的就地勘误），`src/` 与 `tests/` 一字不动
（`.superpowers/sdd/2026-08-07-pkg1-l5-spec/progress.md` 人裁 6/Q2 原文）。逐条汇总：

| ID | 文件层 | 能否在包 1 落盘面内改 | 建议 |
|---|---|---|---|
| T1-M2 | `docs/` (plan) | 能 | 顺手可并（单行 `**` 位置修正，零风险） |
| T1-M3 | `docs/` (spec) | 能 | 非阻塞，留给包 2 批处理更合适 |
| T1-M4 | **`src/`** | **不能** | **必须转包 2** |
| T1-M5 | **`src/`** | **不能** | **必须转包 2**（可与 T1-M4 同一次编辑一起做） |
| T2-M1 | `docs/` (spec) | 能 | 低优先级，可与 T2-M2 合并处理 |
| T2-M2 | `docs/` (spec) | 能 | 同上，同一行 |
| T2-M3 | `docs/` (spec) | 能 | 若包 1 引用此段作 L5 输入，顺手把「上一句」换成逐字引用 |
| T3-M1 | `docs/` (L2 spec) | 能 | **候选顺手项**——L2 是 L5 直接输入文档，若包 1 已在读它，顺手修锚点成本低 |
| T3-M4 | 无需改动 | — | 已关闭 |
| F-M2 | 需新建文档 | **边界不清** | 超出「新建 L5 spec ＋ 必要勘误」字面授权，需人裁是否算「必要」 |
| F-M3 | 无需改动 | — | 已闭合的判断，无剩余动作 |
| F-M4 | 无需改动 | — | 本轮已验真，无剩余动作 |

**唯二明确做不了的**：T1-M4、T1-M5（都落在 `src/persistence/fileStore.ts` 的注释里）。
**唯一边界不清、需人裁的**：F-M2（要不要在包 1 里顺手新建一份「落注惯例」的仓库级约定文档）。
其余各条都是 `docs/` 内的文字修正，技术上都能在包 1 落盘面内做，是否「顺手」取决于包 1
写 L5 spec 时是否本来就要读/引用那份文件——这是内容判断，我没有替包 1 做取舍，只标出候选。

## 6. 我自己发现的同形项（清单外）

**负面证据，支持 §2 的 F-M4 结论**：`docs/superpowers/plans/*.md` 全部 16 份文件的文首（`sed -n '1,10p' | grep -E 'Amend'`）逐份核对，**零份**带「Amendments: N / Amended <date> in N places」这种字母索引式修订总账结构；`docs/superpowers/specs/*.md` 全部 16 份同样核对，**只有 3 份**带这种结构（L1 `2026-07-26`、L1b `2026-07-27`、L2 `2026-07-28`），且三份**都已核实过**（L1b、L2 在包 3 里修过；L1 本次验真）。**=> 这种「同形过期账本」的全仓风险面已经清零，不只是 F-M4 点名的那两个文件。**

**⚠️ `docs/handoff/handoff.md` 自己带一个同形但尚未清扫的活例（不在清单上，我自己撞见的）**：
```
handoff.md:7    本会话从未 `git push`，远端落后
handoff.md:34   git ls-remote origin refs/heads/main    # 本会话从未 push，远端落后
handoff.md:50   1. 未 push。本会话从未跑过 `git push`，远端落后若干笔。
handoff.md:236  1. 没有 push。本会话从未跑过 `git push`；远端落后本地若干笔。
```
`.superpowers/sdd/2026-08-07-pkg1-l5-spec/progress.md:30-39`（**同一天**，包 1 自己的 STEP 0）
已经用 `git rev-parse HEAD` / `git ls-remote` / `git reflog` 三条命令实测证伪这句话——远端**没有**
落后，第 5 次会话外推送已发生——但那份更正**只写进了包 1 自己的台账**，`handoff.md` 里这四处
「远端落后」原样未动。`handoff.md:128` 本身还留着一条对**更早一版**同一断言的作废记录
（「远端落后本地若干笔」——**远端与本地已一致**），**证明这句话已经腐坏过不止一次，
每次都是改一处、留下另几处**——与本轮台账反复记账的「同族一改一没跟」（I-1、I-3 那个形状）
**完全同形**，只是这次的宿主文件是 `handoff.md` 而不是某份 design spec。
**这条不在我的委任范围内**（brief 明确切我的是包 3 台账的 12 条 deferred minor，不含 handoff.md
本身），**原样上报，不建议我自己动手改**——留给控制器判断要不要点名。

## 7. 我没做完的事

1. **F-M2（落注惯例未成文）的检索面不完整**：我只查了 `.superpowers/` 顶层、`docs/superpowers/decisions/`、
   以及几个「convention/housestyle」命名模式，**没有**通读 `CLAUDE.md`、`docs/handoff/handoff.md` 全文、
   或对 `docs/` 全树做无限定词的开放式语义检索去找「有没有某处已经用别的措辞写过这条规则」。
   **明写**：我下的「F-M2 今天仍成立、规约债未还」这个结论，检索面止于「找显式的 convention 类文件」，
   没有覆盖「规则可能以非独立文档的形式散落在某处正文里」这一面。
2. **T1-M3/T2-M1（「比原句更强」「无限定全称否定」）我只核了文字是否原样保留，没有重新做深度语义判断**——
   即没有独立评估「这句话到底该不该改」「全称否定有没有反例」，因为这是内容判断而非「文本是否腐坏」，
   brief 要我查「今天是否仍成立」，我按「文字/论据是否被后续改动推翻」这个口径处理，**没有**重新扮演一次
   评审员去判定这些 Minor 的措辞建议本身是否恰当。
3. **§6 找到的 `handoff.md` 「远端落后」同形项，我只核了 4 处直接命中「远端落后」的行**
   （`grep -n '远端落后\|远端没有落后'`），**没有**扩大到同义表达（比如「未同步」「落后本地」以外的措辞、
   或该文件里其它可能同样过期的时间戳/提交数声明）——如果那类表达还有别的写法，我没有搜到。
4. **没有检查 `docs/superpowers/decisions/` 与 `docs/handoff/` 目录下是否还有别的文件带字母索引式修订总账**——
   §2.4/§6 的「全仓扫描」只覆盖了 `docs/superpowers/specs/` 与 `docs/superpowers/plans/` 两个目录，
   **没有**扩展到 `decisions/`、`handoff/`、`.superpowers/sdd/` 下的历史台账（后者数量很大，
   且台账本身是过程记录，是否该用同一判据核验我没有把握，故未做）。
5. **Token 预算**：本任务的检索深度（定位 L1 文件身份、逐条核验 9+3 条 minor、通读整份 628 行 pkg3 台账、
   扫全部 32 份 specs/plans 头部）明显超出 CLAUDE.md Rule 6 的 12,000 token 单任务预算，**未在中途停下
   重开**，属于静默超支未提前止损——如实记账，供人裁是否需要调整后续同类扫描的任务切分粒度。
6. **没有跑测试套件**——这是遵照 brief 授权边界，不是遗漏。
7. **T3-M1 的修法建议（改 `(k)/(l)` 的引用行号）没有验证改成什么最合适**——我确认了 `:246`/`:248` 不含
   该结论、真出处在「结论速查表」附近（`:20` 一行），但没有去确认那张表今天的确切行号是否也已漂移，
   因为这已经越出「核验今天是否仍成立」的范围，进入「怎么修」的领域，按授权边界不由我处理。
