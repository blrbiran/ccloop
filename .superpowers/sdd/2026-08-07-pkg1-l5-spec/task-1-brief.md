# Brief — 包 1 任务 1：写 L5 spec 的本职部分（cleanup / orphan GC）

你是实施者。你的产物会被一名**未参与过本轮任何一条**的独立评审员逐句核。
**本仓库不接受实施者自证** —— 你写下的每一句事实断言，都会被别人拿命令去验。

--------------------------------------------------------------------------------
## 0. 授权面（超出即违规，不许推定）
--------------------------------------------------------------------------------

**你只写 `docs/`。`src/` 与 `tests/` 一字不动。**（人裁 6 / Q2：包 1 写 spec，不写代码；
`SweepOptions.stderr` 的类型旁注释与钉住它的测试都推给了包 2，那里有 `tests/` 授权。）

被授权的两处落盘：
1. **新建** `docs/superpowers/specs/2026-08-07-cleanup-and-orphan-gc-design.md`
2. **就地勘误一条**：`docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md`
   §10 第 4 条那个已过期的数字（详见 §4 任务 F）。
   **就地注解、原句一字不动** —— 这是本仓库的既有立场，不是可选风格。

**不许**改任何台账、不许改 `docs/handoff/handoff.md`、不许改其它 spec/plan。

--------------------------------------------------------------------------------
## 1. 铁律（本仓库拿缺陷换来的，不得软化）
--------------------------------------------------------------------------------

1. **不接受自证。** 每条事实断言都要能被别人用命令重推。
2. **绝不过滤验证输出**（`grep` 与 `tail` 同罪）。包 3 有过代价被当场量化的先例：
   一条自查输出被摘要成一行，**而那条必修 finding 恰好藏在被摘要掉的部分里**。
3. *** 下「没有任何一处 / 全都 / 从未」之前先确认 grep 面覆盖你断言的范围。 ***
   要么验到覆盖，要么**明写你没验哪一面**。**这个形状在本仓库一轮内栽了 6 次。**
4. *** **spec 正文本身也是断言。** *** 包 3 的教训第 4 条逐字：写每一句前先问 ——
   **今天为真吗？我验过吗？与同一份文档里我自己写的其它句一致吗？**
   （包 3 任务 1 的勘误正文自己引入了一句新的不实描述，还与同一笔提交里自己写的兄弟勘误矛盾。）
5. **每个数字旁附一条能重推它的命令与该命令当时的输出。报不出可重数的计数就不要报数字。**
6. **行号会腐坏**（本仓库已实测腐坏到第三层）。**一律用逐字引用 ＋ 符号名定位**，
   行号只作辅助并标注「<日期> 实测」。
7. **发现给你的前提在今天不成立 —— 原样上报，不许自改判据、不许自裁分级。**
8. **超出 CLAUDE.md Rule 6 的 12,000 token 预算要明写，不许静默超支。**

--------------------------------------------------------------------------------
## 2. ⚠️ 落盘协议（强制。上一会话 12 名 agent 死了 6 名，全部发生在准备落盘时）
--------------------------------------------------------------------------------

**在做任何检索之前**，先 `Write` 一份**只有小节标题的骨架**到 spec 路径并立刻落盘。
之后**每次 `Edit` 只填一节（≤120 行）**，**最有把握的一节最先写**。
该协议已两次救场：一名评审员的 454 行报告在写最后一节时被中断，**已落的十节一字未损**。

--------------------------------------------------------------------------------
## 3. 起点与既有裁决（**不要重开，不要从零 brainstorm**）
--------------------------------------------------------------------------------

**先读这三份，它们是你的输入，不是参考读物：**
- `.superpowers/sdd/2026-08-07-pkg1-l5-spec/progress.md` —— 本轮台账，含人裁 6 / 7 与两份扫描的裁断。
- `.superpowers/sdd/2026-08-07-pkg1-l5-spec/scan-2-design-input.md` —— 设计输入的现状核验（528 行）。
- `.superpowers/sdd/2026-08-05-l5-input-scan/progress.md` —— L5 输入盘点，含人裁 1–5 与三个工作包。

**人裁 3 定死的起点**：`docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md` **§10 第 4 条**
（崩溃残留的临时文件永久留在 run 目录、两条清理路径都够不着、**清理归属未分配**）。
**⚠️ 逐字读它，包括那段「定性要准确」的约束。**

**人裁 7 的四条边界**（已定，照做，不要重新论证）：
1. **只写本职**，另加一节「继承项指针」——**只放指针、不放内容**，指向 L5 输入盘点台账。
   理由：本仓库已有「两份交接清单并存、彼此无指针」的真实文档缺陷，**不许造出第三份**。
2. **`retained` 半边写成完整的一半，不是脚注。**
3. **P1 写成不变式**（见 §4 任务 A），不是待裁选项。
4. **§10 第 4 条那个过期数字顺手落一条就地勘误**（见 §4 任务 F）。

--------------------------------------------------------------------------------
## 4. spec 必须做到的七件事
--------------------------------------------------------------------------------

### 任务 A —— 第一节必须是 P1（人裁 3 逐字明令）

**内容**：`releaseOwnerLease` 全仓**只有 `stop()` 一个生产调用者**
（控制器 2026-08-07 实测：`grep -rn 'releaseOwnerLease' src/ tests/` 在 `src/` 内只有
`leaseHeartbeat.ts:16` 的 import 与 `:254` 的唯一调用，`:254` 落在 `const stop = async () => {…}` 体内）。
**含义**：*** **L5 的本职 GC 一旦自己回收租约，§13 第 3 笔立刻从「未发现可达路径」升级为数据丢失 ——
不需要任何人碰 `stop()`，也不需要常驻形态落地。L5 的正常工作方式恰好会触发它。** ***

**按人裁 7 边界 3，把它写成本 spec 的一条不变式**：**L5 的 GC 不得自行回收租约。**

**同节必须写进那条耦合**（上一轮四份扫描与两条评审车道**都没点出来**）：
第 3 笔「今天不重开『保留即放宽』人裁」的理由**完全依赖「span 外」这个结构**；
*** 若为修第 3 笔把 `writeBoundaryArtifacts` 搬进 span，逃逸点就跑到写的后面，该理由失效，
那条人裁**真的会重开** —— **两者必须一起决策，不能分两轮。** ***

### 任务 B —— 两份委任状，不是一份

L5 受**两份**委任状约束。**逐字引用两份，并自己核实路径与节号**（⚠️ 上一轮控制器把委任状错指到
`2026-07-28-run-registry-design.md` §17，而**该文件止于 §15**，被扫描员当场证伪 ——
**不要采信任何转述，自己跑命令确认**）：

1. `docs/superpowers/specs/2026-07-22-ownership-and-reconciliation-boundaries-design.md` **§17 item 3**
   —— 「**Cleanup / orphan handling design** — how superseded or lost-owner workspaces and evidence
   are **retained** or **cleaned up** safely.」
   *** **同时授权 `retained` 与 `cleaned up`。九份报告一次没提「保留」这半。人裁 3 明令不许只想着删。** ***
2. *** **第二份，九份报告全都没提过** ***：
   `docs/superpowers/specs/2026-07-21-stop-no-progress-stale-boundaries-design.md` **§15 item 3 ＋ §13**
   —— 它对 L5 下的是**禁令与义务**，控制器已复现两句逐字：
   `:297` 「but they may not treat "stale" as permission to delete them.」
   `:299` 「Later cleanup design must consume stale/reconciliation output explicitly rather than
          being fused into it.」
   **方向与第一份互补，不重复。spec 必须同时满足两份。**

### 任务 C —— `retained` 半边（人裁 7 边界 2：完整的一半）

已知的实体（**逐条自己复核，别照抄我这张表**）：
- `src/runtime/types.ts:67` `cleanupStatus: "retained" | "removed"` —— 清理失败即保留，
  并落 `workspace_cleanup_failed` 事件。**这是一个今天就活着的保留判定。**
- 任务 B 第二份委任状的两条禁令/义务。
- 扫描员 2 报告 §4 另列了四条以上「不许删」的既有约束 —— **去读，逐条核**。

**spec 要回答**：什么该保留、保留多久/直到什么条件、谁来判、保留与删除的边界在哪、
以及**「显式消费 stale/reconciliation 输出」这条义务在设计上怎么落**。

### 任务 D —— 今天的事实，不是文档里的旧事实

扫描员 2 已核实、**你必须用今天的数字而不是 §10 第 4 条的旧数字**：

| 事实 | 今天的值（控制器已独立复现） |
|---|---|
| `cleanupOwnerTransferStagingWithoutMarker` 清几个固定名 | *** **10**，不是 §10 写的四个 *** （`awk '/^async function cleanupOwnerTransferStagingWithoutMarker/,/^}/' src/persistence/fileStore.ts \| grep -c "await safeUnlink"` → `10`） |
| 残留面的演化 | 4 → 7（`0f940ea`）→ 10（`dad8a14`），**两笔都在 2026-08-02** |
| 该设计文档最后一次被碰 | `2e30d1c` **2026-08-01**；`grep -c "Amended"` → **0** |
| 临时文件名 | **三种写法在流通**：§10 第 4 条的五段形 `.{basename}.{pid}.{startTime}.{seq}.tmp`（**正确**）／台账与 handoff 的四段 `{stamp}` 形（**错，但源码常量确实叫 `ATOMIC_TEMP_PROCESS_STAMP`**）／§4.1 的另一种四段形。**引用时必须指明引哪一种，并以源码实际生成为准 —— 自己去读生成代码** |
| L1 分层表给 L5 的授权 | `docs/superpowers/specs/2026-07-26-run-lease-and-heartbeat-design.md:41` `\| L5 \| cleanup / orphan GC \| deletion \|`。⚠️ **扫描员明写只能证「只有 L5 被显式标注为 deletion」，不能证「只有 L5 可能删东西」**（现成反例：`git worktree remove --force` 是实质删除却不叫 deletion）。**不许把它写成无限定的全称断言** |

*** ⚠️ 必须原样保留的定性约束 ***：§10 第 4 条逐字写着
**「定性要准确：这是无界垃圾，不是故障」「不要把它上报成缺陷」**，且扫描员 2 复核后**判定该约束今天仍然成立**
（未找到任何功能性破坏路径）。
*** **spec 不许偷偷把它升级成缺陷来给自己找立项理由。** *** 若你在设计过程中真找到了功能性破坏路径 ——
**那是重要发现，原样上报给控制器，不要自己改定性。**

### 任务 E —— 风险面必须写进一条：一道无法证明兑现过的防线

§10 第 3 条写「`writeOwnerTransferRecord` 仍可被误用：只加了注释，没有机制强制。
**真正的防线是 L3 的评审**」。
扫描员 2 的核验：五份 L3 门报告**全在盘上**（推翻了上一轮「门报告没落盘」那个前提），
**但 `grep -c "writeOwnerTransferRecord"` 对五份门报告全部为 0** ——
**缺的不是报告，是报告与 §10 第 3 条之间的对应关系**。今天该函数**零生产调用者、仍 `export`、
无任何测试或 lint 钉住它**。
=> **spec 要把「靠人评审兜住、事后无法证明兑现」这类防线记为 L5 的风险面**，
并说明 L5 自己的设计**不应再依赖同型防线**。

### 任务 F —— 就地勘误（授权范围内的唯一一处改动）

给 `docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md` §10 第 4 条落一条就地勘误：
**「只清 `getOwnerTransferPaths` 的四个固定名」今天是十个**。
- **原句一字不动**，紧随其后加注（本仓库体例：粗体 `**Amended <日期>：**` 开头）。
- 日期用**你落这条注的那一天**（本仓库已由 `git log -S` 独立确立：`Amended <日期>` = 该注自身被提交的日历日）。
- **注文本身也是断言** —— 每一句都要能被重推，附命令与输出。
- **该条要证的事（够不着 `buildAtomicTempPath` 生成的名字）仍然成立**，坏的只是数字。
  **不要顺手推翻它的结论，也不要过度勘误** —— 本仓库立场：**过度勘误本身也是缺陷**。

### 任务 G —— 继承项指针一节（人裁 7 边界 1）

一节，**只放指针、不放内容**：L5 继承但**不在本 spec 解决**的输入（包 2 的三条今天可达的数据丢失、
§13 第 3 笔、组 B 两条债等），一句话指向 `.superpowers/sdd/2026-08-05-l5-input-scan/progress.md`。
**理由必须写进 spec**：本仓库已有「两份交接清单并存、彼此无指针」的真实文档缺陷，本节存在就是为了不造第三份。

**⚠️ 不要在这一节里重述那些项的技术内容** —— 重述就是造第三份清单，与本节目的自相矛盾。

--------------------------------------------------------------------------------
## 5. 文档体例（照抄本仓库既有 spec 的形状）
--------------------------------------------------------------------------------

看 `docs/superpowers/specs/2026-07-26-run-lease-and-heartbeat-design.md` 的开头四行，
文首块的形状是：

```
# <Title>

> Status: <proposed on …>
> Scope: <一句话，做什么、不做什么>
> Parent design: [`…`](…)  ／ Sibling design: […]
```

⚠️ **不要**在新文件文首写一个「Amendments: …」总账 —— **新文件今天零修订**，
写一个空总账等于给未来埋一条会腐坏的计数（**本仓库刚因这个形状买单过一条必修 Important**）。

--------------------------------------------------------------------------------
## 6. 交付契约
--------------------------------------------------------------------------------

一并写一份报告到 `.superpowers/sdd/2026-08-07-pkg1-l5-spec/task-1-report.md`，含：

```
## 0. 我做了什么（最先写）
## 1. 任务 A–G 逐项落点（每项：写在 spec 哪一节、承重断言、重推它的命令与输出）
## 2. 我自己核实过的事实清单（命令 + 输出，未过滤）
## 3. 我的 concern —— 我认为 brief 判错了、或我不确定的地方（强制位，没有就写「无」）
## 4. 我没做完的事 —— 逐条明写，不许拿部分证据凑完整结论
```

**§3 与 §4 是硬要求。** 本仓库把「自报未完成」「原样上报 brief 有误」当**正面行为**记录在案：
上一轮有扫描员当场证伪了派单方（控制器）的前提，那被记为正面样本。
**发现我给的前提错了 —— 直接说，不要替我圆场，也不要自己改判据。**

**提交**：改动自己提交一笔（`git add` 具体文件，不要 `git add -A`），提交信息说清做了什么。
**不要合并、不要碰其它分支。**

最后回给控制器的文本 = **≤30 行摘要**（做了什么 + 承重断言 + concern + 没做完的事），
**不要把 spec 或报告贴回来**（都已落盘，控制器会自己读）。
