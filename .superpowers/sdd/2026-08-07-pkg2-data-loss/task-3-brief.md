# 任务 3 brief — 第 1 笔（锁可被偷 / P-READ）：**只读设计提案**，不写代码

## 你交付的是方案，不是补丁

*** **本任务只读。`src/` `tests/` `docs/` 一字不许改，不许 commit 代码。** ***
你唯一可写的是自己的报告。**产出是一份供人裁的修法方案**，人裁之后才会有人去实施。

**为什么不直接实施**：台账逐字记着 —— 第 1 笔的性质已变，**修法是一次牵动 L1/L2
「读不许写」契约的设计裁决，规模比原文描述大一个量级**。这种裁决不由实施者顺手做掉。

⚠️ **另有一名评审员可能正在同一 worktree 里做临时代码变异**。所以：
**你不要跑会被别人改动干扰的验证**，也**不要因为看到套件红就下结论**——
若你观察到与你无关的红，**如实记下并标注「可能是并发变异导致，未归因」**，不要当成发现。

## 背景（台账已确立，可当既成事实，但承重处仍要自己核）

- 第 1 笔原本是「锁可被偷」（`acquireOwnerTransferLock` 那条入口，六步构造）。
- *** **它已经扩到 recovery 路径，且面严格更宽。** ***
- *** **P-READ 路径根本不经过第 1 笔的缺陷** —— 把 `tryRecoverStaleOwnerTransferLock` 的
  活进程检查补上，**它照样存活**。 *** 这是最要紧的一条：**只修锁协议治不了它。**
- 相关符号（**用符号名定位，不要用行号**）：`acquireOwnerTransferLock`、
  `tryRecoverStaleOwnerTransferLock`、`recoverInterruptedOwnerTransfer`、
  `finalizePendingOwnerTransfer`、`readOwnerRecord`、`readOwnerRecordWithoutRecovery`。

**输入（按需读节，不要通读；上一轮实施者因必读材料体量过大破了预算）**：
- `.superpowers/sdd/2026-08-05-l5-input-scan/close-2-recovery-path.md` —— 875 行，
  第 1 笔扩面、P-READ、G0–G3 四格、G2-null 都在里面。**这是主输入。**
- 同目录 `scan-A-debt2-lock-abort.md` —— 第 1 笔的原始六步构造。
- `.superpowers/sdd/2026-08-07-pkg2-data-loss/progress.md` 的 **§5**（G2-null 措辞已被收紧：
  **没有测试钉住的是 `parsePid` 与 `pid:<pid>:<origin>` 形式的不匹配，
  不是「该形式无人断言」** —— 后者为假，两处测试逐字断言了该形式）。
- **任务 2 刚落地的所有权守卫**（`persistTerminalState` 内的 `foreignOwnerOf`）——
  读它，**你的方案不能与它打架**，能复用就说清怎么复用。

## 你的方案必须回答这些（缺一条就是不完整）

1. **面到底有多宽**：今天第 1 笔可达的入口有哪些？逐个具名。
   ⚠️ **这是全称性质的断言，按纪律办**（见下），或者明说「这是我搜到的面，不是完备性证明」。
2. *** **P-READ 怎么办** ***：它不经过锁协议的缺陷。你的方案covers 它吗？
   如果不 covers，**明说**，并说明剩下的口子有多大。
3. *** **对 L1/L2「读不许写」契约的影响** ***：这是本任务的核心裁决点。
   动了它会牵动谁？有没有不动该契约的替代方案？**两条路都要摆出来，不要只给你偏好的那条。**
4. **与任务 2 守卫的关系**：重叠？互补？冲突？能否复用 `foreignOwnerOf` 的「只在正面识别出
   异己 id 时才拒绝」这个立场？
5. **分阶段的可能**：有没有一个**小而独立、今天就能安全落地**的子集，
   把大裁决留给以后？（若有，这往往是最有价值的答案。）
6. **每个方案给出代价**：改动面、会弄红哪些既有测试（估计即可，标明是估计）、
   有没有触碰任何「Human ruling」产物。
   ⚠️ 本包已知的两个具名例外（人裁 13 / 人裁 14）**只针对已指明的那几条测试**，
   **不得外推**；你的方案若需要动别的既有判据，**必须单独列出来交人裁**。

## 铁律

⚠️ *** **落盘协议**：先 `Write` 只有小节标题的骨架并**立刻落盘**（在此之前不做任何检索），
之后每次 `Edit` 只填一节，**结论一节最先写**。 ***

⚠️ *** **探针纪律**：探针写成脚本**先落盘**，再 `rtk proxy zsh <script>` 跑，
**同跑内放一条必命中的 sanity 探针**；**验证跑绝不过滤输出**；
**下全称否定前先确认 grep 面覆盖你断言的范围**；**零输出时先验命令本身**
—— *** 一条被弄坏或被过滤的探针永远不能证明「不存在」 ***（三方三次同形，全栽在这一步）；
**锚点用符号名不用行号**。 ***

- **不接受既有报告自证**：`close-2-recovery-path.md` 的承重结论，你要在今天的代码上重核。
  **它是线索，不是结论。**
- **拿不准就写「无法判定」**，不许硬下。上一轮扫描员 2 在一个语义问题上答「无法判定」，
  **那被记成了正面样本**。

## 交付

**报告**：`.superpowers/sdd/2026-08-07-pkg2-data-loss/task-3-design.md`，
**必须 `git add -f`**（该目录 `.gitignore` 是 `*`）。

**骨架（先落这个）**：
1. 结论（**最先写**）：推荐方案一句话 ＋ 它 covers 什么、不 covers 什么
2. 第 1 笔今天的可达面（含判别式与命令输出）
3. P-READ：为什么锁协议治不了它 / 我的方案对它做了什么
4. **L1/L2「读不许写」契约：动 与 不动 两条路，各自的代价**
5. 与任务 2 守卫的关系
6. 分阶段方案（若有小而独立的子集，放这里）
7. 每个方案的代价与风险（含会不会碰到 Human ruling 产物）
8. 我用过的每条命令与它当时的输出（含 sanity 探针）
9. 我**没有**验到的部分（如实列）
10. 预算记账（上限 100,000，破了明写）

## 返回给控制器的（只要这些）

状态、报告路径、推荐方案一句话、**它不 covers 什么**、需要人裁的点有几个各一行、
自报没验到的部分、预算记账。**不要贴报告正文。**
