# 任务 2 brief — §5.2 债 3 归类矛盾 ＋ §16「第 11 行」锚点歧义

## 你的身份与铁律（先读完这一节再动手）

你是本仓库的**实施者**。本仓库**不接受实施者自证**：你交付后有一名独立评审员逐条复核。

1. **发现判据在今天文档/代码上不成立 —— 原样上报，不许自改判据。** 本仓库五次「计划自身被实测证明为假」，
   五次处置都是人裁 + 就地勘误，**没有一次是实施者自己改判据**。
2. **验证跑绝不过滤输出**，`grep` 与 `tail` 同罪。
3. **每个数字旁附一条能重推它的命令，并写下该命令当时的输出值。**
4. 任何一处没做，报告里明写没做。

## ⚠️ 落盘协议（强制）

**第一件事**：先 `Write` 报告文件 `.superpowers/sdd/2026-08-05-pkg3-doc-errata/task-2-report.md`，
**只写小节标题的骨架并立刻落盘**，在这之前不要做任何检索。
之后**每次 `Edit` 只填一节（≤120 行）**，最有把握的先写。
（本会话 9 名 agent 中 5 名被中断，全部发生在准备落盘时；采用该协议后交付率 100%。）

## ⚠️ 行号会移位

下面所有行号取自 HEAD `4f3b790`。**任务 1 已经在同一个文件里落过一批勘误，你拿到时行号大概率已经变了。
一律用逐字引用去定位，不要照行号跳。** 你自己编辑时也要**从大行号往小行号倒序处理**。

唯一涉及的文件（两处站点都在它里面）：
`docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md`（下称**本 spec**）

---

## 站点 A — §5.2 `:1143`：同一份文档里对债 3 的归类**自相矛盾**

### A.1 三段事实（都要自己重新读一遍再动手）

**(1) §5.2 结尾 `:1143` 今天逐字写着：**

> **因此一次并发 `stop()` 完全可以在 `writeBoundaryArtifacts` 飞行中 `releaseOwnerLease`。** 改动 A 不覆盖这一段，本层也不覆盖它（覆盖它要么把 artifact 写搬进 span——L1b 刚刚明确否决过——要么另设一层守卫）。**§13 据此把债 3 记为「exclusive span 部分关闭」，span 外那段具名传给 L5。**

**(2) 同一份文档 `:2358` 附近（§19 的 G10 处置正文）逐字写着相反的结论：**

> - **归类错误的实际危害**：写成「债 3 部分关闭」会让 L5 以为自己继承的是一笔**已被裁决过归属**的债，从而不再重新裁决；而它其实是一条**从未被任何裁决记录处理过**的新发现，**归属应当重新裁**。
>
> **因此**：债 3 记为**本层关闭**；span 外那段作为**本轮新发现**具名传 L5（下面第 3 笔，措辞已改），并明写它需要一次归属裁决。

**(3) G10（`:2812` 那一行表格）列出的修订处只有四个：**「§13 表、§13『债 3 的归类更正』、§13 第 3 笔、§14 第 1 条」。
**四个里没有一个是 §5.2。**

### A.2 判定（来自上一轮独立复核 lane 2，已 CONFIRMED）

- §5.2 `:1143` **是全文唯一一处用主动语态宣告该归类的地方**，却恰好被 G10 的修订清单漏掉。
- 于是**同一份文档内今天仍可原样复现 §13 自己点名要防的那个危害** —— 一个只读到 §5.2 的 L5 读者，
  会以为债 3 的归属已被裁过，从而不再重新裁决。
- **这不是「多一句同义句」，是矛盾。**

### A.3 你要做什么

在 §5.2 那句话处**就地勘误**（保留原句、其后另起一段加 `**Amended 2026-08-05：…**`），要点：

1. 明写该归类**已被 §19 的 G10 更正**：债 3 记为**本层关闭**；
2. 明写 span 外那段是**本轮新发现、从未被任何裁决记录处理过、归属应当重新裁**；
3. 指向 §13 与 §19 G10 这两个可跟过去的位置。

**边界（不许越）**：
- **只改归类的措辞，不许改 §5.2 的技术论证**（「并发 `stop()` 可以在 `writeBoundaryArtifacts` 飞行中
  `releaseOwnerLease`」「改动 A 不覆盖这一段」这些**结论今天仍然成立，不要动**）。
- **不要去改 §13、§14、§19** —— 那四处 G10 已经改过了，重复改会制造第二处矛盾。
  （若你读下来认为那四处里有哪一处其实没改到位，**写进报告上报，不要动手**。）

---

## 站点 B — §16「第 11 行」：两处锚点各指错了一行

### B.1 事实

§16「第一轮修订索引」的表里**同时存在两行**：

| 行 | 内容 | 状态 |
|---|---|---|
| `11` | 「S-3 退路完全遗漏」→ 修订处 §4.0.1–4.0.3 | **有效，从未被推翻** |
| `11b` | 「更宽不是更窄」那条**伪造的论证结构** → 修订处 §4.0.4（就地更正） | **已就地更正，行内自带显式警告「不要把这一行当成『已修好』继承下去」** |

**但全文两处引用都写「第 11 行」，而两处说的都是 `11b`：**

- `:209`（§4.0.4 正文）：「**§16 第 11 行**还把这个伪结构固化进了修订索引，**会被 L5 继承**。」
- `:2703`（§16 表头上方）：「**本表的第 11 行**在第二轮被判定为错误结论，已就地更正（见下面表内注）；其余各行仍然有效。」

**照字面读，两句都在说那条有效的 `11` 行错了。**

### B.2 判定（lane 2 CONFIRMED，但**修法比原报告描述的小**）

- **坏的是锚点，不是 `11b` 那一行本身** —— `11b` 已经就地更正过并带着显式警告，内容没有问题。
- *** 因此：**改锚点即可，不要重写 `11b`、不要重写 `11`、不要重构这张表。*** ***
- `:209` 还有**第二处腐坏**：它说伪结构「**会被 L5 继承**」—— 这句写于 `11b` 被更正之前，
  今天 `11b` 已自带警告，**这个未来时的危害预告已经不成立**。勘误要一并处理这半句。

### B.3 你要做什么

1. 把 `:209` 与 `:2703` 两处的「第 11 行」改成**无歧义的锚点**（例如「第 `11b` 行」），
   **`:2703` 那句还要保证「其余各行仍然有效」读起来不会把 `11` 行也捎带否定**；
2. 处理 `:209` 那句已腐坏的「会被 L5 继承」；
3. 表内 `11` 与 `11b` 两行**一个字都不要改**。

**格式选择**：这两处是**锚点更正**，可以直接改写原句而不必挂 `**Amended**` 段
（挂上也可以）。**你选哪种都行，但必须在报告里写明你选了哪种、为什么** —— 本仓库的立场是
「就地注解、不改原件」，直接改写原句是这条立场的例外，需要理由。

---

## 验证（做完必须跑，输出整段贴进报告，不许过滤）

```
export ECC_GATEGUARD=off DISABLE_OMC=1
rtk proxy "npm test -- --run"
```

- **验收 vitest 首行 `RUN` 路径必须是 `/Users/biran/code/skills/loop/ccloop`**，不对一律作废重跑。
- 基线 **30 files / 514 tests / exit 0**。本任务只改 markdown，**不应改变这两个数字；变了就是回归，立刻上报**。
- 收尾自查（输出写进报告）：

```
grep -n "第 11 行" docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md
grep -n "部分关闭" docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md
```

**第一条命令的预期：不再有任何一处歧义的「第 11 行」。第二条：你要能逐条解释每一处命中今天为什么是对的**
（有些命中是在引述被更正的旧结论，那是合法的）。

## 提交

提交到当前分支 `docs/pkg3-errata`（**不要碰 main，不要 push，不要 rebase 任务 1 的提交**）。
主题行 `docs(errata): …`。

## 报告契约

报告写进 `.superpowers/sdd/2026-08-05-pkg3-doc-errata/task-2-report.md`，至少含：

1. 站点 A、站点 B **各自**的原句、勘误正文、理由；
2. 站点 B 的**格式选择理由**（就地注解 vs 直接改写）；
3. 完整未过滤的验证输出 ＋ `RUN` 路径行 ＋ 两条自查命令的输出与逐条解释；
4. **你不确定或认为 brief 判错了的地方**（有就写，没有就明写「无」）。

**返回给我的只要**：状态（DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED）、提交 hash、
一行测试结论、两个站点是否都处理、你的 concerns。**报告正文不要贴回来。**
