# 扫描员 2 brief — 包 2 的开工前冲突扫描（只读）

## 你的身份与铁律

你是**只读**扫描员。**一个字都不许改 `src/` `tests/` `docs/`**，不许 commit。
产出只有一份报告文件（路径见下）。**你与另一名扫描员分工错开，不要去重核三条数据丢失路径的代码面**——那是他的。

⚠️ *** **本仓库最稳定的失败形状：用收窄的搜索面支撑全称否定。** *** 三方三次同形，
**三次全部发生在「下全称否定」那一步上**。硬性要求：
- 下「没有任何一处 / 全仓唯一 / 从未」之前，**先确认 grep 面覆盖你断言的范围**，把**命令与其当时输出**写进报告。
- *** **一条被转义弄坏或被过滤的探针，永远不能证明「不存在」。** *** 零输出时**先验命令本身**。
- **对策（必须照做）**：探针**写成脚本先落盘**，再 `rtk proxy zsh <script>` 跑，
  **同跑内放一条必命中的 sanity 探针**（如 `grep -c '' <file>`）证明检索面是活的。
- **绝不过滤输出**（`grep` 与 `tail` 同罪）；检索直接走 `rtk proxy`，否则输出会被折成 `[N more lines]`。
- **锚点用符号名不用行号**（本项目已有六处失效行号引用案底）。

⚠️ *** **落盘协议（强制）**：先 `Write` 只有小节标题的骨架并**立刻落盘**，在此之前不做任何检索；
之后每次 `Edit` 只填一节，**结论一节最先写**。 ***

## 报告落盘路径

`/Users/biran/code/skills/loop/ccloop/.superpowers/sdd/2026-08-07-pkg2-data-loss/scan-2-report.md`

## 为什么要做这一轮

本仓库已两次证明这一手值钱：组 C 做了开工前扫描，**提前裁掉 7 条冲突、查证后清空 13 条**；
组 A/B 没做，**各赔一轮返工**。边际成本几乎为零。

## 你的任务：找出会咬到包 2 的冲突、腐坏与边界

包 2 = 债 2 → 第 4 笔 → 第 1 笔，**获准写 `tests/`**（人裁 4），**本轮范围不含** `SweepOptions.stderr`
契约测试（人裁 11 已定：等包 1 修复环 2 之后）。

逐条产出：

1. **10 条 deferred minor 当输入清单**（人裁 5 第 3 条定的：并进开工前扫描，不单开清理轮）。
   来源：`.superpowers/sdd/2026-08-05-pkg3-doc-errata/progress.md` 与
   `.superpowers/sdd/2026-08-07-pkg1-l5-spec/scan-1-deferred-minors.md`。
   ⚠️ **数字本身也要重核**：handoff 里同时存在「6 条」与「10 条」两个聚合数，**已知至少一个已腐坏**。
   **报你自己能重数出来的数，并给判别式口径**（「条目」常是人工归并单位，没有命令能重数它——
   若重数不出来，**就不要报数字**，改报可重数的行数并声明那是搜索面、不是完备性证明）。
   逐条判：**与包 2 冲突 / 已腐坏 / 与包 2 无关**。
2. **人裁边界的复述核对**（不是让你重开方向）：人裁 4 的授权边界逐字是什么？
   有没有哪一条会与「给三条数据丢失路径补实跑注入」相撞？
   特别是：**变异必须走三步判据（注入前绿 / 注入后红 / 还原后绿），每个单跑块显示具名测试的非零计数。**
3. **今天的测试面现状**：`tests/` 里今天有没有**已经**覆盖到这三条路径的测试？
   有的话，包 2 是「补」还是「改」？**这决定实施者会不会不小心改到既有判据。**
   ⚠️ 这是最容易下错全称否定的一问，按上面的纪律办。
4. **一条已知的名单外 flake，必须知道，不要重新调查**：
   `tests/controller/runLoop.integration.test.ts > runLoop > persists phase usage evidence from the
   subprocess adapter without recomputing controller totals` —— 全套件 1/4 红、隔离 0/8、代码零改动、
   **根因未证**，人裁 10 已定「记录挂账、不入 flake 名单、不单开根因轮」。
   **你只需回答一件事**：包 2 要动的面，与这条测试所在的面有没有交集？
   ⚠️ **「没有交集」是全称否定** —— 若你验不到，就答「无法判定」，**不要硬下**。
5. **环境陷阱复核**：新 worktree 没有自己的 `node_modules` 时，`tests/validation/evidence.test.ts`
   的 `tsxBin` 用 `process.cwd()` 拼路径，**9 条 `run-scenario CLI` 会以 `spawn ENOENT` 假失败**，
   **看着像真回归**。今天这条是否仍成立？（只读核代码，不要真去建 worktree。）

## 交付格式（先落这个骨架）

1. 结论摘要（**最先写**）：五问各一句话
2. deferred minor 逐条裁断（含你的判别式与重数命令）
3. 人裁边界核对
4. 今天的测试面现状
5. 与那条名单外 flake 的交集判断（**可以答「无法判定」**）
6. 环境陷阱复核
7. 我用过的每一条命令与它当时的输出（含 sanity 探针）
8. 我**没有**验到的部分（如实列）
9. 预算记账（上限 100,000 tokens；破了明写）
