# Brief — 扫描员 1：包 3 遗留 deferred minor 的开工前冲突扫描（只读）

你是包 1（写 L5 spec）开工前冲突扫描的两名只读扫描员之一。另一名扫描员的切片与你**完全不重叠**
（他查 L5 的设计输入本身），你们不互相引用、不互相背书。

**为什么做这件事**：本仓库的组 C 开工前做过一次同类扫描，提前裁掉 7 条冲突、查证后清空 13 条；
组 A 与组 B 没做，**各赔一轮返工**。这一步的边际成本几乎为零，收益已被实测。

--------------------------------------------------------------------------------
## 0. 授权与边界（超出即违规，不许推定）
--------------------------------------------------------------------------------

- **只读。** 你**不得**修改 `src/`、`tests/`、`docs/` 下的任何文件，也不得改任何台账。
  你唯一被授权写的文件是你自己的报告：
  `.superpowers/sdd/2026-08-07-pkg1-l5-spec/scan-1-deferred-minors.md`
- **不许自改判据、不许自裁分级。** 发现某条待办的论据在今天的代码/文档上不成立 ——
  **原样上报**，由人裁 ＋ 就地勘误，不由你处理。
- **不要动手修任何一条 minor。** 你的产出是一张带证据的输入清单，不是修复。
- 允许跑只读命令（`git log` / `git show` / `grep` / `sed -n` 等）。**不要跑测试套件**（不需要）。

--------------------------------------------------------------------------------
## 1. 铁律（本仓库拿缺陷换来的，不得软化）
--------------------------------------------------------------------------------

1. **不接受自证。** 你的结论会被独立复核，所以每条都要留下可复核的痕迹。
2. **绝不过滤验证输出。** `grep` 与 `tail` 同罪。要贴的输出整段贴。
   （包 3 有过一次代价被当场量化：任务 3 把一条自查输出摘要成一行，**而那条必修 finding
   恰好就藏在被摘要掉的部分里**。）
3. *** 下「没有任何一处 / 不再有 / 全都」之前，先确认你的 grep 面覆盖你断言的范围。 ***
   要么把搜索面验到覆盖，要么**明写你没验哪一面**。
   **这个形状在本仓库一轮之内栽了 6 次，控制器自己占 2 次、一名再评审员占 1 次。**
   （典型：某人自查用 `grep -n "第 11 行"`，而目标文本写作 `第 11/11b 行`，中间是斜杠 ——
   模式结构上匹配不到，他据此写下「不再有任何一处」，被一条命令证伪。）
4. **每个数字旁附一条能重推它的命令，并写下该命令当时的输出值。**
   **报不出可重数的计数，就不要报数字** —— 改为给出可重数的行数 ＋ 命令 ＋ 输出，
   并声明那是**搜索面、不是完备性证明**。若要给计数，**必须先给判别式口径**
   （包 3 的有效先例：标签 = 以 `**Amended` / `*Amended` 开头；交叉引用 = 出现在反引号内）。
5. **转述他人结论时不许压缩主语。** 上一轮控制器把「一次并发 `stop()` 没有调用者」压成
   「触发没有调用者」，凭空制造了一处不存在的分歧，害一名 agent 专程去查。
6. **行号会腐坏。** 本仓库的行号锚点已被实测腐坏到第三层（代码 → spec → 纠正 spec 的报告自己也腐坏）。
   **定位一律用逐字引用 ＋ 符号名，行号只当辅助**，并在报告里写「行号为 <日期> 实测值」。
7. **超出 CLAUDE.md Rule 6 的 12,000 token 预算时明写，不许静默超支。**

--------------------------------------------------------------------------------
## 2. ⚠️ 落盘协议（强制。上一会话 12 名 agent 死了 6 名，全部发生在准备落盘时）
--------------------------------------------------------------------------------

**在做任何检索之前**，先 `Write` 一个**只有小节标题的骨架**到你的报告路径并立刻落盘。
之后**每次 `Edit` 只填一节（≤120 行）**，**结论一节最先写**，其余按把握度从高到低。
该协议在包 3 两次救场：一名评审员的 454 行报告在写最后一节时被中断，**已落的十节一字未损**。

--------------------------------------------------------------------------------
## 3. 你的输入清单 —— 12 条，逐条枚举（**不许照抄任何聚合数字**）
--------------------------------------------------------------------------------

来源台账（**唯一可信源，先读它**）：`.superpowers/sdd/2026-08-05-pkg3-doc-errata/progress.md`

> ⚠️ **第一件事就是重数这个清单。** 上一会话的人裁 5 写的是「10 条 deferred minor
> （9 条延后 ＋ 1 条新增）」，而控制器本轮逐条枚举得到 **12 条**，差额具名为 **F-M2 与 F-M3**。
> **那个「10」是控制器给的聚合数，人是在它之上答的「同意」。**
> **请你独立重数并给出判别式口径**，明确回答：分母到底是几，F-M2/F-M3 该不该算，理由是什么。
> **不许照抄 10，也不许照抄我的 12。**

**A 组 —— 三个任务分诊后延后的 9 条**（12 条中撤销 3 条：T1-M1、T3-M2、T3-M3，见台账 `:497-499`）：

| ID | 内容（逐字见台账） | 台账位置 |
|---|---|---|
| T1-M2 | `plan:2020` 的粗体嵌套，把勘误后的正确读法排版成了非粗体 | `:125` |
| T1-M3 | `spec:673` 新引入的「比原句更强」略被说满 | `:126` |
| T1-M4 | `fileStore.ts` 的注释把一个跨两层的调用方写死进了本层注释 | `:127` |
| T1-M5 | 站点 10 是全轮唯一没有回指指针的站点，链接是单向的 | `:128` |
| T2-M1 | `spec:209` 收尾是无限定全称否定「今天不存在会被 L5 无察觉继承的风险」 | `:231` |
| T2-M2 | `spec:209` 前后半句缺转折词，读感冲突 | `:233` |
| T2-M3 | `spec:1153` 用了位置锚点「上一句」，非符号/逐字锚点 | `:234` |
| T3-M1 | `(k)`/`(l)` 把「debt 3 也归 L3」挂在 `:246`/`:248` 两个**不含该结论**的行号上 | `:351` |
| T3-M4 | 报告 concern 3 的假前提「文件把 `(j)` 用了两次」——**产物侧已由 I-1 修掉**，请查**是否还有剩余动作** | `:356` |

**B 组 —— 整分支评审与最终再评审新增的 3 条**：

| ID | 内容 | 台账位置 |
|---|---|---|
| F-M2 | 本分支内部日期口径不一致（规约债 —— 本仓库的落注惯例从未写成文字） | `:517` |
| F-M3 | `spec:209` 的改写超出字面授权，但**判在界内** | `:518` |
| **F-M4** | *** **优先处理** *** 全仓另有**两个同形文件**（L1 的 spec 与 plan，文首同样带修订总账/计数/全称限定），**包 3 从未碰过它们，其自身是否已过期从未被验证** | `:555` |

**明确不在你范围内**：**F-M1（`spec:2322`）** —— 人裁 5 第 2 条已裁定「**维持「不改」，到此为止**」，
两方独立深读后各自给了可复核理由（目的性短语 vs 定义性断言），按 Rule 7 不折中。
**不要重开它，不要在报告里建议改它。**

--------------------------------------------------------------------------------
## 4. F-M4 为什么优先（这不是泛泛提醒，是实测）
--------------------------------------------------------------------------------

包 3 的**唯一一条必修 Important（I-1）**就是这个形状：`2026-07-28-run-registry-design.md`
文首 `Status:` 段仍写「Amended **2026-07-28** in **eight places (a)–(h)**；every amendment is a
document defect」，而 Index 当时已列到 `(l)` —— **日期、条数、性质限定三处皆假**。
而 F-M4 说的是：**L1 的 spec 与 plan 两份文件文首同样带「修订总账 / 计数 / 全称限定」，
包 3 从未碰过，其自身是否过期从未被验证。**

请你：
1. **先定位这两个文件到底是哪两个**（F-M4 原文只说「L1 的 spec 与 plan」，未给路径 ——
   **不要猜，用命令确定，并把定位命令与输出贴出来**）。
2. 逐条核它们文首的**每一个可证伪断言**（日期 / 计数 / 「all」「every」这类全称限定 /
   逐条列举与正文实际条目的对应）。
3. **一条一条给结论：今天为真 / 今天为假 / 无法判断（并说明缺什么）。**
4. **不要动手修。**

--------------------------------------------------------------------------------
## 5. 对 A 组、B 组每一条，你要回答的四个问题
--------------------------------------------------------------------------------

1. **今天是否仍成立？** 给命令与输出。区分**结论腐坏**与**论据腐坏**（本仓库要求分开写）。
2. **它会不会与包 1 撞车？** 包 1 的落盘面是 **只写 `docs/`**（新建 L5 spec ＋ 必要的就地勘误），
   **`src/` 与 `tests/` 一字不动**（人裁 6 / Q2）。凡涉及 `src/` 的条目（如 T1-M4）请明确指出
   **它在包 1 里做不了**，留给包 2。
3. **建议处置**（顺手并进包 1 / 留给包 2 / 撤销并说明理由 / 需人裁）—— **只建议，不执行。**
4. **有没有你自己发现、而清单上没有的同形项？** 单列一节。

--------------------------------------------------------------------------------
## 6. 交付契约
--------------------------------------------------------------------------------

报告写到 `.superpowers/sdd/2026-08-07-pkg1-l5-spec/scan-1-deferred-minors.md`，结构：

```
## 0. 结论（最先写）
## 1. 分母重数：判别式口径、命令、输出、我的数字与控制器的 12 / 人裁的 10 的差异裁断
## 2. F-M4（优先项）：两个同形文件的定位与逐条核验
## 3. A 组 9 条逐条
## 4. B 组 F-M2 / F-M3 逐条
## 5. 与包 1 的撞车面（哪些能顺手并进、哪些做不了）
## 6. 我自己发现的同形项（清单外）
## 7. 我没做完的事 —— 逐条明写，不许拿部分证据凑完整结论
```

**§7 是硬要求。** 本仓库把「自报未完成」当正面行为记录在案；
把没查完的说成查完了，是这里最贵的一类缺陷。

最后回给控制器的文本 = 一段**不超过 30 行**的摘要（结论 + 最要紧的 3 条 + 你没做完的事），
**不要把整份报告贴回来**（报告已落盘，控制器会自己读）。
