# SDD ledger — 包 1：写 L5 spec 的本职部分

授权源三份，全部已入库，**本文件不取代它们**：
- `.superpowers/sdd/2026-08-05-l5-input-scan/progress.md` —— 包 1 的委任状、人裁 1–5、三个工作包。
- `.superpowers/sdd/2026-08-05-pkg3-doc-errata/progress.md` —— 包 3 的全部裁决与四条教训。
- `docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md` §10 第 4 条 —— **人裁 3 定死的起点**。

前置层：包 3 已开门合入 main（门 `e42e062`，`--no-ff` merge，结论在主题行）。包 1 不重做包 3。

--------------------------------------------------------------------------------
STEP 0 — 基线复跑。2026-08-07。控制器亲跑，未过滤，`rtk proxy` 单条命令。
--------------------------------------------------------------------------------

环境 `ECC_GATEGUARD=off DISABLE_OMC=1`（先 `export` 再 `rtk proxy`）。HEAD `ba7b1eb`，
`git status --porcelain` 干净。**不继承包 3 那份绿** —— 门 `e42e062` 之后 main 上又落了
`8190efb` / `a5f548d` / `ba7b1eb` 三笔 docs 提交。

  RUN 路径 = `/Users/biran/code/skills/loop/ccloop`（vitest 首行，组 C 常设规则 2 验收）
  Test Files 30 passed (30) / Tests 514 passed (514) / TEST_EXIT=0 / 17.27s
  TYPECHECK_EXIT=0（`./node_modules/.bin/tsc --noEmit -p tsconfig.json`，按陷阱 8 不用 `npx tsc`）
  BUILD_EXIT=0
  两条允许的 flake 都没出现；没有名单外的失败；没有重跑；全程未过滤。

30 / 514 与 GATE-C、L5 盘点轮 STEP 0、包 3 STEP 0、包 3 合并后复跑五处一致。

--------------------------------------------------------------------------------
三处与交接文字不符 / 需更正的地方，以命令输出为准
--------------------------------------------------------------------------------

1. *** 远端**没有**落后 —— 第 5 次会话外推送已发生。 ***
   handoff 执行摘要第 1 条与包 3 台账结尾的「未 push，远端落后」**均已腐坏**。

     git rev-parse HEAD                     -> ba7b1ebdeaeb4ef07513960eec37f5c852c35d0c
     git ls-remote origin refs/heads/main   -> ba7b1ebdeaeb4ef07513960eec37f5c852c35d0c
     git reflog show refs/remotes/origin/main --date=iso 首行
       -> ba7b1eb refs/remotes/origin/main@{2026-08-07 00:12:44 +0800}: update by push

   该 push 时间戳晚于 handoff 那笔提交自身，本会话从未执行 `git push`。
   **与上一轮记的「第 4 次会话外推送」同形。⇒ 不要据「远端落后」去 push。**

2. **分支 `docs/pkg3-errata` 仍在，worktree 只有主仓库**（与包 3 台账一致，删分支需单独授权）。

     git branch      -> main / backup/evidence-first-v1-… / docs/pkg3-errata
     git worktree list -> 只有 `~/code/skills/loop/ccloop  ba7b1eb [main]`

3. *** 一处转述压缩，两份台账与 handoff 同错，写 spec 时以设计文档原文为准。 ***
   `atomic-write-paths-design.md` §10 第 4 条原文的临时文件名是

     `.{basename}.{pid}.{startTime}.{seq}.tmp`

   而 L5 盘点台账、包 3 台账与 handoff 三处都转写成 `.{basename}.{stamp}.{seq}.tmp`
   —— **`pid` 与 `startTime` 两个字段被压成一个 `stamp`**。
   本仓库已因「转述压缩掉主语」害闭合 3 白跑一趟（上一轮控制器记账第 ②）。
   **⇒ 包 1 的一切 brief 与 spec 正文一律逐字引用设计文档，不引用台账的转述。**

--------------------------------------------------------------------------------
*** 人裁 6。2026-08-07。人对控制器提的两个问题逐字答复「同意」，即采纳以下建议。 ***
--------------------------------------------------------------------------------

**Q1 —「人裁 5 的『先不改』」与「包 1 开工」的字面冲突 => 裁定 (a)：包 1 一开工就带上三条。**
  理由（复核用）：
  (a) 人裁 5 给第 3 条的定案逐字是「并进**包 1 的开工前冲突扫描**当输入清单」——
      那是它唯一的执行时机；若包 1 也不许带，该定案永远落不了地，
      而人裁 5 同时明写「三条都不阻塞包 1」。两句只在 (a) 下自洽。
  (b) 「先不改」答的是包 3 收口时的三条处置建议，而三条建议本身全是
      「拆进已有工作包，不单开一轮」。**「先不改」= 包 1 开工前不要单独去动**，与 (a) 相容。
  **对冲（一并采纳）**：stderr 那句不变式在 spec 里写成**物理独立、可无损撤销的一小节**，
  节内明写「依据人裁 5 第 1 条落下；若该裁定的读法是『包 1 也不带』，整节删除即可，
  不牵动其余章节」。删一节成本近乎零；漏掉扫描输入的成本本仓库已量化（组 A/B 各赔一轮返工）。

**Q2 — `SweepOptions.stderr` 的「类型旁一行注释」超出包 1 授权 => 裁定：包 1 不动 `src/`，注释推到包 2。**
  理由（复核用）：
  (a) **Rule 7 不折中**：人裁 4 是专门划授权边界的（「授权的边界（未被授权的部分不得推定）」，
      正面列举「包 1（写 spec）仍不写代码」）；人裁 5 第 1 条在讲处置方向，**一个字没讲授权面**。
      更专门的那条胜出。
  (b) **人裁 5 自己的时序论证不受损**：那条理由是「契约要先于第二个 stderr 注入点落下」，
      而第二个注入点将由**包 1 设计的 L5 GC** 引入 —— 包 1 只出 spec 不出代码，
      注入点最早也要到实施轮才存在。**spec 那句不变式今天落下，「先于」已满足**；
      注释晚一轮落，中间不产生任何窗口。
  (c) *** 最重的一条：单独落一行没有测试钉住的注释，正是 G2-null 那颗雷的成因。 ***
      闭合 2 查明 `fileStore.ts:603-605` 那条「do not unify it with this one」注释是**唯一防线、
      没有任何测试钉住**，而 `buildProcessInstanceId()` 的形式正好绕过它，触发即数据丢失。
      包 2 有 `tests/` 授权，能在同一笔提交里让注释与钉住它的测试互证。
      **在包 1 里孤零零再造一行无人守护的注释，是照着已知会咬人的形状重来一次。**

  => **包 1 的落盘面：只写 `docs/`（新建 L5 spec ＋ 必要的就地勘误）。`src/` 与 `tests/` 一字不动。**

--------------------------------------------------------------------------------
工作区决策：**不开 worktree**（照包 3 先例，理由逐条写在此，不靠惯例）
--------------------------------------------------------------------------------

包 1 只写 `docs/`，隔离价值为零；而开 worktree 会引入两个已实测的陷阱：
陷阱 5（新 worktree 无 `node_modules` ⇒ 9 条 `run-scenario CLI` 以 `spawn ENOENT` 假失败，
**不在允许 flake 名单内、看着像真回归**）与陷阱 6（subagent 的 bash cwd 在调用间重置
⇒ 跑错仓库、全绿却一条本分支测试都没跑）。
**若后续范围扩到 `src/`/`tests/`，本决策必须重判，不许继承。**

--------------------------------------------------------------------------------
STEP 1 — 开工前冲突扫描（人裁 5 第 3 条指定）。2026-08-07。
--------------------------------------------------------------------------------

**为什么先扫描**：组 C 开工前做过一次，提前裁掉 7 条冲突、查证后清空 13 条；
组 A 与组 B 没做，各赔一轮返工。L5 输入盘点是同一手法的放大版。

派 2 名**只读**扫描员，并行，切片互不重叠，各写各的报告文件：

  scan-1-deferred-minors.md   包 3 留下的 10 条 deferred minor（延后 9 ＋ 新增 F-M4）逐条今天是否仍成立；
                              **优先 F-M4** —— L1 的 spec 与 plan 文首同形修订总账是否已过期
                              （与包 3 那条必修 I-1 同形，已证明会咬人）
  scan-2-design-input.md      §10 第 4 条本身今天是否仍成立（残留面是否被 L3 扩大、该文档是否从未被勘误
                              —— **两条都不从台账继承，命令重推**）；§10 第 3 条是否兑现；
                              委任状 `retained` 那一半在全仓的落点

--------------------------------------------------------------------------------
扫描员 2（L5 设计输入现状核验）已交付。2026-08-07。报告 `scan-2-design-input.md`（528 行，7 节齐全）。
只读边界守住：`git status --porcelain` 干净，`src/` `tests/` `docs/` 与任何台账未动。
--------------------------------------------------------------------------------

*** 结论：人裁 3 定的起点**大体站得住**，但 §10 第 4 条里有一句已被今天的代码证伪。 ***
A1/A2/A3/A5/A6/A7 复核成立。**该条那句约束「定性要准确：这是无界垃圾，不是故障，
不要把它上报成缺陷」今天仍然成立** —— 扫描员未找到任何功能性破坏路径。
**⇒ 包 1 的 spec 不许偷偷把它升级成缺陷来给自己找理由。**

**控制器亲自复现的五条（不采信扫描员的文字，全部独立重跑，无一被证伪）：**

  A4 计数        `awk '/^async function cleanupOwnerTransferStagingWithoutMarker/,/^}/' \
                  src/persistence/fileStore.ts | grep -c "await safeUnlink"`  -> **10**
  从未被勘误      `grep -c "Amended" <atomic-write-paths-design.md>`  -> **0**（exit 1）
  最后一次被碰    `git log --follow --date=short -1 -- <同上>`
                  -> `2e30d1c 2026-08-01 docs: finish the reference sweep 9e554ce claimed to have finished`
  活的保留判定    `src/runtime/types.ts:67:  cleanupStatus: "retained" | "removed";`
  第二份委任状    `2026-07-21-stop-no-progress-stale-boundaries-design.md`
                  `:297` 「but they may not treat "stale" as permission to delete them.」
                  `:299` 「Later cleanup design must consume stale/reconciliation output
                          explicitly rather than being fused into it.」
  分层表          `2026-07-26-run-lease-and-heartbeat-design.md:41:| L5 | cleanup / orphan GC | deletion |`

*** 发现 1 —— A4 被证伪，且**不是疏漏的重复，是可预期的后果**。 ***
  §10 第 4 条写 `cleanupOwnerTransferStagingWithoutMarker`「只清 `getOwnerTransferPaths` 的
  **四个**固定名」——**今天是十个**。该条要证的事（够不着 `buildAtomicTempPath` 生成的名字）
  **仍成立**，腐坏的是数字。
  成因由 B1＋B2 合起来解释：残留面 **4 → 7（`0f940ea`）→ 10（`dad8a14`）**，两笔都在
  **2026-08-02**；而该设计文档最后一次被碰是 **2026-08-01**，此后再没被碰、零 `Amended`、
  全仓 7 份带勘误记号的文件里没有它。
  **⇒ 「L3 把残留面扩大了、而该文档从未被勘误」这两句台账断言今天都成立，且互为因果。**
  附带：台账「三份 pending」的说法**漏了事务标记** —— `writeJsonFileViaFixedTemp` 实有**四个**调用点。

*** 发现 2 —— 任务 C：上一轮 lane 2 的前提为假，但它的结论碰巧还活着。 ***
  lane 2 判「§10 第 3 条是否兑现无法判断，**因为 GATE-A 的门报告没落盘**」——
  **前提不成立**：L3 工作区里**五份门报告全在盘上**（51.9K/55.6K/30.5K/25.7K/56.8K）
  ＋ 43 份评审切片。
  **但结论仍是「无法判断」，换了理由**：`grep -c "writeOwnerTransferRecord"` 对五份门报告
  **全部为 0**，唯一的 L3 工作区命中是 `task-A7-report.md:197` 且讲的是测试夹具。
  **缺的不是门报告，是门报告与 §10 第 3 条之间的对应关系。**
  今天该函数**零生产调用者**、仍 `export`、**无任何测试或 lint 钉住它** —— §10 第 3 条的描述逐字准确。
  ⚠️ **这条要记进 L5 spec 的风险面**：一道「靠人评审兜住」的防线，事后无法证明它兑现过。

*** 发现 3 —— 任务 D（`retained` 那一半）**不是白纸，比人裁 3 预期的多**。 ***
  九份报告一次没提的那半边，今天已有实体：
  1. **一个活的保留判定**：`src/runtime/types.ts:67` `cleanupStatus: "retained" | "removed"`
     —— 清理失败即保留并落 `workspace_cleanup_failed` 事件。
  2. *** **第二份 L5 委任状，九份报告全都没提过** ***：
     `2026-07-21-stop-no-progress-stale-boundaries-design.md` §15 item 3 ＋ §13，逐字两句见上。
     **它对 L5 下的是禁令与义务**（不许拿 "stale" 当删除许可；后续 cleanup 设计必须**显式消费**
     stale/reconciliation 的输出，而不是与之融合），**方向与 `2026-07-22` 那份委任状互补，不重复。**
  3. 另有四条以上「不许删」的既有约束（逐条在报告 §4）。
  **⇒ 包 1 的 spec 必须同时受两份委任状约束，不能只引 `2026-07-22` 那一份。**

*** 发现 4（影响引文准确性，包 1 必须照办）—— 临时名有**三种**写法在流通。 ***
  §10 第 4 条原文的**五段形 `.{basename}.{pid}.{startTime}.{seq}.tmp` 是正确的**；
  台账/handoff 的四段 `{stamp}` 形**作为文件名格式是错的**（但错得有来历 —— 源码常量就叫
  `ATOMIC_TEMP_PROCESS_STAMP`）；⚠️ **§4.1 的「建议形式」是第三种四段写法**。
  **⇒ 包 1 引用临时名时必须指明引的是哪一种，并以源码实际生成为准。**

*** 铁律合规 —— 三条全称否定，扫描员**全部下了限定、没下无限定版**。记为正面样本。 ***
  A8「`src/` 内无按模式删除」只验到 29 个文件，**明写不覆盖** `runLoop.ts:169` 经 `sh -lc`
  执行的用户自带命令；B2 的判别式**盖不住「异地勘误」**；
  E 的「唯一」只用英文 `deletion` 一词做判据，**自陈只能证「只有 L5 被显式标注为 deletion」，
  不能证「只有 L5 可能删东西」** —— 并自己给了现成反例（`git worktree remove --force`
  是实质删除却不叫 deletion），L4 的 `large` 未展开。
  **这正是本仓库反复要求而反复做不到的行为。**

*** 扫描员自报 9 条未完成（报告 §7），最要紧四条 —— 必须进下一步的输入，不许当成已查。 ***
  1. **未读 L3 的评审委任状与门报告正文** —— 任务 C 因此只能停在「无法判断」。
  2. A2（SIGKILL 落在 `writeFile(temp)` 与 `rename` 之间）**无实测**，纯静态。
  3. `buildAtomicTempPath` 的测试未打开。
  4. 任务 D 只覆盖 `docs/` ＋ `src/`，**`tests/` 里可能有更硬的 retained 约束没查**。
  并按 CLAUDE.md Rule 6 明写超出 12,000 token 单任务预算，未静默超支。

*** 第九个环境陷阱（新，值得进清单）***
  **`git show "$c:path"` 在 zsh 下会被当成参数修饰符 `:s`，静默产出全 0 的假计数、退出码 0。**
  扫描员第一次就拿到一张全 0 的假表，**改用 bash 才得到真数据**。
  ⇒ 凡在本仓库跑带冒号的 `git show`，一律 `bash -c` 包一层。

--------------------------------------------------------------------------------
扫描员 1（包 3 遗留 deferred minor）已交付。2026-08-07。报告 `scan-1-deferred-minors.md`（282 行，7 节齐全）。
只读边界守住（除并发的扫描员 2 写自己的报告外无其它改动）。
--------------------------------------------------------------------------------

*** 结论：分母 = **12**（独立重推，非照抄）；F-M4 **验真、可关闭、无需修复**；
A 组 9 条今天全部仍成立，其中 **2 条在包 1 里做不了**。 ***

**判别式与重推路径（可重数，附出处）**：
  A 组 = 台账自报 12 条原始 Minor（`pkg3/progress.md:497`）− 撤销 3（`:498-499`：T1-M1/T3-M2/T3-M3）= **9**
  B 组 = 整分支评审与最终再评审新增 4（F-M1…F-M4）− 排除 F-M1（人裁 5 第 2 条已定案「维持不改」）= **3**
  合计 **12**。**全部可从台账自身文字重推。**

*** 差额出处已锁定实锤（不是猜测）***：`docs/handoff/handoff.md:71` 那张表逐字写
  「**10 条 deferred minor（9 条延后 ＋ 1 条新增）**」，而紧随其后描述的「新增」逐字就是 F-M4
  ⇒ **F-M2、F-M3（更早在整分支评审新增）从未被这张表算进去。**
  **⇒ 本轮控制器记账第 1 次成立**：那个「10」是上一会话控制器写进 handoff 的聚合数，
  人是在它之上答的「同意」。**本轮按 12 条逐条枚举派单，未传播 10。**

*** F-M4（优先项）—— **前提只对一半**，结论是可关闭。 ***
  F-M4 原文说「全仓另有**两个**同形文件（L1 的 spec 与 plan，文首同样带修订总账/计数/全称限定）」。
  **控制器亲自复现：**
    `wc -l docs/superpowers/plans/2026-07-26-run-lease-and-heartbeat.md`        -> 2530
    `grep -c "Amended" <同上>`                                                   -> **0**（exit 1）
    `git log --oneline -- <同上>`                                                -> **只有 `6a32c80` 一笔**
    => *** **L1 的 plan 根本没有修订总账，从未被修订过。F-M4 的「两个」为假，实为一个。** ***
    L1 的 spec 文首 `:4` 逐字：「Amendments: **four**, **all** found by implementing this document
      and marked inline as **Amended 2026-07-26 (a)–(d)** in §4.4, §6, §8.1 and §8.1」
    `grep -n "Amended 2026" <L1 spec>` -> `:4`(文首) ＋ `:70`(a) / `:126`(d) / `:203`(c) / `:231`(b)
      = **恰好 4 处内联标记，字母 (a)–(d) 齐全、日期全为 2026-07-26**，与文首声明逐条吻合。
  *** **与包 3 那条必修 I-1 形状相同、结论相反**：L2 当时是「文首写 (a)–(h)、Index 已到 (l)」
  日期/条数/性质限定三处皆假；L1 spec 今天是**四对四、全部对得上**。 ***
  => **F-M4 关闭，无需修复。**（这正是开工前扫描该有的产出：花一次检索换掉一条悬着的待办。）

*** A 组 9 条：今天全部仍成立 ***（用逐字引用定位；行号已实测漂移，如 `spec:1153` → 今天 `:1155`）。
  *** **T1-M4 与 T1-M5 的落点在 `src/persistence/fileStore.ts` 的注释里** ***
  —— 按人裁 6 / Q2，**包 1 只写 `docs/`，这两条在包 1 里做不了，必须转包 2**（与 stderr 那行注释同批）。
  **T3-M4 核验为无剩余动作**：I-1 的修复注已把 `(j)` 一条勘误两个落点讲清楚，产物侧无残留。

*** 清单外的同形项一条（扫描员撞见，原样上报未处理；控制器已复现）***
  `docs/handoff/handoff.md` 有**四处仍在生效的「远端落后」断言**（`:7` / `:34` / `:50` / `:236`；
  `:128` 是一句撤回引用，不计），**而本轮 STEP 0 已用 `git ls-remote` 证伪**（第 5 次会话外推送）。
  *** 加重情节：`:7` 是执行摘要第 1 条，**下一位 agent 读到的第一段**，且它自己写着「一律自查」。 ***
  **处置：不在扫描轮动手。留到包 1 收口更新 handoff 时一并就地注解**（本仓库立场：就地注解、不改原件）。

*** 扫描员 1 自报 7 条未完成（报告 §7），最要紧三条 ***
  1. F-M2（落注惯例未成文）的检索面**止于几个明显位置**，未做开放式全文语义搜索。
  2. **未重新扮演评审员去判各 Minor 的措辞建议本身是否恰当** —— 只核了「文字是否被后续改动推翻」。
  3. `decisions/`、`handoff/`、`.superpowers/sdd/` 的历史台账**未系统性扫过同形修订总账**
     —— 即 F-M4 那个形状在这三处是否还有第三、第四个实例，**今天未知**。
  并按 Rule 6 明写超出 12,000 token 单任务预算，未静默超支。

--------------------------------------------------------------------------------
STEP 1 收口 —— 两份扫描齐，控制器汇总（尚未经独立复核）
--------------------------------------------------------------------------------

**包 1 开工前必须带进去的四条（都来自本轮扫描，不是从台账继承的）：**
  1. §10 第 4 条的「四个固定名」**今天是十个** —— spec 起点要用今天的事实，不能照抄该条数字。
  2. **两份委任状，不是一份** —— `2026-07-22` §17 item 3（retained ／ cleaned up）
     ＋ `2026-07-21-stop-no-progress-stale-boundaries-design.md` §15 item 3 ＋ §13（禁令与义务）。
  3. **§10 第 3 条那道防线无法证明兑现过** —— 要写进 L5 spec 的风险面。
  4. 临时名**三种写法在流通**，引用必须指明引哪一种，并以源码实际生成为准。

**转包 2 的三条（包 1 做不了，因为都在 `src/`）**：T1-M4、T1-M5、以及人裁 5 第 1 条的
  `SweepOptions.stderr` 类型旁注释（人裁 6 / Q2 已定）。

**关闭一条**：F-M4（前提「两个」为假，实为一个；那一个今天验真）。

**留到收口**：handoff 四处「远端落后」的就地注解。

**两份扫描共同暴露、且都没查完的一块**：F-M4 那个「文首修订总账」的形状，
  在 `decisions/` / `handoff/` / `.superpowers/sdd/` 三处**是否还有更多实例，今天未知**（扫描员 1 §7 第 3 条）。
  **不阻塞包 1**（包 1 不改那三处），记为待办。

--------------------------------------------------------------------------------
*** 人裁 7。2026-08-07。人对控制器提的四条设计边界逐字答复「同意，继续」。 ***
--------------------------------------------------------------------------------

**边界 1 — spec 范围：只写本职，但加一节「继承项指针」（只放指针、不放内容，指向 L5 输入盘点台账）。**
  理由：上一轮查实本仓库有个真实文档缺陷 —— **两份交接清单并存、彼此无指针**。
  若 spec 只字不提继承项，**第三份无指针清单就诞生了**。

**边界 2 — `retained` 半边写成完整的一半，不是脚注。**
  理由：扫描把它从「白纸」变成有既有约束的实体 —— 第二份委任状对 L5 下的是**禁令加义务**
  （不许把 "stale" 当删除许可；必须**显式消费** stale/reconciliation 输出而非与之融合），
  另有活的 `cleanupStatus: "retained" | "removed"`。人裁 3 已明令「不许只想着删」。

**边界 3 — P1 写成 spec 的不变式，不是待裁选项：「L5 的 GC 不得自行回收租约」。**
  理由：一旦 GC 自己 `releaseOwnerLease`，第 3 笔立刻从「未发现可达路径」升级为数据丢失，
  **不需要任何人碰 `stop()`**。这不是权衡，是禁区。
  同节必须明写与「保留即放宽」人裁的耦合：**若为修第 3 笔把 `writeBoundaryArtifacts` 搬进 span，
  那条人裁会重开，两者必须一起决策，不能分两轮。**

**边界 4 — §10 第 4 条那个过期数字（四个 → 十个）顺手落一条就地勘误。**
  理由：包 1 的 spec 要引用这一条，**引用一个已被证伪的数字等于把腐坏原样传给 L5 的实施者**。
  按本仓库立场：**就地注解、原句一字不动**。

--------------------------------------------------------------------------------
STEP 2 — 设计阶段。程序说明（大声记下，不静默跳过）。
--------------------------------------------------------------------------------

*** 本轮**不跑** `superpowers:brainstorming` 的探索式设计，理由具名，非省事。 ***
  该 skill 的作用是探明意图与需求边界。**本包的意图与边界已由三条人裁逐条钉死**：
  人裁 3（起点 = `atomic-write-paths-design.md` §10 第 4 条，**逐字明令「不从零 brainstorm」**、
  第一节写 P1、覆盖 retained 与 cleaned up 两半）、人裁 6（落盘面 = 只写 `docs/`）、
  人裁 7（上面四条边界）。**再跑一次开放式发散，等于把人已经裁掉的选项重新摆回桌上。**
  本仓库立场：用户指令优先于 skill。**此处记录该判断，供复核者推翻。**

  仍然沿用的是 `superpowers:subagent-driven-development` 的执行框架：
  **实施者 → 独立评审员 → 有 Critical/Important 进修复环 → scoped 再评审 → 台账记 complete。
  不接受实施者自证。**

--------------------------------------------------------------------------------
STEP 3 — 写 spec。任务 1 已派（实施者）。BASE = `30cbdd5`。
--------------------------------------------------------------------------------

产物：`docs/superpowers/specs/2026-08-07-cleanup-and-orphan-gc-design.md`（新建）
      ＋ `docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md` §10 第 4 条一条就地勘误。
brief：`task-1-brief.md`（已落盘）。

选 opus 的理由：本任务几乎全是判断 —— 要在**两份委任状**的交叉约束下写一份新 spec，
且要把一条**今天已被证伪的数字**、一条**无法证明兑现过的防线**、一条**禁区级不变式**
同时安放正确，还不许把「无界垃圾不是故障」这条定性偷偷升级成缺陷。
Task 1: implementer DONE_WITH_CONCERNS，提交 `fbeb6fd`（2 files / **+503 −0 纯增**）。
  产物：`docs/superpowers/specs/2026-08-07-cleanup-and-orphan-gc-design.md`（9 节 482 行）
        ＋ `2026-07-29-atomic-write-paths-design.md` §10 第 4 条一条就地勘误（+21 −0）。

  **控制器亲自复核（不信实施者的文字）：**
    git diff --numstat 9f78169..fbeb6fd
      -> `21  0  <atomic-write-paths>` / `482  0  <新 spec>`
      *** **删除列恒为 0 ⇒ 原件未被改写、纯新增** ***（沿用包 3 评审员立的机械判据）
    git diff --stat 9f78169..fbeb6fd -- src/ tests/   -> **空**（授权面守住）
    grep -n "remains unwritten" docs/superpowers/specs/*.md
      -> `2026-07-26-run-lease-and-heartbeat-design.md:45` 逐字命中（C4 坐实）

*** 实施者证伪了 brief 的一条前提 —— 控制器记账（本轮第 2 次）。 ***
  brief 任务 E 写「该函数**无任何测试或 lint 钉住它**」。实施者实测 `tests/` 内有调用，
  **准确说法是「全是 fixture，没有一处断言那条约束本身」**，他按后者写并显式标注，
  **未沿用 brief 措辞、未自改判据**。
  **控制器复核确认前提为假**：`grep -rc "writeOwnerTransferRecord" tests/` ->
    `runLoop.integration.test.ts:2` / `zeroWrite.test.ts:4` / `fileStore.test.ts:17`（3 文件 / 23 行）
  ⚠️ **口径不同，两个数都不传播**：控制器数的是**含该符号的行数 = 23**（判别式：行），
  实施者报的是**调用点 = 22**（判别式：调用），**双方都没验过对方口径**。
  承重的那一点（brief 那句为假）已坐实，**计数留给评审员用一个写明判别式的口径重建**。
  **错因**：该句是控制器从扫描员 2 的报告**原样搬进 brief、未重推**。
  **这与本仓库反复栽的形状同源；实施者的处置是正确的，记为正面样本。**

**实施者自报的 concern（全部转独立评审员，不由控制器自裁）：**
  C1 见上（brief 前提有误）。
  *** C4 —— **本 spec 使一份既有文档过期**：`2026-07-26-…-design.md:45` 逐字写
     「L5 … **remains unwritten**」，本 spec 落盘后该句后半不成立。 ***
     实施者无授权改，已在新 spec §2.1 就地记明。**这是本轮新产生的文档债，需人裁是否派勘误。**
  C3 落勘误后 `atomic-write-paths` 的 `grep -c "Amended"` 不再是 0（原为 0）——
     后续引用该基线需带提交号。
  C7 **token 预算已破，明写不静默超支**。主因值得进规矩：
     *** **rtk 默认改写会把命令输出折叠成 `[N more lines]`；做验证性检索必须直接走 `rtk proxy`，
     否则拿到的是被过滤过的证据。** *** （与铁律 2「验证跑绝不过滤」同源，第一次在检索侧被量化。）

**实施者自报 4 条未做完（必须进评审员输入，不许当成已查）：**
  1. §8.2 那五条验收测试**一条都没写**（无 `tests/` 授权），spec 内已 ⚠️ 明写不要读成已有护栏。
  2. *** **INV-1 今天没有任何机制强制** —— 它与 §10 第 3 条那条注释**完全同型**，
     即实施者自己记为 RISK-1 的形状。**他明写这是本 spec 最大的自身弱点。** ***
     （自己指出自己的产物复刻了自己刚写进风险面的形状 —— 记为正面样本。）
  3. §1.4 那条耦合**只记录、未决策**，需一次人裁；建议单独立项，
     **别让它随本 spec 被当成「已处理」**。
  4. *** **证据链最薄处**：§3.2 四类保留面的依据是两份委任状原文 ＋ `cleanupStatus` 源码实测，
     **未逐条复核扫描员 2 报告 §4 的条目**（预算已破跳过）。**评审员请优先撞这里。** ***

Task 1: 评审完成（独立评审员，opus，未参与过本轮任何一条）。报告 `review-task1.md`。
  **规格符合性 ✅**（A–G 逐条落到、无越界、授权面守住：diff 仅 2 个 `docs/` 文件）。
  **任务质量：0 Critical / 2 Important / 5 Minor。可合入。**
  *** 包 3 那种「勘误正文自引入不实描述」的形状**未复现** ***（四处交叉引用互相闭合、节号全对）。

  **控制器亲自复核三条（不信评审员的文字）：**
    `grep -ro 'writeOwnerTransferRecord' tests/ | wc -l`   -> **23**（含符号的行）
    `grep -ro 'writeOwnerTransferRecord(' tests/ | wc -l`  -> **21**（调用点）
    spec 内 `stash` / `backup` / `validation-runs`         -> **0 / 0 / 0**
    spec 内「零强制/没有任何机制强制」                       -> **零命中，exit 1**（M4 属实）

  *** I1 —— §3.2 完备性（实施者自报的最薄处，评审员替他做了复核）***
    扫描员 2 §4.3 的 6 条「不许删」约束**逐条回原文核实：6 条全真、引文逐字准确**；
    但 spec 对 `stash` / `backup` / `validation-runs` **零命中**，而 §3.2「以下四类默认保留」
    与 §4.5「不在授权范围内」**都是闭合清单语气**。
    场景：L5 实现者按这两节办事，去清 `backup/evidence-first-v1-…` 分支或 retained stash ——
    **三份既有文档逐字禁止，spec 里读不出禁止。**
    ⚠️ 评审员明写 **I1 是下界不是完备清单**（他没扩大检索面，扫描员 2 也未覆盖
    `tests/` / `validation/` / `reference/`）。

  *** I2 —— 计数重建（控制器把它当独立项派出，这一手包 3 已实证有效）***
    **两个判别式都给了，差额 2 恰是两处 import。实施者的「22」在两个口径下都不成立**，
    且 **spec §6.1 与报告 C1 两处同错**。
    但他那句「全是 fixture，没有一处断言那条约束本身」**经复核判为真**
    （23 行 = 2 import + 21 次 fixture 构造调用，无一断言）。**结论不腐，坏的是数字。**

  **五条优先项的裁断（全部由评审员独立重推）：**
    勘误        **未越界、未过度勘误**。`--numstat` 删除列恒为 0；定性两行不在 diff 里；
                注文 6 条新断言全部重推为真，含**逐提交重数** `0f940ea~1`→4 / `0f940ea`→7 /
                `dad8a14~1`→7 / `dad8a14`→10 / `HEAD`→10。
    INV-1 自指  **判可接受**（spec 是约束载体不是执行机制，可判的是诚实度）——
                三条判据过了**两条半**：§8.2 给了 mutation 式判据、⚠️ 明写五条测试一条不存在；
                **差的是 §1.3 就地没说「今天零机制」(→ M4)**。
    定性        **未被升级**：§5.4 逐字承接两句、明写「不升级」，
                **并把立项理由与缺陷脱钩 —— 堵死了最常见的升级动机。**
    deletion    **未写成全称**，且比 brief 要求更远：把限定落成了两条可验收的行为约束。
    C4          **处置正确、措辞准确、双落点上报**，且主动区分「我造的债 / 我发现的债」。

  **Minor ×5**：M1 §1.1 的 grep 输出被摘要（且「leaseStore 3 处」实测为 4 处，承重结论仍属实）／
    M2「逐字首句」引错（真首句是 `// NOT atomic: …`）／M3「五份门报告」的判别式
    `-iname '*gate*'` 撑不起那个计数（输出里有 6 个 `.md`）／M4 见上／
    *** M5 C4 的债今天只记在下游、07-26 无反向指针 —— **本轮亲手造出一条 §7 自己拿来立论的同型债** ***

  **评审员自报 11 条未验，最要紧三条（必须进修复环与后续输入）：**
    1. *** **一条未验的破坏线索，原样交出、明写不据此下结论** ***：若有经 `buildAtomicTempPath`
       的写目标位于 `<runDir>/worktrees/` 下，残留会让 `ensureFreshRunDir` 抛错
       —— **他未枚举调用点**。⚠️ **若成立，会动摇「无界垃圾不是故障」那条定性。**
    2. spec 里两句全称否定（「四份扫描都没点出」「九份报告一次没提保留」）**今天仍无人验过**。
    3. `RUN_MARKER_FILES` / `OBSERVED_FILES` 两条路径未独立复核。
    并按 Rule 6 明写超预算，未静默。

Task 1: fix round 1/5 dispatched（resume 原实施者）。派 I1 ＋ I2 ＋ **控制器折入的 M4**。
  *** 控制器决定与理由（记账，供复核者推翻）***：M4 被评审员定级 Minor，本可延后，
  但**评审员自己判 INV-1「可接受」时的三条判据里，唯一没过的那半条就是 M4**。
  把它留到整分支评审，等于让「INV-1 诚实度」这条裁断带着一个已具名的缺口过夜。
  按包 3 的先例（控制器发现的 C1 折入修复环并显式编号）折入，**明确编号、明确记账**。
  **其余 4 条 Minor 明确不动，留给整分支评审分诊。**

