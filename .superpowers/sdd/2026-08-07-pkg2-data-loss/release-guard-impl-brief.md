# 任务书 —— `release()` 身份校验 ＋ 守护判据（人裁 62/63）

## 0. 你的工作区与环境

- **工作区**：`/Users/biran/code/skills/loop/ccloop/.worktrees/pkg2-release-guard`，分支 `feat/pkg2-release-guard`，基点 `d872532`。
  **只在这里改动。主仓库与其它 worktree 一律不许碰。**
- 环境：`export ECC_GATEGUARD=off DISABLE_OMC=1`。**一切验证命令走 `rtk proxy`。**
- 允许在本分支上做本地提交。**禁止**：`push` ／ 合并 ／ 建删分支或 worktree ／ 改主仓库。

## 1. 要修什么（唯一目标）

`src/persistence/fileStore.ts` 里 `acquireOwnerTransferLock` 返回的 **`release()`**：
它今天**无条件 `safeUnlink(lockPath)`，不校验将删的那把锁是不是自己那一把**。
已实测复现（`pointB-design.md` §6.1，测于 `dbac288`）：**持锁者会删掉一把已经不属于它的锁。**

**人裁 62 定死了行为**：
- 加身份校验；
- *** **校验不通过（含读不出／解析不出）时：不删、不抛、记一条事件。** ***
- *** **必须同时补守护判据** *** —— 否则「修好了但没有任何机制在跑它」，与 D-1 同形。

## 2. 红线（越线就地停住上报，不许自行折中）

1. *** **`tryRecoverStaleOwnerTransferLock` 一行不许动。** *** 人裁 50 的红线仍然有效，**待裁点 B 未裁**。
   （本轮刚测过：任何形态的失败关闭都会推翻 3 个既有 `it()` 块。**那不是你的地界。**）
2. *** **事件通道：若 `fileStore` 这一层今天没有现成的事件通道，就地 BLOCKED 上报，不许自造一条。** ***
   仓库既有的 `owner_transfer_contended` 是在别的层追加的 —— **先查清楚 `release()` 这个位置到底能不能拿到 sink，
   拿不到就停下来问，不要发明一个新参数/新全局/新文件。**
3. **不许改任何既有断言。** 若你发现某条既有判据必然与本改动冲突，**停住上报**（上一轮实施者就是这样做对的）。
4. `release()` 今天在两处 `finally` 里被调用（`fileStore.ts` 内，符号名自己找，不要信任何行号）。
   *** **在 `finally` 里新增可抛的 I/O 会顶掉正在飞的原始错误** *** —— 这正是人裁 62 选「不抛」的原因。
   **你新增的读取/解析/记事件三步，任何一步都不许把异常放出 `release()`。**

## 3. 一个你必须正面处理的设计决策（不许绕过）

旧材料的原型用的是 `holderProcessInstanceId === \`pid:${process.pid}\`` 这一个判据。
*** **它无法区分「同一个进程内先后两把锁」** *** —— 同 pid 的两次取锁，记录里的 holder 值是一样的。

⇒ **在报告里正面回答**：你选了哪种身份判据（仅 pid ／ 取锁时写下的完整记录内容 ／ 别的），
**为什么它足够**，以及**它在同进程重入这一格上的行为是什么**。
**不要求你一定选最强的那个，要求你把选择理由和它盖不住的那一格写清楚。**

## 4. 判据要求（人裁 62 的「守护判据」就是这一节）

- 新判据必须**红在断言上**，不许靠异常/超时变红（**这是本仓库反复出现的根因形状**）。
- *** **必须给出反向对照**：把你的修复回退掉（或做等价变异），证明新判据**确实变红**，
  并把 `Received` 里的真实内容贴进报告。 *** 只说「加了测试且是绿的」**不予采信**。
- 覆盖面至少要包含人裁 62 点名的那一格：**要删的锁不是自己那把 ⇒ 锁必须还在盘上、且不得抛出、且事件已记**。
- 若你的实现让某条既有判据变红 —— 见红线 3。

## 5. 举证与验证纪律（本仓库的铁律，逐条咬过人）

- *** **不接受实施者自证。** *** 你的每条承重结论都要能被别人重跑；下一位评审员会去撞。
- *** **验证跑绝不过滤** *** —— `grep` / `tail` / `sed` 同罪，**过滤显示与过滤落盘同罪**。日志整份落盘、整份读回。
- *** **坏探针不能证明「不存在」** *** —— 任何「没有发生」的论断，必须配一条**必命中的对照**证明探针是活的。
- *** **允许为验证做临时变异，但必须证明还原** *** —— 同时验 `git diff` 与 `git diff --cached` **均为 0 字节**。
  **最佳做法：在 `git archive` 出的副本里做变异**（⚠️ 注意：本轮控制器实测发现 `git archive` 副本**没有 `.git`**，
  会让 `cli.test.ts` 那条 scripted example run 假红 —— 若你要在副本里跑**全套件**，请用 `git clone --local`；
  只跑单文件/探针则 archive 副本够用）。
- **落盘协议**：先 `Write` 一份**只有小节标题的骨架**并立刻落盘，之后每次 `Edit` 只填一节、**结论一节最先写**。
- **预算**：**不自报估计**，只交可数事实（跑了几次全套件、几次变异、几个探针）。

## 6. 收口验收（缺一条都不算完成）

在 worktree 内、`rtk proxy`、**未过滤**：
1. `npm test -- --run` —— **核对 vitest 首行 `RUN` 路径必须是本 worktree**，不是主仓库；
2. `npm run typecheck`；3. `npm run build`。三个退出码都要报，**不许只报「通过」**。
- **本轮基线**：主仓库 HEAD `d872532` 上是 `Test Files 31 passed (31)` / `Tests 533 passed (533)`，无 skipped。
  **你的收口数应当是 533 ＋ 你新增的条数**，**任何一个文件的条数下降都必须解释**。
- **允许的 flake 只有 (B) 与 (F)**；(B) = `evidence.test.ts > run-scenario CLI > records env names only and tracks descendants rooted at the spawned pid`。
  **名单外的失败：按完整测试名挂账上报，既不要重新调查、也不要挥手放过。**

## 7. 交付物

- 代码改动 ＋ 新判据，提交在 `feat/pkg2-release-guard` 上。
- 报告写在本目录的 `release-guard-impl-report.md`（**骨架先落盘**）。报告必须含：
  ① 结论；② §3 那个设计决策的正面回答；③ 反向对照的红/绿证据（含真实 `Received`）；
  ④ 三个退出码与 `RUN` 路径；⑤ 你**没有**验到的（Rule 12，明写「没验」不是「验过没问题」）；
  ⑥ 变异与还原证明；⑦ 可数事实。
- **台账 `progress.md` 只写这个 worktree 里的副本**，主仓库那份不许动。
