# 包 2 修复环第三轮 —— scoped 再评审 brief（**第四个人**）

> 你是**第四个人**：没实施、没做第一轮评审、也没做第二轮的 scoped 再评审。
> **scoped = 只看 `845694b..HEAD`**（第三轮），外加「有没有把前两轮已 ADDRESSED 的弄坏」。
> *** **不接受实施者自证。** ***
>
> ⚠️ **派你的理由要讲清楚**：**第二轮正是在 `acquireOwnerTransferLock` 上引入了一条 Important**
> （`link` 成功后的 staging 清理抛非 ENOENT ⇒ 锁已发布却拿不到 `release`），
> **是第三个人查出来的**；而**第三轮又动了同一个函数**。**这就是你存在的意义。**

## 0. 工作区与落盘协议
- `/Users/biran/code/skills/loop/ccloop/.worktrees/pkg2-wbfix`（分支 `feat/pkg2-wb-fixes`，已 `npm ci`）。
  ⛔ 不碰主仓库；⛔ 不 commit/push/建删分支/合并；⛔ **不得留下未还原的改动**；⛔ **你不修任何东西，只报。**
  ⚠️ **建议照第三个人那一手**：变异在 `git archive` 出来的副本里做，工作树全程不脏。
- 报告：`.superpowers/sdd/2026-08-07-pkg2-data-loss/wbfix-rereview2.md`。**立刻**落骨架，**结论最先填**。

## 1. 必须自己实测的五件事
1. **Imp-1 的修法**：实施者说照抄了 `writeJsonFileAtomically` 的具名先例（「Best effort, and **intentionally not safeUnlink**」），
   抽出 `discardLockStaging`。**核**：修法与先例是否真同形？**两条新判据在未修副本上是否红在断言**、修后是否绿？
   *** **更要紧的一问：这一修有没有引入新的静默失败？** *** —— 「best effort 清理」意味着 staging 文件可能留下来。
   **留下来会怎样？** 它会不会被别的东西看见（枚举、快照、`ensureFreshRunDir`、sweep、registry）？
   **第二轮就是这样出事的：修一个洞，开一个新的。**
2. **人裁 50 的红线仍在**：`tryRecoverStaleOwnerTransferLock` 与 `release()` 相对 `845694b` 是否逐字节未动。
3. **Imp-2 的新列**：`afterResume` 是否真的对**跑过 resume 的那份副本**取样（第二轮的错就是取错了副本）？
   **有没有墙钟字段能漏进去**（实施者称 `crashSnapshot` 只渲染 presence/epoch/marker/pendings）？
   ⚠️ *** **实施者自陈：它造不出「只让新列变红」的变异，所以不声称抓到什么，只说守未来的回归类。**
   **你独立判断这条自陈是否诚实、以及这个新列到底有没有价值** —— 如果它其实是恒真的，那就是又一条自指断言。 ***
4. **Low-4 的探针灵敏度**：实施者重测 10s 得 **6 violations / 3195 bases**（第三个人得 1–4）。
   **自己跑一次**，判断这个探针现在的自陈灵敏度是否老实（含「0 也可能是探针坏了」这句是否落到位）。
5. **零新破坏**：全套件 ＋ typecheck ＋ build（`rtk proxy`、未过滤、整份落盘整份读回、核 `RUN` 路径），
   并**逐文件计数**比对 `dbac288`(524) → `c2db9c7`(529) → `845694b`(531) → HEAD(533)，确认**无一文件下降**。

## 2. 你还要专门判一件
*** **整条分支现在到底关掉了 C-1 的多少？** *** 人裁 50 只批了 **O1 = 原子发布**，
`tryRecoverStaleOwnerTransferLock` 的**两个失败开放出口原封不动**（那是待裁点 B，未裁）。
⇒ **给一句话的准确表述**，供控制器写进台账与开门主题行。
⚠️ **不许写成「C-1 已关闭」** —— 本仓库对第 4 笔有人裁 39 的同形口径（**一律记「降级」**）。
**若你认为连「降级」都过强，就说出来。**

## 3. 铁律
**验证跑绝不过滤**（`grep`/`tail`/`sed` 同罪 —— 实施者本轮自曝用过一次 `sed`）／**坏探针不能证明「不存在」**／
***读代码的机械论证不等于实测***／`rtk proxy` ＋ `ECC_GATEGUARD=off DISABLE_OMC=1`／
**锚点用符号名或完整测试名**／***finding 与处置建议分开写***，并明说是否应本轮修／**Rule 12 fail loud**。
允许的红只有 flake (B)/(F) 与三条已挂账项。⛔ 不碰待裁点 A/B/C、不碰包 1。

## 4. 骨架
```
# 包 2 修复环第三轮 —— scoped 再评审（第四个人）
## 0. 结论（最先填：ADDRESSED / 有无新破坏 / 有无越界 / 有无新引入缺陷）
## 1. 全套件与逐文件计数比对
## 2. Imp-1 修法：先例同形性、双向实测、以及"有没有开新洞"
## 3. 人裁 50 红线
## 4. Imp-2 新列：取样正确性、墙钟泄漏、以及这一列到底有没有价值
## 5. 探针灵敏度自陈是否老实
## 6. 专判：C-1 现在到底关掉了多少（给可直接引用的一句话）
## 7. Findings（分级，finding 与处置建议分开）
## 8. 变异与还原证明
## 9. 没验到的
## 10. 预算：可数事实
```
