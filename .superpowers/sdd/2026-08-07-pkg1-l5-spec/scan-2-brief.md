# Brief — 扫描员 2：L5 设计输入的现状核验（只读）

你是包 1（写 L5 spec）开工前冲突扫描的两名只读扫描员之一。另一名的切片与你**完全不重叠**
（他查包 3 遗留的 deferred minor），你们不互相引用、不互相背书。

**你的题**：包 1 要以 `docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md` **§10 第 4 条**
为起点写 L5 的 spec（人裁 3 定死）。**在动笔之前，必须先确认这个起点今天还站得住**，
以及围绕它的三条既有断言是真是假。**一条都不许从台账继承 —— 台账的每一句都要你用命令重推。**

--------------------------------------------------------------------------------
## 0. 授权与边界（超出即违规，不许推定）
--------------------------------------------------------------------------------

- **只读。** 不得修改 `src/`、`tests/`、`docs/` 下的任何文件，也不得改任何台账。
  唯一被授权写的文件是你自己的报告：
  `.superpowers/sdd/2026-08-07-pkg1-l5-spec/scan-2-design-input.md`
- **不许自改判据、不许自裁分级。** 发现文档论据在今天代码上不成立 —— **原样上报**。
- 允许跑只读命令。**不要跑测试套件。**

--------------------------------------------------------------------------------
## 1. 铁律（与扫描员 1 同，本仓库拿缺陷换来的，不得软化）
--------------------------------------------------------------------------------

1. **不接受自证。** 你的结论会被独立复核。
2. **绝不过滤验证输出**（`grep` 与 `tail` 同罪）。
3. *** 下「没有任何一处 / 全都 / 从未」之前，先确认 grep 面覆盖你断言的范围。 ***
   要么验到覆盖，要么**明写你没验哪一面**。**这个形状在本仓库一轮之内栽了 6 次。**
   *** 你的题里至少有三条是**全称否定**（见 §3），这条对你格外要命。 ***
   正面样本（请照做）：上一轮闭合 2 拒绝下「今天没有测试钉住 P-READ」这句断言，
   理由是自己没通读相关测试文件、搜索面不覆盖断言范围 —— **这被记为正面样本。**
4. **每个数字旁附一条能重推它的命令与它当时的输出。报不出可重数的计数就不要报数字。**
5. **转述他人结论不许压缩主语。**
6. **行号会腐坏。** 一律用**逐字引用 ＋ 符号名**定位，行号只当辅助并标注实测日期。
7. **超 Rule 6 的 12,000 token 预算要明写，不许静默超支。**

--------------------------------------------------------------------------------
## 2. ⚠️ 落盘协议（强制。上一会话 12 名 agent 死了 6 名，全部发生在准备落盘时）
--------------------------------------------------------------------------------

**在做任何检索之前**，先 `Write` 一个**只有小节标题的骨架**并立刻落盘。
之后**每次 `Edit` 只填一节（≤120 行）**，**结论一节最先写**，其余按把握度从高到低。

--------------------------------------------------------------------------------
## 3. 你的任务（五项，按重要性排序）
--------------------------------------------------------------------------------

### 任务 A —— §10 第 4 条逐句核验（**最重要**）

`docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md` 的 `## 10.` 第 4 条。
**先把该条逐字读完**（它约 12 行，含一段「定性要准确」的约束）。它做了这些可证伪的断言：

| # | 断言（逐字见原文） | 你要做的 |
|---|---|---|
| A1 | 崩溃残留的临时文件名形如 `.{basename}.{pid}.{startTime}.{seq}.tmp` | 找到今天真正生成临时名的代码，**逐字核对格式串**。⚠️ 注意：本仓库的两份台账与 handoff **三处都把它转写成 `.{basename}.{stamp}.{seq}.tmp`**（`pid` 与 `startTime` 被压成一个 `stamp`）——**以源码与设计文档原文为准，请明确判定哪个对** |
| A2 | 「`SIGKILL` 落在 `writeFile(temp)` 与 `rename` 之间时会**永久**留在 run 目录」 | 核实这条时序在今天的实现里仍然存在 |
| A3 | 「**两条现存清理路径都够不着它**」：`ensureFreshRunDir` 只对三个具名文件（`loop-contract.json`、`loop-state.json`、`events.jsonl`）加 `attempts/`、`worktrees/` 两个目录做阻塞 | 逐项核实今天是否仍然如此 |
| A4 | `recoverInterruptedOwnerTransfer` 经 `cleanupOwnerTransferStagingWithoutMarker` **只清 `getOwnerTransferPaths` 的四个固定名** | 核实「四个」今天是否仍是四个，函数是否仍只清这些 |
| A5 | 「临时名不在 `RUN_MARKER_FILES`（五个具名文件）里，所以不会把一个目录误认成 run」 | 核实 `RUN_MARKER_FILES` 今天仍是五个且不含临时名模式 |
| A6 | 「L2 只读 `OBSERVED_FILES` 的三个文件，不会读到它」 | 核实今天仍是三个 |
| A7 | 「`ensureFreshRunDir` 也不会因它而拒绝初始化」 | 核实 |
| A8 | *** **全称否定**：`src/` 内没有任何按模式删除临时文件的东西 *** | **这条必须给出搜索面**。要么验到覆盖，要么明写没覆盖哪一面。**不许下无限定的全称否定** |

**⚠️ 同时要保留的约束**：该条原文明写 *** 「**定性要准确：这是无界垃圾，不是故障**」「**不要把它上报成缺陷**」 ***
—— 若你的核验结论与之冲突（例如发现它今天真的能造成功能性破坏），**那正是重要发现，原样上报**；
若不冲突，**请明确记下这条约束仍然成立**，因为包 1 的 spec 不许偷偷把它升级成缺陷来给自己找理由。

### 任务 B —— 两条被反复引用、但从未被重推的台账断言

上一轮的复核车道（lane 2）写下并被 handoff／台账反复引用的两句：

- **B1**：「**L3 把三份 pending 改原子写之后，残留面已扩大**」
  —— **哪三份 pending？L3 具体把哪些写路径改成了原子写？残留面从几条扩到几条？**
  用 `git log` / `git show` / 代码重推，**不要引用台账**。
- **B2**：「**该文档（`2026-07-29-atomic-write-paths-design.md`）从未被勘误**」
  —— 这是一条**全称否定**。给出搜索面（本仓库的勘误标记体例是行首 `**Amended <日期>`／`*Amended`，
  另有中文冒号写法 `Amended <日期>：`；**判别式请自己确立并写明**）。

### 任务 C —— §10 第 3 条是否兑现

§10 第 3 条写「`writeOwnerTransferRecord` 仍可被误用：只加了注释，没有机制强制。
**真正的防线是 L3 的评审**」。
上一轮 lane 2 的判断是「**防线已用掉，是否兑现无法判断，因为 GATE-A 的门报告没落盘**」。
**请你独立核实今天的状态**：那道防线到底有没有落下过可复核的证据？今天 `writeOwnerTransferRecord`
的误用面是什么样？**若确实无法判断，就明写无法判断并说清缺什么** —— 不要拿部分证据凑结论。

### 任务 D —— 委任状的 `retained` 那一半（**九份报告一次没提，人裁 3 点名要求覆盖**）

L5 的委任状逐字在 `docs/superpowers/specs/2026-07-22-ownership-and-reconciliation-boundaries-design.md`
**§17 item 3**（⚠️ **不是** run-registry 那份 —— 上一轮控制器把它错指到 `2026-07-28-run-registry-design.md` §17，
而该文件**止于 §15**，被扫描员当场证伪。**请你自己确认路径与节号，把命令与输出贴出来**）：

> **Cleanup / orphan handling design** — how superseded or lost-owner workspaces and evidence
> are **retained** or **cleaned up** safely.

**人裁 3 明令**：委任状**同时授权 `retained` 与 `cleaned up`**，「保留」这一半九份报告一次没提，
**设计时不许只想着删**。

你要查的是：**全仓今天有没有任何关于「保留」的既有输入或既有行为**？例如
证据保留 / evidence retention / 工作区保留 / stash 保留 / backup 分支 / 「不要删」类的现存约束。
**这是一次开放检索，请写明你用了哪些检索角度、覆盖了什么、没覆盖什么。**

### 任务 E —— L1 分层表给 L5 的授权原文

L1 的分层表里给 L5 的一行据称是 `| L5 | cleanup / orphan GC | deletion |`，
**据说 `deletion` 是全五层唯一的删除授权**。
**逐字核实这一行今天的原文**（给出文件路径与逐字引用），并核实「唯一」这个断言
（**又一条全称否定** —— 请给搜索面）。

--------------------------------------------------------------------------------
## 4. 交付契约
--------------------------------------------------------------------------------

报告写到 `.superpowers/sdd/2026-08-07-pkg1-l5-spec/scan-2-design-input.md`，结构：

```
## 0. 结论（最先写）：起点今天还站得住吗？站不住的是哪几句？
## 1. 任务 A：§10 第 4 条 A1–A8 逐条
## 2. 任务 B：B1「残留面已扩大」/ B2「从未被勘误」各自重推
## 3. 任务 C：§10 第 3 条是否兑现
## 4. 任务 D：retained 那一半的全仓落点
## 5. 任务 E：分层表 deletion 授权原文与「唯一」的核实
## 6. 我自己发现的、清单外的东西
## 7. 我没做完的事 —— 逐条明写，不许拿部分证据凑完整结论
```

**§7 是硬要求。** 本仓库把「自报未完成」当正面行为记录在案。

最后回给控制器的文本 = 一段**不超过 30 行**的摘要（结论 + 最要紧的 3 条 + 你没做完的事），
**不要把整份报告贴回来**（报告已落盘，控制器会自己读）。
