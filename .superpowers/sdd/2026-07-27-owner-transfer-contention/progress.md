# SDD ledger — plan: docs/superpowers/plans/2026-07-27-owner-transfer-contention.md
Task 1: complete (commits 7204754..7beb756, review clean — spec ✅, quality approved, 0 findings)
Task 1: controller independently verified the two "cannot verify from diff" items: 23 files / 360 tests green, typecheck and build clean.
Task 1: minor (deferred): the new heartbeat lock-busy test leans on vitest fake timers; reviewer could only trace it by reading. Passed in the controller's independent run.
Task 1: note for later tasks — leaseHeartbeat.ts needed NO code change; runAffirm's existing `!(error instanceof OwnerTransferPreconditionError)` guard became correct for lock contention purely because the new class is a sibling.
Task 2: complete. Bounded retry (3 attempts, 50ms backoff, ≤150ms ≈ 0.11-0.17% of the 90s TTL) added inside `persistOwnerTransfer` around `writeOwnerTransferArtifacts` only; lock-busy retries, CAS mismatch never does. 3 new tests, each mutation-verified (remove retry / retry CAS mismatch / unbounded attempts → hang). 363 tests green, typecheck and build clean. leaseHeartbeat.ts untouched, as expected.
Task 2: complete (commits 7beb756..64d6c87, review clean — spec ✅, quality approved, 0 Critical/Important)
Task 2: retry budget = 3 attempts / 50ms backoff; only 2 delays actually occur (delay precedes attempts 2 and 3), so worst case 100ms = 0.11% of LEASE_TTL_MS.
Task 2: minor (deferred): the in-code comment says "3 x 50ms <= 150ms" while the code's real bound is 2 x 50ms = 100ms. Harmless, imprecise.
Task 2: minor (deferred): the unbounded-retry mutation is evidenced by a 5s harness timeout rather than a clean assertion failure. Inherent to mutating the bound to Infinity; the brief anticipated it.
Task 2: reviewer independently confirmed writeOwnerTransferArtifacts has exactly one production call site, and that the CAS check runs AFTER lock acquisition so the two failure modes are never conflated at that seam.
Task 3: review (opus) — spec OK but quality NOT APPROVED. 1 Important, 4 Minor.
Task 3: Important — the plausible wrong impl `queue = result.then(() => undefined)` (single-handler on the STORED mapper) typechecks cleanly, passes the delivered requirement-10 test in full, and emits an unhandledRejection that kills the process (no unhandledRejection listener anywhere in src/). Untested. Reviewer verified by execution against Node 22 and the repo's tsc, not by reasoning.
Task 3: adjudication of the implementer's flagged concern — its empirical claim was RIGHT (the dual-handler mirror self-heals for all three in-module consumers) but its conclusion was wrong: the case it never considered is no consumer at all in that turn, where the mirror IS distinguishable. Not worth chasing (needs two `as unknown as` casts to compile); the mutation that matters is the one above.
Task 3: minor (deferred): the correct impl converts a fire-and-forget failure into a silent swallow — puts the whole Rule 12 burden on Task 4's call site awaiting and handling. CARRY INTO TASK 4's REVIEW.
Task 3: minor (deferred): leaseHeartbeat.test.ts:424-426 asserts order after a fixed microtask depth — deterministic, not flaky, but a lower bound that breaks if any .then hop is added.
Task 3: minor (deferred): INERT_LEASE_HEARTBEAT widened from module-private to exported; now an exported mutable singleton literal. No test mutates it.
Task 3: ⚠️ for Task 5's reviewer — stop() snapshots `queue` at :223, so a runExclusive started during the subsequent `await releaseOwnerLease` is not awaited by stop and can race the release. Task 3 correctly took no position.
Task 3: fix round 1/5 dispatched (resumed implementer ac5d14e421dee5353) — add the unhandled-rejection test, correct the report's mutation 2 and its headline claim.
Task 3: fix round 1/5 (2 addressed, 0 open — new unhandled-rejection test in the real-timer block, mutation-verified against the exact realistic poison; report's mutation 2 and headline claim corrected; commits e2dc5bb..8dd130a). No src/ change was needed: the implementation was correct all along, the gap was test coverage plus a wrong claim in the report.
Task 3: complete (commits 64d6c87..8dd130a, review clean after 1 fix round)
Task 4: review (opus) — spec ✅ on every mandated span boundary, quality Approved, but 2 Important. Reviewer independently verified the implementer's microtask argument against leaseHeartbeat.ts:196-203 and CONFIRMED it: `adopt` as last statement in the callback vs. first statement after the `await` are observationally identical, because the queue reassignment registers its reaction on `result` before the caller's continuation. The "unobservable minimal mutation" claim is real, not an excuse.
Task 4: reviewer also cleared, against live source, three named risks: no queue re-entrancy inside the span (only `adopt`, which is unqueued); test 4's readCalls gate lands on the span's own read (the heartbeat uses the different export readOwnerRecordWithoutRecovery); the Task-3 carried-forward fail-loud burden IS discharged (call site awaits runExclusive, no .catch).
Task 4: Important (1) — test 5's header comment asserts a mutation kill that the implementer's own report disproves (report lines 199-204: test 5 passed under BOTH mutation placements). In a repo where mutation evidence is the signal a future editor uses to judge whether a test is load-bearing, an inverted claim is worse than no claim.
Task 4: Important (2) — a 1-in-369 transient full-suite failure was attributed to "the pre-existing flake" without naming the failing test or capturing its output. This diff changes when a shared queue is held and when a shared owner record is read, so an unnamed intermittent failure is exactly what must not be waved through (Rule 12).
Task 4: minor (deferred): three hand-counted `await Promise.resolve()` turns fence the negative assertions; margin is 3x today (runAffirm reaches affirmOwnerLease with zero preceding awaits) but the assertion is silently vacuous if a future refactor adds awaits ahead of the CAS.
Task 4: minor (deferred): the wrap's re-indent pushes one line to ~122 columns; no prettier config or lint script exists to catch it.
Task 4: minor (deferred): the blank line separating the span from `await writeBoundaryArtifacts` was removed — readability loss exactly where the boundary matters.
Task 4: minor (deferred): test 5 does not assert the run's final state where the parallel test 4 does.
Task 4: fix round 1/5 dispatched (resumed implementer a49c39b637fb82cb1)
Task 4: fix round 1/5 (2 addressed, 0 open — test 5's comment rewritten to fence only what it fences and to attribute the adopt-placement clause to test 4's leaseAffirmedAt assertion; the flake NAMED and root-caused; commits 82d0483..6a10b48). No production code changed, no test weakened. Re-reviewer independently verified the mechanism rather than accepting the report: applyPhaseUsage freezes state.budgetSnapshot.timeRemainingMs at runLoop.ts:1009 BEFORE persistBoundaryAnalysis is called at :1025, persistBoundaryAnalysis never mutates the caller's state, and the stop-reason decision at :1030 reads the already-frozen snapshot — so latency added inside the span structurally cannot feed back into a decision made one line earlier.
Task 4: FLAKE NAMED for the final review (pre-existing, NOT introduced here): tests/controller/runLoop.integration.test.ts "treats execute timeout with no adapter result as exhausted..." ties perAttemptTimeoutMs and totalRuntimeBudgetMs to the same 20ms and races on that frozen snapshot. Reproduced 1 in 10 full-suite runs. This is now the SECOND known timing-dependent test in the suite (the first is L1's real-filesystem-timing interleaving test).
Task 4: complete (commits 8dd130a..6a10b48, review clean after 1 fix round)
Task 5: implementer returned DONE_WITH_CONCERNS (commit de23cfa, 371/371, typecheck+build clean). DEVIATION FROM THE BRIEF, flagged by the implementer itself: the write guard is NOT unconditional as the brief specifies — it is conditioned on `nextOwnerEpoch !== null`, i.e. it only guards a write following this process's own just-adopted transfer. Stated cause: an unconditional guard broke two pre-existing baseline tests whose losing-CAS path depends on writeBoundaryArtifacts's own preserveSuccessfulReconciliationIfNeeded safety net.
Task 5: implementer also reports mutation 2 (move the entry guard after the "healthy" early return) is UNFALSIFIABLE — observedStrongProgress is hardcoded false at the only call site, so the healthy branch is dead code and no runtime test can distinguish guard placement relative to it. Flagged rather than fabricated (same honesty as Task 4's unobservable-mutation call).
Task 5: review dispatched (opus) with the conditional-guard deviation named as the central risk — spec requirement 7 is "no artifacts written after supersession", and the delivered condition leaves the non-transfer write path unguarded.
Task 5: review (opus) — spec ❌ (requirement 6 met and well tested; requirement 7 UNMET), 2 Critical, 2 Important, 3 Minor.
Task 5: requirement 6 ✅ — entry guard is the function's first statement, ahead of both the healthy early return and the span's readOwnerRecord. Reviewer checked the guard does not trip the hazard it protects: assertHeld reads via readOwnerRecordWithoutRecovery, not readOwnerRecord. The requirement-6 test asserts FILE STATE (recovery marker + both pending files survive, owner-record still names the rival), not merely that the call threw — it clears the bar the brief set.
Task 5: Critical 1 — requirement 7 unmet on every path where this process does not perform its own transfer. Traced: pass entry guard, verdict not OWNER_LOST (or takeover not allowed, or CAS mismatch) -> nextOwnerEpoch stays null -> rival supersedes during the span -> the guard at :813 is skipped and :817 writes both artifacts. The guarded case is the MINORITY path.
Task 5: Critical 2 — the safety rationale shipped in the code comment is factually wrong twice. (a) boundary-analysis.json is written unconditionally at fileStore.ts:308 with NO preservation logic at all, so a superseded process clobbers the winner's boundary analysis on every unguarded path. (b) preserveSuccessfulReconciliationIfNeeded bails to the raw write when readPersistedSuccessfulTransferArtifacts returns null, and additionally requires transferRepresentsPublishedWinner — a rival that rotated the owner record without publishing owner-transfer.json gets clobbered too. WORSE: that same net calls readPersistedSuccessfulTransferArtifacts -> readOwnerRecord -> recoverInterruptedOwnerTransfer, so the UNGUARDED write performs exactly the recovery-on-read write the entry guard exists to prevent. The two guards as shipped contradict each other.
Task 5: Important 3 — the implementer should have returned BLOCKED per the brief's explicit trap instead of narrowing the guard to keep two tests green. Its stated principle is self-inconsistent: the shipped narrow guard ALSO changes a terminal outcome and its own new test asserts that (stopReason "lease_lost" on a run that would otherwise report "exhausted"). Any refusal here changes the reported outcome, because both call sites precede terminal persistence.
Task 5: Important 4 — GENUINE SPEC-LEVEL CONFLICT, escalated to the human. tests/controller/runLoop.integration.test.ts:1510 "preserves a synthesized winner reconciliation view": the LOSING process's write is the only thing that creates reconciliation-record.json there — it synthesizes the winner's view from the winner's published owner-transfer.json. A literal requirement 7 deletes that repair. Real conflict between the new spec and an established L1 repair path.
Task 5: minor (deferred): mutation 2's unfalsifiability claim VERIFIED — evaluateRunBoundary returns "healthy" only when observedStrongProgress is true and the sole call site hardcodes it false, so the early return is dead code and guard placement across it is unobservable. But the mutation that WOULD discriminate requirement 6 (move the entry guard to just after readOwnerRecord) was never run, and the requirement-6 test is built to catch exactly it.
Task 5: minor (deferred): the new requirement-7 test copies the known-flaky 20ms/20ms timing shape plus partialOutcomeRecoveryWindowMs: 10 against wall clock — the implementer itself identified that shape as flake-prone.
Task 5: reviewer checked and did NOT flag: INERT_LEASE_HEARTBEAT makes both new guards no-ops when runLoopFromState runs without a heartbeat, but it does the same to all fourteen pre-existing assertHeld sites — established behavior, not introduced here.
Task 5: HELD FOR HUMAN RULING before any fix round is dispatched.
Task 5: HUMAN RULING on Important 4 — guard UNCONDITIONALLY, per spec §5.4 and requirement 7 as written. The behavior deliberately given up: a superseded process no longer synthesizes the winner's reconciliation view. Rationale: this layer's whole thesis is "only ever refuse, never grant", and the deleted write is a process that has lost ownership writing into a run it no longer owns — exactly what the layer exists to stop. Accepted risk, to be inherited by L5's spec: if L2-L5 consume reconciliation-record.json as evidence, there is a gap until L5 assigns the synthesis responsibility to someone who still holds the run.
Task 5: fix round 1/5 dispatched (resumed implementer a91a37e70ae632df1) — unconditional guard, correct the false safety comment, re-express (never delete) the two baseline tests to assert the refusal, run the discriminating requirement-6 mutation, widen the new test's timing margins.
Task 5: fix round 1/5 (all findings addressed, 0 open; commits de23cfa..7ee5cd3). Re-reviewer (opus) verified rather than accepted: the §12 claim checked against all nineteen requirements — neither edited test maps to the protected six (2 recycled PID, 5 heartbeat survives own writes, 7 corrupt record refused, 15 resume immediately after transfer, 17 finished run releases lease, 19 assertHeld never throttled), so BLOCKED was correctly not the outcome. The touched requirements were STRENGTHENED, not weakened: req 9's retained-worktree assertion survives byte-identical in both tests, req 13's "artifact write does not happen" is now asserted directly where it previously was not, req 8's lease_lost stopReason is newly asserted in both.
Task 5: re-reviewer confirmed the terminal status moving "exhausted" -> "cancelled" is a NATURAL CONSEQUENCE of the guard throwing, produced by pre-existing untouched code (isLeaseStopError -> persistTerminalState at runLoop.ts:1297-1304); nothing was adjusted to manufacture it.
Task 5: re-reviewer confirmed the three tests failing together under the unwrap mutation is correct rather than redundancy — three different assertions against three different setups (stopReason becoming the phase-timeout message; status reverting to "exhausted"; access(reconciliation-record.json) flipping from rejecting to resolving, which is the synthesis path).
Task 5: minor (deferred, introduced by the fix): tests/controller/runLoop.integration.test.ts:1510's NAME now asserts the opposite of what the test asserts — "preserves a synthesized winner reconciliation view..." on a test whose decisive assertions are that no such view is produced. The comment explains the reversal; the name was left untouched.
Task 5: minor (deferred, introduced by the fix): five retained reconciliation.* assertions in the other baseline test now echo the test's own mock input rather than discriminating anything, since this process's write is refused. Harmless residue, but under Rule 9 they no longer encode why anything matters.
Task 5: SPEC DEFECT OWED (out-of-scope, for the whole-branch pass): spec §2's third non-goal bullet is now contradicted by shipped code AND its reasoning is inverted — it claims that because both persistBoundaryAnalysis call sites precede terminal persistence, no guard here can flip a reported outcome. Preceding terminal persistence is precisely what CAUSES the flip: the refusal reaches isLeaseStopError before persistTerminalState ever runs. task-5-brief.md:19 inherits the same inverted reasoning.
Task 5: complete (commits 6a10b48..7ee5cd3, review clean after 1 fix round)
ALL 5 TASKS COMPLETE. Full suite 371/371, typecheck and build clean.

=== FINAL WHOLE-BRANCH REVIEW (opus) — verdict: With fixes; 0 Critical, 3 Important, 6 Minor ===
Controller independently verified: 23 files / 371 tests green, typecheck clean, build clean, tree clean, 13 commits on the branch.
Reviewer verified by MUTATION rather than by reading reports, in a throwaway copy of the tree: delete the pre-write guard -> 3 tests fail; delete the entry guard -> req 6 fails; retry attempts 3 -> 1 -> req 1 fails; retry CAS mismatch too -> req 3 fails. Also grepped all of src/ for consumers of the split error classes and found exactly the four the spec names — no fifth path missed. Also confirmed the two error classes are structurally unconflatable at the seam: lock-busy is thrown only at fileStore.ts:511/:516 inside acquireOwnerTransferLock, precondition only at :584/:615/:652, all AFTER the lock is held.
CLOSED by the final review (was deferred): the Task-3 fire-and-forget burden is discharged (runLoop.ts:738 awaits runExclusive with no .catch); the discriminating req-6 mutation has now been run and req 6 kills it.
CLEARED: the stop()/runExclusive race Task 3 flagged is NOT reachable today — both runLoop and resumeLoop call stop() in a finally AFTER awaiting runLoopFromState, and both persistBoundaryAnalysis call sites are awaited. Real in the abstract; carry into L2's design notes, not a defect here.
CLEARED: exclusive-span blast radius. Every statement inside the span read; nothing re-enters the queue, adopt is synchronous and unqueued, both assertHeld sites are outside it, worst-case occupancy ~100ms against a 90s TTL.
Final-1 (Important): spec §6 requirement 4 is NOT discharged — its named mutation SURVIVES its own test. Replacing `queue.then(fn, fn)` with `fn()` leaves `queue = result.then(...)` intact, so work queued AFTER the span still blocks correctly; what breaks is the other direction — the span no longer waits for an affirm ALREADY IN FLIGHT. Req 4's test starts the span first and only then calls affirmNow, so it exercises only the surviving direction. The in-flight case is the ORDINARY one (the interval timer fires at arbitrary points), and it is precisely defect 2 from spec §1. Neither Task 3's nor Task 4's mutations targeted it. Fix: add the mirror-direction test.
Final-2 (Important): the catch-path re-read at runLoop.ts:782 is a second, UNGUARDED readOwnerRecord — i.e. it performs the very recovery-on-read write the entry guard exists to prevent — and it sits on the path that most strongly indicates a rival now owns the run, up to 100ms of backoff after the entry guard. Pre-existing line, but this branch is what elevated the hazard to a named, tested design property while leaving this instance out. NOTE THE CONFLICT: spec §5.4 ends "No third guard."
Final-3 (Important): five NEW tests on this branch copy the 20ms/20ms + partialOutcomeRecoveryWindowMs:10 shape that this branch itself diagnosed as flake-prone (leaseLifecycle.integration.test.ts:323,396,489,579,680,877 — all six occurrences are new here; the base file has zero). The req-7 test was widened in the fix round with a comment spelling out why the shape is wrong; that reasoning was never applied to the other five. Not reproduced in 12 isolated + 6 full-suite runs — structural risk, not observed.
Final minors: contention event carries no epochs or correlation where its sibling owner_epoch_transferred does; the retry loop would report success if the bound were ever 0 (unreachable today, fragile shape); test name at runLoop.integration.test.ts:1518 still inverted; five residual assertions echo mock input; the "3 x 50ms <= 150ms" comment overstates a 100ms bound.
Final architecture note (pre-existing, WIDENED here — for L5's inheritance list): the refusal at :807 propagates to runLoop.ts:1297-1304, where persistTerminalState writes run-state.json and appends loop_cancelled INTO A RUN THIS PROCESS NO LONGER OWNS. The layer's thesis is enforced one frame down and violated one frame up. This branch's new guard makes that path materially more frequent.
SPEC AMENDMENTS OWED: (S1) §2 non-goal 3 and the plan's "No terminal-state changes" are both contradicted by shipped code, and the reasoning is INVERTED — preceding terminal persistence is what CAUSES the flip; task-5-brief.md:19 inherits it. Confirmed no exit-code impact: nothing in src/cli.ts maps run status to a process exit code. (S2) §5.1 step 4 and §5.4 name guardedWriteArtifacts, which is unreachable from persistBoundaryAnalysis; the inline assertHeld is correct. (S3) §5.4 must either extend the guard to the catch-path re-read or park it with reasoning. (S4) §6 requirement 4 must require BOTH interleaving directions. (S5) record the new on-disk shape: a completed owner-transfer.json no longer implies a reconciliation record.
HUMAN RULING on Final-2: ADD the guard before the catch-path re-read at runLoop.ts:782, and amend spec §5.4's "No third guard" to say what it actually meant (no guard on the transfer CAS itself). Rationale: the entry guard's own justification applies to that line more strongly, and leaving it out makes the property this branch just made testable self-contradictory.
FINAL FIX WAVE dispatched (one dispatch, fresh implementer, opus): Final-1 mirror-direction test, Final-2 guard + test, Final-3 widen five tests, Minor 6 inverted test name, Minor 8 wrong bound in comment, spec amendments S1-S5.

=== FIX WAVE RE-REVIEW (opus) — all findings addressed, 0 new Critical/Important ===
Re-reviewer verified each claim against the fixtures rather than accepting the report. Notably it did NOT accept the implementer's own justification for Final-3: the implementer argued "each test's decisive assertion still passing is itself the proof its branch is still reached", which is circular. The re-reviewer instead checked, per test, that a positive assertion exists which would throw or fail if the run diverted early (reconciliation-record read, writeCalls counts, owner_epoch_transferred exactly-once, currentOwnerEpoch) — none can pass vacuously. Conclusion stands, the argument did not.
Re-reviewer also confirmed: the branch at runLoop.ts:1049-1091 returns after ONE attempt and reports "exhausted" on BOTH sides of the budget ternary, all six adapters reach timedOut by blocking on waitForAbort rather than racing, none of the six asserts stopReason, and the two gated tests POLL for their observation rather than depending on a fixed microtask depth — so widening the timeout lengthens the poll and cannot skip the observation.
Final-2's covering test verified deterministic against the fixture, not the claim: no sleep, no poll, no wall-clock ordering; LEASE_HEARTBEAT_INTERVAL_MS is 30s so no timer tick can fire inside it; assertions discriminate in BOTH directions (they fail if the mock never ran AND if recovery finalized the staged files). stopReason is lease_lost with or without the guard, which is what makes the file-state assertions the sole discriminator.
Final-1's double-sided evidence confirmed present: the new mirror test fails under the mutation, requirement 4's own test PASSES under the same mutation, and the twin unit test also passes under it.
PARKED (Minor, introduced by the wave): runLoop.ts:788's new guard comment restates the 150ms backoff bound that the same wave corrected at :79-81 (real bound 100ms, because the delay is skipped before attempt 1). Phrased "up to", so not false — but it re-states arithmetic the wave existed to fix. Ruling: real, cosmetic, deferred.
PARKED (Minor, report accuracy): the pre-existing BUDGET_EXHAUSTED_REASON flake family has FOUR members, not the three the report names — runLoop.integration.test.ts:1002, :1258, :1655, :1773. The unlisted one is :1258. Ruling: leaving all four alone was CORRECT — they assert the hasBudgetExceeded === true side of the ternary, so applying this wave's widening would silently flip what they assert, which is the exact failure mode Final-3 warned against. The report's proposed fix (raise perAttemptTimeoutMs only, hold totalRuntimeBudgetMs at 20) is sound and strengthens rather than relaxes them, but needs its own verification pass and must cover four tests.
NOTED (not a defect): owner_transfer_contended is appended at runLoop.ts:774-778 BEFORE the new guard at :794, so a superseded process on the lock-busy path still writes one event before refusing. Ordering predates the fix and matches the human ruling as stated; the guard does not fence that append.
NOTED (carried forward): requirement 4's own test still carries a "Kills:" comment that is only true when read alongside the new mirror test. No test restructuring was permitted in the wave; spec amendment (d) is the mitigation.
CONTROLLER FINAL VERIFICATION: 23 files / 373 tests green, typecheck clean, build clean, tree clean, 17 commits on the branch.
ONE UNIDENTIFIED FAILURE, recorded honestly rather than attributed: an earlier controller verification run reported 372/373 with 1 failure. The failing test's NAME WAS NOT CAPTURED — the controller piped that run through `tail -8` and truncated the failure block. That is the controller's error, not the implementers'. NOT REPRODUCED in 13 subsequent full-suite runs (3 + 10). The likely candidate is the pre-existing BUDGET_EXHAUSTED_REASON flake family (runLoop.integration.test.ts:1002, :1258, :1655, :1773), but "likely" is not evidence and this branch already ruled once (Task 4) that unfalsifiable flake attribution is not acceptable. So: unidentified, unreproduced in 13 runs, name not captured. Whoever next sees a 372/373 must capture the full output.
KNOWN FLAKE DEBT INHERITED BY THE NEXT LAYER (not introduced here, deliberately not fixed here): the four-member BUDGET_EXHAUSTED_REASON family above, plus L1's real-filesystem-timing interleaving test. Fixing the four needs its own verification pass — raise perAttemptTimeoutMs only and HOLD totalRuntimeBudgetMs at 20, because they assert the budget-exceeded side of the ternary and this branch's widening would silently flip what they assert.
