# SDD ledger — L5 输入盘点（scan round，非实施轮）

前置层：L3 已完成并合入 `main`（三组 15 任务、三道门 `e5bf650`(A) / `bafa6a6`(B) / `81f3819`(C)，0 Critical）。
L3 的 ledger 在 `.superpowers/sdd/2026-08-02-sweep-and-transactional-continuation/progress.md`，仍是 L3 范围内的唯一可信进度源。
本文件只记本轮（L5 输入盘点）自己的进度，**不取代**上面那份。

--------------------------------------------------------------------------------
STEP 0 — 基线复跑。2026-08-05。控制器亲跑，未过滤，`rtk proxy` 单条命令。
--------------------------------------------------------------------------------

环境：`ECC_GATEGUARD=off DISABLE_OMC=1`（先 `export` 再 `rtk proxy`，因为 `rtk proxy`
不接受以环境变量赋值开头的引号串）。工作区 `/Users/biran/code/skills/loop/ccloop`，
分支 `main`，HEAD `e9021ef`，`git status --short` 干净。

  RUN 路径 = `/Users/biran/code/skills/loop/ccloop`（vitest 首行，按组 C 的常设规则 2 验收）
  Test Files 30 passed (30) / Tests 514 passed (514) / TEST_EXIT=0 / 18.03s
  TYPECHECK_EXIT=0（`tsc --noEmit -p tsconfig.json`）
  BUILD_EXIT=0
  Guard 1 `grep -cF 'return { ok: false' src/controller/resumeLoop.ts` = 8
  Guard 2 `grep -rnF 'currentOwnerEpoch + 1' src/` 单点命中
          `src/ownership/ownerController.ts:166:  const nextEpoch = ownerRecord.currentOwnerEpoch + 1;`
  两条允许的 flake 都没出现；没有名单外的失败；没有重跑。

30 files / 514 tests 与 GATE-C 在 `b9afbf3` 及合并后 main 上的两次记录一致。GATE-C 之后
main 上又落了三笔 docs 提交（`cf03278` 之后的 `b9afbf3`→`e9021ef` 段），本次是这三笔之后
的第一次复跑，**不继承任何先前的绿**。

--------------------------------------------------------------------------------
两处与交接文字不符的状态，以命令输出为准
--------------------------------------------------------------------------------

1. *** PUSH 已经完成，且不是本会话做的（第 4 次会话外推送）。***
   L3 ledger 结尾的 "STILL NOT DONE" 第 1 条（"NO PUSH. This session has never run
   `git push`"）与 handoff 顶部「下一个动作是 push」**均已腐坏**。

     git rev-parse HEAD                     -> e9021ef87770acf8052bc4c509e56a1aa226523f
     git ls-remote origin refs/heads/main   -> e9021ef87770acf8052bc4c509e56a1aa226523f
     git reflog show refs/remotes/origin/main --date=iso 首行
       -> e9021ef refs/remotes/origin/main@{2026-08-05 09:03:09 +0800}: update by push

   该 push 时间戳晚于 handoff 提交 `e9021ef`（09:02），本会话从未执行 `git push`。

2. *** 组 C 的分支与 worktree 已经不在了。***
   L3 ledger "STILL NOT DONE" 第 2 条说 `feat/l3-group-c-sweep` 与
   `.claude/worktrees/l3-group-c-sweep` **都还在、删除需单独授权** —— 与实测不符：

     git worktree list -> 只有 `~/code/skills/loop/ccloop  e9021ef [main]`
     git branch        -> `main` 与 `backup/evidence-first-v1-before-memory-history-cleanup`

   handoff（更晚）写的「三组的分支与 worktree 都已按授权清理（各自删前都验过
   `--is-ancestor` 且枚举过未入库产物）」与实测一致。**ledger 那一条已腐坏，本条为准。**

   => L3 ledger "STILL NOT DONE" 三条里，第 1、2 条都已完成，只剩第 3 条：L5。

--------------------------------------------------------------------------------
STEP 1 — L5 输入盘点开工。2026-08-05。人已选定入口 A（先盘点 + 独立复核，拿证据再定边界）。
--------------------------------------------------------------------------------

**为什么先盘点**：组 C 开工前做过一次计划冲突扫描，提前裁掉 7 条冲突、查证后清空 13 条；
组 A 与组 B 没做，各赔一轮返工。本轮沿用该做法。

**§13 的条目由控制器自己数过，不从 handoff 抄合计数**（本仓库的规矩：绝不把聚合数字从
交接文档传播进 dispatch）。逐条数：第 1 笔锁可被偷 / 第 2 笔 execute abort 无第二重上界 /
第 3 笔 `writeBoundaryArtifacts` 落 exclusive span 外（**原文明写「本轮新发现，需要一次
归属裁决」**）/ 第 4 笔 输家 reconciliation 写的残余 TOCTOU / 第 5 笔 被拒 run 的无退避
重捡 = **5 笔**，加债 2 = **6 项**，与 spec §13 开头、§13 末尾括注、§14 第 1 条三处自称的
数字一致。

**但清单在第一眼就被发现不全**（控制器读 §13 时当场撞到，作为扫描员 D 的起点而非终点）：

  - spec §13 正文（`finalizePendingOwnerTransfer` 那段末尾）明写 catch 里「三个
    `safeUnlink` 都可能替换正在传播的错误」这个错误掩盖问题「**原样留给 L5**」——
    **在正文里交接，却不在那 5 笔里，也没被计入 6。**
  - §5.4 一带把「人按过 Ctrl-C 的信息只进 events.jsonl、无消费者」记为**属 L2/L5**，
    同样不在 6 项内。
  - spec 约 :209 写「§16 第 11 行把那个伪结构固化进修订索引，**会被 L5 继承**」——
    一条**纯文档**的继承，同样不在 6 项内。

**另一件必须先说清的事：L5 至今没有 spec。**
`docs/superpowers/specs/` 下没有任何 L5 文件；`2026-07-26-run-lease-and-heartbeat-design.md`
逐字写着 L5 "remains unwritten"；L5 的本职是 cleanup / orphan GC（删除），来自父设计
`2026-07-28-run-registry-design.md` §17 item 3。**所以「进 L5」的第一步是设计，不是实施**，
这一点已向人明确提出。

**派发（4 名只读扫描员，并行，切片互不重叠，各写各的报告文件）：**

  scan-A-debt2-lock-abort.md          债 2 ＋ §13 第 1 笔（锁可被偷）＋ 第 2 笔（execute abort）
  scan-B-span-and-toctou.md           §13 第 3 笔（span 外，含 L1 §12 第 17 条的三类失败）
                                      ＋ 第 4 笔（输家 TOCTOU，含 readPersistedReconciliationRecord）
  scan-C-backoff-and-gate-carries.md  §13 第 5 笔 ＋ 门上追加的四项（resumeLoop 并发裸读 /
                                      组 B 两条 / spec:692,751+plan:1004 论据腐坏 /
                                      GATE-C 的 deferred minor 清单）
  scan-D-offlist-sweep.md             **清单外扫荡**：整个 docs/superpowers/ 里所有交给 L5
                                      （或 L2/L5、或含糊指向后续层）却不在那 6 项里的项；
                                      外加三个必答问题（L5 的原始委任状 / §16 第 11 行的伪结构 /
                                      有没有第二条「触发条件已被 L3 改变」的撤回先例）

每名扫描员的 brief 都写死了八条铁律，其中最要紧的一条：**发现文档论据在今天代码上不成立，
原样上报，不许自改判据、不许自裁分级。** 并要求每项区分「结论腐坏」与「论据腐坏」。

**下一步（尚未执行）**：四份报告回来后合并，派**没参与过本轮任何一份**的独立评审员复核，
再带着证据找人定 L5 的边界。**不接受扫描员自证。**

--------------------------------------------------------------------------------
扫描员 D（清单外扫荡）已交付。2026-08-05。报告：`scan-D-offlist-sweep.md`
--------------------------------------------------------------------------------

*** 控制器在派单里写了一条假前提，被扫描员当场证伪。记账在此。 ***

  控制器的 brief 写：「父设计 `docs/superpowers/specs/2026-07-28-run-registry-design.md`
  §17 item 3 是 L5 的原始委任状」。**该文件没有 §17，止于 §15**（D 已贴输出）。
  真正的委任状在**所有权设计** `2026-07-22-ownership-and-reconciliation-boundaries-design.md`
  §17 item 3，逐字是：

    > **Cleanup / orphan handling design** — how superseded or lost-owner
    > workspaces and evidence are retained or cleaned up safely.

  错因：L3 spec §14 写「（父设计 §17 item 3）」、run-registry spec :529 写
  「Parent §17 item 3」，控制器把「父设计 / Parent」错解成 run-registry 本身，
  而它其实指向更上游的所有权设计。**这是本轮控制器的第一次自曝错误**，按本仓库的
  规矩（「控制器也会犯错，且必须记账」）记录，供复核者核。**扫描员的处置是正确的：
  原样上报派单前提有误，没有替控制器圆场、也没有自己改判据。**

*** 最重的发现：L5 的本职工作今天没有任何设计输入。 ***
  委任状只有一行字；L1 分层表给 L5 的授权是 `| L5 | cleanup / orphan GC | deletion |`
  —— **`deletion` 是全五层唯一的删除授权**；而已具名的 6 项输入**无一属于删除 / GC /
  保留域**，只有两个半句触及（D5「被硬杀的进程留下的 run 本层碰不到（属 L5）」、
  D6「本层无自动化入口」），**且这两条都不在那 6 项清单上**。
  => 「进 L5」= 先写设计，不是照单实施。此前向人提出的判断得到独立证据支持。

*** 清单外交接项：13 条（D1–D13），逐条出处与逐字引用在报告内。 ***
  D 拒绝报一个单一的「共 N 条」，理由成立且必须保留：**「条目」是人工归并单位**
  （§13 自己就把若干事「不单开一笔」并进已有笔），**没有命令能重数它**。它改为给出
  两个**可重数**的行数，并附命令与输出：
    `grep -rn 'L5' docs/superpowers/ | wc -l` → 78
    四份门报告里「带到 L5」类 → 23（23 行逐字贴在报告 §3.2）
  **这个处置本身值得沿用：报不出可重数的计数就不要报数字。**

  控制器起手给的三条（finalize catch / Ctrl-C / §16 伪结构）= D1 / D2 / D3，全部坐实。
  另外十条里最要紧的两条：
  - **D7（漏做）**：L2 spec `:57`/`:468` 至今仍写「three debts bequeathed to L5」，
    而 2026-07-29 的归属裁决已把清单降到**只剩债 2 一笔**。债 4 拿到了勘误，债 1/3 没有。
  - **D4（已关闭但未勘误）**：L1b `2026-07-27-owner-transfer-contention-design.md:113`
    的「is L5's problem」**人已裁定关闭**，spec 却从未落勘误；**当时的评审员点名
    「NOT done here / Flag for the human」**，此后无人接。

*** 「第二份交接清单」确认存在，但措辞已按要求降级，且降级是对的。 ***
  台账与四份门报告里另有一整批交给 L5 的项（resumeLoop 并发裸读、spec:692+:751 两句
  过期前提、组 B 两笔债、GATE-C 的 C1-M2/C1-M3/C2-M1..M6/C3-M5/C3-M6/C4-M1 分诊，
  外加一条自曝「本波未记」的 zod 项），**与 §13 那 6 项完全不相交**。
  但它**不是**「评审员偷偷另立了一份清单」——**是有意不折算**，证据是同波记账明写
  「§13 清单 5 笔 / L5 输入 6 项 … 本波一处都没动」。
  => 两份清单并存、彼此无指针，这是**文档缺陷**，不是治理失守。

*** 第三次栽在同一个形状上（D 的 §5.3，本轮最重的文档矛盾）。 ***
  当前 L3 spec 内部：§9 的「回调 = 一次数组 push」已被 `:1578` **就地推翻**，而 `:692`
  与 `:751` 仍以它为**前提**，且 `:751` 是**按名字引用 §9** 的。HEAD 的 `b9afbf3`
  只同步了一处。**这与 GATE-C 交给 L5 的那两句是同一件事**（扫描员 C 独立负责该项，
  两份报告将互为交叉校验）。

*** 「触发条件已被 L3 改变」的第二条撤回先例：找到了。 ***
  「三份 pending 的非原子写」——第二轮作为第 4 笔推给 L5，人裁 pending 原子化后触发
  条件消失，**整笔由本层收回**（spec:201 / :979 / :2489），同轮残余 TOCTOU 顶上，
  形成「一出一进」，故条数与合计未动。
  **另有反方向的第三条**：L1b (e) 已被人裁关闭却未落勘误（= D4）。
  逐条核过**未变**的：D2 与第 5 笔（`observeFields.ts` 在 HEAD 仍是 3 个观测文件，
  输出已贴）、D1、D6。

*** §16「第 11 行」的锚点歧义（D 自己发现的附带一笔）。 ***
  §16 同时存在 `11` 行（S-3 退路遗漏）与 `11b` 行（伪结构），而表头与 §4.0.4 都把
  **后者**称作「第 11 行」。控制器派单时引用的「§16 第 11 行」因此本身是个歧义锚点。
  伪结构的内容：第一轮虚构「裁决记录对『更窄』给了两条依据 (a) 排序主张 / (b) assertHeld
  是写者」，再宣布驳倒 (a)；而裁决记录**只给了 (b) 且 (b) 成立**，(a) 是一条独立否决，
  被自造成论敌。L5 若沉默继承，会拿到两个假前提：「『更窄』已被驳倒一半」「排序主张
  已处理」，以及元层面的「§16 表里每行都已修好」。

*** D 自报的四处不完整（未完成即明写，未拿部分证据凑完整结论）：***
  §5.1 的「§2 vs §5.4」张力缺 `persistBoundaryAnalysis` 触发路径，**无法判断是否真矛盾**；
  spec §4.6「代码零改动」现状未核实（D12 因此悬空）；
  `cleanupOwnerTransferStagingWithoutMarker` 未读源码；
  `docs/handoff/handoff.md` 未读（超派单范围，但 HEAD 恰好动了它）。
  **这四处必须进复核者的输入，不许当成已查。**

--------------------------------------------------------------------------------
扫描员 C（§13 第 5 笔 ＋ 门上追加四项）已交付。2026-08-05。
报告：`scan-C-backoff-and-gate-carries.md`（1329 行）。交付前三次被 API 中断，
第三次改为**分段落盘**（先写空骨架、每次 Edit 只填一节）后成功 —— 机制判断成立：
一次性吐整份大报告的巨型 tool call 会被流截断。**此法建议本轮后续沿用。**
--------------------------------------------------------------------------------

*** 四项的后果分级互不相同，不应被打包成一批待办。C 明确点名了这一点。 ***

  项 C（组 B 两条债）  今天零后果，**一旦触发是数据丢失**（整份 `loop-state.json` 被覆盖）
  项 B（并发裸读）      **假告警**（＋一次可重试拒绝）
  项 A（无退避重捡）    **仅可操作性**
  项 D（数组 push）     **仅文档**

  => L5 的边界讨论必须按这个分级排序，不能按「都是 L3 交下来的」一视同仁。

**项 A（§13 第 5 笔，无退避重捡）—— 完整，仍完全成立且可达。**
  `OBSERVED_FILES` 今天仍是 3 个文件（`grep -nF 'file:' src/registry/observeFields.ts`
  → 4 行命中 `:7/:29/:45/:111`），`reconciliation-record.json` 不在其中。
  `--max-runs` 界的是 `adopted`，只由 `onAdopted` 自增，**被拒 run 不消耗配额、`break`
  不触发**。「每次 2～3 行事件」经复核是精确的：下界 2 = `resume_requested` +
  `resume_denied`，上界 3 = 再加 `lease_expired_observed`。
  分级**仅可操作性**：报告行走 stdout，不触发 cron 的「有 stderr 即告警」。

**项 B（`resumeLoop` 并发裸读）—— 完整。GATE-C 给的分级今天仍成立，C 没有改它。**
  `Promise.all` 读五份 artifact，只有 `readOwnerRecord` 在 `fileStore.ts:1025` 前置
  recovery，另三个是裸 `readFile` + `JSON.parse`。抛出点 `resumeLoop.ts:145` 排在
  CAS(`:161`) 与 heartbeat(`:185`) **之前** ⇒ 除两行 append 外零写 ⇒ **不是数据丢失**；
  经 `classifyThrow` 前缀支路 ⇒ stderr ⇒ **假告警**成立。**分级被独立复核确认，可以继承。**

**项 C（组 B 两条债）—— 完整。两条今天仍不可达，但触发后果是四项里最重的。**
  `isLeaseStopError` 仍是两个 instanceof、未导出（`runLoop.ts:107-109`）；专用分支
  `:1489` 排在谓词分支 `:1507` 之前并提前 return；`writeRunState` → 
  `writeJsonFileAtomically` 是 stringify→temp→rename，**无 CAS**；`.stop()` 的生产调用点
  **恰好 2 个**（3 处命中里有 1 处是注释），都在 `finally` 内。
  **触发条件已被写成可直接贴进未来 brief 的一句**（见报告项 C）。

  *** ⚠️ 一处新的腐坏，GATE-C 时不存在：加宽惰性的实测是在 **487 tests** 时做的，
  今天是 **514** —— 新增的 27 个 case 无人验证过。**L5 若要依赖「加宽今天仍惰性」这个
  结论，必须重跑注入，不许继承。*** 这一条是本轮新增的，不在任何既有交接里。

**项 D（spec:692/:751 + plan:1004 的「一次数组 push」）—— 完整。**
  **论据腐坏、结论不腐坏，判得住**：代码上回调今天是 `options.stderr(...)` + 格式化
  （`sweepRuns.ts:205-209`），调用点 `fileStore.ts:478` 是裸调用、无 try/catch ⇒ 原文
  三条结论**全部存活**。
  *** 但腐坏站点是 **9 处**，不是台账写的 3 处；其中 6 处至今无勘误。 ***
  （台账 `:1707-1711` 只点了 spec:692、spec:751 与 plan:1004。）
  C 已给出最小勘误建议：**3 条勘误覆盖 6 处**，未动手改。
  **与扫描员 D 的 §5.3 交叉校验一致**（D 独立发现 `:751` 是按名字引用 §9、而 §9 已被
  `:1578` 就地推翻、`b9afbf3` 只同步了一处）。两名扫描员从不同入口撞到同一处，**且 C
  给出的站点数比台账多 6 处**。

**项 E（GATE-C 的 deferred minor 清单）—— 不完整，C 自己明写。**
  条数 = ledger 的 15 条 ＋ 只存在于 lane 2 报告里的 3 条 = **18**。
    `grep -c "^Task C[1-4]: minor (deferred" …/progress.md` → **15**
    `grep -n "C2-M5\|C2-M6\|C3-M6" …/gate-c-lane2-report.md` → 4 行命中（`:14/:247/:252/:262`）
    同样三个 ID 在 ledger 里 **0 命中**
  缺口：**6 条未独立复核**（C1-M4/M5、C3-M1/M2/M4、C4-M1）；**lane 1 的 3 条 Minor
  没有任何落点**。

*** 治理层面的四条发现（C 自己找的，都不在任何交接清单里） ***

  1. **GATE-C 的 deferred-minor 分诊结论从未落进 ledger。** GATE-B 有整段逐条处置，
     GATE-C 的同位段落**不存在**；分诊只活在 lane 2 报告的 §4.1/§4.3，
     而 ledger `:1775` 却写着「the deferred-minor list **triaged at this gate**」。
  2. **ledger `:1631` 承诺「logged below as C2-M5, C2-M6, C3-M6 and two doc items」，
     此后这三个 ID 一次都没出现**，两条 doc items 也没有。
  3. **lane 1 的报告可能根本没落盘。** 目录里有 `gate-b-lane2-report.md`、
     `gate-c-lane2-report.md`、`gate-c-fix-wave-report.md`，**没有任何 lane 1 报告**，
     而 lane 1 的 3 条 Minor 在 ledger 里零记录。
  4. **C1-M3 的论据今天不成立**（`tests/cli/cli.test.ts:366` 已用 `resolves.toBe(1)`
     守住那条 `return 1`），**而 lane 2 在 GATE-C 上把 C1-M3 当作「仍成立」的对照物**
     来论证 C2-M5。C2-M5 的事实经复核成立，**但它的论证支柱当时就已过期**。

*** 两条附带的腐坏 ***

  5. **行号锚点已腐坏到第三层**：代码 → spec（`cli.ts:131/135/130`）→ **纠正 spec 的那份
     报告自己也腐坏**（lane 2 写 `:2948`，今天是 `:2958`）。lane 2 提过根因方案
     （改用「符号 + grep」锚点），**GATE-C 没有把它采纳成规则**。
  6. **spec §12 `:2306`「`--max-runs` 界的是付费调用」是一处漏同步的旧措辞** ——
     §6 第四轮已就地改为「界的是进入 `runLoopFromState` 的 run 数，付费上界
     N × maxAttempts」。**这处论据腐坏从未被记成任何一笔债。**
     （控制器注：本轮派给扫描员 C 的 brief 逐字引用了 `:2306` 这句旧措辞并标注为
     「原文明写」。引用是忠实的，腐坏在文档自身；但**这说明该处旧措辞确实还在把
     下游读者引偏**，已由扫描员当场撞出。）

**合规**：C 报告全文两处输出削短均已在原地明写；三处自己的转录笔误已当场标注并保留；
并按 CLAUDE.md Rule 6 明写本任务超出 12,000 token 上限而非静默超支。

--------------------------------------------------------------------------------
扫描员 B（§13 第 3、4 笔）已交付。2026-08-05。报告：`scan-B-span-and-toctou.md`（919 行）。
同样三次中断在「证据核完、准备落盘」处，改分段落盘后一次通过 —— **机制第三次复现**。
--------------------------------------------------------------------------------

*** 归属属性不可互换，B 明确点名 ***
  第 3 笔（span 外）：**需要一次归属裁决**（原文「从未被任何裁决记录处理过的新发现」）。
  第 4 笔（输家 TOCTOU）：**先于本层**。
  第 3 笔的附带第 (iii) 类另带独立属性：**是 L3 自己扩大出来的**。
  => 三种属性对应三种不同的处置权，L5 的边界讨论不许把它们混成一类。

**A-1 `writeBoundaryArtifacts` + `assertHeld` 落 exclusive span 外（主体）**
  结构成立，注释 "stays OUTSIDE" 逐字命中。
  *** 但触发在今天的 `src/` 内**没有调用者** ***：两处 `stop()` 生产调用点实测都在
  `finally` 内（与扫描员 C 在项 C 独立数出的「恰好 2 个」一致）。
  **常驻形态（§14.2 `watch`）落地后可达。**
  分级：常驻形态前 = 仅文档 / 纵深防御；落地后 = **数据丢失**。
  论据腐坏：唯「一次并发 `stop()` 可以在 `writeBoundaryArtifacts` 飞行中
  `releaseOwnerLease`」这句**缺一个可达性限定**，而它的同族句 §5.3 有。

**A-2 L1 §12 第 17 条的三类失败**
  *** 第 (iii) 类**可达**，构造已给：跨进程 P1 SIGKILL → P2 删 stale 锁 → 双 finalize
  竞争。*** 分级 **可重试拒绝**（抛出点排在 CAS 与写之前 ⇒ **零写**），一次性不钉死。
  论据腐坏：行号 `:720/:729/:769` **全部腐坏**（今 `:1122/:1131/:1171`）；
  「不在任何 catch 内」「加了第三个参与文件 + marker parse」**成立**；
  *** 「两条具名抛出」是**低估** —— 今天是三条（`b7bf227` 加的 `finalizeOrderInvalid`），
  方向是低估 L3 自己扩大出来的面。 ***

**B-1 输家 reconciliation 的两条时序 —— 本轮最重的一笔**
  *** 两条**都可达**；时序二**不需要 ENOENT**（原文主张成立）。 ***
  *** 分级：**数据丢失，且两条都静默** —— `reconciliation_published_winner_replaced`
  事件一条都盖不住。 *** （该事件是 GATE-A 时专门为「把静默的记录销毁变响亮」加的，
  这里查实它盖不住这两条。）
  论据腐坏：时序二的论据**全部成立**；**时序一的「裸 `catch { return null }`」已腐坏**
  （`47eb148` 换成三态联合）——**但结论不腐**；「跨两文件的 `Promise.all`」**半腐坏**
  （今为 1+2 三文件两段）。

**B-2 `readPersistedReconciliationRecord` 的 `catch { return undefined }`（B-1 的前提条件）**
  catch 仍在，承重前提成立，**且承重面比原文更宽**。
  *** 原文三条依据里，依据 (c) 只在 `transferRepresentsPublishedWinner` 为真时成立，
  **时序二下不成立**。 *** 「先于本层 ⇒ 不在范围」那条已撤回，属实。
  分级：仅可操作性（是 B-1 的前提条件，非独立缺陷）。

*** B 自己找的六条，全部只上报、未改判 ***
  1. **（最重）§5.2 `:1143` 至今逐字写着「§13 据此把债 3 记为『exclusive span 部分
     关闭』」** —— **这正是 §13 `:2358` 点名要防的那个危害**（写成「部分关闭」会让 L5
     以为继承的是一笔已被裁决过归属的债，从而不再重新裁决）。G10 的修订处清单实测
     **未列 §5.2**。**同一文档内，今天仍可复现该失败模式。**
  2. **§4.4「规则 3 仍然不可达」被它自己的 P2/P3 路径证伪**：marker 的 ENOENT 落进
     `:938` 那个无条件 catch，抛的正是规则 3 的 `OwnerTransferMarkerUnreadableError`
     （消息含 "could not be **read** or parsed"）。A-2 第 (iii) 类的可达面因此比原文宽一条。
  3. 具名 fail-closed 抛出今天是**三条**不是两条（同上）。
  4. `47eb148` 把读#1 移出 `Promise.all` 单独 await，**方向上放大了时序二的间隙**
     （不量化 —— 原文已两次因量化被下一轮证伪而禁止量化）。
  5. 两条时序均**静默**。
  6. **`fileStore.ts:469` 的注释仍写 "a single array push, no I/O"**，而 sweep 回调实测
     是即时 stderr 写；`b9afbf3` **只同步了 spec，源码注释没跟**。
     => **与扫描员 C 的项 D 联动**：C 在文档侧数出 9 处腐坏站点（台账只记 3 处），
     B 在**源码注释**侧又找到一处。**复核者必须核这处在不在 C 的 9 处之内。**

*** 交叉校验（B 主动做的，且做得对） ***
  B 与 C 不冲突：路径不同（C 核 `resumeLoop` 裸读，B 核 `writeBoundaryArtifacts` 输家
  保护），形状一致 —— B 在 A-2 独立得到同型结论（抛出点 `:1131` 排在读 `:1132` /
  CAS `:1134` / 写 `:1139` 之前 ⇒ 零写）。
  *** B 明写「我未独立验证 C 的 `resumeLoop` 结论，上述不构成背书」。 ***
  **这是本轮最该被复制的行为**：形状相似不等于互证，B 拒绝把自己的独立结论
  借给别人当背书。

--------------------------------------------------------------------------------
扫描员 A（债 2 ＋ §13 第 1、2 笔）已交付。2026-08-05。报告：`scan-A-debt2-lock-abort.md`
（577 行）。同样四次中断在「证据核完、准备落盘」处 —— **机制第四次复现，四名无一例外**。
--------------------------------------------------------------------------------

**项 A — 债 2（`persistTerminalState` 往已不拥有的 run 写）：可达，数据丢失。**
  调用点计数**结论未腐坏**：`grep -cF 'persistTerminalState('` = 16 ⇒ **15 个调用点**
  （含定义行），其中 **4 个**由 lease-loss 到达（`:1062/:1110/:1455/:1514`）——
  与 run-registry plan 写的「十五个中的四个」一致。
  *** `git log -S` 证明最后一次变数是 `cfde8b9`（2026-07-26，L1 期），**L3 一个没增没减** ***
  ⇒ spec §13「本层对债 2 的接触面为零」**属实**，且该结论所依赖的 §5.3 选 (a) 也属实。
  可达路径：`leaseLoss.lost !== null` → `:1062` 裸写，`persistTerminalState` **无所有权守卫**。
  分级：债 2 本身 **数据丢失**（`cancelled: []` 无出边 ＋ 不在 `RESUMABLE_STATUSES`，
  写的是别人的 run）；「接触面为零」这个结论本身 **仅文档**。
  论据腐坏 2 处：§5.3 的 `grep -nF 'isLeaseStopError'` 实测由 3 行变 **4 行**
  （L3 自己的注释含该符号名）；若干行号位移。

**项 B — 锁可被偷：可达，数据丢失。原文推论逐环节全部成立。**
  六步构造已给：零长度锁窗口（`:821`↔`:824`）→ `JSON.parse("")` 进 catch（`:801`）
  → *** catch 里没有活进程检查 *** → `hasStagedArtifacts` 靠**残余 staging** 为真
  → 删掉活人的锁。
  `hasStagedArtifacts` **命中仍 2 行、仍是「定义 ＋ 使用」**（今天 `:802/:807`，原文
  写 `:542/:547` —— **行号腐坏，但条数与符号对得上**）；三路径判定与「第三份 pending
  不在其中」逐字成立。
  *** 一句不可核，原样上报 ***：「今天由 epoch 不等挡住」描述的是 L3 **合入前**的代码，
  今天的 main 上**既无法证实也无法证伪**。

**项 C — execute abort 无第二重上界：可达，仅可操作性。论据一条都没腐坏。**
  两条构造。**构造二（scripted adapter 根本不读 `abortSignal`）证明：补 SIGKILL 也关不掉它。**
  分级 **仅可操作性**：挂起不写盘、状态停在非终态 `executing`，仍可被下次 sweep 捡起。

  *** 本项的关键裁断，必须带进 L5 的边界讨论 ***
  **§13 第 2 笔与 §14 第 3 条不是同一件事，后者是严格子集。**
  §5.4 的 ⚠️ 点了**三个独立缺口**：(1) `await operationPromise`（`:454`）外无第二重
  上界（**控制器侧，与 adapter 无关**）；(2) 无 SIGKILL 升级；(3) git 子进程完全无超时
  且不在任何 `runPhaseWithTimeout` 内。**§14 第 3 条只覆盖 (2)。**
  => **第 2 笔不得因为 §14 第 3 条被立项就勾销。**

*** A 自己找的四条（报告内 6 条） ***
  1. *** 最重：`evaluateResumeEligibility` 的八条判据**无一比较进程身份**。 ***
     ⇒ 项 B 那份「owner-record 归 A、transfer 归 B」的矛盾记录**不只是通过 epoch 三元组，
     而是通过全部八条**，`resume_adopted` 会照常发生。**这把第 1 笔的后果面放大了。**
  2. 夺锁的**次生后果**原文没写：B 一进 `writeOwnerTransferArtifacts` 就跑
     `recoverInterruptedOwnerTransfer(lockHeld:true)`，marker 未存在时**删光 A 正在暂存的
     pending** → A 的 finalize 命中规则 2 抛出（`:976`）。**这是「规则 2 可达」的第三条
     路径，而 §15 验收 2 只写了另一条。**
  3. `recoverInterruptedOwnerTransfer` 的 `:1017` 分支夺锁成功后**不重新 open 锁就直接
     finalize**（`:1021`）—— §13 第 1 笔只讲了 `acquireOwnerTransferLock` 那条入口。
     *** A 明写「只做了静态阅读，未推到底，记为未完成」。 *** 必须进复核者输入。
  4. `observeChangedPathsBestEffort`（`:497`）无 `timeout` 无 `signal`，其调用点
     （`:1206`）**恰在项 C 已失去上界的那条路径上**再挂一段无上界的 git ——
     §5.4 的 ⚠️ 只点了 `createAttemptWorkspace`/`cleanupAttemptWorkspace` 两个，
     **漏了这第三个**。

*** 三条交叉校验输入全部核对，无冲突（A 逐条重跑，未照抄） ***
  `.stop()` 独立重跑确认 **3 命中 / 1 注释 / 2 个生产调用点且都在 `finally`**
  （`cli.ts:169` 注释、`resumeLoop.ts:215`、`runLoop.ts:989`）—— 与 B、C 一致，三方独立。
  行号整体腐坏与观察一致（`updateOwnerRecordWithPrecondition` 今 `:1122`、
  `releaseOwnerLease` 今 `:1171`），并已按要求把「结论腐坏」与「论据腐坏」分开写。
  fail-closed 抛出**今天确是三条**，A 补了独立证据（`:940/:949/:976`，全在
  `finalizePendingOwnerTransfer` 内）—— **与 B 的发现独立互证。**

*** 一条需要人裁的条款，扫描员正确地只转达、不自裁 ***
  三项原文均**未**明写需重裁归属。**但债 2 的裁决记录自带一条条件复议条款**：
  `docs/superpowers/decisions/2026-07-29-technical-debt-attribution.md:159`
  「若 L3 的工期显著超出预期，应重新审视是否把债 2 提前到 L3 之后、L5 之前单独处理」。
  **L3 实际跑了六波以上评审、三组十五任务、五处计划被证伪。是否已触发该条款是人的判断。**
  => **已上报给人，控制器不自裁。**

--------------------------------------------------------------------------------
四份扫描报告全部交付。合并态势（控制器汇总，尚未经独立复核）。
--------------------------------------------------------------------------------

产物：`scan-A-…md`(577行) / `scan-B-…md`(919行) / `scan-C-…md`(1329行) / `scan-D-…md`(48.8K)

**按后果分级重排（这是本轮最有用的产出，取代「6 项 / 10 项」的平铺清单）：**

  数据丢失，今天可达：
    - 债 2（`persistTerminalState` 无所有权守卫，写别人的 run）
    - §13 第 1 笔 锁可被偷（六步构造，**且矛盾记录能通过全部八条判据**）
    - §13 第 4 笔 输家 TOCTOU 两条时序（**且两条都静默，既有事件盖不住**）
  数据丢失，需常驻形态落地才可达：
    - §13 第 3 笔 span 外写（今天 `src/` 内无调用者）
  可重试拒绝 / 假告警：
    - 第 3 笔附带第 (iii) 类（**L3 自己扩大出来的**，跨进程构造已给）
    - `resumeLoop` 并发裸读（GATE-C 分级经独立复核确认）
  仅可操作性：
    - §13 第 2 笔 execute abort（**注意：与 §14 第 3 条不是一回事，后者是严格子集**）
    - §13 第 5 笔 无退避重捡
  今天零后果但触发即数据丢失：
    - 组 B 两条债（**且「加宽仍惰性」的实测停留在 487 tests，今天 514，必须重跑注入**）
  仅文档：
    - 「一次数组 push」9 处（台账只记 3 处）＋ 源码注释 1 处（B 找到，需核是否在 9 之内）
    - §5.2 仍写「债 3 部分关闭」（**正是 §13 点名要防的危害，修订清单漏了它**）
    - §16「第 11 行」伪结构 ＋ 锚点歧义
    - D7 L2 spec 仍写「three debts」；D4 L1b 已人裁关闭却未落勘误
    - spec §12 `:2306` 旧措辞从未被记成任何一笔债

**归属属性三分（不可互换）**：需重裁（第 3 笔）／先于本层（第 4 笔）／L3 自己扩大
（第 (iii) 类、fail-closed 第三条抛出）。另有债 2 的**条件复议条款**待人裁。

**L5 的本职（cleanup / orphan GC，全五层唯一的 `deletion` 授权）今天没有任何设计输入。**

**四份报告各自明写的未完成项（必须进复核者输入，不许当成已查）：**
  A：`recoverInterruptedOwnerTransfer:1017` 夺锁后不重开锁那条只做了静态阅读
  B：三处小缺口 ＋ 一处全局限制（报告内逐条）
  C：项 E 不完整 —— 18 条里 6 条未复核、lane 1 的 3 条无落点
  D：§2 vs §5.4 张力无法判断、spec §4.6「零改动」未核实、一符号未读源码、handoff 未读

--------------------------------------------------------------------------------
独立复核 lane 1（代码侧，全部独立重推）已交付。2026-08-05。
报告：`review-lane1-code.md`（1215 行）。评审员未参与过本轮任何一份扫描。
--------------------------------------------------------------------------------

*** 八条承重主张：无一被证伪，无一被高估。七条完整，第 6 条不完整（构造二未验）。***

  1  债 2 可达、数据丢失                  **CONFIRMED**（15 调用点 / 4 个由 lease-loss 到达；
                                          函数体三行**无任何守卫**；`cancelled: []` 无出边
                                          且不在 `RESUMABLE_STATUSES`）
  1b `git log -S` 证 L3 没增没减          **结论 CONFIRMED，论据形式需更正**：裸符号会跑出
                                          L3 期的 `6935578`（实为一行注释）；**带 `(` 则计数
                                          在 L3 前后恒为 16**。A 未写明带不带 `(`。
  2  六步夺锁构造                          **CONFIRMED，无一环报废**（`:821`↔`:824` 零长度窗口 /
                                          `JSON.parse("")` 进 `:801` / **catch 内零活进程检查**，
                                          `isProcessActive` 只在 `:798` 成功路径 /
                                          `:781` 解构不取 `reconciliationPendingPath`）
  3  八条判据无一比进程身份                **CONFIRMED**（本轮最需独立判的一条）。判据 4/6 是
                                          唯二跨文件比较、比的都是 epoch；函数体 `:40–:69`
                                          搜身份字段**零命中**；`applyOwnerEpochTransfer` 写死
                                          `ownerStatus:"current"` / `supersededByEpoch:null` /
                                          `eligibleForContinuation:true` ⇒ 两进程 epoch 同为
                                          N+1 时**矛盾三元组确实全过**。
  4  两条时序可达且都静默                  **CONFIRMED**。时序一走 `:408` 早退，**该 return
                                          根本没有 `publishedWinnerReplacedDetail` 字段 ⇒
                                          事件结构性不可达**；时序二被 `:298` 第二析取项挡掉。
                                          **该事件一条都盖不住，分级没错。**
  5  `.stop()` 生产调用点恰 2 个           **CONFIRMED**（3 命中，`cli.ts:169` 是注释；
                                          `runLoop.ts:989` / `resumeLoop.ts:215` 都是 `finally`
                                          内第一条语句）。**三方独立未一起错。**
  6  第 2 笔 ⊋ §14 第 3 条                **裁断成立**（缺口(1) `:454` 裸 await 无二重上界，
                                          唯一 `awaitAbortedResult:true` 在 `:1195`；
                                          缺口(3) `:497` 无 timeout 无 signal 且调用点 `:1206`
                                          正在失去上界的路径上 —— 补 SIGKILL 都不触及）。
                                          *** 但构造二未验，本条记为不完整。***
  7  487→514 且须重跑注入                  **CONFIRMED**（ledger `:711`/`:826` 记 487，评审员
                                          实跑 **514 / 0 失败**，差 27）。**要求成立**：
                                          「加宽仍惰性」是**全称否定命题**，论域扩大后原穷举
                                          不再穷举，且该结论被用于**授权改动**。
  8  fail-closed 是三条                    **CONFIRMED（低估）**（`:940/:949/:976` 全在
                                          `finalizePendingOwnerTransfer` `:931–:1005` 内）

*** 铁律合规：四份扫描报告均未违反。 ***
  无编造、无过滤、**无一份自改判据或自改分级**。评审员点名的正面行为：
  A 主动记下**对自己不利**的输出；B 明写**不为 C 的结论背书**；C 保留自己的转录笔误并标注；
  D 当场证伪**派单方**（控制器）的前提。
  唯一记账项：A 的 `git log -S` 未写明带不带 `(` —— **论据瑕疵，非铁律违反，结论仍成立**。

*** 高估：零。低估：四处。 ***
  fail-closed 三条 / `published_winner_replaced` 的覆盖面（其实一条都盖不住）/
  无上界 git 的第三处 / 「数组 push」的站点数。

*** 两处「证据强度被说得比实际高」——必须改述，这条影响 L5 的排序 ***
  - **「组 B 两条债不可达」应改述为「未发现可达路径」。**
  - 「八条判据全过」中，**判据 2/3/8 依赖输入前提，不是代码写死**。

*** 阻塞 L5 边界的未完成项，只有三条（评审员给了排序，未把全部都说成重要） ***
  1. **C 的项 E**（18 条里 6 条未复核 ＋ lane 1 的 3 条无落点 ＋ 门 lane 1 报告可能从未落盘）
     —— **唯一会让输入清单本身缺项的一条。清单未闭合就不能划边界。**
  2. **组 B 的重跑注入** —— 直接决定组 B 是「顺手补」还是「需一轮完整验证」，
     且**只能真跑，不能靠阅读**。
  3. **A 的 `recoverInterruptedOwnerTransfer:1017`** —— 评审员独立读到同一形状并确认：
     偷锁成功后 `:1021` **直接 finalize，中间无 `acquireOwnerTransferLock`**。
     **可能把第 1 笔从「锁协议」扩大到「recovery 路径」。**
  不阻塞：B 三处、D 四处、C 的项 D 九处（仅文档）。

*** 评审员自报 9 条未完成（报告 §12），最要紧两条 ***
  构造二未验；**brief 指定要核的「B 那处源码注释在不在 C 的 9 处之内」没做到**
  —— 该交界仍悬空，留给 lane 2 或下一轮。
  顺带发现：`:1017` 有**两条**不持锁 finalize 的路径（`readOwnerRecord` 常规读路径也算一条），
  **A 只记了偷锁那条**，评审员未推到底。

--------------------------------------------------------------------------------
独立复核 lane 2（文档侧 ＋ 盘点完备性）已交付。2026-08-05。
报告：`review-lane2-docs.md`（1033 行）。评审员未参与过本轮任何一份扫描。
--------------------------------------------------------------------------------

*** 本轮最重的单条发现：四名扫描员集体漏掉一条交接，而它推翻了本轮的头号结论。 ***

  `docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md` **§10 第 4 条**：
  崩溃残留的 `.{basename}.{stamp}.{seq}.tmp` 永久留在 run 目录，两条清理路径都够不着，
  **「清理归属未分配」**。
  *** 这就是委任状字面意义上的 cleanup / orphan GC，带完整分析与分级指令，是全仓唯一
  一条为 L5 本职预先写好的设计输入。 ***
  四份扫描报告**零命中**（D 读过该文件的 §2.2，没读到 §10）。
  今天仍成立：`fileStore.ts:628` 生成、`src/` 内无按模式删除、`RUN_MARKER_FILES` 仍五个。
  *** ⚠️ 且 L3 把三份 pending 改原子写之后**残留面已扩大**，该文档**从未被勘误**。 ***

  => **控制器此前向人陈述的「L5 的本职今天没有任何设计输入」为假，已更正并告知人。**

  同源另两条（较轻）：
  - §10 第 3 条「真正的防线是 L3 的评审」—— 防线已用掉，**是否兑现无法判断，
    因为 GATE-A 的门报告没落盘**（与治理第 3 条同因）。
  - 裁决记录 `:255` **自陈是自评审**，并明写「若后续层依赖本记录的某条结论，
    值得再撞一次」—— *** 而债 2 的归属正出自它。四份报告都没提。 ***

*** 治理四条：全部 CONFIRMED，且缺口比扫描员 C 报的更宽。 ***
  1. GATE-C 分诊未进台账：全仓 `DEFERRED-MINOR TRIAGE` 只有 `:431`(A) / `:1032`(B)；
     GATE-C 段（>1580）内**任何 C 组 minor ID 零出现**，而 `:1775` 写着「triaged at this gate」。
  2. `:1631` 承诺的三个 ID：**全仓唯一命中就是那句承诺本身**。加重情节：同一段一边宣告
     「补上了缺失的那一半」，一边留下从未兑现的「logged below」。
     附带：`C3-M3` 走另一记法（`:1450`），**按 ID 枚举会错位**。
  3. *** 三道门六份评审报告只落盘 2 份，全是 lane 2；GATE-A 连 lane 2 都没有 ***
     （C 没报这一点）。GATE-C lane 1 的 3 条 Minor 报告不存在、台账无记录 ——
     *** 证据已灭失，不可补救。lane 2 判定这是本轮最要紧的单条发现：
     一道门有一半的评审证据不存在。 ***
  4. C1-M3：**比 C 判得更重 —— 写下时就为假**。`git log -S` 证明该测试由 C1 自己那次
     提交 `c15b499` 加入，`git show` 显示 `it` 与 §7 注释同为 `+`。失效链完整：
     假前提 → 门 lane 2 拿它当「已记账的同形先例」→ 论证补记 C2-M5 → 承诺进 `:1631`
     → 从未兑现。*** C2-M5 需重新论证，不能继承原论证。 ***

*** 本轮唯一一处两份报告直接矛盾，且 C 错。 ***
  `fileStore.ts:469` 那条源码注释**不在** C 数出的 9 处之内。
  C 在同一句里承认自己的 grep 只跑了 spec/plan，却下了「`src/`/`tests/` 没有任何一处」的
  **全称否定**；一条 `grep -rn "array push\|数组" src/ tests/` 直接证伪（唯一命中即 `:469`）。
  *** 这正是本仓库反复栽的形状：用收窄的 grep 去支撑绝对断言。 ***
  => **最小勘误面是 10 处，不是 C 建议的 6 处；C 的「3 条勘误覆盖 6 处」不要照抄。**

*** 八条文档侧主张的复核结论 ***
  1 数组 push      **CONFIRMED，但 C 低估**：C 只搜 `push`，漏了 `spec:671`（选 (b) 那条
                   决定性论证的落脚点）/ `spec:2006` / `plan:1735`（后两条是**变异指令，
                   会被执行**）。C 的 9 处里有 3 处已带勘误；**真正无勘误的是 9 处。**
  2 `:469`         **不在 9 处内**（见上）
  3 §5.2           **CONFIRMED**：`spec:1143` 与 `:2358` 同文档内直接矛盾；G10（`:2812`）
                   四个修订处**无一是 §5.2**，而 §5.2 恰是唯一主动语态宣告该归类的地方
  4 §16 第 11 行    **锚点歧义与伪结构内容 CONFIRMED，但 D 的危害机制不准**：`11b` 本身
                   已就地更正并带显式警告；坏的是 `:209`/`:2703` 两处**指错行的锚点**。
                   **修法比 D 描述的小 —— 改锚点，不重写行。**
  5 D7             **CONFIRMED 且更强**：裁决记录 `:246`/`:248` **亲自下过**「债 1 移到
                   L3 / L5 清单只剩 1 笔」的更正指令，L2 spec `:57`/`:468` 从未执行；
                   债 4 有 `Amended (j)`，债 1/3 没有
  6 D4             **CONFIRMED**，四要素俱全（人裁 `:36` ＋ 评审员「NOT done here /
                   Flag for the human」`:44`，**全仓唯一一处** ＋ L1b 勘误只到 `(e)`、无 `(f)`）
  7 `:2306`        **CONFIRMED 且更重**：与同节 5 行之上的 `:2301` 直接矛盾（`:2301` 明标
                   「N＝付费次数」为错误读法）；§19 M2 的处置是「各补一段」而非就地改，
                   故旧句存活；**且它就长在把第 5 笔交给 L5 的那句话里**
  8 委任状          **CONFIRMED，D 的更正正确**（run-registry 止于 §15；两处「parent §17
                   item 2/3」与 ownership §17 逐字对齐）。
                   *** 附带：委任状同时授权 **retained** 与 **cleaned up**，
                   「保留」这一半四份报告一次没提。 ***

*** 盘点完备性 ***
  78 ✅ 精确复现（逐文件 16+2+7+4+2+4+43）；23 ✅ 精确复现（19+1+1+2）。
  **但「四份门报告」这个标签错** —— 其中一份是 `group-c-preflight-scan.md`；被排除的两份
  gate-a 产物实测均为 0，**数字不受影响**。盲区：该七词中文 grep 扫不到台账（英文措辞，实测 0）。
  **「报不出可重数的计数就不报数字」：处置正确，应立为规则。**
  **但须限定：这两个数是搜索面，不是完备性证明** —— §12 那条漏项两个数都扫不到。
  D1–D13 抽查 6 条（D8/D9/D10/D11/D12/D13）：出处 6/6 正确、逐字 6/6 准确、6 项内判定 6/6 正确。
  **D12 例外并被升级**：D 判「无法判断」，lane 2 一步查出 `plan:225` 裁定「改 §4.6 那句话」、
  `spec:1012` 至今未改、代码返回类型已改为 `ReconciliationWriteDecision`
  ⇒ **D12 是确凿活缺陷（与 D4、`:2306` 同形第三次）。lane 2 已就地补完并关闭。**

*** 铁律 / 高估低估 ***
  四份**均未违反**铁律（lane 2 抽样重跑 6 条命令逐字复现；削短均在原地自陈；
  无自改判据或分级）。**B 合规度最高**（三处显式「不自行改判」＋ 拒绝为 C 背书）。
  **高估一处：scan-C `:359` 的全称否定**（见上）。
  **低估一处：scan-D 的 D12**（诚实标了未完成，不算违规）。

*** 阻塞 L5 边界的未完成项（lane 2 的排序，与 lane 1 有一处不同） ***
  1. **C 的项 E** —— **L5 输入清单的分母今天未知，分母未知就画不出边界。**
     补法：照 GATE-B `:1032` 的形状回填 15+3 条；**门 lane 1 那 3 条明写不可恢复。**
  2. **A 的 `recoverInterruptedOwnerTransfer:1017`** —— 决定第 1 笔的面有多宽，
     从而决定它排不排在债 2 前面。属 lane 1 车道。
  3. *** lane 2 新增、lane 1 没提：D 的「§2 vs §5.4 张力」—— B 判「今天无调用者」、
     D 判「无法判断」，**同一件事两份报告结论不一致，不收敛就给不了第 3 笔定级**。 ***
  不承重：D12（已关闭）、`cleanupOwnerTransferStagingWithoutMarker` 未读源码、
  handoff 未读（已证两处腐坏、非权威源）、B 的三处小缺口。

*** lane 2 自报未完成 ***
  未核 C2-M5 自身事实；未核那 15+3 条 minor 的技术真伪；§12 漏项未推到设计层；
  **代码可达性一律未碰 —— 明写不构成对任何可达性结论的背书。**
  并按 CLAUDE.md Rule 6 明写本任务超出 12,000 token 预算而非静默超支。

--------------------------------------------------------------------------------
两条车道齐。控制器需向人更正一条先前的陈述（已做）：
「L5 的本职今天没有任何设计输入」为假 —— `atomic-write-paths-design.md` §10 第 4 条
就是，且今天仍成立、且 L3 已扩大其残留面、且从未被勘误。
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
闭合 3（B/D 分歧收敛）已交付。2026-08-05。报告：`close-3-span-tension.md`（485 行）。
--------------------------------------------------------------------------------

*** 控制器的第二次自曝错误：本台账的一次转述压缩，凭空制造了一处「两份报告分歧」。 ***
  B 的报告原文写的是「**一次并发 `stop()`** 没有调用者」。
  **控制器在本台账里把它压成「触发没有调用者」，丢掉了主语。**
  闭合 3 查明：**B 的报告本身没有问题，分歧的表面来源就是这次压缩。**
  => 记账在此。**转述他人结论时压缩主语，等于制造一个不存在的缺陷并让下一轮去查它。**
  （本轮控制器错误累计 2 次：① 派单时把 L5 委任状错指到 run-registry §17；② 本条。）

*** 裁定：B 与 D 不是同一个问题。B 在自己的题上正确；D 的题可以答完，答完后张力消解。 ***
  - **B 问的是**「今天有没有生产路径让一次 `stop()` 与飞行中的 `writeBoundaryArtifacts`
    重叠」。**成立。** 闭合 3 把 grep 面从 `heartbeat.stop()` 放宽到
    `.stop(|stop()|stop,|{ stop|stop:`，再**从危害侧独立收口**：
    *** `releaseOwnerLease` 全仓只有一个生产调用点，就在 `stop()` 里 ***
    ⇒ 这个「不可达」是**穷举的**，不是收窄 grep 的产物。
  - **D 问的是**「一个 run 盘上留下 eligible transfer 的条件」。**可答**：
    `persistBoundaryAnalysis` 调用点**恰好 2 个**（`runLoop.ts:1217`/`:1271`，
    `grep -rnc 'persistBoundaryAnalysis('` = 3 含定义行），都在 `runLoopFromState` 内、
    execute 相位之后；**只有 `:1217` 能发布 transfer**（`:1271` 传
    `executionRecovery=undefined` ⇒ `continuitySuspicion: []` ⇒ 永远拿不到 `stale_candidate`）。

*** §2 vs §5.4 **不是**真矛盾。 ***
  两句量化在**不相交的群体**上：§2 `:46` 说的是**从未发布过 transfer** 的 run（sweep 被
  `resumeLoop.ts:43` 挡住，永远看不见）；§5.4 `:1400` 说的是**已被 sweep 捡起、盘上早有
  transfer** 的 run。
  **D 的「这个前提两处都没写」为假**：§2 `:46` 自己写了整条发布链；§5.4 `:1350` 逐字写着
  「owner epoch 未变、门依然通过——该 run 下一次 sweep 仍然 eligible」。
  代码侧佐证：`registerStopHandlers` 只挂在 `cli.ts` 的 `sweep` 分支上。
  D 自己标了未完成，**属未完成项内的前提误判，非违反铁律，方向是高估矛盾。**

*** 第 3 笔定级（闭合 3 独立推出，与 B 相同）***
  **今天 = 仅文档 / 纵深防御；§14.2 常驻形态落地后 = 数据丢失。**
  措辞按 lane 1 的要求为「**未发现可达路径**」，全部静态阅读，未跑测试。
  归属属性原样保留：**需要一次新的归属裁决**，不是「债 3 的未关闭部分」。

  **该定级依赖 5 条前提，任一变了必须重判：**
    P1 `releaseOwnerLease` 只有 `stop()` 一个调用者
    P2 `stop()` 两个调用点都在 `finally` 且严格在被 await 的 `runLoopFromState` 之后
    P3 无常驻 `watch`
    P4 两个 `persistBoundaryAnalysis` 调用点都在 `runLoopFromState` 内
    P5 信号处理器不碰心跳

  *** P1 是本报告新增、三份既有文件都没写的一条，且对 L5 最危险： ***
  *** **L5 的本职 GC 一旦自己回收租约，第 3 笔立刻升级为数据丢失 —— 不需要任何人碰
  `stop()`，也不需要常驻形态落地。** ***
  => **这条必须写进 L5 spec 的第一节。L5 的正常工作方式恰好会触发它。**

*** open 项 4 的核查：那条人裁**今天没有**重新打开。 ***
  今天**确实存在恰好一条**不立刻走终态的路由 —— B1 加的 `runExclusive` 抛
  `RunHeartbeatStoppedError`（`leaseHeartbeat.ts:212`）→ 外层 catch（`runLoop.ts:1489`）
  → `return state`（非终态）。**open 项 4 的字面条件已被满足过一次。**
  但台账在 B1 动码前就把它交独立验证者判过：判决 **(B)「the bound's subject disappears」,
  不重开**。闭合 3 在 HEAD 上复核了它的三条支柱，**全部仍成立**（拒绝先于 `fn` 调用；
  `assertHeld` 函数体 `:275` 起零命中 `stopped`；写仍在 span 收口 `:873` 之后）。
  组 C 也做过自己的确认，同样不重开。

*** 一条耦合，本轮四份扫描与两条评审车道**都没点出来** ***
  **不重开的理由完全依赖「span 外」这个结构。** 若 L5 为了修第 3 笔把
  `writeBoundaryArtifacts` **搬进 span**，逃逸点就跑到写的后面，该理由失效，
  **那条「保留即放宽」的人裁真的会重开。**
  => *** 第 3 笔的修法与「保留即放宽」人裁**必须一起决策**，不能分两轮。 ***

  闭合 3 自报 7 条未完成（报告 §6），最要紧三条：未跑注入、未排查反射/动态导入、
  未逐字核 §14.2 的编号与措辞。

--------------------------------------------------------------------------------
闭合 2（recovery 路径）已交付。2026-08-05。报告：`close-2-recovery-path.md`（875 行）。
--------------------------------------------------------------------------------

*** 决定性问题的答案：第 1 笔**已经扩到 recovery 路径，且面严格更宽**。 ***

  **P-STEAL**（派单说的偷锁分支）：`readOwnerRecord` → `recoverInterruptedOwnerTransfer`
  → `:1017` → `tryRecoverStaleOwnerTransferLock` 的 `JSON.parse` catch → `safeUnlink(lockPath)`
  → `:1021` 直接 finalize。**今天可达，九步跨进程构造已给。数据丢失。**

  *** **P-READ**（评审员顺带发现那条，本轮最重的单条发现）***：`:1017` 的第二合取项
  `pathExists(lockPath)` 为假即短路，`tryRecoverStaleOwnerTransferLock` **一次都没被调用**，
  直落 finalize。
  *** **今天可达，且构造不需要任何人犯错、不需要零长度窗口、不需要残余 staging。**
  数据丢失。 ***

  **三条硬论据（为什么是「严格更宽」而不是「同一个洞的另一说法」）：**
  1. recovery 路径上 `hasStagedArtifacts`（`:803` 第一析取项）**由入口闸 `:1010` 的同一个
     marker 免费满足** —— 第 1 笔原文要求的「零长度窗口 ＋ 残余 staging」**两个偶然塌缩成
     一个**，而剩下那个不是偶然，**是这条代码存在的理由**。
     ⇒ **原文写的是这个面上较难的那一半。**
  2. *** **P-READ 根本不经过第 1 笔的缺陷** —— 把 `tryRecoverStaleOwnerTransferLock` 的
     catch 补上活进程检查，**P-READ 完整存活**。 ***
     ⇒ 问题从「锁协议有个洞」升级为「**recovery 的发布步骤根本不在互斥内**」。
     修法是一次**设计裁决**（要么让 `!lockHeld` 也取锁、要么让 `readOwnerRecord` 不再
     finalize），**两条都牵动 L1/L2 的「读不许写」契约**。
  3. **后果类型变了**：不是「互斥失效」，是**两个 finalizer 并发跑在同一组固定临时名上**
     （`:574-576` 无 pid 无序号，注释逐字说明固定名是为跨进程可恢复才选的；
     `writeJsonFile` `:589-591` 是裸 `writeFile`）
     ⇒ **三份权威产物可落成半截 JSON**，而 `readOwnerRecordRaw:661` **无 catch**，
     此后**永久抛**。

  **G2-null（闭合 2 自己补的第三条独立格，今天未发现可达路径）**：锁 JSON 合法但
  `holderProcessInstanceId` 不匹配 `^pid:\d+$` ⇒ `pid===null` ⇒ 落 `:812` **无条件删锁，
  连 `hasStagedArtifacts` 都不查**。唯一写者今天写死 `pid:${process.pid}`。
  *** 唯一防线是 `fileStore.ts:603-605` 一条「do not unify it with this one」的注释，
  **没有任何测试钉住它**；而 `buildProcessInstanceId()` 的 `pid:<pid>:<origin>` 形式
  **正好不匹配**。 *** ⇒ 一颗埋着的雷，触发即数据丢失。

  **没有第五个发布点**：搜索面写明 —— `src/` 全部四个 `rename` 调用点
  （`:645/:991/:1107/:1119`），除 `:991` 外全部持锁。不持锁 finalize 只有 `:1021`
  一处机制、四格进入条件。

*** 排序：闭合 2 不替人裁，给了债 2 vs 第 1 笔的八维对照表。 ***
  **对排序最有分量的一条：第 1 笔的修法规模比原文描述的大一个量级。**
  **反方向也写了**（未只挑对自己有利的）：第 1 笔仍需「磁盘上已有 marker ＋ 两参与者
  交错」，而**债 2 单进程即触发**。

*** marker 的角色（先消歧再用）***
  `RUN_MARKER_FILES`（五个无点前缀文件）与事务 marker `.owner-transfer.transaction.json`
  是两个概念，**后者不在前者内**。事务 marker 在 P-STEAL 上**一物两用**（入口闸 ＋ 唯一
  偷锁判据），在 P-READ 上只当入口闸。
  L3 加的 marker `readFile`+`JSON.parse`（`:937`）与三条抛出中的**两条**（`:940`/`:976`）
  **是双 finalize 的可观测签名**；`:949` 不是（`finalizeOrder` 由 `:1047-1058` 写死，
  竞态改不了）。
  ⇒ *** scan-B 的「规则 3 经 marker ENOENT 可达」在这两条上**同样成立且不需要 SIGKILL**，
  可达面又宽一格。 ***
  「第三份 pending 不在 `hasStagedArtifacts` 里」这条不对称**在这两条路径上实际影响为零**
  （`||` 首项短路）—— **是原入口的缺陷，不是 recovery 路径的成因**，报告里明确划了界。

*** 顺带上报（不改）***：`observeFields.ts:29-35` 的注释前提「owner-record.json published
  by rename ⇒ 观测永远完整」**在双 finalize 下不成立** —— 分级仅文档。

*** 派单前提的一处偏差（控制器第三次记账）***
  派单说「两条路径」，实测是 **4 格 ＋ 1 格今天不可达**。**归并本身对，格数不对。**

*** 五条「已确立事实」全部独立重推、全部仍成立 ***
  含 `evaluateResumeEligibility` 八条判据（`:43/:46/:49/:52/:55/:58/:61/:64`，
  `grep -c 'ProcessInstanceId'` = **0**）。

*** 未完成项 7 条，最要紧三条 ***
  1. **两条路径都只有静态可达性论证，无实跑注入** —— 闭合它需要一个**允许写 `tests/`**
     的任务，只读铁律挡住了。
  2. 半截 `owner-record.json` 的 sweep 侧告警分级未推，§5.4 因此没有可操作性分级。
  3. *** **「今天没有测试钉住 P-READ」这句全称否定，闭合 2 拒绝下** *** —— 未通读
     `tests/persistence/fileStore.test.ts` 的相关用例，**搜索面不覆盖断言范围**。
     **这正是本轮抓到那个形状之后应有的行为，记为正面样本。**
  并按 Rule 6 明写超预算而非静默。

--------------------------------------------------------------------------------
闭合 1（项 E，清单分母）已交付。2026-08-05。报告：`close-1-item-e.md`。
三条闭合全部交付，本轮盘点结束。
--------------------------------------------------------------------------------

*** 18 条的分类统计 ***
  出处：台账 15 ＋ 仅 `gate-c-lane2-report.md` 3 = **18**
  代码 / 文档：**13 / 5**（C2-M2 跨类，闭合 1 归代码并明写；若归文档则 12/6）
  今天仍成立：**15**
  今天已失效：**3**，且**三条失效性质互不相同、不可打包**：
    C1-M1 —— **被解决**（C3 结构性解决）
    C1-M3 —— *** 写下时即为假 ***
    C3-M4 —— **被修**（GATE-C 修复波把 `17+4` 改成 `13+4=17` 并带勘误）
  **只有第三种会污染下游论证 —— 它确实污染了 C2-M5。**
  lane 2 已逐条下过处置 18/18；**落进台账 0 条。**

*** 新查明（扫描员 C 与门 lane 2 都没定位到）***
  台账 `:1631` 承诺的「two doc items」= lane 2 报告 §5.5（C3 §9 变异清单过期）
  与 §5.6（C4 §B 论证错误），两条处置均为「只记录」。
  => **台账欠的是 5 条，不是 3 条。** 两条题面仍在报告里、可回填，且未被裁「带到 L5」。
  **若人认为「只记录」也占分母位，分母是 20。**

*** ⚠️ C3-M3 陷阱已量化 ***
  `grep -c "^Task C[1-4]: minor (deferred"` = **15**（按记法）；
  lane 2 分诊表 = **16**（按 ID，含记作 `minor (folded into fix round 1)` 的 C3-M3）。
  **两个数都对，数的是不同集合。** C3 的 deferred 序列是 M1/M2/**M4**/M5。

*** C2-M5 重新论证：事实成立，原论证作废。 ***
  C1-M3 经闭合 1 独立重跑确认**写下时就为假**：`git log -S` → `c15b499`；
  `git show` 显示 `it` 与 §7 注释同为 `+`；`c15b499` 在 GATE-C 区间
  `2713c20..4a24a94` 内**且是 C1 自己那笔**；`sweepRuns.ts:122` 的 `return 1`
  经 `cli.ts:227` 直通 exit code，`cli.test.ts:373` 的 `resolves.toBe(1)` 会红。
  C2-M5 的**事实**经独立重推成立：`cli.ts:174` 的 `?? ((code) => process.exit(code))`
  是**唯一生产装配**（`:224` 不注入），全 514 测试里**被构造过、从未被调用**
  （`process.emit("SIGINT"|"SIGTERM")` 在 `tests/` 全目录只有 `cli.test.ts:428/432`，
  都在注入了 `exit` 的那条测试内）。
  *** 但门 lane 2 的理由行必须废弃 ***：它说「同一形状被两种标准处理」——
  今天两者**不同形**（C1-M3 有会红的守卫，C2-M5 没有）。
  *** 真实的不对称方向相反：**被记账的那条从来不是缺陷，没被记账的那条才是。** ***
  新依据三条：唯一生产装配未被调用 / 被守护的是具名契约（exit 130）/
  本仓库反复栽的 DI 接缝形状。⚠️ **未做变异实测，仅结构性证明。**

*** 分母判断：**18** —— 不是「18 + 未知的 3」，也不是干净的 18。 ***
  门 lane 1 三条 Minor 的灭失是**原生的**，不是可逆的：
    `find .` 全仓零命中；
    `git log --all --diff-filter=D -- '*lane*' '*gate-*report*'` 零输出；
    `git rev-list --all --objects | grep -i "lane1\|lane-1"` 零命中。
  **四条依据：**
  1. 18 可枚举可验收，3 不可枚举 —— **相加会造出一个永远无法宣告完成的分母**。
  2. 按 GATE-B 的处置闭集，Minor 默认档是 `record only`，进 L5 需一次显式
     `carry to L5`，**这三条从未拿到**。
  3. 它损伤的是 **GATE-C 的效力**（唯一有资格评代码正确性的车道产出为零），
     应记成 **GATE-C 名下的证据灭失治理债，不进 L5 清单**。
  4. *** 不许用「反正是 Minor」抹平 *** —— 同仓 GATE-B 有
     `B2 M-1 UPGRADED from record-only` 的先例，「Minor ⇒ 无关紧要」**在本仓库已被证伪**。
     闭合 1 明写：能确证的只有「从未被显式 carry」，**不能确证「不重要」**。

*** 闭合 1 自报 10 条未查完（报告 §5），最要紧三条 ***
  1. *** C2-M4 的四个 spec 站点沿用了 scan-C 的定位、**未重跑 grep** ——
     闭合 1 自己点名这违反了「每个数字附命令」，**明写而非假装**。 ***
  2. C3-M5 的全称命题与 C3-M6 的「12d(i) 失明」两次注入**均未重跑，不背书**。
  3. C2-M2 的 `{}` 场景**未真跑**。
  并按 Rule 6 明写超出单任务 12,000 token 预算。

--------------------------------------------------------------------------------
*** 本轮盘点全部结束。产物清单与控制器记账。 ***
--------------------------------------------------------------------------------

  四份扫描：scan-A(577行) / scan-B(919行) / scan-C(1329行) / scan-D(48.8K)
  两条复核：review-lane1-code(1215行) / review-lane2-docs(1033行)
  三条闭合：close-1-item-e / close-2-recovery-path(875行) / close-3-span-tension(485行)
  全部只读交付，`src/` `tests/` `docs/` 一字未动。

  **控制器本轮自曝错误 3 次，全部记账：**
  ① 派单把 L5 委任状错指到 `run-registry-design.md` §17（该文件止于 §15）——
     由扫描员 D 当场证伪；
  ② 本台账把 B 的「一次并发 `stop()` 没有调用者」压缩成「触发没有调用者」，
     **丢主语，凭空制造一处「两份报告分歧」**，害闭合 3 专程去查 —— 由闭合 3 查明；
  ③ 派闭合 2 时说「两条路径」，实测是 **4 格 ＋ 1 格今天不可达** —— 由闭合 2 更正。
  **三次都是被下游 agent 发现的，没有一次是控制器自己发现的。**
  这与本仓库「十五波修复十五次自带缺陷、无一由作者自己发现」是同一个形状。

  **本轮环境事实（供后续沿用）**：9 名 agent 中 5 名被流中断，**全部发生在准备落盘时**。
  最初判断为「巨型 tool call 被截断」，**后被 lane 1 在『准备写骨架』时中断证伪** ——
  更准确的说法是**本会话存在更一般的流不稳定，大 payload 只是放大它**。
  **有效对策**：先落只有小节标题的骨架、每次 Edit 只填一节、按把握度排序。
  采用该协议后交付率 100%。

--------------------------------------------------------------------------------
*** 人裁（3 条）。2026-08-05。人逐字答复「这三条，同意按你的建议」。 ***
--------------------------------------------------------------------------------

**人裁 1 —— 债 2 的复议：认定已触发，但不按条款字面单独提前处理。**
  两条**独立**的复议理由（本轮才查明第二条）：
  (a) 裁决记录 `2026-07-29-technical-debt-attribution.md:159` 的条件条款
      「若 L3 的工期显著超出预期，应重新审视是否把债 2 提前到 L3 之后、L5 之前单独处理」
      —— L3 实跑三组十五任务、三道门、六名门评审员、**五处计划被实测证伪**，已触发。
  (b) *** 更硬的一条：债 2 的归属出自一份**自陈是自评审**的裁决记录，而该记录 `:255`
      自己写着「若后续层依赖本记录的某条结论，值得再撞一次」。 ***
      这不是排期问题，是**归属本身的证据强度问题**。
  **不照字面执行的理由**：条款写于 2026-07-29，当时不知道今天这三件事 ——
  债 2 今天**可达且是数据丢失**；另两条（第 1 笔、第 4 笔）**同级且同样今天可达**；
  三条**共享同一个根因面**（所有权/持锁的判定与写入之间没有守卫）。
  单独提前清债 2 = 把三条同类缺陷里的一条拎出来单独设计一遍。
  **裁定：三条合成 L5 的第一个工作包（见包 2），条款的实质诉求由此满足。**

**人裁 2 —— 第 2 笔（execute abort 无第二重上界）加防遗忘勘误。**
  已确立：**§13 第 2 笔 ⊋ §14 第 3 条**，后者（SIGKILL 升级）只覆盖 §5.4 三个独立缺口
  中的第 (2) 个；且闭合验证**补 SIGKILL 也关不掉它**（构造二：scripted adapter 根本不读
  `abortSignal` —— **该构造 lane 1 标记为未验，勘误措辞不得依赖它**）。
  **裁定：在 §13 第 2 笔与 §14 第 3 条两处各加一句就地勘误，明写包含关系与三个缺口的编号。**
  **理由不是「结论可能被推翻」，而是它已具备本仓库最经典的失效形状**：
  一笔债被一个**看起来同名**的独立任务承接，读者一对上名字就勾销。

**人裁 3 —— L5 的本职从 `atomic-write-paths-design.md` §10 第 4 条起步，不从零 brainstorm。**
  该条（崩溃残留 `.{basename}.{stamp}.{seq}.tmp`、两条清理路径都够不着、**清理归属未分配**）
  是**全仓唯一一条为 L5 本职预先写好的设计输入**，今天仍成立，**且 L3 把三份 pending 改
  原子写后残留面已扩大、该文档从未被勘误**。
  **裁定：以它为 L5 spec 的起点。第一节必须写进闭合 3 新查明的前提 P1：**
  *** **L5 的本职 GC 一旦自己回收租约，第 3 笔立刻升级为数据丢失 —— 不需要任何人碰
  `stop()`，也不需要常驻形态落地。L5 的正常工作方式恰好会触发它。** ***
  **并且：委任状同时授权 `retained` 与 `cleaned up`，「保留」这一半九份报告一次没提，
  设计时不许只想着删。**

--------------------------------------------------------------------------------
*** 人裁 4（追加）。2026-08-05 深夜。人逐字答复「包 2 写 tests/ 的授权 => 现在」。 ***
--------------------------------------------------------------------------------

**授权内容**：包 2 的实施者**获准写 `tests/`**，用于给闭合 2 的两条数据丢失路径补**实跑注入**
（今天只有静态论证）。上一轮的只读铁律**只约束那一轮**，不再约束包 2。

**授权的边界（未被授权的部分不得推定）**：
  - 授权对象是**包 2**。包 3（纯文档勘误）仍不写 `tests/`；包 1（写 spec）仍不写代码。
  - 授权的是**补测试**，不含「为了让测试变绿而改判据」。**发现文档/计划的论据在今天代码上
    不成立 —— 原样上报、人裁 ＋ 就地勘误，不许实施者自改判据**（铁律未软化）。
  - 变异仍走三步判据（注入前绿 / 注入后红 / 还原后绿），每个单跑块显示具名测试的**非零**计数。

**这条授权解除的具体阻塞**（台账包 2 原文第一条必带项）：
  「闭合 2 的两条路径只有静态论证、无实跑注入 —— 需要一个允许写 `tests/` 的任务，
   本轮只读铁律挡住了」。**现已解除。** 没有实跑注入，包 2 仍会退回「实施者自证」。

--------------------------------------------------------------------------------
*** 由三条人裁确定的三个工作包（已授权，尚未开工）。 ***
--------------------------------------------------------------------------------

**包 1 — 写 L5 spec 的本职部分。** 起点 = `atomic-write-paths-design.md` §10 第 4 条。
  第一节写 P1。覆盖 retained 与 cleaned up 两半。

**包 2 — 今天可达的数据丢失，三条一起做。建议顺序：债 2 → 第 4 笔 → 第 1 笔。**
  理由：前两条能独立收口；**第 1 笔的性质已变** —— P-READ 不经过锁协议的缺陷，
  修法是一次牵动 L1/L2「读不许写」契约的**设计裁决**，规模比原文大一个量级。
  必带两项：
  - *** 闭合 2 的两条路径**只有静态论证、无实跑注入** —— 需要一个**允许写 `tests/`**
    的任务，本轮只读铁律挡住了。 ***
  - **G2-null 那颗雷**：唯一防线是 `fileStore.ts:603-605` 一条注释，**没有测试钉住**，
    而 `buildProcessInstanceId()` 的 `pid:<pid>:<origin>` 形式**正好不匹配**。

**包 3 — 纯文档勘误，独立一轮，不进 L5。**
  **10 处**「数组 push」（**不是 scan-C 建议的 6 处** —— C 用收窄的 grep 支撑了全称否定，
  已被 lane 2 一条命令证伪）、§5.2、§16 锚点（**改锚点即可，不必重写行**）、
  D7、D4、`:2306`、D12（lane 2 已补完可直接落）。
  理由：只需人裁 ＋ 就地勘误，不需要设计；塞进 L5 只会稀释。

**明确不在 L5 开工：第 3 笔**（今天「未发现可达路径」，仅文档 / 纵深防御，等 §14.2 `watch`）。
  *** 但它的修法**必须与「保留即放宽」那条人裁一起决策** —— 不重开的理由完全依赖
  「span 外」这个结构；若为修它把 `writeBoundaryArtifacts` 搬进 span，逃逸点跑到写的
  后面，**该人裁真的会重开**。不能分两轮。 ***

**另记 GATE-C 名下（不进 L5 清单）**：
  门 lane 1 证据灭失（**不可补救**，三条命令已证）；台账欠的 **5 条**回填
  （照 GATE-B `:1032` 的形状）；C2-M5 的新论证（**事实成立、原论证作废，
  且真实的不对称方向与门评审所述相反**）。

