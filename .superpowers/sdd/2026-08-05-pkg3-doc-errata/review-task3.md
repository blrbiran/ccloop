# Task 3 独立评审 — 跨文档勘误（D7 / D4 / spec:2326 / D12）

范围：`ce934c4..d5b3e79`（3 files，+21 −1）
评审员：独立评审员（未参与本轮任何实施）
日期：2026-08-05

---

## 0. 结论

- **规格符合性：❌**
- **任务质量：有 findings —— Critical 0 / Important 4 / Minor 4**

四条勘误正文本身的**事实断言全部为真**（我逐条自己重跑命令核实，见 §2–§5），
`--max-runs` 半句未被误伤，那一处 `−1` 确实是机械副作用。
❌ 不是因为写错了，而是因为**两类「同族一改一没跟」**：

1. §12 里 `--max-runs 界的是付费调用` 这句话在**同一节里出现两次**（`:2322` 与 `:2326`），
   只改了后一处；`:2322` 在实施者自己的收尾自查输出里出现过，报告全程未裁。
2. 两份被改文件的**文首修订摘要**（L1b `:4` 的「Amendments: five … (a)–(e)」、
   L2 `:3`–`:31` 的 Index）都是维护中的总账，本次新增的 `(f)`/`(k)`/`(l)` 一条都没进去。

外加：`(f)` 的日期 `2026-08-02` 与本仓库自己的日期约定和这条注自己的「landed now」矛盾。

---

## 1. 评审方法与我实际重跑的命令

我没有采信报告里任何一句「我验过了」。下列命令全部由我在 HEAD `d5b3e79` 上重跑，输出未过滤：

| 目的 | 命令 | 我拿到的输出（关键值） |
|---|---|---|
| 确认评审基线 | `git log --oneline -3` | `d5b3e79` / `ce934c4` / `26b0709`，与评审包一致 |
| D12 代码侧 | `grep -n "preserveSuccessfulReconciliationIfNeeded" src/persistence/fileStore.ts` | 4 命中：`:234`（…FromArtifacts）/ `:392` / `:417` / `:449` |
| D12 返回类型 | `sed -n '388,420p' src/persistence/fileStore.ts` | `:392` 函数头，`:395` 为 `): Promise<ReconciliationWriteDecision> {` |
| 类型存在性 | `grep -rn "ReconciliationWriteDecision" src/` | 2 命中：`fileStore.ts:346`（type 定义）、`:395` |
| 裁定三定位 | `grep -n "裁定三\|Promise<ReconciliationRecord>\|这句话为假" plans/…md` | `:225` 标题、`:231` 理由、另 `:909`/`:1943`/`:1964` |
| 裁定三逐字 | `sed -n '218,235p' plans/…md` | `:225` 标题 / `:229` 「裁定：改 §4.6 那句话…」/ `:231` 「今天返回 `Promise<ReconciliationRecord>`」 |
| 裁决记录 | `sed -n '240,252p' decisions/2026-07-29-…md` | `:246` / `:248` 逐字命中（见 §3） |
| 裁决表 | `sed -n '1,40p' decisions/2026-07-29-…md` | `:18` 债1→L3、`:19` 债2→L5、`:20` 债3→L3、`:23` 「四笔债里两笔（1、3）是 L3 的前置」 |
| L3 台账 | `sed -n '30,46p' .superpowers/sdd/2026-08-02-…/progress.md` | `:34` 人裁、`:37` (b)、`:42` 评审员点名，三处逐字与勘误引文一致 |
| L2 字母分布 | `grep -n "Amended" specs/2026-07-28-…md` | 13 命中：`:3 :29 :60 :96 :180 :230 :274 :295 :383 :402 :480 :484 :531` |
| L1b 字母分布 | `grep -n "Amended" specs/2026-07-27-…md` | 9 命中：`:4 :17 :27 :29 :95 :113 :115 :125 :141` |
| §12 同族分布 | `grep -n "界的是" specs/2026-08-01-…md` | **6 命中：`:1453 :1462 :2321 :2322 :2326 :2905`** |
| 提交日期 | `git log -1 --format='%H %ad %cd' --date=iso d5b3e79` | `2026-08-06 01:17:57 +0800` |
| 台账落盘日 | `git log --diff-filter=A --format='%ad' --date=iso -- …/progress.md` | `2026-08-02 13:18:51 +0800` |

**我没有做的事（明写）**：没有重跑 30 files / 514 tests 全套件（brief 禁止）；
本轮四处编辑全是 markdown，与 `src/`、`tests/` 无交集，我用 `git show --stat d5b3e79`
（评审包已含，3 files 全在 `docs/superpowers/specs/`）确认这一点，没有独立重跑测试。
因此「无回归」这个结论我**采信了 numstat 的改动面**，没有采信实施者的测试输出本身。

---

## 2. D12 —— `preserveSuccessfulReconciliationIfNeeded` 事实核验（不采信报告）

**结论：勘误正文的每一句都为真。我自己去源码核的，没有采信报告或 lane 2。**

`sed -n '388,420p' src/persistence/fileStore.ts` 逐字：

```
async function preserveSuccessfulReconciliationIfNeeded(   ← :392
  runDir: string,
  nextReconciliationRecord: ReconciliationRecord,
): Promise<ReconciliationWriteDecision> {                   ← :395
  if (nextReconciliationRecord.eligibleForContinuation) {
    return { kind: "write", record: nextReconciliationRecord };
```

`grep -rn "ReconciliationWriteDecision" src/` → 恰 2 行：`fileStore.ts:346`（`type ReconciliationWriteDecision =`）
与 `:395`。**今天的返回类型确为 `Promise<ReconciliationWriteDecision>`，判别式联合，
`abandon` 一格存在（`:411` `return { kind: "abandon", error: … }`）。**

`sed -n '218,235p' plans/2026-08-02-…md` 逐字（我数出的行号）：
- `:225` `### 裁定三 —— §4.6「`preserveSuccessfulReconciliationIfNeeded` 代码零改动」这句话为假，予以推翻`
- `:229` `**裁定：改 §4.6 那句话，不去为了保住「零改动」而把整块判定上移。**`
- `:231` `**理由**：… 今天返回 `Promise<ReconciliationRecord>`（… 返回类型行是 `): Promise<ReconciliationRecord> {`）…`

**逐条对勘误正文（`:1031`）**：
- 「上面那条『理由已过时』的注只否定了理由，没有否定『代码零改动』这个断言本身」——**真**。
  我读了 `:1029` 全句，末句逐字是「**两侧都不需要改它的代码**，所以结论「代码零改动」仍然成立，只是靠的是另一条依据。」
  它明确**重申**了结论。
- 引 `plan:225`–`:229` 的两句——**逐字准确**（`:225` 标题句 + `:229` 裁定句，均在该区间内）。
- 「返回类型是判别式联合 `Promise<ReconciliationWriteDecision>`（`fileStore.ts:392`–`:395`）」——**真且行号准确**。
- 「计划记录的改动前形态是 `Promise<ReconciliationRecord>`（`plan:231`）」——**真且行号准确**。

**没有引入新的不实断言。** 注意它措辞上的一个可取之处：它没有断言「改动前的真实代码是 X」，
而是说「**计划记录的**改动前形态」——这正确地把陈述限制在计划文档上，避免了对历史代码的无据断言。

**报告与产物的一处出入（Minor，见 §11 M2）**：报告正文写「我复验时读到裁定三标题在 `:223`」，
并贴了一段 `sed -n '223,231p'` 的「输出」，其首行是裁定三标题。
我实跑 `sed -n '223,231p' | cat -n` 得到的首行是 `3. **不选「把整块判定上移进 …**`，
标题在该输出的**第 3 行**、即文件 `:225`。**报告贴的那段不是该命令的真实输出。**
落盘的勘误引的是 `:225`，是对的——产物没受影响，受影响的是报告的自证可信度。

---

## 3. D7 —— 两处「three debts」勘误与裁决记录逐字核对

**结论：两处都留了可跟到出处的指针；引文逐字准确；但引用的行号有半条支撑不住（Minor M1）。**

**指针完整性**：`:60` 的 `Amended (k)` 与 `:480` 的 `Amended (l)` 各自都写全了
`docs/superpowers/decisions/2026-07-29-technical-debt-attribution.md:246`、`:248` 两个锚点，
且 `(k)` 额外指向「§13 below」，`(l)` 额外指向「item 1 / item 3」。**两处都不是裸勘误，都能跟到出处。**

**裁决记录逐字核（我自己跑 `sed -n '240,252p'`）**：

```
:246  2. **L5 继承清单第 1 条描述有误**：「reconciliation 合成责任无人认领」不成立，见债 1。该条应改写为跨文件事务性缺陷，并从 L5 清单移到 L3。
:248  4. **L5 继承清单现在只剩 1 笔**（债 2），不是 4 笔。
```

勘误里那句带引号的 `"L5 继承清单现在只剩 1 笔（债 2），不是 4 笔."` 与 `:248` **逐字一致**
（尾部句号被改成半角 `.`，是排版不是篡改）。

**勘误正文的其余事实断言，逐句核**：
- `(k)`「debt 1 was misdescribed as unowned reconciliation synthesis; the real defect is cross-file transactional」
  —— **真**，`:246` 逐字「『reconciliation 合成责任无人认领』不成立 … 该条应改写为跨文件事务性缺陷」。
- `(k)`「§13 below still lists three items under L5」—— **真**。我读了 §13（`:473`–`:531`）：
  `:475` 段落说 Debts 1–3 归 L5，下面 1/2/3 三项原样，item 4 单独标为 new。
- `(l)`「Neither item 1 nor item 3 below carries an `Amended` note」—— **真**。
  `grep -n "Amended"` 在 §13 区间只命中 `:480`/`:484`（新增的 (l) 自己）与 `:531`（item 4 的 (j)）。
- `(l)`「item 3 has an inline sentence pointing at "the deferred queue layer"」—— **真且逐字**。
  item 3 末句原文：`**This debt must be re-evaluated by whichever layer adds a triggering caller**, which is the deferred queue layer, not this one.`
- `(l)`「it does not correct this paragraph's "bequeathed to L5" framing」—— **真**，该句没有提 L5 归属。
- `(l)`「the three-item list below was never renumbered」—— **真**。

**这一条我特别去核了 brief 点名的「实施者自曝的假话」（concern 1）**：他最初写的是
「item 1 已有匹配注、item 3 没有」。**改正后的正文写的是「Neither item 1 nor item 3 …」，
这是对的**（两项都没有 `Amended`）。自曝并改正的结果**正确**，没有残留。

**M1（Minor）**：`(k)` 与 `(l)` 都把「and debt 3 to L3」这半句挂在 `:246`、`:248` 两个锚点下。
**这两行都不说债 3 去哪。** `:246` 只讲债 1，`:248` 只讲「L5 只剩 1 笔」。
债 3 → L3 的实际出处是同一文件的**结论速查表 `:20`**（`| 3 | heartbeat.stop() 释放窗口 | **L3** |`）
与 `:23`（「四笔债里两笔（1、3）是 L3 的前置」）。**结论为真，但引给读者的两个行号跟不出来。**

---

## 4. D4 —— L1b `(f)` 勘误

**结论：内容与格式对，四要素引文逐字准确；日期错（Important I4），文首摘要没跟（Important I3）。**

我自己跑 `sed -n '30,46p' .superpowers/sdd/2026-08-02-…/progress.md`，逐行数得：

- `:34` `Task A4: HUMAN RULING … "L3 debt 1 transactionalises reconciliation so the SAME CAS publishes it. Human ruled: L3's transactionalisation SUPERSEDES L1b (e)."` ✅ 与勘误引文逐字一致
- `:37` `(b) L5's inherited-input list is unaffected in NUMBER: … Record that this L1b-side assignment is now closed by L3 rather than inherited by L5.` ✅ 逐字一致，省略号处省掉的是「the L3 spec's 13 five-item list …」，不改变语义
- `:42` `3. … Repo convention is an in-place *Amended (f)* note … NOT done here … Flag for the human.` ✅ 逐字一致

**brief 特别要求核的「已被 L3 关闭」措辞**：实施者没有自选说法，直接引台账 `:37` 的
`now closed by L3 rather than inherited by L5`。**处置正确，符合铁律 1。**

**格式一致性**：`(f)` 沿用了 `(a)`–`(e)` 的 `**Amended <date> (letter): <粗体结论>.** This corrects a
defect in *this document*, not in the implementation.` 句式，逐字同构。✅

**没有引入新的不实断言**：我把 `(f)` 的每一句都对回了台账原文，没有一句是台账没说的。
唯一的自撰句是结尾 `this paragraph is that note, landed now.` —— 这句为真
（这条注确实是本次才落的），但它与开头的日期 `2026-08-02` 自相矛盾，见 §11 I4。

---

## 5. `spec:2326` —— `--max-runs` 更正与同节第四轮更正的一致性

**一致性：✅ 对得上。** `:2321` 逐字（我自己 `sed -n '2310,2330p'` 读的）：
「`--max-runs N` 界的是**进入 `runLoopFromState` 的 run 数** … **付费上界是 `N × maxAttempts`。**」
新插入的更正逐字：「`--max-runs N` 本身界的是进入 `runLoopFromState` 的 run 数，
付费调用的真实上界是 `N × maxAttempts`，不是 N。」——**用词、机制、结论三层都对齐，没有夸大也没有改口。**
我另去 §6 `:1453`/`:1462` 核了同一条更正的更早形态，结论同样一致
（`:1462`「两条界叠乘才是 sweep 的付费上界」）。

**「不界事件追加」那半句有没有被误伤：✅ 没有，一个字没动。**
`--word-diff` 那一段是决定性的：被删的整段是
`界的是**付费调用**，不界事件追加。一次`，新增的整段以**同样这串字符开头**
（`界的是**付费调用**，不界事件追加。` 原封不动），插入句之后再接 `一次`。
新增的更正句里还专门写了「「不界事件追加」这半句不受此更正影响，今天仍然成立，
是本处交给 L5 的实质内容」——**这正是 brief 要求保住的那半句，且被显式加固了。**

**但同节还有第三处同族句没跟（Important I1，见 §9 与 §11）。**

---

## 6. numstat 那一处删除（−1）是否只是机械副作用

**是。我自己看的 `--word-diff`，没有采信报告。**

评审包 `=== WORD-DIFF ===` 最后一段：

```
@@ -2324 +2326 @@
**本节不界的东西，明写出来**：`--max-runs` [-界的是**付费调用**，不界事件追加。一次-]{+界的是**付费调用**，不界事件追加。**⚠️ 就地更正：…** 一次+} sweep 扫到 M 个…
```

删除块 `[-…-]` 的全部内容 `界的是**付费调用**，不界事件追加。一次`
**逐字符地出现在紧随其后的新增块 `{+…+}` 的开头与结尾**。
即：被记为「删除」的字符一个不少地回到了新行里。

**为什么 word-diff 也显示成整块替换而不是纯插入**：`--word-diff` 默认按空白切词，
而这段是中文，`界的是**付费调用**，不界事件追加。一次` 之间没有空格，
整串被当作**一个「词」**，行内插入因此表现为「一词删、一词增」。
这与实施者给的解释（「按行 diff 把行内插入记成整行替换」）方向一致，但**他给的机制不完整**
——真正让 `--word-diff` 也不显纯插入的是 CJK 无空格分词，不只是「单行长段落」。
这个差别不影响结论：**该 `−1` 是机械副作用，没有任何原有内容被删。**

我另外用 `git show d5b3e79 --stat` 交叉验证了改动面：3 files，+21 −1，
且三个文件全在 `docs/superpowers/specs/`，`src/` 与 `tests/` 零接触——
这也是我不重跑测试套件而仍接受「无回归」的依据。

---

## 7. 裁定：文首修订计数（L1b 第 4 行 / L2 第 3 行）是否腐坏

**裁定：算缺陷。两处都算，级别都是 Important。理由不是「数字对不上」，而是有档案证明这两段是维护中的总账。**

### 7.1 L2（`2026-07-28-run-registry-design.md`）—— 证据最硬，先判这个

控制者引的是 `:3` 那句「Amended 2026-07-28 in eight places (a)–(h)」。
**单看这句，「不算缺陷」的辩护是成立的**：它带日期限定，只描述 2026-07-28 那一轮。

**但这句话后面紧跟 `Index:` 与一张逐条清单，而那张清单已经长到 `(j)` 了。** 我读的 `:6`–`:31`：

- `(a)`–`(h)` 八条 —— 对应 `:3` 说的那八处。
- `(i)` §11 —— 说明里逐字写着「**Found while writing the implementation plan**」，
  即**不是** 2026-07-28 那一轮的产物。
- `(j)` §8.1 / §13 item 4 —— 逐字写着「**unlike (a)–(i) this is not a document defect** …
  **Amended 2026-07-30 by that branch**」，明确是 07-30 的、由另一个分支做的。

**所以这张 Index 不是「07-28 那一轮的清单」，是这份文档修订的总账**：
两次后续修订 `(i)`、`(j)` 都被补进去了，且各自注明了自己的来历与日期。
本次新增的 `(k)`、`(l)` 落在了正文 `:60` / `:480`，**Index 一条都没加**。

**这就是本轮通篇在治的形状本身**：`(i)` 和 `(j)` 跟了，`(k)` 和 `(l)` 没跟。
不是「文首那句本就只管一轮」的问题——`(i)`/`(j)` 已经证否了那个辩护。

**可构造场景（两个，都不需要假设读者粗心）**：

1. **字母碰撞。** 下一个要给这份 spec 落勘误的人，按本仓库惯例先看文首 Index 挑下一个可用字母。
   Index 到 `(j)` 为止，他挑 `(k)` —— 与 `:60` 已存在的 `(k)` 撞车。
   届时 `(k)` 在同一文档里指两件互不相干的事，而**没有任何一处声明这一点**
   （对比 `(j)` 的两个落点是文首明写过的，见 §8）。
2. **「这条裁决执行了没有」的审计落空。** 2026-07-29 裁决记录 `:246`/`:248` 下过更正指令。
   下一个审计员要查「L2 spec 执行了没有」，最省力的入口就是文首 Index —— 十条里没有一条提债务归属。
   他据此判「未执行」，再开一次同样的勘误任务。**这正是 D7 这条债之所以拖到今天的机制的重演。**

### 7.2 L1b（`2026-07-27-owner-transfer-contention-design.md`）—— 同判，理由略弱一档但仍成立

`:4` 逐字：`Amendments: five, all found by the final whole-branch review of the implementation
and marked inline as **Amended 2026-07-28 (a)–(e)** in §2, §5.1, §5.3, §5.4 and §6.`
后面逐条列举 (a)–(e)。

L1b **没有** `(i)`/`(j)` 那样的先例可以直接证否「这句只管那一轮」的辩护 —— `(f)` 是这份文档的第一条后续勘误。
所以我不能靠先例定案，只能看句子本身。**看句子本身，它也是坏的**：

- 主语是 `Amendments:`（这份文档的勘误），谓语是一个**无条件的基数** `five`。
  它不是「2026-07-28 的勘误有五条」，而是「勘误：五条」。今天是六条。
- 后半句 `all found by the final whole-branch review of the implementation` 是一个**全称限定**：
  「全部由那次全分支评审发现」。`(f)` 不是那次评审发现的（它是 2026-08-02 人裁 + A4 评审员点名的产物），
  所以这个 `all` 今天为假。
- 句尾的章节列表 `in §2, §5.1, §5.3, §5.4 and §6` **仍然为真**（`(f)` 落在已列出的 §5.3），
  这是唯一没坏的部分。

**可构造场景**：L5 的读者按本仓库惯例先读文首摘要建立「这份文档被改过哪些地方」的地图。
他读到「五条，全部来自 07-28 那次全分支评审」，于是把 §5.3 当作
「07-28 定稿、此后未再变动」。他接着读 §5.3，读到 `(e)` 的「is L5's problem」
—— 而 `(f)` 就在 `(e)` 的下一段。他是否会读到，取决于他是否逐段读下去；
**而文首已经先告诉他「这里只有五条、都是 07-28 的」，这恰好降低了他继续读下去的必要性。**
`(e)` 本身长达一整段密排文字，`(f)` 在其后 —— 这不是理论风险，
而是 D4 这条债当初能存活的同一条路径。

**为什么是 Important 而不是 Critical**：被误导的读者得到的是**关于文档修订史的错误信念**，
不是关于系统行为的错误信念。它不会直接导致写错代码；它导致的是**重复劳动与重复勘误**
——也就是本轮任务存在的原因。够严重，但不到 Critical。

**为什么不是「不算」（正面回答控制者的预设辩护）**：
「文首那句本就只描述 2026-07-28 那一轮、不承诺是全文修订总账」这个辩护，
在 L2 上被 `(i)`/`(j)` **已经被补进 Index 且各自注明非 07-28 来历**这一事实直接证否；
在 L1b 上被 `Amendments: five` 的无条件基数与 `all found by…` 的全称限定直接证否。
**两边都没有站得住的「本就不承诺」解释。**

---

## 8. L2 文首「eight places (a)–(h)」与实际字母不符 —— 「不修」边界判定

**先纠正一个前提**：控制者数出的「a,b,c,d,e,f,j,j，`(j)` 出现两次且无 g/h/i」，
以及实施者 concern 3 里的「文件把 `(j)` 用了两次指两处不同内容，是既有不一致」
—— **两者都不准确，我自己数了。**

`grep -n "Amended" docs/superpowers/specs/2026-07-28-run-registry-design.md` 完整 13 行，
正文侧的字母标记是：

```
:96  *Amended (a).*
:180 *Amended (b).*
:230 *Amended (c), (h).*     ← 一行两个字母
:274 *Amended (d) — …
:295 *Amended (j) — …        ← §8.1
:383 *Amended (e).*
:402 *Amended (f), (g).*     ← 一行两个字母
:531 *Amended (j) — …        ← §13 item 4
```

即正文里 (a)–(h) 与 (j) 都在（`(g)`/`(h)` 分别与 `(f)`/`(c)` 合并标注），
`(i)` 是 §11 表格新增的一行、说明在文首 Index。**没有「缺 g/h/i」这回事。**

**`(j)` 出现两次也不是缺陷**：文首 Index 的 `(j)` 条目逐字写着
`**(j)** §8.1, §13 item 4 — …`，即**一条勘误、两个落点，文首已经声明过**。
`(c),(h)` 与 `(f),(g)` 同理是「一处正文承载两条勘误」。
**这份文件的字母体系是自洽的。**

**因此实施者 concern 3 的判断本身是错的**（他把「一条勘误两个落点、且文首已声明」
读成「一个字母指两处不同内容的既有不一致」）。这个错误判断**没有污染产物**
—— 它只出现在报告里，见 §11 M4 —— 但它是他用来给「不修」划边界的那条理由。

**「eight places (a)–(h)」这个数字本身**：它确实与 Index 的十条不符，
但如 §7.1 所述，`(i)`/`(j)` 各自在 Index 里注明了自己的来历与日期，
**这句 `eight … (a)–(h)` 可以正当地读成「07-28 那一轮八条」**，它不是全文总账
（总账是它下面那张 Index）。

**所以「不修」这个边界划得对吗 —— 结论对，位置错。**

- **对的部分**：`eight places (a)–(h)` 这个数字不必改。它有日期限定，
  且文件已用 `(i)`/`(j)` 的自述来源建立了「计数句管一轮、Index 管全部」的读法。
  实施者把它排除在本任务之外，**这个决定本身是对的**，符合 Rule 3。
- **错的部分**：他排除的是**错的对象**。真正必须跟的不是那个数字，是它下面那张 **Index**
  —— 而 Index 的缺口不是「既有缺陷」，是**他这次新增 `(k)`/`(l)` 直接造成的新缺口**。
  Rule 3 的「只清理自己的烂摊子」在这里恰恰指向「补 Index」，而不是「别碰」。
- 而且他给出的理由（「`(j)` 重复是既有不一致，不是我引入的」）**建立在一个假前提上**，
  所以即便结论碰巧对，这个「不修」的论证是不成立的。

---

## 9. 完备性 —— 我自己独立设计的搜索

**我没有复用实施者的 8 条模式，也没有复用 brief 里的任何模式。**
我的四族搜索改从「**同族断言的语义邻居**」入手（继承清单／字面取代关系／零改动的宿主函数／
`界的是` 这个动词短语本身），而不是从被点名的那两句话入手。

| # | 我的命令 | 命中数 | 判读 |
|---|---|---|---|
| S1 | `grep -rn "继承清单\|inherited-input\|inherited debt\|Inherited Debts" --include="*.md" docs .superpowers` | 22 | 见下 |
| S2 | `git grep -n "L5's problem"`（**全仓，含 `src/`、`tests/`，不限 `*.md`**） | 15 | 见下 |
| S3 | `git grep -n "零改动\|代码零改动\|zero code change\|no code change" -- '*.md'` | 60 | 见下 |
| S4 | `git grep -nE "three debts\|Debts 1\|debts 1\|三笔\|四笔债\|four debts\|bequeath" -- '*.md'` | 25 | 见下 |
| S5 | `git grep -n "SUPERSEDES L1b\|supersedes L1b\|L1b (e)\|L1b(e)"` | 8 | 见下 |
| S6 | `grep -n "界的是" docs/superpowers/specs/2026-08-01-…-design.md` | **6** | **命中第五处，见下** |

### 9.1 找到的第五处 —— **有，就在 §12 里，`:2322`**

`grep -n "界的是"` 的六条命中：`:1453`、`:1462`、`:2321`、**`:2322`**、`:2326`、`:2905`。

`:2322` 逐字（我 `sed -n '2310,2330p'` 读出的原文）：

> - **配额在「四道门全过、`resume_adopted` 已追加」那一刻计入**（§6 的「计入时点改判」）。
>   被门拒绝的 run 一次付费调用都没发生，让它吃掉配额**既不符合「界的是付费调用」这个目的**，又会造成确定性饿死。

**位置关系**：`:2321` 是那条第四轮更正，`:2322` 紧挨其下一行，`:2326` 是这次被改的那句。
**三句同在 §12（`:2312`–`:2326`）。被改的是最远的那一句，中间隔着的那一句没动。**

**为什么这是同族**：`:2326` 新增的更正逐字写着
「**「界的是付费调用」这半句**与本节上方「N 不等于付费调用次数（第四轮更正）」矛盾」。
这是一句**关于该措辞的一般性判定**。`:2322` 用的是**逐字相同的措辞**，
且同样把它当作 `--max-runs` 配额的**目的陈述**。按 `:2326` 自己刚立下的判据，`:2322` 同样矛盾。

**我也认真考虑了反方**（这不是机械字符串匹配）：`:2322` 的那句话论证的是
「被拒的 run 零付费，所以不该吃配额」，这条推理在 `N × maxAttempts` 更正之后**依然成立**，
而 `:2321` 也确实保留了「『有界批准』仍然成立」。所以 `:2322` 的**论证**不假。
**但被判为矛盾的从来不是论证，是措辞**——`:2326` 那半句的论证（「不界事件追加」）也不假，
它照样被就地更正了。**同一份文档、同一节、同一措辞、同一天，一处更正一处不更正，且没有任何一句话解释为什么。**

**这一处出现在实施者自己的输出里，且被他自己的摘要吞掉了**：报告「收尾自查」一节
把 `grep -n "界的是"` 的输出写成「`1453, 1462, 2321, 2322, 2326（含新插入的…）, 2905
——完整输出见「条目 3」与「完备性自查」两节，此处不重复贴同一段长文本」。
`2322` 就在这行里。**他打印了它、列举了它、然后一次都没有裁它**——既没改，也没写「不改，因为……」。
这恰好是 brief 铁律 2（「验证跑绝不过滤输出」）要防的那件事：
**被摘要掉的那一行正是漏网的那一行。**

### 9.2 其余四族：我没有找到第五处

- **S1（继承清单族）**：22 条命中里，`docs/` 侧只有 `handoff.md:682`
  「**L5 的继承清单因此从 4 笔降到 1 笔**（只剩债 2）」和 `handoff.md:95`
  「L5 的继承清单经上方那轮盘点已大幅改写」——**两条都是今天的正确状态，不需要勘误**。
  sweep spec `:2337` 那条是「术语先说清」的自我更正记录，不是活的过期断言。
  其余全在 `.superpowers/sdd/` 的调查报告与本轮 brief／report 里（记叙性文件，不是规范文本）。
- **S2（`L5's problem` 族，全仓不限 md）**：`docs/` 侧只有 L1b `:113`（原句）与 `:115`（新 `(f)`）。
  **`tests/controller/leaseLifecycle.integration.test.ts:1731` 也引了这句** ——
  我去读了 `:1720`–`:1745` 全段：它**已经**在引文之后逐字写了
  「Per human ruling: L3's transactionalization SUPERSEDES this amendment…」，
  **是同步的，不是漏网的第五处。**（这是台账 `:36` 那条 (a) 的执行结果。）
- **S3（零改动族）**：60 条命中我逐条扫过。`docs/` 侧与
  `preserveSuccessfulReconciliationIfNeeded` 有关的只有三处：
  spec `:1020`（原句）、`:1029`（既有「理由已过时」注）、`:1031`（新增 `Amended`）；
  外加 spec `:567`（§4.3 第六波）——**它已经明写「处置一落地之后那句话为假」并把裁定推给计划阶段，
  是同步的指针，不是过期断言**。plan `:225`/`:227`/`:229`/`:231`/`:909`/`:1943`/`:1964` 全是裁定三自身的记录。
  其余「零改动」全部指 `src/registry/`、`resumeLoop` 透传参数等**别的对象**，今天仍为真。
- **S4（三笔债族）**：`docs/` 侧只有 L2 `:57`/`:475`（两条原句）与 `:60`/`:480`/`:486`（三处新注引用）。
  `decisions/…:23`「四笔债里两笔（1、3）是 L3 的前置」是裁决记录自己的表述，正确。
  `handoff.md` 里的「三笔」全是「三笔提交」之类的无关用法。**无第五处。**
- **S5（取代关系族）**：8 条，全部在台账／调查报告／新 `(f)` 内，无未同步的规范文本。

### 9.3 我的搜索面覆盖了什么、没覆盖什么（不下无限定全称否定）

**覆盖**：HEAD `d5b3e79` 工作树上，`git grep` 可见的**全部已跟踪文件**
（S2/S3/S4/S5 用的是 `git grep`，其中 S2 不限扩展名，覆盖 `src/`、`tests/`、`scripts/`）；
S1 额外覆盖了 `.superpowers/` 下的未跟踪路径（用 `grep -rn`）。

**未覆盖，明写**：
1. **历史提交与其它分支**：我没跑 `git log -S` 或 `git grep <rev>`。
   本轮范围是 HEAD 上的当前文档，我同意这个范围，但不替 brief 做这个判断。
2. **未跟踪文件与 stash**：S2–S5 用 `git grep`，**看不见未跟踪文件**。
   我没有独立跑 `git status --porcelain -uall` 复核（实施者报告称输出为空，我未复验）。
3. **其它措辞的天花板**：我的四族词是我主观设计的语义邻居。
   我能确认的是「我设计的这几族在上述文件面上没有第五处（除 `:2322` 外）」，
   **不能排除存在我没想到的第六种同义改写。**
4. **`.superpowers/sdd/` 下的历史调查报告与任务报告我按「记叙性文件」处理，没有逐条勘误**
   —— 若本仓库认为那些也需要保持正确，我这一面没有覆盖。

---

## 10. 对实施者四条 concern 的独立判断

**concern 1（D7 起草时的自造假话，自曝并改正）—— 改正后的正文正确。**
我逐条核了：`(l)` 现在写的是「Neither item 1 nor item 3 below carries an `Amended` note」，
与 §13 实况一致（`grep -n "Amended"` 在 §13 区间只命中新增的 `(l)` 自己与 item 4 的 `(j)`）。
他附带的那句「item 3 有一句指向 deferred queue layer 但不纠正本段措辞」也逐字为真。
**自曝行为可嘉，且结果确实对了。**

**concern 2（brief 关于 lane 2「已补完可直接落」略有夸大）—— 他的判断成立，自撰正文质量高，没有走偏。**
控制者已认账这是 brief 的错，我只判产物：`:1031` 那段自撰勘误
（a）没有照抄 lane 2 的调查性叙述；（b）沿用了本文件的 `Amended` 句式；
（c）它引的三个锚点（`plan:225`–`:229`、`plan:231`、`fileStore.ts:392`–`:395`）**我逐个复验，全部准确**；
（d）它在「改动前形态」上措辞谨慎——写的是「**计划记录的**改动前形态」，
没有对历史代码下无据断言。**自撰反而比照抄更稳，没有因此走偏。**

**concern 3（`(k)`/`(l)` 字母选择 + 「不修既有 `(j)` 重复」）—— 字母选对了，理由是错的。**
`(k)`/`(l)` 是 Index 到 `(j)` 之后的下两个自由字母，**选择正确**。
但他给「不修」的理由建立在假前提上：`(j)` 出现两次是**文首明写过的「一条勘误两个落点」**，
不是既有不一致（详见 §8）。且他真正该做而没做的不是修那个 `(j)`，**是把 `(k)`/`(l)` 补进 Index**。

**concern 4（`(f)` 的日期取 `2026-08-02`）—— 选错了。这是 Important I4。**
本仓库的日期约定是**做出勘误的那一天**，有两个数据点：
（i）L1b 文首 `:2` 逐字「Status: proposed on 2026-07-27; **implemented and amended on 2026-07-28**」，
而 `(a)`–`(e)` 全标 `2026-07-28`；
（ii）L2 的 `(j)` 逐字「**Amended 2026-07-30 by that branch**」，
而它记录的那个分支叫 `2026-07-29-atomic-write-paths` —— **标的是落注的 07-30，不是分支的 07-29**。
`(f)` 这条注是在 `d5b3e79` 落的，`git log -1 --format='%ad' --date=iso d5b3e79` → **`2026-08-06 01:17:57 +0800`**。
`2026-08-02` 是被勘误事件（台账文件）的日期
（`git log --diff-filter=A --format='%ad' --date=iso -- …/progress.md` → `2026-08-02 13:18:51 +0800`），
**正是控制者提醒要区分的那一类。**
更硬的一点：这条注**自己的结尾写着 `this paragraph is that note, landed now`** ——
「landed now」与标头的 `2026-08-02` 在同一段里互相打架。

---

## 11. Findings

**Critical：0。** 四条勘误正文的事实断言我逐条复验，全部为真；`--max-runs` 那半句未被误伤；
那一处 `−1` 确为机械副作用。没有一条足以让读者对系统行为形成错误信念。

### Important

**I1 — §12 里 `界的是付费调用` 出现两次，只改了一处，另一处既没改也没裁。**

- **锚点**：`docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md`
  - 未同步的那句（逐字）：「被门拒绝的 run 一次付费调用都没发生，让它吃掉配额**既不符合「界的是付费调用」这个目的**，又会造成确定性饿死。」
  - 本次改的那句（逐字）：「**本节不界的东西，明写出来**：`--max-runs` 界的是**付费调用**，不界事件追加。**⚠️ 就地更正：「界的是付费调用」这半句与本节上方…矛盾…**」
  - 两句同在「## 12. 治理与付费调用」内，中间只隔「**`--max-runs` 的完整落地面**」一段。
- **场景**：L5 的实施者自上而下读 §12。他先读到「N 不等于付费调用次数（第四轮更正）」，
  再读到「既不符合「界的是付费调用」这个目的」——未加注；最后读到同一措辞被明写为
  「与本节上方…矛盾」。新注下的是一条**关于该措辞的一般判定**，于是他必须自己决定
  中间那句算不算也错。文档不回答。他要么重开已被三轮裁定钉死的「计入时点」结论，
  要么第三次提交同一条勘误。
- **加重情节**：这一行出现在实施者自己「收尾自查」的 `grep -n "界的是"` 输出里
  （他写的是「1453, 1462, 2321, **2322**, 2326…, 2905 ——…此处不重复贴同一段长文本」），
  **打印了、列举了、一次都没裁**。
- **修法**：在那句的「这个目的」之后就地加一句短注，说明本处「界的是付费调用」是**目的陈述**、
  在 `N × maxAttempts` 更正之后仍成立；**或**改为「界的是付费规模」这类不与更正撞词的说法。
  两条路都行，**但必须留下一句显式裁定，不能沉默**。

**I2 — L2 文首那张维护中的 Index 没跟上本次新增的 `(k)`/`(l)`。**

- **锚点**：`docs/superpowers/specs/2026-07-28-run-registry-design.md`
  - Index 末条（逐字）：「**(j)** §8.1、§13 item 4 — … **Amended 2026-07-30 by that branch**。」
  - Index 倒数第二条（逐字）：「**(i)** §11 — … **Found while writing the implementation plan** …」
  - 正文新增（逐字）：「*Amended (k) — item 6's "three debts" is stale.…*」／「*Amended (l) — "Debts 1–3 are bequeathed to L5" is stale.…*」
- **为什么这不是「文首只管 07-28 那一轮」**：`(i)` 与 `(j)` **都不是** 07-28 那一轮的产物，
  却都被补进了这张 Index 并各自注明来历与日期。**这张 Index 是全文修订总账，已有两次维护先例。**
- **场景（两个）**：(1) 下一个给这份 spec 落勘误的人按惯例看 Index 挑下一个自由字母，
  看到 `(j)` 为止，挑 `(k)` —— 与正文已有的 `(k)` 撞车，且**没有任何一处声明这次重用**
  （对比 `(j)` 的两个落点是 Index 明写过的）。
  (2) 审计「2026-07-29 裁决的更正指令在 L2 执行了没有」的人，从 Index 入手，
  十条里没有一条提债务归属，判「未执行」，重开同一条勘误 —— **正是 D7 拖到今天的同一条路径。**
- **修法**：在 Index 的 `(j)` 之后补 `(k)`、`(l)` 两条，各一行，
  沿用 `(i)`/`(j)` 的写法注明「2026-08-05 落，来自 2026-07-29 债务归属裁决」。

**I3 — L1b 文首「Amendments: five … (a)–(e)」在 `(f)` 落地后为假，未同步。**

- **锚点**：`docs/superpowers/specs/2026-07-27-owner-transfer-contention-design.md` 文首（逐字）：
  「`Amendments: five, all found by the final whole-branch review of the implementation and marked
  inline as **Amended 2026-07-28 (a)–(e)** in §2, §5.1, §5.3, §5.4 and §6.`」
  正文新增（逐字）：「`**Amended 2026-08-02 (f): the preceding sentence's "is L5's problem" is superseded…**`」
- **为什么算**：主语是 `Amendments:`（本文档的勘误），谓语是一个**无条件基数** `five`；
  后半句 `all found by the final whole-branch review` 是**全称限定**，而 `(f)` 来自
  2026-08-02 人裁 + A4 评审员点名，不是那次评审发现的。两处今天都为假。
  （句尾的 `in §2, §5.1, §5.3, §5.4 and §6` 仍为真，`(f)` 落在已列出的 §5.3。）
- **场景**：L5 读者按惯例先读文首摘要建立「这份文档改过哪儿」的地图，
  得到「五条、全部 07-28」，于是把 §5.3 当作 07-28 定稿。
  `(e)` 是一整段密排文字，`(f)` 紧随其后 —— 文首那句恰好降低了他读下去的必要性。
  **这正是 D4 当初能存活的同一条路径。**
- **修法**：把该句改成「Amendments: six —— (a)–(e) 于 2026-07-28 由全分支评审发现；
  (f) 于 2026-08-05 落，执行 2026-08-02 的 A4 人裁」，并在其后的逐条列举里补一条 (f)。

**I4 — `(f)` 的日期取的是被勘误事件的日期，不是落注日期，且与它自己的结尾自相矛盾。**

- **锚点**：同上文件，逐字「`**Amended 2026-08-02 (f):` … `this paragraph is that note, landed now.**`」
  —— 标头 `2026-08-02` 与结尾 `landed now` 在同一段里互相打架。
- **本仓库的日期约定（两个数据点，见 §10 concern 4）**：
  L1b 文首「implemented and **amended on 2026-07-28**」而 (a)–(e) 全标 07-28；
  L2 的 `(j)` 标「**Amended 2026-07-30** by that branch」而它记录的分支叫 `2026-07-29-atomic-write-paths`。
  **约定是「做出勘误的那一天」。** 本注落在 `d5b3e79`，提交时间 `2026-08-06 01:17:57 +0800`。
- **场景**：审计员想知道「A4 评审员点名『NOT done here / Flag for the human』之后，
  这个缺口开了多久」。他读 `Amended 2026-08-02 (f)`，而台账里评审员点名也在 `2026-08-02`
  的那份文件里 —— 他得出「当天就补上了」。真相是**开了三天多，直到本轮勘误任务才关**。
  这是一份专门用来记录「这个缺口何时关闭」的注，却把关闭时间记错了。
- **修法**：改为 `Amended 2026-08-05 (f)`（或 08-06，取提交日），
  把 `2026-08-02` 保留在正文里作为**人裁发生日**，两个日期分开写。

### Minor

**M1 — `(k)`/`(l)` 把「debt 3 也归 L3」挂在两个不含该结论的行号上。**

- **锚点**：`(k)`／`(l)` 逐字「…ruling (`docs/superpowers/decisions/2026-07-29-technical-debt-attribution.md:246`,
  `:248`) reattributed debt 1 to L3 … **and debt 3 to L3**…」。
  而 `:246` 逐字只讲债 1（「L5 继承清单第 1 条描述有误…从 L5 清单移到 L3」），
  `:248` 逐字只讲总数（「L5 继承清单现在只剩 1 笔（债 2），不是 4 笔」）。
  债 3 → L3 的实际出处是同文件结论速查表逐字「`| 3 | heartbeat.stop() 释放窗口 | **L3** |`」
  与「四笔债里两笔（1、3）是 L3 的前置」。
- **场景**：读者按指针跳到 `:246`/`:248`，找不到关于债 3 的任何一个字，
  于是怀疑这条勘误是推断而非引用，把它降级为「未经证实」，重新去查。
- **修法**：在两处的行号列表里补上结论速查表那一行的锚点。

**M2 — 报告里 `sed -n '223,231p'` 的「输出」不是该命令的真实输出，且据此写下的行号 `:223` 是错的。**

- **锚点**：`task-3-report.md` 逐字「我复验时读到裁定三标题在 `:223`」，
  其上贴的代码块首行是「`### 裁定三 —— …`」。
  我实跑 `sed -n '223,231p' … | cat -n`，首行是「`3. **不选「把整块判定上移进 …**`」，
  标题在该输出**第 3 行**，即文件 `:225`。
- **影响面**：**产物没受影响** —— 落盘的勘误引的是 `:225`–`:229`，是对的。
  受影响的是报告的自证可信度：本仓库铁律要求「每个数字旁附一条能重推它的命令并写下输出」，
  而这条命令重推不出那个输出。
- **场景**：下一个评审员照报告的 `:223` 跳过去，落在一段无关的理由文字上，
  判「引文不可复现」，把一条其实正确的勘误退回。
- **修法**：报告里把 `:223` 改成 `:225`，并把代码块换成命令的真实完整输出。

**M3 — 四条收尾自查里的第三条输出被摘要，违反 brief 的「输出未过滤」铁律，而漏网的那一行就在被摘要掉的部分里。**

- **锚点**：`task-3-report.md` 逐字「`1453, 1462, 2321, 2322, 2326（含新插入的 ⚠️ 就地更正句）, 2905`
  ——完整输出见「条目 3」与「完备性自查」两节，**此处不重复贴同一段长文本**」；
  brief 逐字「**验证跑绝不过滤输出**，`grep` 与 `tail` 同罪」。
  「完备性自查」一节的 8 条模式同样是叙述命中数而非贴原始输出。
- **场景**：评审员（或下一轮的实施者）读这份报告，看到六个行号被压成一行摘要、
  且摘要文字只解释了 `2326`，就默认另外五个都已裁过 —— `2322` 因此第二次逃脱。
  **这不是假设：I1 就是这么发生的。**
- **修法**：把这条 `grep` 的完整原始输出贴回报告，并对输出里每一行给一句「改了／不改，因为……」。

**M4 — 报告 concern 3 断言「文件把 `(j)` 用了两次指两处不同内容，是既有不一致」，该断言为假。**

- **锚点**：L2 文首 Index 逐字「**(j)** §8.1、§13 item 4 — …」
  —— **一条勘误、两个落点，文首已明写**。同理正文另有逐字「*Amended (c), (h).*」与「*Amended (f), (g).*」，
  是「一处正文承载两条勘误」。这套字母体系是自洽的。
- **影响面**：同样**没有污染产物**（`(k)`/`(l)` 的字母选择本身是对的），
  但它是他用来论证「不修文首」的那条理由 —— 结论碰巧对（那个 `eight … (a)–(h)` 确实不必改，见 §8），
  **论证不成立**，而真正该跟的 Index 因此被漏掉（= I2）。
- **场景**：后来者读到「本文件既有 `(j)` 重用缺陷」，去修一个不存在的缺陷，
  或据此认为本文件的字母体系不可信、另起一套编号。
- **修法**：报告里撤回该断言；处置上按 I2 补 Index。

### 给控制者的一句话

**这一波也自带缺陷，而且是被预测到的那两种形状同时出现**：
一处「同族第三处漏掉」（I1，且藏在被摘要的输出里，M3），
两处「一改一没跟」（I2/I3，文首总账没跟正文）。
**四条勘误的正文本身没有引入任何不实断言** —— 任务 1 的形状这次没有重演。
