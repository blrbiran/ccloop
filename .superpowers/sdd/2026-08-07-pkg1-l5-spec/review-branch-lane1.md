# 包 1 整分支评审 —— Lane 1（规格 / 文档面）

## 0. 结论（最先写）

### 判词：**需修复环 2**

**技术理由（两句）**：
1. **N1 今天在 `c842257` 的正文上完全成立，我判 Critical**（不是 Important）。理由不是"保险起见"：
   §4.2 把授权面限定为"位于一个 run 目录内"，而**全 spec 没有任何一处定义"run 目录"的外延**；
   而 INV-4 把 §4.2 立成**唯一授权源**，同时 §3.2.2 与 §4.5 都**逐字声明自己不是限制清单**
   （`:196`「**不是为了定义 INV-4 的边界**」、`:314`「本节**不是**一张「禁止删除清单」」）
   ⇒ 一个 L5 实现者按字面办事，会去删 `.validation-runs/<run>/` 下的五段形残留，
   **而那棵树被三份既有文档逐字禁止清理**。这命中 brief 的 Critical 判据原文
   「读者按字面办事会删掉不该删的东西」，并且**同时命中第三条**：I1 的整个论证
   （"把完备性压到只有一个面的授权侧"）依赖那个面**有边界**，今天它没有 ⇒ **承重论证只立了一半。**
2. 另有 **4 条 Important**，其中两条是**新发现、不在 6 条 deferred minor 之内**：
   §6.1「五份 L3 门报告全部在盘上（这推翻了上一轮「门报告没落盘」那个前提）」——
   该"推翻"依赖一次**未声明的定义放宽**，而本仓库 handoff `:228` 逐字仍记
   「三道门六份**评审**报告只落盘 2 份（全是 lane 2，GATE-A 连 lane 2 都没有）」；
   以及 §2.1「本仓库此前的九份报告一次没提「保留」这半」——**我用九份逐份检索证伪了它**。

### 与人裁 8 的关系

- **N1**：人裁 8 已把它升级为必撞项。我撞了、重推了、**定级比再评审员高一档（Critical）**，并给了具体封口修法（§2.4）。
- **INV-4 未进 §8.2**：我**不建议**现在补（遵守顺序约束）。但我发现**另有一条不受该顺序约束的修法**：
  §6.2 那条自立规则的**第二个分支**（"回答不出来的，就明写成「今天没有机制强制」"）
  今天对 INV-4 **一个字都没写**。补这一句**不需要先封口 N1**，因为它记录的是"零强制"这个事实本身。见 F-I1。
- **C4**：我复核了 `2026-07-26-run-lease-and-heartbeat-design.md:45` 今天**仍逐字**是
  `L5 corresponds to the third follow-on spec named in the ownership design §17 and remains unwritten.`，
  **该文件内我未发现任何反向指针**（限定：我只 grep 了 `remains unwritten` / `L5` / `cleanup / orphan` 三个模式，
  **未对该文件做 `Amended` 全文检索**）。处置（派一条一行勘误、时机未到）**站得住**，我不建议改动。

### 计数（附判别式）

| 级别 | 数 | 判别式 |
|---|---|---|
| Critical | **1** | N1（§4.2 授权面外延未封口）。判别式 = "spec 读者按字面办事会删掉被既有文档逐字禁止删除的东西" |
| Important | **4** | F-I1（§6.2 自立规则漏 INV-4）／F-I2（§2.1 全称否定被证伪）／F-I3（§6.1 门报告前提靠未声明的定义放宽）／F-I4（handoff 三处"远端与本地一致"的**判词**在写下时即为假） |
| Minor | **8** | 见 §9。判别式 = "条目"（一条 finding 记一条），**不是**"落点"（M1 有 2 个落点仍记 1 条） |

**我自报未完成 9 条**（§10），**预算破了约 4×**（§11）。

### 一个前提声明（brief §7.2 要求）

**本报告 §3.5、以及我对 §5.4「无界垃圾不是故障」定性的"未被升级、成立"判断，
以该定性本身成立为前提。若 Lane 2 证伪该定性（`<runDir>/worktrees/` 那条破坏线索），
§3.5 与 F-I3 之外的 §5.4 相关结论需重判，且整份 spec 的立项理由段（`:407`–`:410`）要重写。**
我**没有**去查那条线索（Lane 2 切片）。

## 1. 评审范围与我实际读了什么

### 范围（自己重跑，未过滤）

```
git diff --stat 30cbdd5..c842257   -> 8 files changed, 1964 insertions(+), 11 deletions(-)
git log --format='%h %ad %s' --date=short 30cbdd5..c842257  -> 9 笔（9f78169 … c842257）
```

与 brief 的"9 笔 / 8 文件 / +1964 −11"**逐字吻合**。

**⚠️ 一处范围事实，控制器可能想知道**：`git rev-parse HEAD` 今天 = **`d1a8af6`**，
其后还有 `d22748c`（"STEP 4 入台账"）。即今日 HEAD 已在评审基线之上多两笔。
`git rev-list --count refs/remotes/origin/main..HEAD` 因此是 **10** 而不是 brief 写的 9——**两个数都对，测点不同**。
我**严格按 `30cbdd5..c842257` 评审**，`d1a8af6` / `d22748c` 不计入。
`git status --short` 空（我除自己的报告外未改动任何文件）。

**我确认过范围说明**：`.superpowers/sdd/2026-08-07-pkg1-l5-spec/` 下
`scan-1-brief.md` / `scan-1-deferred-minors.md` / `scan-2-brief.md` / `scan-2-design-input.md`
四份**不在 `30cbdd5..c842257` 的 diff 内**，按 brief §4 它们是输入不是产物，**我不报"漏了"**。
`review-branch-lane*-brief.md` 与两份 lane 报告同理（落在 `d1a8af6` 上）。
**入库完整性检查**：`git diff --numstat` 列出的 5 份 `.superpowers/` 产物（progress / re-review / review-task1 /
task-1-brief / task-1-report）**全部在 diff 内** ⇒ `git add -f` 没有漏产物。**这一项无 finding。**

### 我实际逐字读完的（不是扫过）

| 文件 | 读法 |
|---|---|
| `docs/superpowers/specs/2026-08-07-cleanup-and-orphan-gc-design.md` | **全文 551 行逐行读** |
| `.superpowers/sdd/2026-08-07-pkg1-l5-spec/progress.md` | **全文 608 行逐行读** |
| `docs/handoff/handoff.md` | diff 全文 ＋ `grep -n 'Amended\|远端\|一致'` 的 22 行命中逐条打开 ＋ `:135`–`:150` 原文 |
| `2026-07-29-atomic-write-paths-design.md` | 勘误 diff 全文 ＋ §4（`:78`–`:100`）原文 |
| `2026-07-26-run-lease-and-heartbeat-design.md` | `:41` / `:45` 逐字 |
| `src/persistence/fileStore.ts` | `:670`–`:690`（`writeOwnerTransferRecord` 注释块）原文 |
| 九份 L5 输入盘点报告 | **逐份**跑了两轮检索（`retained`/`cleaned up` 与 `第 4 条`/`固定名`/`getOwnerTransferPaths`/`buildAtomicTempPath`），命中行**全部打开看了上下文** |

### 我**没有**读的（诚实边界）

- `task-1-report.md` / `review-task1.md` / `re-review-task1-fix1.md` **三份报告我没有逐行读**。
  我读的是台账 `progress.md` 里对它们的转述（`:318`–`:498`），**并对其中每一条我要引用的结论回原文/回命令重推**。
  这是**有意的取舍**：brief 的铁律 1 说"不接受实施者自证"，所以这三份报告对我而言只是线索来源，
  ⇒ **凡我在本报告里写"成立/不成立"的，都是我自己跑命令或读正文得出的**；
  凡我引用它们而没有重推的，我在文中就地写了"未独立复核"。
- **`src/` 与 `tests/` 的代码事实我基本没查**（Lane 2 切片）。唯二例外：
  `releaseOwnerLease` 在 `tests/` 的 8 行（为验 M1）、`writeOwnerTransferRecord` 的注释块（为验 M2）。

### 方法（铁律合规自陈）

- **所有验证性检索都写进脚本文件再 `rtk proxy zsh <script>` 跑**（铁律 4 的对策），共 8 个脚本。
  **没有在命令行嵌过引号**，**没有用 `grep`/`tail` 过滤过任何验证输出**（铁律 2/5）。
- **每个数字都附了重推命令与判别式**（铁律 6）。
- **我下的每一条全称否定都带检索面限定**（铁律 3）——见 §6 与 §9 的"⚠️ 限定"标注。
- **零输出我验过命令本身**：本轮唯一一次异常是 `p4.sh` 以 exit 1 结束——
  我确认那是最后一条 `grep -c` 命中 0 导致的正常退出码，**输出未被截断**（8 个 `for` 循环块全部完整打印）。

## 2. 必撞项 N1 —— §4.2 授权面外延

**方法声明**：brief 的事实链我**没有照抄**。下面每一环我都回 `c842257` 的正文逐句核过，引文逐字。

### 2.1 事实链逐句复核 —— **五环全部成立**

| 环 | brief 的说法 | 我在正文上核到的逐字原文 | 判 |
|---|---|---|---|
| 1 | I1 的论证是"把完备性压到只有一个面的授权侧"，理由是代价不对称 | §3.2.1 逐字：「采用默认拒绝的理由很直接：**保留清单的遗漏会造成数据丢失，授权清单的遗漏只会让 GC 少干活。** 两种错误的代价不对称，故把完备性要求压在**授权**一侧（§4.2 只有一个面，可以穷举），而不是压在**保留**一侧（§3.2.2 已证明会漏）。」 | **成立** |
| 2 | 为此新增 INV-4 | §3.2.1 逐字：「> **INV-4：L5 只允许删除本 spec §4.2 明确授权的那一个面。其余一切默认保留。** > **不在授权清单上 ≠ 可以删；不在保留清单上，也 ≠ 可以删。**」 | **成立** |
| 3 | §4.2 的空间限定只有一句"位于一个 run 目录内" | §4.2 逐字：「- 目标必须**同时**匹配 `buildAtomicTempPath` 的五段形（§5.2）**且**位于一个 run 目录内；」 | **成立** |
| 4 | 全 spec 从未定义"run 目录"的外延 | `grep -n 'run 目录' <spec>` 三处命中，**逐处打开**：`:208`（§3.2.2 第 5 条，引 07-17 计划的 `Never delete prior run directories`）、`:282`（§4.2 那句限定本身）、`:319`（§4.5 举例"任何 run 目录本身"）。**三处都在*使用*这个词，没有一处*定义*它。** 另跑 `grep -n 'runDir\|run dir\|run-dir'`，5 处命中全是源码引文与 `getOwnerTransferPaths(runDir)`，无定义 | **成立**（判别式："定义"= 给出该词的外延边界，如"哪些目录算、哪些不算"） |
| 5 | §3.2.1 那句"可以穷举"尚未兑现 | §4.2 给的是**四条谓词形状**（五段形匹配／位于 run 目录内／生成进程不再活着／该 run 无进行中 owner-transfer 事务），**没有任何一处是枚举**。而"可以穷举"是 §3.2.1 用来支撑"把完备性压在授权侧"的**唯一理由** | **成立** |

**额外一环（brief 没写，我补上，它使 N1 更硬）**：
**两张本可以救场的表，都逐字放弃了救场资格。**
- §3.2.2 逐字：「列出它们是为了给 INV-4 提供具体例证，**不是为了定义 INV-4 的边界**」
- §4.5 逐字：「本节**不是**一张「禁止删除清单」—— 写成清单就会重蹈 §3.2 的覆辙」
  ＋「⚠️ **正确的读法是**：想删某个东西时，问「§4.2 明确授权了它吗？」—— **答案不是「是」，就不许删。**」

⇒ §4.5 的逻辑是**单向的**：「§4.5 提到 ≠ 授权」。它**从未说**"§4.5 提到 ⇒ 禁止"。
**所以当 §4.2 说"是"的时候，§4.5 里的举例挡不住它。** 这是 I1 修法的直接后果，不是残留的旧问题。

### 2.2 可构造场景（谁、按哪一节、删了什么、后果）

**谁**：L5 的实现者（本 spec §8.2 明写这五条测试要"随 L5 实现一起落地"，所以他确实是 spec 的目标读者）。

**按哪一节**：§4.5 的操作指令逐字「想删某个东西时，问「§4.2 明确授权了它吗？」」⇒ 他去查 §4.2 的四条谓词。

**对象**：一次崩溃在 `.validation-runs/<run-id>/` 下留下的 `.events.jsonl.48213.1754500000000.3.tmp`。
- 谓词 1 五段形 —— **匹配**。
- 谓词 2「位于一个 run 目录内」—— `.validation-runs/<run-id>/` **字面上就是一个 run 的目录**，
  且本 spec §3.2.2 第 5 条自己引的 07-17 计划逐字用的正是 `prior run directories` 这个词来指它们。**他判匹配。**
- 谓词 3 生成进程不再活着 —— 崩溃残留，**匹配**。
- 谓词 4 该 run 无进行中 owner-transfer 事务 —— 一个历史 validation run，**匹配**。
⇒ **§4.2 授权删除。INV-4 逐字说"只允许删 §4.2 明确授权的那一个面"——这一个面授权了，他删。**

**后果**：他删掉的是位于三份既有文档逐字禁止清理的树下的东西：
- §3.2.2 第 6 条引 `2026-07-21-docs-and-backlog-truth-alignment-design.md` 逐字
  「deletion or mutation of `.validation-runs/`, backup branches, or stashes;」（在"本次不包含"清单内）
- §3.2.2 第 7 条引 `2026-07-19-a04-branch-assessment-and-merge-readiness-design.md` 逐字
  「preserved real-run evidence lives only under `.worktrees/evidence-first-v1/.validation-runs/`
  and **must not be cleaned or rewritten**;」
- §3.2.2 第 5 条引 07-17 计划逐字「**Never delete** prior run directories, retained worktrees, stashes, or evidence.」

**同一场景的第二个实例（更贴 §4.5 的举例）**：`<runDir>/worktrees/<attempt>/` 下的五段形残留。
§4.5 举例逐字把「任何 `worktrees/` 下的条目（它归 run 生命周期，不归 GC）」列为非授权面；
但该文件**位于一个 run 目录内**（谓词 2 成立），§4.2 授权，**INV-4 让 §4.2 赢**。
两节直接对撞，而 spec 没有给冲突裁决规则——**准确说，它给了，就是 INV-4，而 INV-4 判 §4.2 赢。**

### 2.3 分级：**Critical**，判据

brief 的 Critical 判据逐字有三条，**这条命中两条**：
1. ✅「**读者按字面办事会删掉不该删的东西**」—— §2.2 已构造。这不是"读者可能误解"，
   是**按 §4.5 亲自给的操作指令一步步走**得到的结果。
2. ✅「**承重论证塌了**」—— I1 的整个修法建立在"授权侧只有一个面、可以穷举"。
   **今天那个面既没有边界、也没有被穷举**，§3.2.1 的代价不对称论证因此**没有兑现**：
   它承诺把风险从"保留清单会漏"搬到"授权清单不会漏"，实际搬到的是"授权面外延未定"——
   **而未定外延的授权面，其遗漏方向恰恰是"多删"，也就是它本要避免的那一侧。**
3. ❌ 数据丢失 —— **今天不成立**（spec 未实现，`src/`/`tests/` 零改动）。

**为什么不降为 Important**：因为 1 与 2 各自独立命中。
brief 逐字警告「不要因为想显得温和就把真洞降级」。
**唯一的减轻情节**是它今天没有可执行代码，但那减轻的是**紧迫性**，不是**等级**——
一份 spec 的作用面就是读者，读者按字面办事会删错东西，就是这份 spec 的 Critical。

**⚠️ 我与再评审员的分歧要明写**：他判 Minor。我认为他的定级发生在**他自己发现"INV-4 让 §4.2 赢"之前的框架里**
（他把它记成"两表不一致"的延续）。人裁 8 已把它升级为必撞项，**我把它继续升到 Critical，并给出上面两条判据**。

### 2.4 修法建议（要具体，且不制造下一个未定外延）

**不要**去定义"run 目录"这个词——那正是"用一个未定外延换另一个未定外延"。
**改成用一个已经在代码里存在、可判定的谓词**。三步：

1. **把空间限定改成"由本仓库自己的 run 识别逻辑判定"**，而不是一个中文名词。
   本 spec §5.4 已经引到了那个现成判据：`RUN_MARKER_FILES`（`scanRuns.ts:30-36`，五个具名文件）。
   建议改写 §4.2 谓词 2 为：
   > 目标所在目录**本身**必须是一个被 `RUN_MARKER_FILES` 判定为 run 的目录（**不含其任何子目录**），
   > 且该目录**不在** `.validation-runs/` 或 `.worktrees/` 之下。
   —— 三个条件全部可用一条 `stat` + 一次路径前缀比较判定，**没有留下自然语言外延**。
   ⚠️ **`RUN_MARKER_FILES` 的具体内容我未独立复核**（Lane 2 被指派了这条），若它不适合，用同样形状换别的可判定谓词。

2. **补一条排他子句（这才是真正封口的那一句）**，写进 §4.2 而不是 §4.5：
   > **INV-4 的授权只在上述谓词全部成立时生效。任何一个谓词判不出来（包括"这算不算 run 目录"判不出来），
   > 按 §3.3「判不出来就保留」处理，不删。**
   这一句把 N1 的**剩余不确定性导向保留侧**，与 §3.3、INV-3c 已有的取向合流，**不需要穷举任何东西**。

3. **兑现或撤回 §3.2.1 那句「§4.2 只有一个面，可以穷举」**。二选一：
   (a) 谓词封口后改成「§4.2 那个面由三条可判定谓词完全刻画」（**这才是它真正想说的**）；
   (b) 若做不到，就**明写"该面的外延依赖 §4.2 谓词的准确性，今天尚未被独立验证"**——
   与本 spec §1.3 对 INV-1 的诚实处置同型。
   **无论选哪个，都不要留着"可以穷举"这句今天为假的话。**

**顺序**：这三步做完之后，才轮到人裁 8 第 2 条（INV-4 进 §8.2）。**我不建议提前。**

### 2.5 §6.2 那份枚举漏 INV-4 —— **今天仍成立**

见 §9 的 **F-I1**（我在那里给了独立重推的 `grep -n 'INV-4'` 七行输出与逐条判定）。
**结论：再评审员这条今天完全成立，且我给它比他更高的一档（Important，他记 Minor 缺口）。**

## 3. spec 内部一致性与自指诚实度

### 3.1 INV-1 —— 写成不变式而非待裁选项？**是。M4 已落，我独立复核。**

- **不变式而非选项**：§1.3 逐字「> **INV-1：L5 的 GC 不得调用 `releaseOwnerLease`，也不得以任何其它方式写 owner-record 的租约字段。**」
  —— 祈使句、单条、无"待裁/可选/建议"字样。§8.2 表首行也给了 mutation 式达标判据
  （逐字「**把该断言删掉后测试必须变红**」）。**判：写成了不变式。**
- **M4（§1.3 就地写明零强制）今天在盘上**。我不采信台账那句"已落"，自己回正文核，逐字：
  > *** **INV-1 今天零强制 —— 明写在此，不藏在报告里。** ***
  > 在 L5 实现落地之前，INV-1 **没有任何机制强制**：没有测试会因为违反它而变红，没有 lint 钉住它，
  > 它今天的全部存在形式就是本节这段文字。
  > **这使 INV-1 与 §6.1 那条注释完全同型** —— 即本文自己在 §6.2 记为 RISK-1 的那个形状：
  > 约束写在文本里，靠后来者读到并遵守。**本文因此无法宣称自己免疫于它所批评的问题。**
  **"与 §6.1 那条注释完全同型"这句 brief 点名要核的，逐字在盘上。判：M4 完全 ADDRESSED。**
- **自指诚实度整体**：§1.3 结尾逐字「**本 spec 是约束的载体，不是约束的执行机制。**…
  **在此之前它是一条未兑现的防线**。读者不应把本节读成「这条已经安全了」。」
  —— **这是本分支质量最高的一段，我记为正面样本，没有 finding。**
- ⚠️ **但同一套诚实度标准没有被施加到 INV-4 身上**（见 F-I1）。**这就是 §3 的主要 finding。**

### 3.2 §1.4 那条耦合 —— 安放正确？**是（一处 Minor）。**

§1.4 逐字：「=> **两者必须一起决策，不能分两轮。** 本 spec 不做这个决策（它不在 L5 本职范围内），
但**明确记下：谁先动其中一边，谁就必须同时把另一边摆上桌。**」
＋ §8.1 非目标第 4 条逐字「**不重开「保留即放宽」人裁**，也不改动 §13 第 3 笔的现有判定 —— 只在 §1.4 记下二者的耦合。」

⇒ **实施者自报的"只记录、未决策，别让它随本 spec 被当成已处理"，在正文里落实了，且落在两处（§1.4 ＋ §8.1）。判：安放正确。**
⚠️ **但"上一轮四份扫描与两条评审车道都没点出这条耦合"这句是有问题的**——见 §9 的 **N3**。

### 3.3 §8.2 五条测试一条没写 —— ⚠️ 明写了？**是。**

§8.2 表下逐字：
> ⚠️ 上表是**要求**，不是已完成事项 —— **本 spec 落盘时这五条测试一条都不存在**，
> 因为本轮无 `tests/` 授权。它们随 L5 实现一起落地。

＋ 文首 Status 行逐字「**未实现**：本文只定边界与不变式，`src/` 与 `tests/` 本轮一字未动。」
＋ §8.1 非目标第 1 条。**三处冗余声明。判：不会被读成已有护栏，无 finding。**

### 3.4 RISK-1 的描述是否准确？**结论准确，机制描述有一处失真 ⇒ F-I3。**

- **准确的部分**：RISK-1 逐字「> **RISK-1：「靠人评审兜住、事后无法证明兑现」的防线，在本仓库已有一个实例
  走完了整个生命周期并且无法证明兑现过。**」——这个结论我认为成立且**比 spec 自己论证的更强**（见 F-I3）。
- **失真的部分**：紧接的形状描述逐字「约束写在注释里 → 声明由评审兜底 → 评审确实发生了、
  **报告确实落盘了** → **但报告里没有任何一处能对上那条约束**」。
  **"报告确实落盘了"对 GATE-A 为假**（handoff `:228` 逐字："GATE-A 连 lane 2 都没有"）。**详见 F-I3。**
- **另一处我核了、没问题**：§6.1「该函数今天**零生产调用者**、仍 `export`」——
  我读了 `fileStore.ts:670`–`:690`，源码注释 `:687`–`:688` 逐字自己就写着
  `every call site is under tests/ (fileStore, runLoop.integration, registry/zeroWrite); production has none.`
  **⇒ 该断言为真**（限定：我只核了这条注释与 `:689` 的 `export` 声明，**没有跑 `src/` 全仓 grep**，那是 Lane 2 切片）。

### 3.5 §7 继承项指针节 —— **只放了指针，没有偷放内容。核实通过。**

§7 全节 14 行，我逐行看过。它含：
1. **一句范围声明**：「L5 继承了一批输入，但**它们不在本 spec 解决**：包 2 的三条今天可达的数据丢失、
   §13 第 3 笔、组 B 的两条债，以及 L5 输入盘点里的其余各项。」
2. **一个指针**（唯一）：`.superpowers/sdd/2026-08-05-l5-input-scan/progress.md`。
3. **理由**：逐字「本仓库已经有过一个真实的文档缺陷 —— **两份交接清单并存、彼此之间没有指针**…
   **本节存在的全部目的就是不造出第三份。**」
4. **一条自我约束**：逐字「⚠️ **因此本节刻意不重述那些项的技术内容。** 在这里复述它们，就等于造出第三份清单，
   与本节的目的直接自相矛盾 —— 要看内容，去上面那份台账。」
5. **一个具名例外**：§1.4 那条耦合，并给了为什么它不算继承项的理由。

**判**：第 1 点里的"包 2 的三条数据丢失 / §13 第 3 笔 / 组 B 的两条债"是**标签**（谁），
**不含任何技术内容**（是什么、怎么触发、怎么修一律没有）⇒ **不会腐坏**。**这一节我判为正面样本，无 finding。**

**⚠️ 但 C4 使这条立论出现一个自反缺口**（人裁 8 第 3 条已定、我只报证据）：
§7 拿"两份清单彼此无指针"立论，而本轮**亲手造出一条同型债**——
新 spec §2.1 有指向 `2026-07-26` 的指针，`2026-07-26:45` **无反向指针**（我复核，见 M5）。
**§7 的立论本身没塌**（它讲的是清单，C4 是文档），但**同型性是真的，值得在修复环 2 一并处理。**

### 3.6 §5.4 定性 —— **未被升级，今天仍成立**（**前提：Lane 2 未证伪**）

§5.4 逐字承接两句原文（「定性要准确：这是无界垃圾，不是故障。」「不要把它上报成缺陷。」），
并逐字写「**本 spec 承接这个定性，不升级它。** 本 spec 的立项理由不是「有缺陷要修」，而是
§2 那两份委任状**明确点名**了一份尚未存在的 cleanup / orphan 设计。」

⇒ **立项理由与缺陷脱钩，这一手堵死了最常见的升级动机。判：正面样本仍成立，无 finding。**
＋ 它还留了正确的后门：「若后续有人找到，那是重要发现，应原样上报并由人裁重新定性，**不由实施者自改**。」

⚠️ **本小节结论以「无界垃圾不是故障」成立为前提；若 Lane 2 证伪该定性，本节需重判。**

## 4. 勘误的越界与准确性

### 4.1 越界检查 —— **未越界。机械判据我自己重跑了。**

```
git diff --numstat 30cbdd5..c842257
258  0  .superpowers/.../progress.md
242  0  .superpowers/.../re-review-task1-fix1.md
327  0  .superpowers/.../review-task1.md
202  0  .superpowers/.../task-1-brief.md
278  0  .superpowers/.../task-1-report.md
 85 11  docs/handoff/handoff.md
 21  0  docs/superpowers/specs/2026-07-29-atomic-write-paths-design.md   <- 勘误
551  0  docs/superpowers/specs/2026-08-07-cleanup-and-orphan-gc-design.md
```

**勘误文件删除列 = 0 ⇒ 原件未被改写、纯新增。** 包 3 评审员立的机械判据在这里通过。
**`src/` 与 `tests/` 在 `--numstat` 全表中零出现 ⇒ 人裁 6 的授权面守住。**

**唯一有删除的是 `docs/handoff/handoff.md`（−11）**——它不是 spec，机械判据不适用。
我逐 hunk 核了那 11 行的去向（`git diff` 全文，未过滤）：
**10 行 = 旧执行摘要的标题行 ＋ 9 条编号行**（整段被改写，这是 handoff 执行摘要的既定用法）；
**1 行 = 旧的 `# 【最新】2026-08-07：包 3 已开门合入 main —— 本节取代下方**一切**状态描述` 标题**，
被改写成 `# 【已被上方 2026-08-07 晚那节取代，保留作历史】2026-08-07：包 3 已开门合入`。
⇒ **零正文内容被删除，只有"最新"标签的转移。判：合规。**

### 4.2 勘误内容的准确性 —— **逐条重推，5 条全真；⚠️ 我发现一句措辞需要限定**

我把注文拆成可独立证伪的断言，逐条重推（判别式 = "一句可独立判真假的断言"）：

| # | 注文逐字断言 | 我的重推 | 判 |
|---|---|---|---|
| 1 | 「上句「**四个固定名**」在本文写下时为真，今天是**十个**」 | `awk '/^async function cleanupOwnerTransferStagingWithoutMarker/,/^}/' src/persistence/fileStore.ts \| grep -c "await safeUnlink"` -> **10** | **真** |
| 2 | 「**在本注落盘之前**，本文最后一次被碰是 `2e30d1c`（2026-08-01）」 | `git log --format='%h %ad %s' --date=short -1 2e30d1c` -> `2e30d1c 2026-08-01 docs: finish the reference sweep 9e554ce claimed to have finished`；且该文件在 `30cbdd5..c842257` 内只被 `fbeb6fd` 一笔碰过 | **真** |
| 3 | 「4 → 7（`0f940ea`…）→ 10（`dad8a14`…）」两笔都在 2026-08-02 | 两条 `git log -1` 逐字吻合（日期均 2026-08-02，主题行与注文引的一字不差） | **真**。⚠️ **逐提交重数（`0f940ea~1`→4 等）我未重跑**——任务 1 评审员做过，**这在我这里是"未独立复核"** |
| 4 | 「临时名由 `buildAtomicTempPath` 生成，带一个进程戳…与一个自增序号，因此**不可能**落在任何固定名集合里」 | 新 spec §5.2 引的源码逐字含 `${ATOMIC_TEMP_PROCESS_STAMP}.${atomicTempPathSequence}`；结论为演绎，不依赖计数 | **真**（结论未腐坏，这正是"坏的只是数字"） |
| 5 | 「本条其余部分（含下段的定性）未作改动」 | `git diff` 上下文行显示"定性要准确：这是无界垃圾，不是故障"整段以**上下文行**出现（无 `+`/`-`） | **真** |

### 4.3 ⚠️ 一处措辞我要限定（**不构成 finding，但请修复环 2 的人知道**）

注文断言 2 逐字是「**在本注落盘之前**，本文最后一次被碰是 `2e30d1c`」。
**这个"之前"限定得非常准确**——它避开了包 3 评审员踩过的那种"写下时就为假"的坑
（本注一落盘，"最后一次被碰"就变成 `fbeb6fd` 了）。
新 spec §5.1 也做了同一件事，逐字：「⚠️ 注意时序：在本 spec 与那条勘误落盘**之前**，
`grep -c "Amended" …` 实测为 `0`；勘误落盘之后该计数不再是 0。**引用这个计数必须带上「在哪个提交上测的」**。」

**⇒ brief §7.3 要撞的那个形状（"勘误自己引入一句新的不实描述"）我撞了，没撞到。**
**5 条新断言全真、时序限定正确、结论未越界。这条正面样本今天成立。**

### 4.4 过度勘误检查

注文**没有**顺手改动 §10 第 4 条的其余部分，**没有**改动定性段落，**没有**改动 07-29 的任何其它节。
新 spec §9 逐字自陈「**本仓库立场：过度勘误本身也是缺陷**，故未顺手改动该条的其余部分」。
**判：未过度勘误。**

⚠️ **但注意一处未被勘误、且今天已腐坏的邻居**：07-29 §10 第 4 条原句里那半句
「而临时名按 §4.1 的要求本就必须不在那四个之内」中的"那四个"，
在注文把数字更正为十之后，**原句仍写"那四个"**——这是"就地注解、原句一字不动"立场的**必然代价**，
注文已用"坏的只是数字"覆盖了它。**我判这是可接受的，不报 finding。**
但**新 spec §4.2 把这句照搬过来时把"那四个"改成了"任何固定名集合"，却漏改了"§4.1"** ⇒ **这就是 N2**（见 §9）。

## 5. 临时名三种写法的引用准确性

### 5.1 新 spec 内每一处临时名引用 —— **逐处检查，无混用**

判别式 = "spec 内含 `tmp` 的行"。`grep -n 'tmp' <spec>` **全部命中只有 3 行**（未过滤）：

| 行 | 逐字 | 是哪一种 | 指明了吗 |
|---|---|---|---|
| `:376` | `` `.${basename(targetPath)}.${ATOMIC_TEMP_PROCESS_STAMP}.${atomicTempPathSequence}.tmp`, `` | **源码原文**（`buildAtomicTempPath` 函数体） | 是——它在一个标注了文件与符号名的代码块里 |
| `:381` | 「**实际生成的名字是五段**：`.{basename}.{pid}.{timeOrigin}.{seq}.tmp`。」 | **五段·源码展开形** | 是——"实际生成的" |
| `:382` | 「起点文档 §10 第 4 条写的五段形 `.{basename}.{pid}.{startTime}.{seq}.tmp` **与源码一致**（`startTime` 即 `Math.trunc(performance.timeOrigin)`）」 | **五段·07-29 §10 第 4 条形** | 是——点名了出处，并给了两者的字段对应 |

⇒ **新 spec 从头到尾只用五段形，三次引用每次都指明了出处，`.tmp` 字样零处出现在别的语境。**
**§4.2 的删除谓词也指名了**：逐字「匹配 `buildAtomicTempPath` 的五段形（§5.2）」，**带节号指针。**
**判：brief §7.4 那个"会直接毁掉引文可核性"的形状，在新 spec 正文里没有发生。**

### 5.2 四段 `{stamp}` 形 —— **处理得当**

§5.2 逐字：
> 台账与 handoff 里流通的四段 `{stamp}` 形是**模板层面的描述**（模板字面量确实只有四个插值），
> 但 `ATOMIC_TEMP_PROCESS_STAMP` 自己会展开成两段，所以**四段形描述的是模板、不是落盘的文件名**。
> **本 spec 一律引用五段形；任何文档若按四段形去匹配落盘文件名，都匹配不上。**

这**既解释了错法的来历、又给了操作性判据**（"按四段形匹配会匹配不上"）。**记为正面样本。**

### 5.3 ⚠️ **第三种写法被换掉了 —— 这是本节的 finding（N4）**

台账 `:169`–`:172` 逐字定的三种是：
> §10 第 4 条原文的**五段形 … 是正确的**；台账/handoff 的四段 `{stamp}` 形**作为文件名格式是错的**…；
> ⚠️ **§4.1 的「建议形式」是第三种四段写法**。
> **⇒ 包 1 引用临时名时必须指明引的是哪一种，并以源码实际生成为准。**

**新 spec §5.2 标题写"三种写法在流通"，但它清点的三种是 {源码 / §10 第 4 条 / 台账·handoff}
—— 把台账点名的第三种（`2026-07-29` §4.1 的"建议形式"）换成了"源码"，且换得无声无息。**

我回 07-29 §4.1 核了原文（`:78`–`:96`），逐字：
> **建议形式**：`.<basename>.<pid>.<单调递增序号>.tmp`，序号为模块级计数器，用于隔离同一进程内的并发写。
> **不强制具体格式**，但必须满足：同一进程内并发调用得到不同路径，不同进程之间得到不同路径。

**为什么这一种最危险**（这也是我把它单列为 finding 的理由）：
- 它是**规范性文本**（"建议形式"，且带一条"必须满足"的契约），不像台账那样只是转述；
- 它**四段、无 `timeOrigin`**；
- 而新 spec §4.2 的删除谓词逐字依赖 `timeOrigin`：
  「⚠️ 仅凭 pid 不够 —— pid 会复用，`timeOrigin` 正是用来消歧的那一段」。

**可构造场景**：L5 实现者去读 §4.1（新 spec §4.2 恰好在同一句里指了"§4.1"——见 N2 那个悬空引用，
**两条缺陷在这里叠加**），照 §4.1 的"建议形式"写匹配器 ⇒ **四段、无 `timeOrigin`**
⇒ 他的匹配器**要么匹配不上任何真实文件**（GC 空转，无害），
**要么**——如果他为了"不强制具体格式"而写成宽松匹配——**匹配面比五段形更宽**，
这与 §4.2 的收敛意图相反，**并与 N1 叠加**（一个更宽的形状 × 一个未封口的空间）。

**分级：Minor**。它不改变任何结论，且 §5.2 已明令"一律引用五段形"。
**修法**：§5.2 补一句「第三种是 `2026-07-29` §4.1 的**建议形式** `.<basename>.<pid>.<序号>.tmp`
——**四段、无 `timeOrigin`**，它是规范性建议而非实测格式；**不得据它匹配落盘文件名**，
`timeOrigin` 是 §4.2 消歧 pid 复用所必需的那一段。」

### 5.4 台账与 handoff 侧的临时名 —— 已核

`grep -n 'tmp' progress.md docs/handoff/handoff.md`：台账三处（`:49` / `:51` / `:170`）
**全部是在讨论"三种写法"这件事本身**、各自带出处标注，**不是在无标注地使用某一种**；
handoff 侧 `:225` 只出现"崩溃残留 tmp"这个不带格式的说法。**⇒ 无混用，无 finding。**

## 6. 两条全称否定的验证

**这是 Lane 1 被点名指派的活。两条我都验了，结论一真一假。**

### 6.0 检索面与判别式（先声明，否则我自己就犯了要查的那条错）

**检索面**：`.superpowers/sdd/2026-08-05-l5-input-scan/` 下**九份报告**，逐份、具名：
`scan-A-debt2-lock-abort.md` / `scan-B-span-and-toctou.md` / `scan-C-backoff-and-gate-carries.md` /
`scan-D-offlist-sweep.md` / `review-lane1-code.md` / `review-lane2-docs.md` /
`close-1-item-e.md` / `close-2-recovery-path.md` / `close-3-span-tension.md`。
（`ls -1` 实测该目录 10 个 `.md`，第 10 个是 `progress.md` 台账，**不算报告**——
这与 brief §7.5 给的"四份 `scan-*` ＋ 两份 `review-lane*` ＋ 三份 `close-*` 共九份"逐字吻合。）

**命令**：写进 `p4.sh` / `p5.sh` 两个脚本，`rtk proxy zsh <script>` 跑，**逐份 `for` 循环、输出未过滤**。

**⚠️ 覆盖面限定（我必须先下这个限定）**：我用的是**关键词检索 ＋ 命中行逐条打开**，
**不是九份报告的逐行通读**（九份合计数千行，预算不允许）。
⇒ **我能证的是"命中即存在"（下面第 2 条），不能证"零命中即不存在"**——
第 1 条那种"未命中"的结论，我下的是**带判别式的限定否定**，不是无限定全称。

---

### 6.1 第二条 —— 「九份报告一次没提「保留」这半」：**证伪。**（⇒ F-I2，Important）

**锚点**：新 spec §2.1 逐字「**本仓库此前的九份报告一次没提「保留」这半**，人裁明令不许只想着删。」

**判别式**：文件内出现 `retained` 或 `cleaned up`，且命中处是在讨论**委任状那半边**（不是别的语境）。

**逐份计数**（`grep -c`，未过滤）：

```
close-1-item-e.md          retained 0   cleaned up 2
close-2-recovery-path.md   retained 0   cleaned up 0
close-3-span-tension.md    retained 0   cleaned up 0
review-lane1-code.md       retained 0   cleaned up 0
review-lane2-docs.md       retained 3   cleaned up 3     <- 命中
scan-A-debt2-lock-abort.md retained 0   cleaned up 0
scan-B-span-and-toctou.md  retained 0   cleaned up 0
scan-C-backoff-and-gate…   retained 0   cleaned up 2
scan-D-offlist-sweep.md    retained 2   cleaned up 2     <- 命中
```

**命中行逐条打开**（这一步是必须的——`close-1`/`scan-C` 那 4 处 `cleaned up` 全部是
引 `Recorded, not to be "cleaned up"` 这句**与委任状无关**的话，按判别式**不计**）：

- `review-lane2-docs.md:495` 逐字：
  > …retained or cleaned up safely"）。**这一行同时授权「retained」与「cleaned up」两个方向**，
- `review-lane2-docs.md:468` / `:874`：逐字引委任状原文 `how superseded or lost-owner workspaces and evidence are retained or cleaned up safely.`
- `scan-D-offlist-sweep.md:400`：同上逐字引委任状原文。
- `scan-D-offlist-sweep.md:418` 逐字：
  > **委任状本身只有一行**：「how superseded or lost-owner workspaces and evidence are retained or cleaned up safely」。
  > **没有**说什么可以删、**什么必须保留**、保留多久、由谁触发、删除是否需要授权。

**判：该全称否定为假，且不是擦边——`review-lane2-docs.md:495` 那句与 §2.1 想说的话几乎是同义。**
**两份**（九份中的 2/9）明确点到了保留那半。**详见 F-I2 的分级与修法。**

⚠️ **同源腐坏**：台账 `progress.md:159` 逐字「**九份报告一次没提的那半边**，今天已有实体：」
——**同一句错在台账里也有一份**。修 spec 时必须一并注解台账，否则只修掉一半。

---

### 6.2 第一条 —— brief 写的「四份扫描都没点出（§10 第 4 条）」：**spec 正文里没有这句话**

我 `grep -n '没点出\|一次没提\|全都没有\|从未\|没有任何\|一次都没' <spec>`（6 行命中，未过滤），
**没有任何一处说"四份扫描都没点出 §10 第 4 条"。** 最接近的两处是：

1. **§1.4 `:81` 逐字**：「上一轮**四份扫描与两条评审车道**都没点出**这条耦合**，在此记明：」
   ——**指的是 §1.4 那条耦合，不是 §10 第 4 条。**
2. **§2.1 `:106`**：就是 6.1 那条（已证伪）。

⇒ **brief §7.5 第 1 条的表述与 spec 正文不符**。我按正文实际有的那句来验：

**验 §1.4 `:81` 那句**（判别式：报告内出现「保留即放宽」或 `writeBoundaryArtifacts`，
且命中处是在陈述"第 3 笔的修法与该人裁绑定"）：

```
grep -rn '保留即放宽' <九份>：
  scan-B-span-and-toctou.md:516      （"「L3 期间一个字节不动（保留即放宽）」的核实："——只是引用，未讲耦合）
  close-3-span-tension.md:453        <- 命中
grep -rc 'writeBoundaryArtifacts'：close-3=8 / review-lane1-code=2 / scan-B=20 / scan-C=6 / scan-D=5，其余 0
```

- `close-3-span-tension.md:453` 逐字：
  > => **第 3 笔的修法与「保留即放宽」那条人裁是绑定的，不能分开决策。**
  —— **这与 §1.4 的结论是同一句话。**
- 另在 L5 输入盘点台账 `progress.md:690`–`:691` 逐字：
  > **那条「保留即放宽」的人裁真的会重开。**
  > => *** 第 3 笔的修法与「保留即放宽」人裁**必须一起决策**，不能分两轮。 ***

**判：§1.4 那句全称否定，在它自己下的限定面内（"四份扫描与两条评审车道"）—— 成立。**
`close-3-span-tension.md` **不是**扫描、**不是**评审车道，它是闭合报告；`scan-B:516` 只是引用该人裁的名字、未讲耦合。
⇒ **spec 这条限定下得是准的，铁律 3 合规。这是一条正面样本。**

⚠️ **但限定的选择本身有问题**（⇒ **N3**，Minor）：那个限定面**恰好把唯一含该结论的三份 `close-*.md`
排除在外**，于是"在此记明"读起来像首次记录，而实际上 `close-3:453` 与盘点台账 `:690`–`:691`
都已逐字写过。**这与 §7"不造出第三份无指针清单"的立场直接冲突——§1.4 就是第三份。**
**修法**：把 `close-3-span-tension.md:453` 与 `2026-08-05-l5-input-scan/progress.md:690`–`:691`
加成指针，一行即可。

## 7. handoff 与台账

### 7.1 handoff 是否遵守"就地注解、不改原件"？**基本遵守，一处需说明**

- **−11 行的去向已逐 hunk 核实**（见 §4.1）：10 行是旧执行摘要整段改写、1 行是"最新"标签转移。
  **零正文内容被删除。**
- **执行摘要被整段改写是否算"改原件"？** 我判**不算**：handoff 的执行摘要按其自身设计
  逐字是「下一位 agent 读这 9 行就能开工」，是**滚动更新的当前视角**，不是历史记录。
  且旧那节的历史正文**全部保留在下方**并被就地打了"已被取代"标签。**判：合规。**
- **四处"远端落后"的注解**：扫描员 1 点名的四处（旧 `:7` / `:34` / `:50` / `:236`）
  今天全部被处理（`:7` 整句改写成对冲表述、其余三处就地加 `Amended` 注）。**覆盖完整。**
  ⚠️ 但**注解内容本身有缺陷 ⇒ F-I4**。

### 7.2 那条新腐坏 —— **我独立重推了，控制器的方向对，一处细节我要纠正**

**结论先写：控制器 brief §7.6 的核心断言（"远端落后本地，而 handoff 写的是一致"）成立。**
详细重推、可构造场景、分级、修法**见 §9 的 F-I4**（我把它写在那里以免重复）。

**我要纠正 brief 的一处推断**（brief 逐字要求"若我错了明写"）：
brief 暗示 `:123` 引的 `00:12:44` 那次 push 与结论对不上（因为最后一次 push 是 `01:01:36`）。
**这个怀疑不成立。** 台账 `:14` 记 STEP 0 时 `HEAD = ba7b1eb`，而 `00:12:44` 那次 push 的落点
**正是 `ba7b1eb`**（`git log -1 ba7b1eb` -> `2026-08-07 00:09:33`）。
⇒ **`:123` 引的证据是对的、时点是对的、"STEP 0 两端一致"这个事实是真的。**
**坏的不是事实，是判词**——三处都以「「远端落后」**已腐坏**」作结，而到 `c842257`（09:12:12）落盘时
本地已在 `30cbdd5` 之上叠了 8 笔，**"远端落后"重新为真**。

**元讽刺我确认**：`:105` 那段注文自己下面第三行就是 `git ls-remote origin refs/heads/main # 一律现跑`。

### 7.3 台账（`progress.md`）质量 —— **总体高，三条自曝如实，两处可改进**

我通读了 608 行。

**✅ 三次控制器记账全部如实记录、口径可重推**：
1. **记账 1（聚合数 10 vs 12）** `:208`–`:212`：给了实锤出处（`handoff.md:71` 那张表逐字）
   ＋ 差额归因（F-M2/F-M3 从未被算进去）＋ 明写"本轮按 12 条逐条枚举派单，未传播 10"。
   **我复核：`docs/handoff/handoff.md:144` 今天逐字仍是「**10 条 deferred minor**（9 条延后 ＋ 1 条新增）」**
   ⇒ 台账的记账为真。⚠️ **但那处至今未被注解 ⇒ N5**。
2. **记账 2（brief 前提未重推）** `:330`–`:340`：明写错因"从扫描员 2 的报告原样搬进 brief、未重推"，
   **并且明确不传播两个口径的数字**（逐字「口径不同，两个数都不传播…计数留给评审员用一个写明判别式的口径重建」）。
   **这是本台账最好的一段。**
3. **记账 3（坏探针）** `:448`–`:451`：逐字给出了那条坏命令
   `rtk proxy "bash -c \"grep -n 'A\|B' …\""`，并写"我差点据此判 M4 未落"。
   **我复核 M4 确已落**（§3.1 给了逐字引文）⇒ 记账为真。

**✅ 聚合数处理**：`:200`–`:206` 把分母 12 的**重推路径写成了算式**
（`12 − 3 = 9` ＋ `4 − 1 = 3`，合计 12，逐条附台账行号）⇒ **不是聚合数，是可重推的枚举。合规。**
`:497` 的"6 条 minor deferred"也逐条具名（M1/M2/M3/M5/N1/N2）⇒ **可重数。**

**✅ 转述压缩掉主语**：我专门找了这个形状。台账 `:46`–`:54` **自己就在处理一例**
（`{pid}.{startTime}` 被压成 `{stamp}`），且下了正确的规矩（逐字「包 1 的一切 brief 与 spec 正文
一律逐字引用设计文档，不引用台账的转述」）。**我在本轮台账新增的 258 行里没有发现新的主语压缩。**
⚠️ **限定**：我的判别式是"转述句是否保留了原文的施动者/条件"，**靠人读，不是靠命令**；
我读的是全部 258 行新增，**但这是一个主观判据，不是可重推的**。

**⚠️ 改进 1（⇒ N5，Minor）**：台账 `:208`–`:212` 认定 `handoff.md` 那个"10"是错的，
且 `:263` 逐字把"handoff 四处「远端落后」的就地注解"列进了"留到收口"，
**却没有把"那个 10"一并列进收口清单**。结果：收口那笔（`c842257`）注解了远端四处、**漏了聚合数那处**，
今天 handoff 内 `:13`（6 条）与 `:144`（10 条）并存，而后者已被本轮自己证伪。

**⚠️ 改进 2（不构成 finding）**：台账 `:159` 复制了 spec §2.1 那句已被我证伪的全称否定
（「九份报告一次没提的那半边」）。**它不是台账自己产生的错**（同源于扫描员 2 的报告），
但按"台账的错会传播下去"这条立场，**修 F-I2 时要一并修台账 `:159`**。

### 7.4 台账 STEP 4 一节（**不在评审范围内，仅作交叉核对**）

`progress.md:543`–`:564` 已经把远端腐坏这条完整记下，数字与我重跑的一致
（远端 `30cbdd5`、`rev-list --count` 在 `c842257` 上是 9）。
**⚠️ 这一节落在 `d22748c` 上，不在 `30cbdd5..c842257` 内 ⇒ 它不能替 handoff 免责。**
**F-I4 说的是 handoff 里那三句读者会照着行动的话，它们今天仍在盘上、未被更正。**

## 8. 6 条 deferred minor 的分诊

**brief §7.7 要的是分诊不是重报。** 再评审员明写 M1/M2/M3/M5 "缺陷本身是否仍成立**未重验**"
（只验了未被碰）—— **我重验了。四条今天全部仍成立。**

| 条 | 我的重验（命令 + 输出 + 判别式） | 仍成立？ | **分诊** |
|---|---|---|---|
| **M1** | spec §1.1 括号注逐字「（其余命中全在 `tests/`：`leaseLifecycle.integration.test.ts` 的 4 处注释、`leaseStore.test.ts` 的 3 处）」。`grep -rn 'releaseOwnerLease' tests/` → **8 行**，判别式=行：`leaseLifecycle.integration.test.ts` `:233 :266 :790 :1605` = **4**（✅ 且确实全是注释）；`tests/persistence/leaseStore.test.ts` `:9 :92 :96 :116` = **4**（❌ spec 写 3） | **仍成立** | **随分支带走**。承重结论（`src/` 内唯一调用点在 `stop()`）不依赖这个数；但它是 §1.1 那段"grep 输出被摘要"的直接后果，**修复环 2 若开，顺手改成 4 并把括号注换成完整输出，成本一行** |
| **M2** | spec §6.1 逐字「逐字**首句**是 `// Production must publish owner-transfer.json only through finalizePendingOwnerTransfer.`」。读 `fileStore.ts:670`–`:690`：该注释块**首句实为** `// NOT atomic: this goes through the bare writeJsonFile, so a concurrent reader can observe a`（`:681`），被引的那句在 `:685` | **仍成立** | **本轮修**（"首句"→"其中一句"或"第 5 行起"）。**附带**：`:687`–`:688` 逐字 `every call site is under tests/ …; production has none.` —— §6.1 重推了 13 行的结论，源码注释本来就写着，可以直接引 |
| **M3** | spec §6.1 逐字「**五份门报告（`.md`）全部为 0。**」。`find .superpowers/sdd -iname '*gate*' -type f \| sort` → **8 行**，其中 `.md` **6 个**、`.diff` 2 个。正文只解释了 `.diff` 里非零的那个，**第 6 个 `.md`（`scan-C-backoff-and-gate-carries.md`）从未被交代** | **仍成立**，且比原评价更重 | **本轮修** —— 它与 **F-I3** 是同一处文本，**必须一起改**（F-I3 是这处的定性问题，M3 是这处的计数问题） |
| **M5** | `2026-07-26-run-lease-and-heartbeat-design.md:45` 今天逐字仍是 `L5 corresponds to the third follow-on spec named in the ownership design §17 and remains unwritten.`，**该文件内我未发现指向新 spec 的反向指针** ⚠️ 限定：判别式=对该文件 `grep -n 'remains unwritten\|L5\|cleanup / orphan'`（4 行命中），**我未对该文件做 `Amended` 全文检索**，故我下的是限定否定不是全称 | **仍成立** | **不动** —— 人裁 8 第 3 条已定"派一条一行勘误、时机未到"。**我评估该处置站得住**：它是文档债不是设计缺陷，且必须与包 1 开门时机绑定（本 spec 未合入前，`remains unwritten` 在 `main` 上仍为真） |
| **N1** | 见 §2 整节 | **仍成立** | **升级为 Critical，进修复环 2** |
| **N2** | spec §4.2 逐字「**按 §4.1 的要求本就必须不在任何固定名集合之内**」。新 spec `### 4.1` 实测标题 = `INV-2 —— stale 不是删除许可`，全节与固定名无关。该句源自 07-29 §10 第 4 条原文（那里的 §4.1 = 07-29 自己的 `### 4.1 临时文件名必须进程唯一`，实测 `:80`） | **仍成立** | **本轮修**（一处限定词：「按 `2026-07-29` §4.1 的要求」）。⚠️ **优先级比 Minor 该有的高一点**：它与 **N4** 叠加时会把读者送到 07-29 §4.1 那个**四段、无 `timeOrigin`** 的"建议形式"上去（见 §5.3 的场景） |

### 分诊汇总

- **进修复环 2（4 条）**：**N1（Critical）** ＋ M2 ＋ M3 ＋ N2。
  后三条都是**一到两行的改动**，且 M3 与 F-I3 同处、N2 与 N4 同链 ⇒ **搭 N1 的车做完，边际成本近零**。
- **随分支带走（1 条）**：M1。
- **不动（1 条）**：M5（人裁 8 已定）。

### 我对"6 条"这个数本身的核

判别式 = 台账 `:497` 具名列出的条目数。逐条点名：M1 / M2 / M3 / M5 / N1 / N2 = **6**。
**⚠️ 但我本轮新增了 4 条**（M2′ 归并入 M2 不另计；N3 / N4 / N5 ＋ 3 条 Important 是新的）
⇒ **"6 条 deferred minor 随分支带走"这个说法在本报告落盘后不再准确**，
handoff `:13` 与台账 `:497` 两处都需要在修复环 2 之后重算。**这一条请控制器记账。**

## 9. Findings（Critical / Important / Minor 分级）

### C1 —— §4.2 授权面外延未封口（= N1，我定 **Critical**，比再评审员高一档）

**锚点**：新 spec `## 4.2 L5 的本职删除面：崩溃残留的原子写临时文件`，逐字
「- 目标必须**同时**匹配 `buildAtomicTempPath` 的五段形（§5.2）**且**位于一个 run 目录内；」（`:282` 辅助）
＋ `#### 3.2.1 INV-4（承重规则）` 逐字「§4.2 只有一个面，可以穷举」（`:190` 辅助）。

**完整重推、可构造场景、分级依据、修法建议**：见 §2 整节（我在那里逐句回原文核过，引文逐字）。

**为什么它重要（一句）**：INV-4 把删除许可**收敛到一个点**，那个点**没有边界**——
收敛本身放大了它，而不是缩小了它。

---

### F-I1 —— §6.2 的自立规则漏了 INV-4（**Important**）

**锚点**：`### 6.2 这对 L5 意味着什么` 结尾三条枚举，第三条逐字：
> 凡是本 spec 写下「不得 / 必须」的地方，都要能回答**「哪条测试会在它被违反时变红」**。
> 回答不出来的，就明写成「今天没有机制强制」，**不要写成「由评审保证」**

**今天是否仍成立：是。** 独立重推（`grep -n 'INV-4' <spec>`，输出 7 行：`:182 :184 :186 :195 :196 :223 :313`）：
INV-4 在 §6.2 的枚举里**零命中**，在 §8.2 验收表里**零命中**，
且**没有任何一处对它写「今天没有机制强制」**——而 §1.3 对 INV-1 就写了（`:68` 逐字
「**INV-1 今天零强制 —— 明写在此，不藏在报告里。**」）。

**可构造场景**：一名后来者按 §6.2 那条规则给 spec 做自查（这正是该规则存在的目的），
逐条问"哪条测试会变红"。INV-1/INV-2/INV-3 都能在 §8.2 找到行；
INV-4 **既没有行、也没有"零强制"声明** ⇒ 他要么以为 INV-4 有护栏（错），
要么以为 §6.2 的枚举是完备的、INV-4 不算"不得/必须"（也错——INV-4 逐字写着「只允许」「≠ 可以删」）。

**分级依据**：不是措辞瑕疵——**这是 spec 用自己写下的规则去检查自己时会失败**。
本 spec 的自指诚实度是它承重的一部分（§1.3 花了整整 11 行做这件事），少一条就是少一条。
不是 Critical，因为它不会让人删掉东西。

**修法建议（不违反人裁 8 的顺序约束）**：**不要**现在把 INV-4 加进 §8.2（那要先封口 N1）。
**改在 §3.2.1 INV-4 正文之后就地补一句**，与 §1.3 同型：
「INV-4 今天零机制强制，且它的边界依赖 §4.2 的授权面描述——该外延今天未封口，见 N1。
待 N1 封口后随 §8.2 一并落一条验收行。」这**只记录事实、不固化判据**，与人裁 8 的硬理由不冲突。

---

### F-I2 —— §2.1 的全称否定「九份报告一次没提「保留」这半」**被证伪**（**Important**）

**锚点**：`### 2.1 第一份 —— 授权：retained 与 cleaned up 两者`，逐字：
> **本仓库此前的九份报告一次没提「保留」这半**，人裁明令不许只想着删。

**证伪证据**（判别式 = "文件内出现 `retained` 或 `cleaned up` 且用于讨论委任状那半边"，
检索面 = `.superpowers/sdd/2026-08-05-l5-input-scan/` 下九份报告**逐份**，
命令写进脚本再 `rtk proxy zsh` 跑，未过滤）：

- `review-lane2-docs.md:495` 逐字：「…retained or cleaned up safely"）。**这一行同时授权「retained」与「cleaned up」两个方向**，」
- `review-lane2-docs.md:468` / `:874` 逐字引了委任状原文 `... are retained or cleaned up safely.`
- `scan-D-offlist-sweep.md:400` 逐字引同一行；`:418` 逐字：
  「**委任状本身只有一行**：「how superseded or lost-owner workspaces and evidence are retained or cleaned up safely」。**没有**说什么可以删、**什么必须保留**、保留多久…」

⇒ **九份里至少两份明确点了「保留」这半**，其中 `review-lane2-docs.md:495` 是**逐字同义**的表述。

**分级依据**：本 spec 开篇 `:10` 逐字承诺「**本文的每一条事实断言都附了能重推它的命令与该命令当时的输出**」——
**这一条没有附任何命令**，而它是一句无限定全称否定。这正是本仓库一轮内栽过 6 次的形状，
写进了一份以"引文可核"为卖点的 spec 的第 106 行。不是 Critical：该句不承载任何设计约束（承重的是委任状原文本身）。

**修法建议**：改成有判别式的限定，例如
「四份 `scan-*.md` 与三份 `close-*.md` 未把 `retained` 这半当作独立方向处理；
两份 `review-lane*.md` 提到了（`review-lane2-docs.md:495`），但均未展开为设计要求。」
并附上重推命令。**注意 `progress.md` 台账 `:159` 有同一句的同源表述（「九份报告一次没提的那半边」），
若改 spec 请一并就地注解台账，否则会留下第二处腐坏。**

---

### F-I3 —— §6.1「五份 L3 门报告全部在盘上，推翻了「门报告没落盘」那个前提」（**Important**）

**锚点**：`### 6.1 事实`，逐字：
> **五份 L3 门报告全部在盘上**（这推翻了上一轮「门报告没落盘」那个前提），
> 但它们全都没有提过这个函数名：
> …
> **五份门报告（`.md`）全部为 0。** 唯一非零的 `gate-a-package-tests.diff` 是一份测试 diff、不是门报告。

**独立重推**（`find .superpowers/sdd -iname '*gate*' -type f | sort`，8 行输出，未过滤）：
5 个 `gate-*.md` = `gate-a-fix-wave-report.md` / `gate-a-option2-report.md` /
`gate-b-lane2-report.md` / `gate-c-fix-wave-report.md` / `gate-c-lane2-report.md`。
**其中只有 2 份是评审报告**（两份 `lane2`），另 3 份是修复波/方案报告（实施侧产物）。

**与本仓库自己的记录冲突**：`docs/handoff/handoff.md:228` 逐字
> **GATE-C 有一半评审证据不存在** —— 三道门六份评审报告只落盘 2 份（全是 lane 2，GATE-A 连 lane 2 都没有）。

⇒ 上一轮的前提"门报告没落盘"，在**评审报告**这个口径下**今天仍然为真**（6 份里落盘 2 份）。
本 spec 是通过**把修复波报告也算作"门报告"**才把它"推翻"的，**而这次口径放宽没有被声明**。

**可构造场景**：读者读 §6.1，得到"L3 三道门的评审证据齐全，只是没对上 §10 第 3 条"。
他于是不会去追"GATE-A 的评审证据根本不存在"这件真事——**而后者恰恰是 RISK-1 更强的例证**。

**为什么它重要**：§6.2 的 RISK-1 把这个形状定义为
「评审确实发生了、**报告确实落盘了** → 但报告里没有任何一处能对上那条约束」。
**"报告确实落盘了"这半句对 GATE-A 为假。** RISK-1 的**结论**（无法证明兑现）不变、甚至更强；
**塌的是它给出的机制描述**，而这份 spec 把该机制描述当成 L5 设计的推理起点（`:481`「=> L5 自己的设计不应再依赖同型防线」）。

**分级依据**：Important 而非 Critical——结论方向不变、不导致删除行为。但它是**承重段落的前提失真**，超出措辞范畴。

**修法建议**：把"五份 L3 门报告"改成可重数的两句：
「`find … -iname '*gate*'` 出 5 个 `.md`，其中 **2 份是门评审报告**（`gate-b-lane2` / `gate-c-lane2`）、
3 份是修复波/方案报告；**GATE-A 的门评审报告不在盘上**（handoff `:228`）。
`grep -rc 'writeOwnerTransferRecord'` 对这 5 份**全部为 0**。」——**结论一字不用改。**

---

### F-I4 —— handoff 三处「远端与本地一致」的**判词**在写下时即已为假（**Important**）

**锚点**：`docs/handoff/handoff.md` 三处（今日行号 `:105`–`:106` / `:123` / `:310`），逐字：
- `:105`「⚠️ **Amended 2026-08-07：上面这句「远端落后」写下之后已腐坏** —— 包 1 的 STEP 0 实测远端与本地一致」
- `:123`「> **Amended 2026-08-07：本条的「远端落后」已腐坏。**…」
- `:310`「> **Amended 2026-08-07：同上，「远端落后」这半句已腐坏**（远端已被会话外推进过五次；包 1 STEP 0 实测两端一致）。」

**独立重推**（控制器在 brief §7.6 给的数我全部重跑，**结论一致、且我拿到的数更大**）：

```
git ls-remote origin refs/heads/main                 -> 30cbdd5cacabd896bb5e5e371f68b87260d55f7d
git rev-parse HEAD                                   -> d1a8af6a03ef59f00a8b45b4ca83acc9deb516d7
git rev-list --count refs/remotes/origin/main..HEAD  -> 10        （brief 写 9，因它测于 c842257；两者都对）
git reflog show refs/remotes/origin/main --date=iso 首行
  -> 30cbdd5 refs/remotes/origin/main@{2026-08-07 01:01:36 +0800}: update by push
git log --date=iso -1 c842257 -> 2026-08-07 09:12:12
```

**⚠️ 一处控制器的推断我要纠正（brief 说"若我错了明写"）**：brief 暗示 `:123` 引的
`2026-08-07 00:12:44` 那次 push 是可疑的。**它不可疑，是对的。** 台账 `:14` 记 STEP 0 的
`HEAD = ba7b1eb`，而 `00:12:44` 那次 push 的落点**正是 `ba7b1eb`**
（`git log -1 ba7b1eb` -> `2026-08-07 00:09:33`，`git merge-base --is-ancestor ba7b1eb 30cbdd5` -> 真）。
⇒ **三处修订注引的"事实"部分全部为真**，坏的是**别的东西**：

**真正的缺陷是判词，不是事实**：三处都以「「远端落后」**已腐坏**」作结。
到 `c842257` 落盘（09:12:12）时，本地已在 `30cbdd5` 之上叠了 8 笔，远端仍停在 `30cbdd5`
（该推送发生在 01:01:36，**早于**这笔 handoff 提交）⇒ **"远端落后"这句在被宣判"已腐坏"的那一刻，是真的。**
这条修订注**把一句真话注解成了假话**。

**可构造场景**：下一位 agent 读 `:310`「远端落后…已腐坏…两端一致」，
判断包 1 的九笔产物已经发布，于是不去请求 push 授权 / 或以为可以安全地在本地 reset。
**实际上包 1 的全部产物只在本地。**

**分级依据**：Important。三处都保留了"一律自查"的对冲句（`:105` 逐字
「**一律以本命令的现跑输出为准，不要读上面那句结论。**」），这挡住了大部分伤害，故不是 Critical。
但**它是本分支亲手引入的一处新腐坏**，形状恰好是本分支在修的那一种，且**发生在一段自称"一律自查"的文字里**。

**修法建议**：三处的修订注不要删（它们记录的 STEP 0 事实为真），
**在其后各补一句**：「⚠️ 再注 2026-08-07：本注写下时（`c842257`，09:12:12）远端仍停在 `30cbdd5`，
本地已领先 8 笔 ⇒ **原句"远端落后"当时重新成立**。**"腐坏"指的是 STEP 0 那一瞬间，不是此后。**」
（**这条我建议进修复环 2**，因为它是一句读者会照着行动的假话。）

---

### Minor（8 条，判别式 = "条目"，一条 finding 记一条，不按落点数）

| # | 内容 | 今天是否仍成立 | 分诊 |
|---|---|---|---|
| **M1** | §1.1 的 `grep` 输出被摘要成一句括号注（「leaseLifecycle 的 4 处注释、leaseStore 的 3 处」）。**实测 `grep -rn 'releaseOwnerLease' tests/` = 8 行**，其中 `leaseStore.test.ts` 是 **4 行**（`:9 :92 :96 :116`）不是 3 | **仍成立**（数字仍错，4→写成 3；总计 7≠8） | **随分支带走**。承重结论（`src/` 内唯一调用在 `stop()`）不受影响 |
| **M2** | §6.1「逐字首句是 `// Production must publish owner-transfer.json only through finalizePendingOwnerTransfer.`」。**实测该注释块首句是 `// NOT atomic: this goes through the bare writeJsonFile, so a concurrent reader can observe a`**（`fileStore.ts:685` 才是被引的那句） | **仍成立** | **本轮修**（一个词的事：把"逐字首句"改成"其中一句"）。⚠️ 附带发现见下 |
| **M2′** | *(M2 的附带，新)* 该注释块 `:687`–`:688` 逐字已写 `every call site is under tests/ (fileStore, runLoop.integration, registry/zeroWrite); production has none.` ——**§6.1 花了 13 行重推的结论，源码注释里本来就有**，spec 未提这一点 | 新发现 | **随分支带走**（不是错，是可以更省的引证） |
| **M3** | §6.1 的判别式 `find … -iname '*gate*'` 撑不起"五份"：输出块里有 **6 个 `.md`**，正文只解释了那个 `.diff`，第 6 个 `.md`（`scan-C-backoff-and-gate-carries.md`）**未被交代** | **仍成立** | **本轮修**（与 F-I3 同一处，一并改） |
| **M5** | C4 的债今天只记在下游（新 spec §2.1），`2026-07-26-…-design.md:45` **无反向指针** | **仍成立**（`:45` 逐字 `remains unwritten` 今日仍在）⚠️ 限定：我未对该文件做 `Amended` 全文检索 | **人裁 8 已定"派一条一行勘误、时机未到"** —— 不动 |
| **N2** | §4.2 逐字「**按 §4.1 的要求本就必须不在任何固定名集合之内**」——**新 spec 的 §4.1 是 INV-2（stale 不是删除许可），与固定名无关**。该句是从 `2026-07-29` §10 第 4 条原文照搬的，那里的"§4.1"指的是 **07-29 自己的 §4.1**（实测 `:80` = 「临时文件名必须进程唯一」） | **仍成立** | **本轮修**（改成「按 `2026-07-29` §4.1 的要求」，一处限定词） |
| **N3**（新） | §1.4 逐字「上一轮**四份扫描与两条评审车道**都没点出这条耦合，在此记明」。该限定**为真**，但 `close-3-span-tension.md:453` 逐字已写「**第 3 笔的修法与「保留即放宽」那条人裁是绑定的，不能分开决策。**」，L5 输入盘点台账 `:690`–`:691` 亦有逐字同结论 ⇒ **限定面恰好切掉了含该结论的三份 `close-*.md`**，"在此记明"的新颖性是假象 | 新发现 | **本轮修**（把 `close-3` 与台账 `:690` 加成指针；否则 §7"不造第三份清单"的立场在 §1.4 被自己破了一个口） |
| **N4**（新） | §5.2 标题「**三种写法在流通**」，但正文清点的三种是 {源码生成 / 07-29 §10 第 4 条 / 台账·handoff 四段 `stamp`}。**台账 `:169`–`:172` 定的三种是 {§10 第 4 条 / 台账四段 / **07-29 §4.1 的「建议形式」**}** ——§4.1 那种（实测逐字 `.<basename>.<pid>.<单调递增序号>.tmp`，**无 `timeOrigin`**）在新 spec 里**一次没被提**。而它才是最危险的一种：它是**规范性的"建议形式"**，一个实现者照它写匹配器会漏掉 `timeOrigin` 段 | 新发现 | **本轮修**（补一句"§4.1 的建议形式是第三种，缺 `timeOrigin`，不得据它匹配落盘文件名"）。这正是台账 STEP 1 收口第 4 条派给包 1 的活 |
| **N5**（新） | handoff `:144` 仍逐字写「**10 条 deferred minor**（9 条延后 ＋ 1 条新增）」——**这个聚合数已被本轮扫描员 1 证伪（真值 12），台账 `:208`–`:212` 把它列为"控制器记账第 1 次"**。本次 handoff 更新注解了四处"远端落后"，**唯独没注解这一处**，于是同一文件里 `:13`（6 条）与 `:144`（10 条）并存 | 新发现 | **本轮修**（就地加一行注：「Amended：该"10"是聚合数，实为 12，见包 1 台账 `:208`–`:212`；包 1 收口后的现状是 6 条」） |

**Minor 计数 = 8**（M1 / M2 / M2′ / M3 / M5 / N2 / N3 / N4 / N5 共 9 行，其中 **M2′ 归并入 M2**，故计 8 条）。

## 10. 我没做完的事（自报，逐条）

**本仓库的规矩：自报未验项比虚假完整更值钱。9 条，按重要性排序。**

1. *** **N1 我只做了文本推理与场景构造，没有在仓库里找到一个实际存在的五段形残留文件。** ***
   我只跑了 `ls -d .validation-runs .worktrees`（两个目录都存在），**没有**去 `find` 五段形文件。
   ⇒ **我证的是"spec 文本授权了这种删除"，不是"今天真有这种文件在那里等着被删"。**
   这与再评审员自报的第 2 条**是同一个洞，我没有补上它**。若 Lane 2 顺手枚举了，请以他的为准。

2. *** **勘误注文的"逐提交重数"我没重跑。** *** 任务 1 评审员做过
   （`0f940ea~1`→4 / `0f940ea`→7 / `dad8a14~1`→7 / `dad8a14`→10 / `HEAD`→10）。
   **这在我这里是"未独立复核"，不是"已确认"。** 我只重跑了 `HEAD`→10 与两笔提交的日期/主题行。

3. **三份任务报告（`task-1-report.md` / `review-task1.md` / `re-review-task1-fix1.md`）我没有逐行读。**
   我读的是台账对它们的转述并对要引用的结论回原文重推。
   ⇒ **这三份报告内部是否有自身缺陷（比如评审员写错了一条引文），我没查。**

4. **F-I2 的证伪是"命中即存在"，反方向我没证。** 我**没有**逐行通读九份报告，
   ⇒ 我**不能**断言"只有两份提到了保留"。可能更多。**我的断言是"至少两份"。**

5. **M5 的反向指针我下的是限定否定。** 我只对 `2026-07-26-…-design.md` grep 了三个模式，
   **没有对该文件做 `Amended` 全文检索、也没有 grep 全仓有没有别处已经补了这个指针。**

6. **§6.1「该函数今天零生产调用者」我没有跑 `src/` 全仓 grep。**
   我采信的是源码注释 `:687`–`:688` 的自陈 ＋ `:689` 的 `export` 行。
   **这是"依据文本"不是"依据检索"** —— 它是 Lane 2 的切片，但我在 §3.4 引用了它，故在此声明。

7. **`RUN_MARKER_FILES` / `OBSERVED_FILES` 我没核。** 我在 §2.4 的修法建议里**用了** `RUN_MARKER_FILES`
   作为封口谓词的候选，**而我没有打开 `scanRuns.ts` 确认它的内容适不适合这个用途。**
   ⇒ **§2.4 第 1 步是一个方向建议，不是一个已验证的方案。** 采纳前必须核。

8. **"台账没有新的主语压缩"这条判断是主观的、不可重推。** 我读了全部 258 行新增，
   但用的是人读判据，**没有可重跑的命令**。⇒ 它是我的意见，不是证据。

9. **`docs/handoff/handoff.md` 我没有全文通读**（1000+ 行）。
   我读的是 diff 全文 ＋ `grep -n 'Amended\|远端\|ls-remote\|一致'` 的 22 行命中逐条打开
   ＋ `:135`–`:150`。⇒ **本分支之外的 handoff 区域是否还有别的腐坏，我没查**
   （N5 是我在查 deferred minor 计数时**顺带**撞到的，不是系统性扫描的结果——
   **这意味着同类腐坏可能还有，我没扫**）。

**另外两条边界声明（不是未完成，是切片）**：
- **Lane 2 的破坏线索我一行没查**（`buildAtomicTempPath` 的写目标是否落在 `<runDir>/worktrees/` 下）。
  §3.6 与 §0 已就地写明本报告哪些结论以该定性成立为前提。
- **我没有做任何变异注入、没有跑测试套件**（只读边界；且 `src`/`tests` 零改动，无可注入面）。

## 11. 预算（Rule 6）

**破了。明写，不静默超支。**

- 预算：单任务 12,000 token。
- **实际约 48,000–55,000 token**（≈ **4×**）。判别式：本次会话我发出的全部 tool 调用的
  输入/输出 token 之和的估算，**不含**本报告的写入 token。
- **主要开销在哪（供控制器决定下次怎么切）**：
  1. **新 spec 全文 551 行 + 台账全文 608 行逐行读 ≈ 一半预算。** 这两份我认为不能跳读——
     N1 的五环复核与 §3 的自指诚实度核验都要求逐字比对。
  2. **九份报告的两轮逐份检索 + 命中行逐条打开**（§6）≈ 15%。这是被点名指派的活。
  3. **8 个探针脚本**（铁律 4 的对策：脚本先落盘再 `rtk proxy zsh` 跑）≈ 20%。
     **这个成本是值得的**——我一次也没被引号坑到，且拿到的都是未过滤输出。
- **我做的取舍**：为了把预算保住在 4× 而不是 6×，我**主动放弃了**三份任务报告的逐行通读（§10 第 3 条）
  与九份报告的逐行通读（§10 第 4 条），改成"关键词检索 + 命中逐条打开 + 下限定否定"。
  **这个取舍我认为是对的**：它让我在 §6 拿到了一条硬证伪（F-I2），代价是不能下反方向的全称断言——
  而我把这个代价**明写成了限定**，没有伪装成完整覆盖。
- **本轮已有两名 agent 明写超支 2–3×，再评审员明写约 3×。我是 4×，是目前最高的。**
  若控制器认为这个成本不可接受，**可切的是 §8 的四条 deferred minor 重验**（约 10% 预算），
  但那样就得接受"再评审员那句『四条是否仍成立未重验』继续挂着"。
