# Re-review: Task 3 fix round 1 (scoped)

## 0. 结论

**范围**：`d5b3e79..6a714b1`，3 files，+11 −4。HEAD 现为 `6a714b1`，工作树干净（`rtk proxy "git status --porcelain"` 空，exit 0）——已用 `rtk proxy` 复验，未受本机全局 hook 过滤影响。

| 项 | 裁定 |
|---|---|
| I1 | **ADDRESSED** — 「不改」的理由本身站得住（见 §1.I1） |
| I2 | **ADDRESSED** |
| I3 | **ADDRESSED** |
| I4 | **ADDRESSED** |
| C1 | **ADDRESSED** |

**超范围修复（D7 `(k)`/`(l)` 缺日期）**：判断成立（与 C1 同形），修得对，**在本轮流程下可接受**（机械同形补齐，非改判据，已原样自报、回退成本低）。

**日期约定**：独立确认为「**落注日**」——即这条 `Amended` 注释本身被写入并提交（commit）到文档的那个日历日（本地时区 +0800），不是被勘误事件/裁决/分支创建的日期。证据见 §3，横跨本分支两个独立样本（10 处 `Amended 2026-08-04` 全部精确匹配落地提交 `b427c8b`＝2026-08-04；本任务 10 处 `Amended 2026-08-05` 全部精确匹配落地提交 `811a2e7`＝2026-08-05 23:54:56）。本轮 I2/I3/I4/C1 新写的 7 处标签取 `2026-08-06`，与其落地提交 `6a714b1`＝2026-08-06 09:16:26 精确匹配，**日期取舍正确**。

**标签计数**（4f3b790..6a714b1 范围，区分标签与交叉引用）：新增行里字面 `Amended 2026-08-0[0-9]` 共 **28** 处（与控制器数字一致）；其中**标签（TAG，主张某处此刻被勘误）17 处，交叉引用（CROSS-REF，引用别处已有标签作证据）11 处**。17 个标签逐一核对，**日期全部正确**，零错配。明细见 §3。

**有无新破坏**：**没有**。`git diff d5b3e79..6a714b1 | grep -c "Amended 2026-08-05"` → **0**（独立复验，与控制器一致），任务 1/2 已收口的勘误一处未碰。`--word-diff` 显示全диff范围内只有两处真删除／替换，且都精确对应 I4（日期串替换）与 C1（前导短语重排），无其它意外改动。

**第（五）项**：I3 新增注文里的「the paragraph above ... is left as-is」**该留**——同类「逐字不变」「原样保留」「一个字不改」措辞是本仓库 Amended 注释的既有惯例（多处在案，见 §5.1），不是本轮引入的新风格。「`(j)` 重复使用说法为假」的认领**写清楚了**——引用了自己原话、指出错误机制（先看正文模式后判断、没先读 Index 定义）、明确不再援引。

## 1. 逐条裁定

### I1 — §12 "界的是付费调用" 双处

**裁定：ADDRESSED.** 实施者的「不改」理由本身经得起独立复核。

**我独立读的原文**（`git show 6a714b1:docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md`，`sed -n '2310,2330p'` 对应区间，逐字确认，未采信报告转述）：

- `:2321`（第四轮更正）：「`--max-runs N` 界的是**进入 `runLoopFromState` 的 run 数**……**付费上界是 `N × maxAttempts`**。『有界批准』仍然成立（两条界都有限），但操作者按横幅里的 N 理解付费规模会低估一个 `maxAttempts` 倍……把『横幅里的 N 就是付费次数』这个读法明确标为错误。」
- `:2322`（未改）：「配额在『四道门全过、`resume_adopted` 已追加』那一刻计入……被门拒绝的 run 一次付费调用都没发生，让它吃掉配额既不符合『界的是付费调用』这个目的，又会造成确定性饿死。」
- `:2326`（已改）：原句「`--max-runs` 界的是付费调用，不界事件追加」，现追加「⚠️ 就地更正：……按那条更正，`--max-runs N` 本身界的是进入 `runLoopFromState` 的 run 数，付费调用的真实上界是 `N × maxAttempts`，不是 N。『不界事件追加』这半句不受此更正影响……」，且原句紧邻「这一笔具名传给 L5（§13）」——**是 L5 交接清单的原句**。

**逐条核实施者的三点论证**：

1. **句法形状不同**——成立。`:2326` 是「`--max-runs` **界的是** X」，对旗标本身下定义式断言，与 `:2321` 的技术更正正面矛盾（旗标字面上界定的是 run 数，不是付费调用数）。`:2322` 的「界的是付费调用」被嵌在「不符合……这个目的」这个短语里，语法上是修饰「目的」的定语，不是对 `--max-runs` 的谓语断言——这是可验证的文本事实，我逐字核对属实。
2. **底层论证在更正后依然成立**——成立。`:2322` 的实质主张是「零付费调用的 run 不该吃掉配额，否则造成确定性饿死」，这条推理不依赖 N 是否精确等于付费次数：即使在 `N × maxAttempts` 模型下，被拒 run 依然是零付费，依然不该计入。`:2321` 明写「『有界批准』仍然成立」，没有否定"配额应服务于约束付费敞口"这个总目标。
3. **读者受害面不同**——成立。`:2326` 后接「这一笔具名传给 L5」，是显式交接句；`:2322` 是 §12 内部一条设计取舍的论证支撑句，不出现在任何交接清单里。

**我自己额外核的一点（报告未覆盖）**：`:2326` 被判"矛盾"的关键不是论证假，而是**测辞与 `--max-runs` 的定义式断言撞车**；`:2322` 从未对 `--max-runs` 本身下定义，只是借用同一短语描述配额机制的**设计目的**。两处论证都不假，但只有 `:2326` 处在"这句话字面上等于给旗标下了一个被上文推翻的定义"的位置上——这正是区分两者是否需要就地更正的关键，而不是"用词是否相同"。

**结论**：控制器原话授权「改」或「写明不改的理由」两种结论，且明确警告「不要反射性地去改一句本来对的话」。实施者选择了后者，给出的三点理由分别是可独立验证的文本事实和逻辑事实，我逐条核实成立。**这满足 I1 的核心要求——不是沉默漏判，而是显式裁定并给出可复核的理由。** 唯一的残留弱点：修法未在 `:2322` 本身留一句短注消除"同一措辞、一处标矛盾一处不标"带来的表层观感（原评审"修法"选项 1 建议的做法），但控制器授权的两条路径里，"在报告里写明理由"本身就是被允许的第二条路，不构成缺陷。

### I2 — L2 Index 未同步

**裁定：ADDRESSED.**

独立读取 `6a714b1` 的活文档（`docs/superpowers/specs/2026-07-28-run-registry-design.md:1`–`38`）：Index 在 `(j)` 之后新增

```
- **(k)** §2 item 6 — **"discharge any of the three debts bequeathed to L5" is
  stale, and unlike (a)–(i) this is not a document defect.** It was accurate on
  2026-07-28; the 2026-07-29 debt-attribution ruling then reattributed debts 1
  and 3 to L3, leaving only debt 2 for L5. Amended 2026-08-06.
- **(l)** §13 — the same "Debts 1–3 are bequeathed to L5" claim, restated where
  the inherited-debts section opens. Amended 2026-08-06, same ruling as (k).
```

格式与 `(i)`/`(j)` 一致（章节锚点 + 加粗结论 + 来历说明 + 落款日期），与原评审"修法"建议的形状吻合。

**字母连续性独立复验**（不采信报告数字，自己重跑）：
```
$ git show 6a714b1:.../2026-07-28-run-registry-design.md | grep -oE "^\- \*\*\(([a-z])\)\*\*"
(a)(b)(c)(d)(e)(f)(g)(h)(i)(j)(k)(l)   -- 12 个，连续无跳号
```
Index 12 字母与正文实际带内联标记的 11 个字母（`a,b,c,d,e,f,g,h,j,k,l`；`i` 只是新增行，历来无内联标记）一一对应，无孤儿字母、无遗漏。

**原 finding 的危害是否解除**：评审员给出的两个场景——(1) 下一个落勘误的人按 Index 挑字母会撞上正文已有的 `(k)`；(2) 审计"2026-07-29 裁决是否执行"的人从 Index 入手会读不到债务归属信息而误判"未执行"——两者均因 Index 现在完整覆盖 `(k)`/`(l)` 而解除。**没有半改**：Index 与正文（在体标记）两侧同步补齐，非只改一侧。

### I3 — L1b "five / all / (a)–(e)" 基数与全称限定

**裁定：ADDRESSED.**

独立读取活文档头部（`docs/superpowers/specs/2026-07-27-owner-transfer-contention-design.md:1`–`6`）：原句「Amendments: five, all found by the final whole-branch review … **Amended 2026-07-28 (a)–(e)** in §2, §5.1, §5.3, §5.4 and §6」**一字未删**，紧随其后新插一行：

```
> **Amended 2026-08-06 — note on the count above**: the paragraph above describes only the
> original whole-branch-review batch and is left as-is. A sixth amendment, **Amended
> 2026-08-06 (f)**, now also exists in §5.3 (alongside (e)); it is not part of the
> "five... found by the final whole-branch review" — it corrects a later, separately-discovered
> defect (an L3 human ruling on task A4 superseding this document, not a finding from that
> review round). §5.3 is already in the location list above and needs no addition; only the
> count and the "all found by" qualifier are stale.
```

**处置方式与原评审"修法"建议不同，但等价有效**：原评审建议直接把"five"改成"six"并在正文追加一条 (f) 列举；实施者选择保留原句、紧邻处插入更正注——这与本仓库贯穿全部 Amended 注释的房规一致（原文只增不删，就地注解，"就地更正 / 原样保留"是主流做法，不是直接改写，参见 D7 的 `(j)`/`(k)`/`(l)` 与 D12/L1b 其余全部注释）。两种做法都能让 L5 读者在文首就获知"five/all"已过期，效果等价。

**两处坏点是否都被处理**：
- 基数「five」→ 注文明写"A sixth amendment … now also exists"，纠正基数。
- 全称限定「all found by the final whole-branch review」→ 注文明写 `(f)` "it is not part of the 'five... found by the final whole-branch review'"，纠正全称。
- §5.3 是否需要补一个小节名——不需要，`(f)` 落在 §5.3（与 `(e)` 同节），原句"in §2, §5.1, §5.3, §5.4 and §6"里已列出 §5.3，注文明写这一点，不留给下一个人自己查。

**读者危害是否解除**：评审员的场景是"L5 读者先读文首摘要建立地图，得到『五条、全部 07-28』，把 §5.3 当作 07-28 定稿"——新注紧跟在原句之后、读者建地图的同一处，直接告知 §5.3 还有第六条、且日期不同，危害解除。

### I4 — (f) 日期取事件日而非落注日，且与 "landed now" 自相矛盾

**裁定：ADDRESSED.**

活文档 `:116`（`docs/superpowers/specs/2026-07-27-owner-transfer-contention-design.md`）现读作：

```
**Amended 2026-08-06 (f): the preceding sentence's "is L5's problem" is superseded — this is
now closed by L3, not inherited by L5.** … this paragraph is that note, landed now.
```

日期从 `2026-08-02` 改为 `2026-08-06`，其余文字一字未动（仅日期串被 `--word-diff` 标为替换：`[-2026-08-02-]{+2026-08-06+}`，已独立复验）。

**日期是否正确**：见 §3 的独立日期约定核查——`2026-08-06` 正是本注被写入并提交（`6a714b1`，`2026-08-06 09:16:26 +0800`）的日历日，符合本仓库"落注日"约定。原来的 `2026-08-02` 是被勘误事件（L3 台账文件 `.superpowers/sdd/2026-08-02-.../progress.md` 的落盘提交日）的日期，混淆了"事情发生的日子"与"这条注被写下的日子"，评审员的诊断成立。

**"landed now" 自相矛盾是否解除**：现在标头 `2026-08-06` 与结尾 "landed now" 指向同一天（本注确实是 `2026-08-06` 这天落地的），矛盾解除。

### C1 — D12 的 Amended 注缺日期

**裁定：ADDRESSED.**

活文档 `:1031`（`docs/superpowers/specs/2026-08-01-sweep-and-transactional-continuation-design.md`）由

```
Amended（计划阶段裁定三，落在此处）：上面那条「理由已过时」的注只否定了理由……
```

改为

```
Amended 2026-08-06：上面那条「理由已过时」的注只否定了理由，没有否定「代码零改动」这个断言本身
——断言本身也已被推翻（计划阶段裁定三，落在此处）。
```

格式补齐为房规 `Amended <日期>：`，原来的括注说明挪到句尾作附注，**内容一字未删**（`--word-diff` 独立复验：唯一被替换的是这个前导短语本身，正文其余部分逐字不变）。

**日期正确性**：同文件另有 6 处 `Amended 2026-08-05：` 同格式注释（全部是本文档自身的既有惯例，即该注释被写入本文档的那一天），`2026-08-06` 与本注实际落地提交 `6a714b1`（`2026-08-06 09:16:26 +0800`）的日历日精确匹配，与同文件内 6 个兄弟样本的取date方式完全一致（见 §3 独立验证）。

**危害是否解除**：房规要求每条 `Amended` 注可追溯"何时被谁纠正"，缺日期的注无法参与"这个缺口开了多久"一类审计。补齐后与全文档其余 9 处 `Amended <date>：` 格式统一，可追溯性恢复。

## 2. 超范围修复：D7 (k)/(l) 缺日期

**判断成立性（① 与 C1 同形与否）**：成立。修复前，L2 正文在体标记读作 `*Amended (k) — item 6's "three debts" is stale. …*` 与 `*Amended (l) — "Debts 1–3 are bequeathed to L5" is stale. …*`——都缺日期，与 C1（D12 注 `Amended（…）：` 缺日期）是同一种缺陷形状：都不满足 `Amended <日期>` 房规。

**修得对不对（②）**：对。`--word-diff` 显示改动是纯粹的日期插入（`*Amended (k)` → `*Amended 2026-08-06 (k)`，`(l)` 同形），日期 `2026-08-06` 与落地提交 `6a714b1` 的日历日一致，格式与同文件内其它带日期注释（L1b 的 `(f)`、D12 的 C1 注）一致。

**一个值得记录的细节（我独立发现，报告未提）**：L2 文件里 `(a)`–`(j)` 的**全部**在体标记本身都是不带日期的裸写法（`*Amended (a).*`、`*Amended (c), (h).*`、`*Amended (j) — …*`，日期只在 Index 或文件头统一给出，如"Amended 2026-07-28 in eight places (a)–(h)"）。`(k)`/`(l)` 的在体标记从最初落地（`d5b3e79`）起就已经是大段解释性文字（不是裸写法），风格上更接近 L1b/D12 那种"每条自带日期"的段落式注释，而不是 L2 自己 `(a)`–`(j)` 的"裸字母，日期外置"惯例。**给 `(k)`/`(l)` 补日期严格来说不是修复"L2 本地既有惯例的违反"（L2 本地惯例是在体标记不带日期），而是让它们与自己已经采用的"段落式富注释"风格（这一风格本仓库处处要求带日期）保持自洽。** 这个区分不影响判断成立与否——`(k)`/`(l)` 一开始就没有遵循 L2 的裸写法，是段落式注释，段落式注释在本仓库无一例外都带日期，所以按其自己的风格类别，缺日期确是缺陷。

**③ 超范围自行修复是否可接受**：**可接受**。理由：
1. 这不是"发现判据不成立"（需要原样上报、不得自行改判）的情形——铁律针对的是评审判据/裁决本身有误时不得自行推翻。这里是**机械补齐同形缺陷**（加日期字符串），不涉及对任何判据、裁决、事实断言的重新裁定。
2. 实施者已经**原样自报**（"修复环不确定/存疑之处"第 3 条明写"没有单独请示就直接改了"，交代了指令名单边界、判断依据、回退成本），不是隐瞒操作。
3. 回退成本确实低——`git diff` 层面只是给两处加 8 个字符的日期串，不触及任何原文正文或论证。
4. 不修复的代价更高：如果只报告不动手，下一轮评审员大概率会把它列为新的 Important/Minor 重新走一轮修复环，而这轮本就在处理"缺日期"这同一类缺陷（C1），顺手补齐是低风险、高确定性的操作。

**结论**：超范围修复本身合规，处置正确。

## 3. 日期约定的独立确立与标签计数

### 3.1 约定是什么

**独立确立：`Amended <日期>` 里的日期 = 这条注释本身被写入并提交（commit）到该文档的那个日历日（本地时区 +0800），不是被勘误事件、外部裁决、分支创建的日期。**

**证据（独立于实施者/控制器给出的两点，另找了两组更大样本验证）**：

1. **同一提交内的大样本，跨两个文件，精确匹配**：
   ```
   $ rtk proxy "git log --format='%H %ad %s' --date=iso -S 'Amended 2026-08-04：本条判据的前提在方案' \
     -- docs/superpowers/plans/2026-08-02-sweep-and-transactional-continuation.md"
   b427c8b … 2026-08-04 19:25:24 +0800 docs(b1): record the mutation-5 kill …
   ```
   文本 `Amended 2026-08-04` 恰好是 `b427c8b` 这个提交引入的，提交日历日正是 `2026-08-04`。
   ```
   $ rtk proxy "git log --format='%H %ad %s' --date=iso -S 'Amended 2026-08-05：上段' \
     -- docs/superpowers/plans/2026-08-02-sweep-and-transactional-continuation.md"
   811a2e7 … 2026-08-05 23:54:56 +0800 docs(errata): 就地勘误「回调=一次数组 push」族仍无勘误的 10 处
   ```
   同理，`Amended 2026-08-05` 由 `811a2e7` 引入，落地日历日正是 `2026-08-05`（即使提交时间已近午夜）。这一条提交恰好新增了 **10** 处 `Amended 2026-08-05`（commit message 自称"10 处"，与我 §3.2 独立数出的 10 个标签精确吻合）。
2. **L1b/L2 既有的两点（复验，未改判）**：L1b 文首"implemented and amended on 2026-07-28"，`(a)`–`(e)` 全标 `2026-07-28`——文档自身宣称的"完成并勘误"日与勘误标签日一致。L2 的 `(j)` 标"Amended 2026-07-30 by that branch"——**这一条我额外查证发现并非本约定的直接反例**：`(j)` 明写"unlike (a)–(i) this is not a document defect"，它记录的不是"这条 L2 注释何时被写入"，而是"外部代码被那条分支改动的日子"，是一种不同性质的记述（性质上更像交叉引用一个外部事实的日期，不是"落注日"标签）。我用 `git log -S` 定位到 `(j)` 这段文字本身由提交 `1c96c8d`（`2026-07-31 10:08:03`）引入，与其标注的 `2026-07-30` 差一天——**这是一个孤立的、与"落注日"约定本身无关的既有一天误差（可能是原作者估算分支落地日时的失误），不构成对约定的推翻**：本约定在其它 26 个样本（含下方 3.2 的 17 个标签）上零误差，`(j)` 是这条注释性质本身特殊（记的是外部事件日不是本注落地日）导致的个例，不影响本轮新增标签该怎么定日期的判断。

**结论对本轮修复的适用**：I2/I3/I4/C1 新写的标签一律取 `2026-08-06`，与其落地提交 `6a714b1`（`2026-08-06 09:16:26 +0800`）的日历日精确匹配，**取舍正确**。实施者自陈"没有验证到规则、是我的判断"——现已由独立证据坐实为规则，不再是未验证的判断。

**顺带确认的一条（超出任务范围，仅记录）**：`26b0709`（任务 2 站点 A）落于 `2026-08-06 00:39:43`，但其内写的是 `Amended 2026-08-05`——按本约定，这一处**确实偏了一天**，与控制器的怀疑一致。这不在本任务 3 修复环范围内（属于任务 2 已收口的产物），留给整分支评审处置，此处仅作为独立确认。

### 3.2 标签计数（区分标签 vs 交叉引用）

**范围**：`4f3b790..6a714b1`（新增行）。命令与输出：
```
$ rtk proxy "git diff -U0 4f3b790..6a714b1" | grep '^+' | grep -o "Amended 2026-[0-9-]*" | sort | uniq -c
   3 Amended 2026-08-04
  17 Amended 2026-08-05
   8 Amended 2026-08-06
（合计 28，与控制器数字一致）
```

**逐条读取上下文分类**（标签 TAG = 在当前位置主张"这里此刻被勘误"，通常以 `**Amended <date>` / `*Amended <date>` / `> **Amended <date>` 起句，或 Index 里给出该字母自己的落款日；交叉引用 CROSS-REF = 反引号内引用另一处已存在的标签作证据，不主张新勘误）：

| 文件 | TAG 数 | TAG 日期分布 | CROSS-REF 数 |
|---|---|---|---|
| plan（2026-08-02-sweep-…md） | 4 | 08-05 ×4 | 5（08-04×3, 08-05×2） |
| L1b spec（07-27-owner-transfer…md） | 2 | 08-06 ×2 | 1（08-06，指向同段内 (f) 自己的标签） |
| L2 spec（07-28-run-registry…md） | 4* | 08-06 ×4 | 0 |
| D12/sweep spec（08-01-sweep-…design.md） | 7 | 08-05 ×6, 08-06 ×1 | 5（08-05×5） |
| **合计** | **17** | 08-05×10 / 08-06×7 | **11**（08-04×3 / 08-05×7 / 08-06×1） |

\* L2 的 4 个标签只对应 **2 处独立勘误**（`(k)`、`(l)`）——L2 惯例是 Index 与在体标记各自独立落款日期，同一条勘误被双重标注，这正是控制器警示的"朴素 grep 会把标签和交叉引用混在一起"的另一种变体（这里不是标签/引用不分，是"一条勘误两个日期戳"未被朴素计数区分）。17+11=28，与前面 grep 总数一致，逐条核对无遗漏无重复。

**逐一核对 17 个标签的日期正确性**：
- 08-05 的 10 个（plan 4 + spec 6）：全部由提交 `811a2e7`（2026-08-05 23:54:56）引入，该提交自称"10 处"，精确匹配。**0 错配。**
- 08-06 的 7 个（L1b 2 + L2 4 + D12/C1 1）：对应 I2×4（Index (k)(l) + 正文 (k)(l)，`6a714b1` 新增）、I3×1（count-note，`6a714b1` 新增）、I4×1（`(f)` 标签——因范围起点 `4f3b790` 早于 `(f)` 首次落地的 `d5b3e79`，`(f)` 的"引入 08-02 错误日期→改判为 08-06"两步在这个 diff 区间里被压缩成一次性出现、直接显示最终正确日期 08-06，已在 I4 一节单独核验其改判过程）、C1×1（D12 注，`6a714b1` 修正）。**0 错配。**

**结论**：17 个标签、11 个交叉引用，逐一核验日期与其落地提交的日历日精确对应，**本轮范围内零日期错配**。

## 4. 修复 diff 有无新破坏

**范围**：只看 `d5b3e79..6a714b1`，3 files，+11 −4。

**任务 1/2 已收口的勘误有没有被顺手动过（独立复验控制器的 0 命中）**：
```
$ rtk proxy "git diff d5b3e79..6a714b1" | grep -c "Amended 2026-08-05"
0
```
与控制器一致，独立复验成立——本次 fix diff 完全没有触碰任何 `Amended 2026-08-05` 字样（即任务 1/2 落地的全部勘误）。

**逐字核实际改动面**（`--word-diff`，只看真删除/替换段，不看纯插入）：
```
$ rtk proxy "git diff --word-diff d5b3e79..6a714b1" | grep -oE '\[-[^]]*-\]'
[-2026-08-02-]
[-**Amended（计划阶段裁定三，落在此处）：上面那条「理由已过时」的注只否定了理由，没有否定「代码零改动」这个断言本身——断言本身也已被推翻。**-]
```
全 diff 范围内**只有两处真替换**，且精确对应：
1. I4——`(f)` 的日期字符串 `2026-08-02` → `2026-08-06`；
2. C1——D12 注的前导短语重排（日期插入 + 括注挪到句尾）。

**numstat 核对**（`2 1` / `8 2` / `1 1`，3 files +11 −4）：
- L1b（2 插入 1 删除）：1 插入是 I3 的 count-note 整段，1 插入是 I4 涉及行本身（因日期替换导致该行计为改动），1 删除对应 I4 的日期串替换。
- L2（8 插入 2 删除）：6 插入是 I2 的 Index 两条新增（各 3 行），2 插入是 D7 (k)/(l) 超范围修复涉及的两行整行替换的插入侧，2 删除是同两行替换的删除侧（纯日期字符串插入导致的整行计法，`--word-diff` 已证明只增字符不删原文）。
- D12 spec（1 插入 1 删除）：C1 的日期插入导致该行整行替换计法，`--word-diff` 已证明只是前导短语重排，正文一字未删。

**结论**：全部改动都能一一映射到 I2/I3/I4/C1 ＋ 自报的 D7 超范围修复，**没有发现任何意料之外的改动**，没有新破坏。

## 5. 实施者自报的两条

### 5.1 I3 新增注文里的元话语（"the paragraph above is left as-is"）

**判断：该留，不是需要清除的元话语。**

实施者自己担心这句"自证清白"的措辞可能不该出现在正文里。我独立检索了本仓库其它 Amended 注释是否有类似"强调没有删除原文"的惯例，而不是只看这一处：

```
$ rtk proxy "git grep -n '一个字不改\|逐字不变\|原样保留' -- 'docs/superpowers/specs/*.md' 'docs/superpowers/plans/*.md'"
```
命中包括（节选）：
- `plan.md:878`「**且上段的结论一个字不改**」
- `plan.md:1008`「**上段其余部分逐字不变、且仍然成立**」
- `spec.md:673`「**⚠️ 结论不动**……选型与本条前半段……一个字不改」
- `spec.md:698`「**上段其余部分逐字不变、且仍然成立**」
- `spec.md:759`「**且上段的结论一个字不改**」
- `spec.md:995`「`writeBoundaryArtifacts` 之前那个**无条件 `assertHeld()` 原样保留**」

**这类"强调本次编辑没有删改周边内容"的元话语，在本仓库的 Amended 注释里是贯穿全文档、反复出现的既有写法，不是本轮引入的新风格。** I3 新注的"the paragraph above ... is left as-is"与上述惯例同构，只是用了英文措辞（因为该文档整体是英文）。它对读者也有实际信息量：明确告知"文首那句原文没有被我改写，你看到的仍是原文"，避免读者误以为整段被重写过。**判断：留。**

### 5.2 (j) 重复使用说法为假 —— 认领是否写清楚

**判断：写清楚了。**

`task-3-report.md`「concern 3 认领：`(j)` 两次使用的说法为假」一节（对应原评审 M4）逐项做到：
1. **引用自己原话**——"本文件已有的既有惯例把字母 `(j)` 用了两次指两处不同内容……这是文件自身已存在的不一致"，指名是自己在"不确定/存疑之处"第 3 条写的。
2. **重新核实并给出真实的 Index 定义**——引 Index 原句"`(j)` §8.1, §13 item 4 — the two 'Atomic? no' rows are no longer true..."，指出 `(j)` 是"一条勘误、两个落点"，不是两条不同勘误共用一个字母。
3. **说明错误机制**——自陈"看到正文里 `(j)` 出现两次就直接推断『字母被重复使用标记不同内容』，没有先去读 Index 里 `(j)` 词条自己怎么定义它覆盖的范围"，点出这正是"自己去核实、不要凭表面模式下判断"这条本任务反复强调的原则的一次反例。
4. **说明后果并限定范围**——承认这个错误判断是"不去修文首标题"这一决定的部分依据，但同时区分"那个决定本身现在看仍站得住（Rule 3，不在本轮范围内）"与"支撑它的理由不能包含这句假话"，没有借错误结论碰巧正确来回避认领。
5. **明确停止援引**——"已在此认领，不再在报告任何地方援引『`(j)` 重复使用是既有不一致』这个错误说法"。

五点俱全（复述原话、核实真相、说明错误机制、限定影响范围、停止援引），认领**清楚、完整，没有含糊带过**。

## 6. 范围外观察（不进修复环）

1. **`26b0709`（任务 2 站点 A，落于 `2026-08-06 00:39:43`）写的是 `Amended 2026-08-05`**——按本报告 §3.1 独立确立的"落注日"约定，这一处的日历日应为 `2026-08-06`，疑似偏了一天。不在本任务 3 修复环范围内（该提交属于任务 2 已收口的产物），留给整分支评审逐条核对全部日期标签时一并处理。
2. **L2 文件 `(j)` 条目"Amended 2026-07-30 by that branch"与其自身落地提交 `1c96c8d`（`2026-07-31 10:08:03`）相差一天**——经查证，这条记的是"外部代码分支被改动的日子"而非"本注被写入的日子"，性质与 `(a)`–`(i)`/`(k)`/`(l)`/D12 等"文档缺陷"类标签不同，可能是原作者对分支落地日的估算误差，也可能是故意记录外部事件日。真实归因需要进一步查那次 `atomic-write-paths` 分支的精确合入时间（`3ac12e0` 合并提交是 `2026-07-31 13:32:37`，晚于该注声称的 `07-30`，也晚于该注自身落地的 `07-31 10:08`——三个时间点互相不完全吻合），但这是既有历史文档的既存瑕疵，不在本任务 3 授权范围内，仅记录供整分支评审参考。
3. **L2 文件 `(a)`–`(j)` 的在体标记全部是不带日期的裸写法**（`*Amended (a).*` 等），日期只在 Index 或文件头统一给出；而本轮新增的 `(k)`/`(l)` 在体标记是段落式富注释、且现在带了日期——这是文件内两种标注风格并存（裸字母 vs 段落式）的既有分化，不是本轮引入的新不一致，已在 §2 详述，仅在此重复标注供整分支评审知悉。
