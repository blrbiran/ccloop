# 扫描员 1 brief — 包 2 三条路径的「今天代码面」重核（只读）

## 你的身份与铁律

你是**只读**扫描员。**一个字都不许改 `src/` `tests/` `docs/`**，不许 commit。
你的产出只有一份报告文件（路径见下）。

**不接受实施者自证**：你要重核的那些断言出自更早的报告，**它们的作者不能给自己作证**。
逐条自己打开今天的源码验，**不要转述报告的措辞当结论**。

⚠️ *** **本仓库最稳定的失败形状：用收窄的搜索面支撑全称否定。** *** 一轮内出现过 6 次，三方三次同形，
**三次全部发生在「下全称否定」那一步上**。硬性要求：
- 下「没有任何一处 / 全仓唯一 / 从未」之前，**先确认你的 grep 面覆盖你断言的范围**，并把**命令与其当时输出**写进报告。
- *** **一条被转义弄坏或被过滤的探针，永远不能证明「不存在」。** *** 拿到零输出时**先验命令本身**。
- **对策（已实证，必须照做）**：把探针**写成脚本文件先落盘**，再 `rtk proxy zsh <script>` 跑；
  **同一次跑里放一条必命中的 sanity 探针**（例如 `grep -c '' <file>` 出总行数），证明检索面是活的。
- **验证跑绝不过滤输出**（`grep` 与 `tail` 同罪）。做检索必须直接走 `rtk proxy`，
  否则 rtk 默认改写会把输出折成 `[N more lines]`，你拿到的是被过滤过的证据。
- **锚点用符号名，不要用行号**（本项目已有六处自造的失效行号引用案底）。

⚠️ *** **落盘协议（强制）**：先 `Write` 一个**只有小节标题的骨架**并**立刻落盘**，
**在此之前不要做任何检索**；之后**每次 `Edit` 只填一节**，**结论那一节最先写**。 ***
（本仓库一会话 12 名 agent 死 6 名，全部发生在准备落盘时；采用本协议后交付率 100%。）

## 报告落盘路径

`/Users/biran/code/skills/loop/ccloop/.superpowers/sdd/2026-08-07-pkg2-data-loss/scan-1-report.md`

## 背景（一句）

包 2 = 「今天可达的数据丢失，三条一起做」，顺序 **债 2 → 第 4 笔 → 第 1 笔**。
包 2 已获人裁 4 授权写 `tests/`（**补测试，不含为了让测试变绿而改判据**）。**你这一轮仍是只读。**

## 你的任务：重核三条路径今天在代码面还成不成立

可信来源（**只当线索，不当结论**）：
- `.superpowers/sdd/2026-08-05-l5-input-scan/progress.md` —— 工作包定义在「包 2 —」那一段；
  债 2 / 第 1 笔 / 第 2 笔的扫描结论在「扫描员 A」那节
- 同目录 `scan-A-debt2-lock-abort.md`（债 2 ＋ §13 第 1 笔 ＋ 第 2 笔）
- 同目录 `close-2-recovery-path.md`（875 行；第 1 笔已扩到 recovery 路径、P-READ、G2-null）

逐条回答，每条都要**今天的源码证据**：

1. **债 2**：`persistTerminalState` 往**已不拥有**的 run 写。
   - 今天它的调用点有几个？其中几个可由 lease-loss 到达？（原报告的数是 15 / 4，**自己重数**）
   - 「`cancelled: []` 无出边 ＋ 不在 `RESUMABLE_STATUSES`」今天是否仍属实？
2. **第 4 笔**：输家 reconciliation 写的残余 TOCTOU（含 `readPersistedReconciliationRecord`）。
   - 原报告称「两条时序**都静默**，既有事件盖不住」——今天是否仍属实？给出事件枚举的判别式。
3. **第 1 笔**：锁可被偷，且**已扩到 recovery 路径**、面严格更宽；**P-READ 路径不经过锁协议的缺陷**。
   - 今天 `tryRecoverStaleOwnerTransferLock` / `recoverInterruptedOwnerTransfer` 的形状是否仍支持该结论？
4. **G2-null 那颗雷**：唯一防线是 `fileStore.ts` 里一条**注释**、**没有测试钉住**，
   而 `buildProcessInstanceId()` 的 `pid:<pid>:<origin>` 形式**正好不匹配**。
   - 今天是否仍是「只有注释、没有测试」？**这句是全称否定，按上面的纪律办**。

## 交付格式（报告的小节标题，先落这个骨架）

1. 结论摘要（**最先写**）：四条各判 `仍成立 / 已腐坏 / 无法判定`，各一句话
2. 债 2 的今日证据
3. 第 4 笔的今日证据
4. 第 1 笔的今日证据
5. G2-null 的今日证据
6. 我用过的每一条命令与它当时的输出（含 sanity 探针）
7. 我**没有**验到的部分（如实列，不许留白）
8. 预算记账（本任务上限 100,000 tokens；破了就明写，不许静默）
